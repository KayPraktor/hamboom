# ARCHITECTURE_DECISIONS.md — هم‌بوم

> مرجع تصمیمات فنی پروژه. **هر session جدید Claude Code باید این فایل را قبل از کدنویسی بخواند.**
>
> **قاعده:** اگر تصمیمی اینجا ثبت شده، تغییرش نیاز به تایید صریح مالک پروژه دارد.
> اگر session ای مجبور شد خلاف یک ADR عمل کند، باید یک ADR جدید با وضعیت
> «جایگزین ADR-XXX» اضافه کند — نه اینکه فایل قدیمی را ویرایش کند.
>
> **وضعیت‌ها:** `پذیرفته‌شده` | `پیشنهادی` | `منسوخ` | `نیاز به تایید مالک`
>
> نشان ⚠️ یعنی این تصمیم بر پایه‌ی اطلاعاتی است که باید در زمان پیاده‌سازی
> از منبع رسمی تایید مجدد شود (مثلاً API درگاه یا endpoint آروان).

---

## فهرست

| # | تصمیم | وضعیت |
|---|---|---|
| [ADR-001](#adr-001) | Fastify به‌جای NestJS برای API | پذیرفته‌شده |
| [ADR-002](#adr-002) | pnpm workspaces + Turborepo | پذیرفته‌شده |
| [ADR-003](#adr-003) | مسیر پلکانی Excalidraw: npm → patch → fork | پذیرفته‌شده |
| [ADR-004](#adr-004) | Yjs به‌جای پروتکل اختصاصی excalidraw-room | پذیرفته‌شده |
| [ADR-005](#adr-005) | SQL خام + Kysely، بدون ORM سنگین | پذیرفته‌شده |
| [ADR-006](#adr-006) | مقیاس‌پذیری WebSocket روی K8s آروان | پذیرفته‌شده |
| [ADR-007](#adr-007) | `Y.Map` کلیددار + fractional index برای عناصر | پذیرفته‌شده |
| [ADR-008](#adr-008) | محاسبه‌ی قطعی کانکتور به‌جای همگام‌سازی نقاط | پذیرفته‌شده |
| [ADR-009](#adr-009) | پایداری سند: update log در Postgres + snapshot در S3 | پذیرفته‌شده |
| [ADR-010](#adr-010) | جدایی `type` رندر از `kind` محصولی | پذیرفته‌شده |
| [ADR-011](#adr-011) | احراز هویت: OTP + JWT کوتاه‌عمر + refresh چرخشی | پذیرفته‌شده |
| [ADR-012](#adr-012) | نقش موثر (effective role) به‌جای ACL تخت | پذیرفته‌شده |
| [ADR-013](#adr-013) | abstraction اجباری روی Object Storage | پذیرفته‌شده |
| [ADR-014](#adr-014) | زرین‌پال با آداپتور درگاه + idempotency | پذیرفته‌شده |
| [ADR-015](#adr-015) | پول به‌صورت `BIGINT` ریال | پذیرفته‌شده |
| [ADR-016](#adr-016) | RTL: logical properties + `dir` روی ریشه | پذیرفته‌شده |
| [ADR-017](#adr-017) | فونت Vazirmatn، خودمیزبان | پذیرفته‌شده |
| [ADR-018](#adr-018) | تاریخ: UTC در ذخیره، جلالی در نمایش | پذیرفته‌شده |
| [ADR-019](#adr-019) | Export سمت سرور با Chromium headless | پذیرفته‌شده |
| [ADR-020](#adr-020) | مانیتورینگ و لاگ خودمیزبان | پذیرفته‌شده |
| [ADR-021](#adr-021) | `packages/shared-types` تنها قرارداد بین ماژول‌ها | پذیرفته‌شده |
| [ADR-022](#adr-022) | استروک freedraw از کانال ephemeral | پذیرفته‌شده |
| [ADR-023](#adr-023) | متن فارسی: CSS برای فونت، یک patch برای bidi | پذیرفته‌شده |
| [ADR-024](#adr-024) | تشخیص جهت با اکثریت کاراکترها، نه اولین کاراکتر قوی | پذیرفته‌شده |
| [ADR-025](#adr-025) | wrapper روی `fillText` به‌جای patch — پروژه روی پله‌ی A می‌ماند | پذیرفته‌شده |
| [ADR-026](#adr-026) | گروه‌بندی undo با `captureUpdate` — یک ژست، یک ورودی | پذیرفته‌شده |

---

<a id="adr-001"></a>
## ADR-001 — Fastify به‌جای NestJS برای API

**وضعیت:** پذیرفته‌شده | **جایگزین‌ها:** NestJS، Hono، Express

**تصمیم:** `apps/api` روی **Fastify 5 + TypeScript** ساخته می‌شود، با یک ساختار پوشه‌ای اجباری (هر ماژول: `routes.ts` / `service.ts` / `repo.ts` / `schema.ts`) که نظم NestJS را بدون هزینه‌اش می‌دهد.

**دلیل:** NestJS نظم می‌دهد ولی سه هزینه دارد که برای این پروژه‌ی خاص جدی‌اند. اول، لایه‌ی DI و decorator ها حجم قابل‌توجهی boilerplate تولید می‌کنند که برای یک توسعه‌دهنده‌ی تنها فقط سطح کد را بالا می‌برد بدون اینکه مسئله‌ای حل کند — DI وقتی ارزش دارد که چند تیم روی یک کدبیس کار کنند. دوم، مصرف حافظه و زمان بوت NestJS محسوس بیشتر است و روی آروان‌کلاود مستقیماً به هزینه‌ی ماهانه تبدیل می‌شود؛ Fastify با همان منابع تعداد request بیشتری جواب می‌دهد. سوم و مهم‌تر، هم‌بوم یک API معمولی نیست: باید WebSocket، آپلود stream، و پاسخ باینری (snapshot سند Yjs) بدهد — Fastify در هر سه مستقیم‌تر و کم‌واسطه‌تر است. مزیت اضافه اینکه Fastify از JSON Schema برای اعتبارسنجی و serialization استفاده می‌کند و ما همان schema های zod را (با `zod-to-json-schema`) به OpenAPI تبدیل می‌کنیم، پس مستندسازی رایگان می‌آید. ریسک پذیرفته‌شده: بدون ساختار تحمیلی NestJS، انسجام کد به انضباط ما وابسته است — برای همین ساختار پوشه اجباری و ESLint rule برای جلوگیری از import های خلاف قاعده تعریف می‌شود.

**پیامدها:** هر ماژول API باید دقیقاً الگوی چهارفایلی را رعایت کند. هیچ منطق تجاری در `routes.ts` نوشته نمی‌شود. `buildApp()` بدون `listen` صادر می‌شود تا تست‌ها بدون پورت اجرا شوند.

---

<a id="adr-002"></a>
## ADR-002 — pnpm workspaces + Turborepo

**وضعیت:** پذیرفته‌شده | **جایگزین‌ها:** Nx، Lerna، npm workspaces خالی

**تصمیم:** pnpm workspaces برای مدیریت وابستگی، Turborepo فقط برای orchestration و کش build.

**دلیل:** pnpm با content-addressable store، فضای دیسک و زمان نصب را در یک مونوریپو با چند اپ React به‌شدت کم می‌کند، و مهم‌تر: `node_modules` تخت نمی‌سازد، پس اگر پکیجی وابستگی‌ای را declare نکرده باشد فوراً می‌شکند — این دقیقاً همان چیزی است که در پروژه‌ای با چند session موازی جلوی وابستگی‌های شبح‌وار را می‌گیرد. Turborepo نسبت به Nx سطح مفهومی بسیار کمتری دارد (فقط یک `turbo.json` با تعریف task ها و وابستگی‌شان) و همین برای پروژه‌ی ما کافی است؛ Nx قدرت بیشتری دارد ولی generator ها و plugin هایش لایه‌ی یادگیری اضافه‌ای می‌آورند که ارزشش را ندارد. کش build اهمیت عملی دارد چون هنگام کار روی `apps/api` نباید منتظر build دوباره‌ی `canvas-core` بمانیم.

---

<a id="adr-003"></a>
## ADR-003 — مسیر پلکانی Excalidraw: بسته npm → patch → فورک کامل

**وضعیت:** پذیرفته‌شده | ⚠️ **این یکی از دو تصمیم پرریسک پروژه است**

**تصمیم:** به‌جای فورک کردن Excalidraw از روز اول، یک مسیر سه‌پله‌ای با معیار عبور مشخص:

| پله | روش | معیار عبور به پله بعد |
|---|---|---|
| **A** | مصرف `@excalidraw/excalidraw` از npm + wrapper در `packages/canvas-core` | وقتی تغییری لازم شود که از طریق props / `customData` / `excalidrawAPI` / CSS override ممکن نباشد |
| **B** | همان + `pnpm patch` برای اصلاحات جراحی، هر patch مستند در `patches/README.md` | وقتی تعداد patch از **۸ فایل** یا **۴۰۰ خط** بگذرد، یا وقتی patch ها با هر ارتقای نسخه بشکنند |
| **C** | فورک کامل در `vendor/excalidraw` به‌عنوان git submodule، با فرآیند rebase مستند | — |

**دلیل:** فورک کردن یک کدبیس با ده‌ها هزار خط، برای یک توسعه‌دهنده‌ی تنها، یعنی از آن لحظه به بعد همه‌ی رفع‌باگ‌ها و بهبودهای بالادست را از دست می‌دهی یا باید دستی merge کنی — این هزینه‌ای است که تا سال‌ها پرداخت می‌شود و در ابتدای پروژه بسیار دست‌کم گرفته می‌شود. از طرف دیگر، انکار نمی‌کنم که هم‌بوم تغییرات عمیقی لازم دارد (RTL، نوع عنصر جدید، رفتار sticky) که احتمالاً از props نمی‌گذرند. راه‌حل این است که فورک را **به تعویق بیندازیم تا وقتی که دقیقاً بدانیم چه چیزی را می‌خواهیم تغییر دهیم** — نه اینکه از روز اول کورکورانه فورک کنیم. پله B با `pnpm patch` یک میانه‌ی واقعی است: تغییرات جراحی روی کد کتابخانه بدون از دست دادن ارتباط با بالادست، و هر patch به‌عنوان یک diff قابل‌بازخوانی باقی می‌ماند که در صورت فورک شدن مستقیماً تبدیل به commit می‌شود. اگر پله C اجتناب‌ناپذیر شد، این patch ها نقشه‌ی راه فورک هستند.

**پیامدها:** گام ۲ فایل [TODO.md](TODO.md) یک spike اجباری برای سنجش متن فارسی است. اگر آن spike نشان داد RTL بدون تغییر عمیق ممکن نیست، مستقیماً به پله B (نه C) می‌رویم و نتیجه اینجا به‌عنوان ADR-003b ثبت می‌شود. **نسخه‌ی Excalidraw باید pin شود (نه `^`) تا patch ها تصادفی نشکنند.**

**⚠️ الزام لایسنسی:** قبل از هر vendor کردن، فایل `LICENSE` مخزن Excalidraw و لایسنس جداگانه‌ی فونت‌های همراهش (Excalifont/Virgil و مشابه) باید جداگانه بررسی شوند. فونت‌های همراه استفاده نمی‌شوند — ما Vazirmatn را جایگزین می‌کنیم ([ADR-017](#adr-017)) — ولی نباید ناخواسته در bundle بمانند.

---

<a id="adr-004"></a>
## ADR-004 — Yjs به‌جای پروتکل اختصاصی excalidraw-room

**وضعیت:** پذیرفته‌شده

**تصمیم:** لایه‌ی همکاری با Yjs (CRDT) ساخته می‌شود؛ سرور `excalidraw-room` و مکانیزم reconciliation داخلی Excalidraw استفاده نمی‌شوند.

**دلیل:** `excalidraw-room` برای مدل «جلسه‌ی موقت با چند نفر» طراحی شده: یک relay رمزنگاری‌شده‌ی سرتاسری که کل صحنه را broadcast می‌کند و سرور محتوا را نمی‌فهمد. این برای Excalidraw عالی است ولی برای هم‌بوم سه مشکل دارد. اول، ما **باید** محتوای بورد را سمت سرور بفهمیم — برای thumbnail، جستجو، export، محدودیت اندازه، و بازیابی. دوم، مدل broadcast صحنه‌ی کامل با بوردهای بزرگ و ماندگار (نه جلسه‌ی موقت) به‌سرعت گران می‌شود. سوم، بدون CRDT واقعی، حل تعارض روی متن یک استیکی که دو نفر همزمان تایپ می‌کنند به‌درستی ممکن نیست. Yjs هر سه را حل می‌کند: ادغام قطعی و بدون تعارض، update های تفاضلی و کوچک، پشتیبانی بومی از ویرایش همزمان متن (`Y.Text`)، و کار آفلاین با همگام‌سازی بعدی. هزینه‌اش این است که باید خودمان binder بین Y.Doc و state موتور رندر بنویسیم — که کار دشواری است ولی محدود و یک‌بار.

**پیامدها:** بورد از دید ما هرگز «یک فایل JSON» نیست؛ یک لاگ از update هاست. Export به JSON یعنی materialize کردن سند، نه خواندن مستقیم.

---

<a id="adr-005"></a>
## ADR-005 — SQL خام + Kysely، بدون ORM سنگین

**وضعیت:** پذیرفته‌شده | **جایگزین‌ها:** Prisma، TypeORM، Drizzle

**تصمیم:** migration ها **SQL خام ترتیبی** در `apps/api/migrations/*.sql`. دسترسی به داده با **Kysely** (query builder با تایپ کامل) روی درایور `pg`.

**دلیل:** ORM هایی که schema را از روی مدل تولید می‌کنند (Prisma، TypeORM با synchronize) در پروژه‌ای که چند session مستقل روی آن کار می‌کنند خطرناک‌اند: یک تغییر ناهماهنگ در مدل، migration ای تولید می‌کند که ممکن است داده را نابود کند. با SQL خام، هر تغییر schema یک فایل قابل‌بازبینی است که دقیقاً همان چیزی است که روی دیتابیس اجرا می‌شود — هیچ لایه‌ی جادویی در میان نیست. Kysely برخلاف ORM ها هیچ runtime abstraction ای اضافه نمی‌کند؛ فقط SQL می‌سازد و تایپ‌ها را از یک interface دستی‌نوشته می‌گیرد. این یعنی هر query ای که در PostgreSQL ممکن است (CTE، window function، `FOR UPDATE SKIP LOCKED` برای صف پرداخت، partial index) بدون فرار به raw string نوشته می‌شود. Prisma به‌طور خاص برای این پروژه بد است چون engine باینری‌اش حجم image را بالا می‌برد و کنترل دقیق روی connection pool و تراکنش‌ها را سخت می‌کند — و ما برای پرداخت به تراکنش‌های دقیق نیاز داریم.

**پیامدها:** interface تایپ‌های دیتابیس (`apps/api/src/db/types.ts`) دستی نگهداری می‌شود و باید با هر migration به‌روز شود؛ یک تست که schema واقعی را با interface مقایسه می‌کند این را تضمین می‌کند.

---

<a id="adr-006"></a>
## ADR-006 — مقیاس‌پذیری WebSocket روی K8s آروان

**وضعیت:** پذیرفته‌شده | ⚠️ رفتار دقیق Ingress/LB آروان باید در زمان دیپلوی تایید شود

**تصمیم:** سه‌مرحله‌ای، به ترتیب زمان:

**فاز ۱ — تک‌نود (تا ~۲۰۰ کاربر همزمان):** یک replica از `apps/realtime`. همه‌ی اتاق‌ها در حافظه‌ی همان نود. ساده، بدون هیچ هماهنگی.

**فاز ۲ — چندنود با Redis pub/sub (تا ~۲۰۰۰ کاربر همزمان):** هر نود می‌تواند هر اتاقی را سرو کند. هر update دریافتی روی کانال `hb:board:<boardId>` در Redis منتشر می‌شود؛ نودهای دیگری که همان اتاق را باز دارند آن را روی Y.Doc محلی‌شان اعمال و به کلاینت‌هایشان پخش می‌کنند. awareness هم روی کانال جداگانه‌ی `hb:aware:<boardId>` (بدون پایداری). **این کار درست است چون update های Yjs جابه‌جایی‌پذیر (commutative) و idempotent اند** — ترتیب رسیدن و تکرار مهم نیست. برای جلوگیری از حلقه، هر پیام منتشرشده با `nodeId` مبدأ برچسب می‌خورد و نود مبدأ آن را نادیده می‌گیرد. پایداری در این فاز به یک نود «صاحب» واگذار می‌شود که با یک قفل Redis (`SET hb:owner:<boardId> <nodeId> NX EX 30` با تمدید دوره‌ای) انتخاب می‌شود، تا چند نود همزمان روی `board_updates` ننویسند.

**فاز ۳ — تمایل اتاق (room affinity):** اگر مصرف حافظه مسئله شد (چون در فاز ۲ هر نود نسخه‌ی کاملی از هر سند فعال دارد)، یک لایه‌ی مسیریابی با hashing سازگار (`boardId → node`) اضافه می‌شود تا هر بورد فقط روی یک نود زندگی کند. این کار Redis pub/sub را حذف نمی‌کند، فقط ترافیکش را تقریباً صفر می‌کند.

**دلیل:** ترتیب معکوس (شروع با sharding پیچیده) خطای رایج است. خاصیت CRDT بودن Yjs باعث می‌شود fanout ساده‌ی pub/sub در فاز ۲ **از نظر صحت کاملاً درست** باشد و فقط از نظر حافظه بهینه نباشد — و حافظه مسئله‌ای است که فقط در مقیاس واقعی معنا پیدا می‌کند. تا آن موقع پیچیدگی sharding فقط باگ تولید می‌کند.

**الزامات K8s آروان (⚠️ در زمان دیپلوی تایید شود):**
- Ingress باید WebSocket upgrade را پاس بدهد و timeout اتصال idle آن باید بیشتر از `RT_HEARTBEAT_INTERVAL_MS` (۲۵ثانیه) باشد. اگر لودبالانسر timeout سخت‌گیرانه‌ای دارد، heartbeat کوتاه‌تر شود.
- `sessionAffinity` روی Service برای فاز ۱ و ۲ **لازم نیست** (هر نود هر اتاقی را می‌پذیرد) ولی برای فاز ۳ الزامی می‌شود.
- `terminationGracePeriodSeconds` باید به اندازه‌ی کافی بلند باشد (≥۶۰ ثانیه) تا نود در حال خاموش‌شدن بتواند snapshot بگیرد و کلاینت‌ها را با کد بسته‌شدن مودبانه (`1001 Going Away`) بفرستد تا فوراً به نود دیگر وصل شوند.
- HPA بر اساس تعداد اتصال فعال (متریک سفارشی)، نه CPU — چون WebSocket بیشتر حافظه‌محور است تا CPU-محور.
- شبکه‌ی ایران: کلاینت باید اتصال مجدد با exponential backoff + jitter داشته باشد و update های آفلاین را در IndexedDB (`y-indexeddb`) صف کند.

---

<a id="adr-007"></a>
## ADR-007 — `Y.Map` کلیددار + fractional index برای عناصر

**وضعیت:** پذیرفته‌شده | **جایگزین‌ها:** `Y.Array` از عناصر، یک `Y.Map` تخت با کلید ترکیبی

**تصمیم:** `doc.getMap("elements")` که کلیدش `elementId` و مقدارش خودش یک `Y.Map` از property های آن عنصر است. ترتیب لایه‌ها (z-order) با یک property رشته‌ای `index` بر اساس **fractional indexing** مدیریت می‌شود.

**دلیل:** اگر عناصر در یک `Y.Array` باشند، ترتیب آرایه هم معنای z-order می‌دهد و هم محل عنصر — و این دو با هم تعارض ایجاد می‌کنند: وقتی دو نفر همزمان عنصری را اضافه یا مرتب می‌کنند، ایندکس‌ها جابه‌جا می‌شوند و اشاره‌گرها (مثلاً binding کانکتور) نامعتبر می‌شوند. با `Y.Map` کلیددار، دسترسی به هر عنصر O(1) و پایدار است. جدا کردن z-order به یک property رشته‌ای که با fractional indexing تولید می‌شود، اجازه می‌دهد «این را بین این دو ببر» به یک تغییر تک-property تبدیل شود که هیچ عنصر دیگری را لمس نمی‌کند — دقیقاً چیزی که CRDT در آن قوی است. اینکه هر عنصر خودش یک `Y.Map` است (نه یک آبجکت JSON) اهمیت عملی زیادی دارد: دو نفر می‌توانند همزمان رنگ و موقعیت یک استیکی را عوض کنند و هر دو تغییر بماند؛ با آبجکت ساده، آخرین نویسنده کل عنصر را بازنویسی می‌کرد.

**هزینه‌ی پذیرفته‌شده:** `Y.Map` تودرتو سربار حافظه‌ی بیشتری از آبجکت ساده دارد. برای property های آرایه‌ای که ویرایش همزمانشان بی‌معنی است (`points` یک خط، `groupIds`) مقدار به‌صورت آرایه‌ی ساده ذخیره می‌شود و رفتار LWW دارد — این عمدی است.

---

<a id="adr-008"></a>
## ADR-008 — محاسبه‌ی قطعی کانکتور به‌جای همگام‌سازی نقاط

**وضعیت:** پذیرفته‌شده

**تصمیم:** مسیر یک کانکتور متصل، از روی `startBinding`/`endBinding` و هندسه‌ی دو سر، در **هر کلاینت به‌صورت مستقل و قطعی** محاسبه و رندر می‌شود. آرایه‌ی `points` در Y.Doc فقط توسط کلاینتی نوشته می‌شود که کاربرش عملاً کانکتور یا یکی از دو سرش را دستکاری کرده، و فقط یک‌بار در پایان ژست.

**دلیل:** اگر هر بار که کسی یک استیکی را حرکت می‌دهد، نقاط همه‌ی کانکتورهای متصل هم در CRDT نوشته شوند، حجم ترافیک به‌شدت بالا می‌رود و بدتر: دو کلاینت که همزمان دو سر یک کانکتور را حرکت می‌دهند، `points` را همزمان بازنویسی می‌کنند و نتیجه می‌پرد. با تلقی مسیر به‌عنوان یک **تابع مشتق‌شده از حالت** (derived state) به‌جای حالت ذخیره‌شده، این کلاس از تعارض کاملاً حذف می‌شود. شرط لازم این است که تابع مسیریابی خالص و قطعی باشد: ورودی یکسان در دو مرورگر مختلف باید خروجی بیت‌به‌بیت یکسان بدهد — پس هیچ `Math.random`، هیچ وابستگی به زمان، و دقت اعشاری کنترل‌شده (گرد کردن به ۲ رقم).

---

<a id="adr-009"></a>
## ADR-009 — پایداری سند: update log در Postgres + snapshot در Object Storage

**وضعیت:** پذیرفته‌شده | **جایگزین‌ها:** ذخیره‌ی state کامل در هر تغییر، y-leveldb، y-redis

**تصمیم:** هر update باینری Yjs به‌صورت append در جدول `board_updates` نوشته می‌شود. هر `RT_SNAPSHOT_EVERY_UPDATES` update یا `RT_SNAPSHOT_EVERY_MS` میلی‌ثانیه (هر کدام زودتر)، سند فشرده و به‌عنوان یک شیء در bucket snapshots ذخیره و رکورد آن در `board_snapshots` ثبت می‌شود؛ سپس update های قدیمی‌تر از آن snapshot حذف می‌شوند. بارگذاری اتاق = آخرین snapshot + update های بعد از آن.

**دلیل:** نوشتن state کامل در هر تغییر برای بورد بزرگ فاجعه است (هر جابه‌جایی یک استیکی = نوشتن چند مگابایت). لاگ تفاضلی این را حل می‌کند ولی اگر هرگز فشرده نشود، بارگذاری بورد قدیمی کند می‌شود — پس snapshot لازم است. اینکه لاگ در Postgres و snapshot در Object Storage است عمدی است: لاگ کوچک، پرتراکنش و نیازمند تضمین ترتیب و دوام است (کار Postgres)؛ snapshot بزرگ، کم‌تعداد و فقط-خواندنی است (کار Object Storage، که به‌مراتب ارزان‌تر است). y-leveldb برای استقرار چندنود روی K8s مناسب نیست (فایل محلی) و y-redis داده را در حافظه‌ی گران نگه می‌دارد.

**تضمین دوام:** update قبل از ack به کلاینت نوشته می‌شود، یا حداکثر با ۱ ثانیه تاخیر batch — تصمیم دقیقش در پیاده‌سازی M2 با اندازه‌گیری تاخیر گرفته می‌شود و اینجا به‌روز می‌شود. نشانگر «ذخیره شد» در UI باید حقیقت را نشان دهد، نه خوش‌بینی.

---

<a id="adr-010"></a>
## ADR-010 — جدایی `type` رندر از `kind` محصولی

**وضعیت:** پذیرفته‌شده

**تصمیم:** هر عنصر دو برچسب دارد: `type` که موتور رندر می‌فهمد (`rectangle`, `arrow`, `text`, …) و `customData.hb.kind` که محصول می‌فهمد (`sticky`, `connector`, `frame`, `card`, …).

**دلیل:** افزودن یک نوع عنصر جدید به موتور رندر Excalidraw یعنی دست‌زدن به ده‌ها نقطه از کدبیس بالادست (رندر، hit-test، سریال‌سازی، تبدیل، undo). با این تفکیک، یک استیکی‌نوت از دید موتور فقط یک مستطیل با متن مقید است — یعنی همه‌ی رفتارهای موجود (انتخاب، تغییر اندازه، چرخش، گروه‌بندی، undo) رایگان کار می‌کنند — ولی از دید هم‌بوم یک موجودیت درجه‌یک با ابزار، پالت و رفتار خاص خودش است. این تصمیم مستقیماً هزینه‌ی [ADR-003](#adr-003) را کم می‌کند: هرچه بیشتر بتوانیم معنا را در `customData` بگذاریم، دیرتر مجبور به فورک می‌شویم.

**پیامد:** هیچ کد محصولی نباید مستقیماً روی `type` شرط بگذارد؛ همیشه از `getKind(element)` استفاده شود. یک ESLint rule سفارشی این را چک می‌کند.

---

<a id="adr-011"></a>
## ADR-011 — احراز هویت: OTP + JWT کوتاه‌عمر + refresh چرخشی

**وضعیت:** پذیرفته‌شده

**تصمیم:** ورود با شماره موبایل + کد یک‌بارمصرف پیامکی. خروجی: access token از نوع JWT با عمر ۱۵ دقیقه (در حافظه‌ی جاوااسکریپت، نه localStorage) + refresh token مبهم و تصادفی در cookie با `HttpOnly; Secure; SameSite=Lax`. هر refresh، توکن قبلی را باطل و توکن جدید صادر می‌کند (rotation)؛ استفاده‌ی مجدد از توکن باطل‌شده کل خانواده‌ی session را می‌سوزاند (reuse detection).

**دلیل:** access token کوتاه‌عمر و JWT است چون سرور realtime باید بتواند آن را **بدون تماس با API** اعتبارسنجی کند — این برای مقیاس WebSocket حیاتی است. اما JWT قابل ابطال نیست، پس عمرش باید کوتاه باشد و منبع واقعی session یک refresh token مات در دیتابیس است که هر لحظه قابل ابطال است. نگه‌داشتن access token فقط در حافظه (نه localStorage) سطح حمله‌ی XSS را کم می‌کند. rotation با تشخیص reuse استاندارد صنعتی برای مقابله با سرقت توکن است. OTP پیامکی به‌جای رمز عبور، هم با انتظار کاربر ایرانی هم‌خوان است و هم کلاس کاملی از مسائل (نشت رمز، بازیابی رمز، رمز ضعیف) را حذف می‌کند — به قیمت هزینه‌ی پیامک و وابستگی به درگاه SMS، که با محدودیت نرخ سخت‌گیرانه و OTP ثابت در محیط لوکال مدیریت می‌شود.

**اتصال WebSocket:** توکن اتصال یک JWT جداگانه با عمر ۶۰ ثانیه و scope مخصوص یک `boardId` است ([`GET /boards/:id/rt-token`]) — تا access token اصلی در query string ظاهر نشود (query string در لاگ لودبالانسر می‌ماند).

---

<a id="adr-012"></a>
## ADR-012 — نقش موثر (effective role) به‌جای ACL تخت

**وضعیت:** پذیرفته‌شده

**تصمیم:** دسترسی کاربر به یک بورد یک مقدار **محاسبه‌شده** است، نه یک ردیف ذخیره‌شده. تابع `effectiveBoardRole(user, board, team, link)` نقش نهایی را از ترکیب اینها می‌گیرد و **بیشترین** را برمی‌گرداند: نقش staff پلتفرم → مالک بورد → نقش مستقیم در `board_members` → نقش عضویت در تیم نگاشته‌شده به نقش بورد → دسترسی لینک اشتراک. این تابع در `packages/auth-core` است و **هم API و هم سرور realtime از همان یک پیاده‌سازی استفاده می‌کنند**.

**دلیل:** اگر دسترسی به‌صورت ردیف‌های ACL ذخیره شود، هر تغییر نقش تیمی باید در ده‌ها بورد propagate شود — و هر جایی که یادت برود، یک حفره‌ی امنیتی است. محاسبه در لحظه این کلاس از باگ را کاملاً حذف می‌کند و قیمتش یک query اضافه است که کش‌شدنی است. اینکه API و realtime از یک تابع مشترک استفاده می‌کنند حیاتی است: رایج‌ترین باگ امنیتی در محصولات مشابه این است که REST درست چک می‌کند ولی WebSocket نه، و کسی با نقش viewer می‌تواند مستقیماً update بفرستد. **سرور realtime باید نقش را در هر update بررسی کند، نه فقط هنگام اتصال** — چون نقش ممکن است وسط session عوض شود.

---

<a id="adr-013"></a>
## ADR-013 — abstraction اجباری روی Object Storage

**وضعیت:** پذیرفته‌شده

**تصمیم:** فقط `packages/storage` مجاز به import کردن `@aws-sdk/client-s3` است. رابط صادرشده: `putObject`، `getObject`، `deleteObject`، `presignPut`، `presignGet`، `headObject`، `listPrefix`. هیچ نام سرویسی (`minio`، `arvan`) در امضای توابع نیست.

**دلیل:** این تنها نقطه‌ای است که وعده‌ی «سوییچ از MinIO به آروان فقط با env» عملاً تضمین می‌شود. تجربه نشان می‌دهد که اگر SDK مستقیماً در چند ماژول import شود، تفاوت‌های ریز پیاده‌سازی‌ها (path-style در برابر virtual-host، رفتار presign، پشتیبانی از multipart، header های CORS) در کد نشت می‌کنند و مهاجرت به یک پروژه‌ی چندروزه تبدیل می‌شود. با یک نقطه‌ی تماس، تفاوت‌ها همان‌جا با یک شرط روی config حل می‌شوند. `S3_FORCE_PATH_STYLE` عمداً یک متغیر مستقل است چون همین یک مورد رایج‌ترین علت شکست مهاجرت بین سرویس‌های S3-compatible است.

**تایید شده (۱۴۰۵/۰۴/۳۱، توسط مالک):** آروان از presigned URL پشتیبانی می‌کند، پس مسیر آپلود مستقیم کلاینت→Object Storage قطعی است و مسیر جایگزین (آپلود stream از طریق API) لازم نیست پیاده شود. تنظیمات CORS باکت همچنان در زمان ساخت حساب باید ست شود.

---

<a id="adr-014"></a>
## ADR-014 — زرین‌پال با آداپتور درگاه + idempotency سخت‌گیرانه

**وضعیت:** پذیرفته‌شده | **تایید شده (۱۴۰۵/۰۴/۳۱، توسط مالک):** حالت sandbox زرین‌پال فقط با یک فلگ فعال می‌شود و نیازی به تاییدیه‌ی کسب‌وکار ندارد — پس کل جریان از روز اول قابل ساخت و تست است و `ZARINPAL_MODE` تنها سوییچ لازم است.

**تصمیم:** یک interface به نام `PaymentGateway` با سه متد (`createPayment`، `verifyPayment`، `refund?`) تعریف می‌شود. پیاده‌سازی‌ها: `ZarinpalGateway`، `MockGateway` (برای تست خودکار)، و بعداً `IdpayGateway`. جریان: ساخت رکورد `payments` با وضعیت `pending` و `idempotency_key` → درخواست به درگاه → ذخیره‌ی `authority` → ریدایرکت کاربر → callback → **verify سمت سرور** → فعال‌سازی اشتراک در همان تراکنش دیتابیس.

**دلیل:** جدا کردن آداپتور از منطق تجاری از روز اول (نه بعداً) لازم است چون آیدی‌پی در نقشه‌ی راه هست و چون بدون `MockGateway` نمی‌شود جریان پرداخت را در CI تست کرد. سه قاعده‌ی غیرقابل‌مذاکره در پیاده‌سازی:

1. **هیچ‌گاه به پارامترهای callback اعتماد نکن.** `Status=OK` در URL برگشتی صرفاً یک ادعای مرورگر است. اشتراک فقط پس از پاسخ موفق `verify` مستقیم سرور-به-سرور فعال می‌شود.
2. **verify باید idempotent باشد.** کاربر ممکن است روی callback رفرش بزند. رکورد پرداخت با `SELECT … FOR UPDATE` قفل می‌شود؛ اگر قبلاً `paid` است، همان نتیجه برگردانده می‌شود بدون فعال‌سازی دوباره.
3. **آشتی‌دهی (reconciliation) اجباری است.** کاربر ممکن است پول بدهد و قبل از برگشت، مرورگر را ببندد. یک job دوره‌ای همه‌ی پرداخت‌های `pending` قدیمی‌تر از N دقیقه را دوباره verify می‌کند. بدون این، پول گرفته می‌شود و سرویس داده نمی‌شود — که هم مشکل حقوقی است هم اعتماد را می‌سوزاند.

مبلغ ارسالی به درگاه همیشه از سرور محاسبه می‌شود؛ هیچ مبلغی از کلاینت پذیرفته نمی‌شود.

---

<a id="adr-015"></a>
## ADR-015 — پول به‌صورت `BIGINT` ریال

**وضعیت:** پذیرفته‌شده

**تصمیم:** همه‌ی مبالغ در دیتابیس، API و منطق تجاری، عدد صحیح **ریال** در `bigint` هستند. تبدیل به تومان (تقسیم بر ۱۰) و قالب‌بندی با جداکننده‌ی هزارگان و ارقام فارسی، فقط در لایه‌ی نمایش انجام می‌شود.

**دلیل:** استفاده از اعداد اعشاری برای پول همیشه غلط است، ولی در ایران یک تله‌ی اضافه هم هست: ابهام ریال/تومان. اگر بعضی جاها تومان و بعضی جاها ریال ذخیره شود، دیر یا زود یک ضریب ۱۰ در فاکتور کسی ظاهر می‌شود. انتخاب ریال (نه تومان) به این دلیل است که واحد رسمی و واحد پذیرفته‌شده‌ی درگاه‌های پرداخت است، پس در مرز سیستم تبدیلی لازم نیست. `bigint` نه `integer`، چون مبالغ سالانه‌ی تیم بزرگ به‌راحتی از سقف ۲٫۱ میلیارد `int4` می‌گذرد. در جاوااسکریپت این مقادیر تا `Number.MAX_SAFE_INTEGER` امن‌اند (≈۹ کوادریلیون ریال)، ولی درایور Postgres باید طوری تنظیم شود که `int8` را به `number` تبدیل کند نه `string`، و این در یک جا (`packages/config` یا bootstrap دیتابیس) تنظیم و مستند شود.

---

<a id="adr-016"></a>
## ADR-016 — RTL: logical properties + `dir` روی ریشه

**وضعیت:** پذیرفته‌شده

**تصمیم:** `<html dir="rtl" lang="fa">`. در CSS، **فقط** از logical properties استفاده می‌شود: `margin-inline-start` به‌جای `margin-left`، `padding-inline`، `inset-inline-start`، `border-inline-end`، `text-align: start`. هیچ کلاس `rtl:` شرطی و هیچ stylesheet جداگانه‌ی RTL ساخته نمی‌شود. یک ESLint/Stylelint rule استفاده از property های فیزیکی جهت‌دار را خطا می‌کند.

**دلیل:** الگوی رایج «یک CSS برای LTR و یک نسخه‌ی آینه‌ای برای RTL» دو برابر کد و دو برابر باگ تولید می‌کند، و همیشه یک گوشه‌ای فراموش می‌شود. logical properties در همه‌ی مرورگرهای مدرن پشتیبانی می‌شوند و مسئله را در ریشه حل می‌کنند: کد یک‌بار نوشته می‌شود و مرورگر جهت را اعمال می‌کند. این تصمیم باید **از خط اول** اعمال شود؛ تبدیل بعدی یک بازنویسی کامل است.

**استثنای مهم — بوم:** فضای مختصات بوم ریاضی است و RTL ندارد. `x` همیشه به راست افزایش می‌یابد. RTL فقط به **متن داخل عناصر** و **رابط کاربری اطراف بوم** مربوط می‌شود. این تفکیک باید در `canvas-core` کاملاً صریح باشد وگرنه به یک کلاس گیج‌کننده از باگ‌های هندسی می‌رسیم.

---

<a id="adr-017"></a>
## ADR-017 — فونت Vazirmatn، خودمیزبان

**وضعیت:** پذیرفته‌شده | **نیاز به تایید مالک برای انتخاب نهایی (Q9 در PLAN.md)**

**تصمیم:** فونت اصلی رابط و بوم: **Vazirmatn** با لایسنس SIL Open Font License 1.1. فایل‌ها در `apps/web/public/fonts/` میزبانی می‌شوند، با `font-display: swap` و subset فارسی + لاتین + ارقام. هیچ درخواستی به Google Fonts یا CDN خارجی زده نمی‌شود (اصل P2).

**دلیل:** SIL OFL اجازه‌ی استفاده‌ی تجاری، توزیع و حتی تغییر (با شرط تغییر نام) را می‌دهد، پس با محدودیت لایسنسی پروژه سازگار است. Vazirmatn متغیر (variable font) است و همه‌ی وزن‌ها را در یک فایل می‌دهد که برای حجم bundle خوب است، و ارقام فارسی و لاتین هر دو را با کیفیت دارد. خودمیزبانی کردن نه فقط به‌خاطر اصل P2، بلکه به‌خاطر دسترس‌پذیری از داخل ایران هم ضروری است.

**نکته‌ی فنی مهم برای بوم:** رندر متن روی canvas به اندازه‌گیری دقیق نیاز دارد. فونت باید **قبل از اولین رندر بوم** لود شده باشد (`document.fonts.ready`)، وگرنه عرض‌های اندازه‌گیری‌شده با فونت fallback محاسبه و متن بعداً جابه‌جا می‌شود. `canvas-core` باید یک gate صریح برای این داشته باشد.

---

<a id="adr-018"></a>
## ADR-018 — تاریخ: UTC در ذخیره، جلالی در نمایش

**وضعیت:** پذیرفته‌شده

**تصمیم:** همه‌ی زمان‌ها در دیتابیس `timestamptz` و در UTC. همه‌ی زمان‌ها در API به‌صورت ISO-8601 با `Z`. تبدیل به تقویم جلالی و منطقه‌ی زمانی تهران **فقط** در لایه‌ی نمایش، از طریق `packages/i18n` با `Intl.DateTimeFormat` و کلندر `persian` (بومی مرورگر، بدون کتابخانه‌ی اضافه).

**دلیل:** ذخیره‌ی تاریخ جلالی در دیتابیس، محاسبات بازه‌ای (مثلاً «اشتراک‌هایی که در ۷ روز آینده منقضی می‌شوند») را به کابوس تبدیل می‌کند. `Intl` با کلندر persian در همه‌ی مرورگرهای مدرن و Node پشتیبانی می‌شود، پس نیازی به کتابخانه‌ی تبدیل تاریخ نیست. یک استثنا: **شماره‌ی فاکتور** سال شمسی را در خود دارد (`HB-1405-000123`) — این یک رشته‌ی نمایشی است، نه یک تاریخ، و در زمان صدور از `issued_at` محاسبه و ذخیره می‌شود.

---

<a id="adr-019"></a>
## ADR-019 — Export سمت سرور با Chromium headless

**وضعیت:** پذیرفته‌شده | **جایگزین:** export سمت کلاینت

**تصمیم:** خروجی PNG/PDF در `apps/worker` با Playwright + Chromium تولید می‌شود: worker یک صفحه‌ی مخصوص render را باز می‌کند، سند را از snapshot لود می‌کند، و تصویر می‌گیرد. خروجی SVG و JSON بدون مرورگر و مستقیماً از سند تولید می‌شوند. کلاینت هم برای بوردهای کوچک export فوری سمت مرورگر دارد (مسیر سریع)، ولی مسیر معتبر و مقیاس‌پذیر سرور است.

**دلیل:** export سمت کلاینت برای بورد بزرگ مرورگر کاربر را قفل می‌کند یا حافظه کم می‌آورد، و برای PDF چندصفحه‌ای عملاً کار نمی‌کند. مهم‌تر: export سرور امکان استفاده در thumbnail خودکار، پیش‌نمایش لینک اشتراک، و ایمیل را هم می‌دهد. هزینه‌اش یک image سنگین (Chromium ≈ ۴۰۰MB) است — برای همین worker یک Deployment جدا با منابع خودش است، نه بخشی از API.

---

<a id="adr-020"></a>
## ADR-020 — مانیتورینگ و لاگ خودمیزبان

**وضعیت:** پذیرفته‌شده

**تصمیم:** لاگ ساخت‌یافته با `pino` (JSON، با `requestId` در همه‌ی خطوط). متریک و trace با OpenTelemetry SDK → Prometheus + Grafana + (اختیاری) Tempo، همه خودمیزبان روی همان کلاستر. ردیابی خطا با یک نمونه‌ی **self-hosted** سازگار با Sentry، یا در نبود آن یک جدول ساده‌ی `error_events`.

**دلیل:** اصل P2 هر SaaS خارجی را رد می‌کند، و مستقل از آن، فرستادن لاگ محصول ایرانی به سرور خارجی هم ریسک دسترسی دارد هم ریسک حریم خصوصی کاربران. OpenTelemetry انتخاب شده چون استاندارد و vendor-neutral است؛ اگر بعداً به راهکار دیگری مهاجرت کردیم، instrumentation دوباره نوشته نمی‌شود. اصل P7 (بدون PII در لاگ) با یک redactor در پیکربندی pino اعمال می‌شود که کلیدهای حساس (`phone`, `code`, `token`, `authority`, `password`) را به‌صورت خودکار ماسک می‌کند — و این باید در `packages/config` مرکزی باشد نه در هر اپ.

---

<a id="adr-021"></a>
## ADR-021 — `packages/shared-types` تنها قرارداد بین ماژول‌ها

**وضعیت:** پذیرفته‌شده

**تصمیم:** همه‌ی شکل‌های داده‌ای که از مرز یک ماژول عبور می‌کنند (DTO های API، انواع عناصر، کدهای خطا، enum ها، پیام‌های WebSocket) در `packages/shared-types` با **zod** تعریف می‌شوند و type ها با `z.infer` استخراج می‌شوند. هیچ ماژولی اجازه‌ی تعریف موازی همان شکل را ندارد.

**دلیل:** این پروژه با چند session مستقل (و شاید موازی، در worktree های جدا) ساخته می‌شود. بدون یک قرارداد واحد و اجباری، هر session نسخه‌ی خودش از `Board` را تعریف می‌کند و در نقطه‌ی ادغام همه‌چیز می‌شکند. zod به‌جای interface خالص انتخاب شده چون یک تعریف، هم تایپ زمان کامپایل می‌دهد، هم اعتبارسنجی زمان اجرا، هم schema برای OpenAPI — سه چیز که در غیر این صورت باید جدا نگه داشته می‌شدند و از هم دور می‌افتادند.

**قاعده‌ی فرآیندی:** تغییر `packages/shared-types` باید **دستی توسط مالک پروژه تایید شود**. هیچ session ای نباید بدون اطلاع، این پکیج را تغییر دهد — چون تغییرش همه‌ی ماژول‌های دیگر را می‌شکند. اگر session ای به تغییری نیاز داشت، پیشنهادش را در `PROGRESS.md` بنویسد و منتظر بماند.

---

<a id="adr-022"></a>
## ADR-022 — استروک freedraw و حرکات موقت از کانال ephemeral

**وضعیت:** پذیرفته‌شده

**تصمیم:** هر داده‌ای که «در حال انجام» است و ارزش ماندگاری ندارد — استروک قلم قبل از رها شدن، لیزر پوینتر، ری‌اکشن‌های شناور، پیش‌نمایش کشیدن یک شکل — از کانال `HB_EPHEMERAL` (روی awareness) پخش می‌شود و **هرگز وارد Y.Doc نمی‌شود**. فقط نتیجه‌ی نهایی در `pointerup` به‌عنوان یک عنصر commit می‌شود، پس از ساده‌سازی مسیر.

**دلیل:** یک استروک قلم متوسط ۲۰۰ تا ۵۰۰ نقطه دارد که در طول ۲ ثانیه تولید می‌شوند. اگر هر نقطه یک update در CRDT باشد، یک خط ساده صدها ورودی در تاریخچه، صدها پیام شبکه و رشد دائمی سند تولید می‌کند — و این تاریخچه هرگز جمع نمی‌شود چون CRDT حذف واقعی ندارد. تفکیک «داده‌ی در حال شکل‌گیری» از «داده‌ی نهایی» این را کاملاً حل می‌کند و به‌عنوان یک اصل معماری (نه یک بهینه‌سازی) ثبت می‌شود، چون همین الگو برای هر ابزار آینده‌ای (کشیدن شکل، تغییر اندازه‌ی زنده) هم صادق است.

---

<a id="adr-023"></a>
## ADR-023 — متن فارسی: فونت با CSS، جهت با یک patch

**وضعیت:** پذیرفته‌شده (تایید مالک، ۱۴۰۵/۰۴/۳۱) | **شواهد:** [docs/spike-persian-text.md](docs/spike-persian-text.md)

**تصمیم:** پشتیبانی فارسی روی بوم با سه لایه ساخته می‌شود، به ترتیب کم‌هزینه به پرهزینه:

1. **فونت — بدون patch.** Vazirmatn زیر نام خانواده‌ی خودِ موتور (`Excalifont` و
   بقیه) با `unicode-range` محدود به بازه‌ی عربی ثبت می‌شود. مرورگر برای هر کاراکتر
   face متناسب را برمی‌دارد، پس فارسی با Vazirmatn و لاتین با فونت اصلی رندر می‌شود.
2. **اندازه‌گیری — بدون patch.** مسیر اندازه‌گیری موتور برای فارسی درست است. اگر
   بعداً جایی خطا دیدیم، `setCustomTextMetricsProvider` یک API عمومی برای
   جایگزینی `getLineWidth` است.
3. **جهت (bidi) — یک patch.** موتور `ctx.direction` را هرگز ست نمی‌کند، پس متن
   فارسی با جهت پایه‌ی LTR چیده می‌شود و نویسه‌های خنثی (نشانه‌گذاری، پرانتز، عدد
   لاتین) در دو سر خط جای اشتباه می‌افتند. این تنها موردی است که از طریق props،
   `customData`، `excalidrawAPI` یا CSS قابل حل نیست.

**دلیل:** spike گام ۱٫۳ نشان داد سه ترس اصلی ما بی‌مورد بوده‌اند. شکل‌دهی حروف
کار می‌کند — اثباتش عددی است: رشته‌ی فارسی تا ۳۸٪ باریک‌تر از جمع عرض تک‌تک
کاراکترهایش اندازه‌گیری می‌شود، در حالی که شاهد لاتین دقیقاً برابر است؛ این فقط
وقتی ممکن است که حروف به شکل چسبان رندر شوند. شکست خط روی مرز کلمه می‌افتد و
نیم‌فاصله را نمی‌شکند. اندازه‌گیری عرض هم درست است — انحراف باقی‌مانده روی شاهد
لاتین از فارسی بیشتر است، پس مسئله‌ی فارسی نیست. در مقابل، `ctx.direction` روی
بوم زنده `"ltr"` است و آزمون هش پیکسلی ثابت کرد این مقدار خروجی رندر را واقعاً
عوض می‌کند. ترتیب لایه‌ها عمدی است: هرچه بیشتر بتوانیم با CSS و API عمومی حل کنیم،
سطح تماس با کد بالادست کوچک‌تر می‌ماند و [ADR-003](#adr-003) دیرتر به فورک می‌رسد.

**پیامدها:** پروژه از پله‌ی A به **پله‌ی B** در ADR-003 می‌رود — با دقیقاً یک patch.
`ENGINE_STAGE` در `packages/canvas-core/src/index.ts` باید به `"patch"` برود
(یک تست این مقدار را می‌پاید). هر patch باید در `patches/README.md` ثبت شود.
نسخه‌ی `@excalidraw/excalidraw` از قبل pin شده است، پس ارتقا یک تصمیم آگاهانه
خواهد بود نه اثر جانبی.

**دو محدودیت پذیرفته‌شده:**

- کلمه‌ای که از ظرفش پهن‌تر باشد، وسط کلمه شکسته می‌شود و اتصال حروف پاره می‌شود
  (اثبات عددی: جمع عرض خطوط ۳٫۶٪ از عرض کل کلمه بیشتر می‌شود). فقط در حالت
  مصنوعی رخ می‌دهد؛ رفع نمی‌شود.
- فارسی از فهرست زبان‌های رابط موتور حذف شده (ترجمه‌ی ۸۴٪ در برابر آستانه‌ی ۸۵٪).
  بی‌اهمیت است چون رابط کاربری را در گام ۴٫۲ کاملاً با رابط فارسی خودمان
  جایگزین می‌کنیم.

**⚠️ موردی که هنوز اثبات نشده:** ترتیب bidi تنها بخشی از spike است که چشمی تایید
نشده — امکان اسکرین‌شات در session انجام spike در دسترس نبود. شواهد عددی قطعی‌اند،
ولی تایید بصری روی صفحه‌ی `#spike` با مالک است.

---

<a id="adr-024"></a>
## ADR-024 — تشخیص جهت متن با اکثریت کاراکترها، نه با اولین کاراکتر قوی

**وضعیت:** پذیرفته‌شده | **شواهد:** [spike بخش ۱۰](docs/spike-persian-text.md)
**جایگزین رد شده:** الگوریتم استاندارد `dir="auto"` (اولین کاراکتر قوی)

**تصمیم:** `detectBaseDirection(text)` در `packages/canvas-core/src/text/bidi.ts`
جهت پایه را از **نسبت کاراکترهای قوی RTL به کل کاراکترهای قوی** تعیین می‌کند، نه
از اولین کاراکتر قوی. هر عنصر متنی یک property صریح
`direction: "rtl" | "ltr" | "auto"` دارد (طبق [PLAN بخش ۷٫۳](PLAN.md)) و مقدار
`auto` با همین تابع حل می‌شود — نه با `dir="auto"` مرورگر.

**دلیل:** الگوریتم استاندارد یونیکد برای `dir="auto"` جهت را از اولین کاراکتر قوی
می‌گیرد. spike نشان داد این برای اعداد مشکلی نمی‌سازد (رقم‌ها «قوی» نیستند، پس
«۱۲۳ مورد از ۴۵۶» و «456 مورد باقی مانده» هر دو درست `rtl` می‌شوند) و برای
گیومه‌ی فارسی هم مشکلی ندارد. ولی روی رشته‌ای که با یک **کلمه‌ی لاتین** شروع شود
شکست می‌خورد: «board برای تیم ماست» جهت `ltr` می‌گیرد در حالی که یک جمله‌ی فارسی
است. این الگو در یک بورد فارسی نادر نیست — نام ابزار، اصطلاح فنی یا کلمه‌ی
انگلیسی در ابتدای جمله کاملاً رایج است. شمارش اکثریت این مورد را درست حل می‌کند و
هزینه‌اش یک پیمایش رشته است. جایی که heuristic کافی نیست، کاربر می‌تواند جهت را
صریح روی عنصر ست کند — به همین دلیل property `direction` از ابتدا در مدل داده هست.

**پیامدها:** این تابع در **سه جا** مصرف می‌شود و هر سه باید یک منبع داشته باشند:
مقدار `ctx.direction` در patch رندر (ADR-023)، صفت `dir` روی textarea ویرایشگر
(به‌جای `auto`)، و پیش‌فرض `textAlign` عنصر. اگر این سه از هم جدا شوند، متن هنگام
ورود و خروج از حالت ویرایش می‌پرد.

---

<a id="adr-025"></a>
## ADR-025 — wrapper روی `fillText` به‌جای patch، مشروط به تایید تجربی

**وضعیت:** پذیرفته‌شده (تایید تجربی + تایید مالک، ۱۴۰۵/۰۴/۳۱) | **جایگزین می‌کند:** بخش patch در [ADR-023](#adr-023)

**تصمیم:** به‌جای اعمال patch روی کد minify شده‌ی موتور، متد
`CanvasRenderingContext2D.prototype.fillText` (و `strokeText`، و معادل‌های
`OffscreenCanvas`) در زمان اجرا wrap می‌شوند و `ctx.direction` از روی **خود متنی
که رسم می‌شود** ست می‌گردد. پیاده‌سازی: `packages/canvas-core/src/engine/canvas-direction.ts`.

**دلیل:** تنها کاری که patch تاییدشده (P-1) قرار بود بکند این بود که قبل از
`fillText` مقدار `ctx.direction` را از روی متن ست کند. ولی خودِ `fillText(text, …)`
متن را به‌عنوان آرگومان می‌دهد — پس همان کار از بیرون هم شدنی است. مزیت‌ها:
پروژه روی پله‌ی A از [ADR-003](#adr-003) می‌ماند، با ارتقای نسخه‌ی پکیج نمی‌شکند
(به هیچ نام minify شده‌ای گره نمی‌خورد)، منطق در کد خودمان است و مستقیماً تست
می‌شود، و build های dev و prod موتور یکسان رفتار می‌کنند. هزینه‌اش این است که
روی **همه‌ی** رسم متن canvas در صفحه اثر می‌گذارد — که چون جهت از محتوای خود
رشته می‌آید، رفتار درستی است و نه یک اثر جانبی.

**شواهد تایید:**

- **شمارنده‌ی فراخوانی hook هنگام کار با بوم بالا می‌رود** — یعنی موتور واقعاً از
  همین مسیر متن می‌کشد. (تایید مالک روی صفحه‌ی `#spike`، ۱۴۰۵/۰۴/۳۱.)
  این تنها حلقه‌ی مفقوده بود؛ در session پیاده‌سازی قابل تولید نبود چون
  رویدادهای مصنوعی اشاره‌گر/چرخ را موتور نمی‌پذیرفت و هیچ رندر مجددی رخ نمی‌داد.
- wrapper نصب می‌شود و `direction` را روی هر context درست ست می‌کند (مرورگر واقعی)
- مقدار `ctx.direction` واقعاً خروجی رندر متن مخلوط را عوض می‌کند
  (آزمون هش پیکسلی، [spike بخش ۶](docs/spike-persian-text.md))
- تست واحد: سیم‌کشی، idempotency، حذف، عبور آرگومان‌ها

**پیامدها:**

- **پروژه روی پله‌ی A از [ADR-003](#adr-003) می‌ماند.** `ENGINE_STAGE` همچنان
  `"npm"` است و `patches/` خالی. patch تاییدشده‌ی P-1 اعمال **نمی‌شود** — ولی
  به‌عنوان راه‌حل پشتیبان با متن آماده در `patches/README.md` می‌ماند، چون اگر
  نسخه‌ی بعدی موتور مسیر رندر را عوض کند دوباره لازم می‌شود.
- شمارنده‌ی فراخوانی **در کد می‌ماند**، نه به‌عنوان آمار بلکه به‌عنوان
  تشخیص‌دهنده‌ی رگرسیون: اگر روزی بعد از ارتقای نسخه صفر شد، یعنی موتور دیگر از
  این مسیر رد نمی‌شود و باید به P-1 برگردیم. در چک‌لیست ارتقا ثبت شده است.

---

<a id="adr-026"></a>
## ADR-026 — گروه‌بندی undo با `captureUpdate`

**وضعیت:** پذیرفته‌شده (تایید تجربی، ۱۴۰۵/۰۵/۰۱) | **جایگزین‌ها:** پیاده‌سازی
undo stack خودمان، راه‌حل غیرمستقیم مثل ورود به ویرایش متن

**تصمیم:** هر عملیاتی که باید **یک ژست کاربر** باشد (حرکت فریم با فرزندانش،
تغییر رنگ چند استیکی، گروه‌بندی) در **یک** فراخوانی `updateScene` با همه‌ی
عناصر تغییریافته و `captureUpdate: "IMMEDIATELY"` انجام می‌شود — که موتور آن را
یک increment و در نتیجه یک ورودی undo ثبت می‌کند. تغییراتی که از راه دور
می‌آیند (`applyRemoteChanges` در M2) با `captureUpdate: "NEVER"` نوشته می‌شوند
تا در undo محلی کاربر ظاهر نشوند.

**دلیل:** برخلاف «ورود به حالت ویرایش متن» که API عمومی نداشت و راه‌حل غیرمستقیم
خواست، اینجا یک مسیر عمومی تمیز وجود دارد: ثابت `CaptureUpdateAction` صادر شده و
فیلد `captureUpdate` بخشی از تایپ عمومی `SceneData` است. پس نه undo stack خودمان
را لازم داریم، نه patch. مهم‌تر، granularity دقیقاً همان چیزی است که می‌خواهیم و
**تجربی در مرورگر تایید شد**، نه فقط از روی types: سه عنصر در یک
`updateScene(IMMEDIATELY)` با یک undo همگی برگشتند؛ همان سه عنصر در سه
`updateScene` جدا، هر undo فقط یکی را برگرداند. پس واحد undo برابر یک فراخوانی
capture است، نه یک عنصر.

**پیامدها:**

- سازنده‌های عنصر (`elements/*`) شیء برمی‌گردانند؛ **نوشتن به صحنه و انتخاب
  `captureUpdate` وظیفه‌ی لایه‌ی بالاتر است** (دمو الان، `apps/web` بعداً). یک
  ژست = یک `updateScene`.
- این با [ADR-004](#adr-004)/[ADR-022](#adr-022) هماهنگ است: استروک freedraw و
  داده‌ی ephemeral که نباید در تاریخچه بمانند، اصلاً از مسیر capture نمی‌روند.
- **مکمل نگهبان echo** ([sync/contract](../packages/canvas-core/src/sync/contract.ts)):
  ADR-012 می‌خواست undo محلی کار کاربران دیگر را برنگرداند ولی مکانیزمش معلوم
  نبود. حالا هست — M2 باید `applyRemoteChanges` را با `captureUpdate: "NEVER"`
  اعمال کند. این در `sync/README.md` برای M2 ثبت شده است.
- خودِ «یک undo یک ژست را برمی‌گرداند» رفتار موتور است و در jsdom (که undo
  ندارد) آزمودنی نیست؛ در تست مرورگری گام ۶٫۱ پوشش داده می‌شود.

---

## تصمیم‌های معلق (هنوز ADR نشده‌اند)

| موضوع | چرا هنوز تصمیم نگرفته‌ایم |
|---|---|
| کتابخانه‌ی state سمت کلاینت (Zustand در برابر Jotai) | تا وقتی `canvas-core` شکل نگیرد، نمی‌دانیم مدل state چقدر گرانولار است. تصمیم در گام ۳ از TODO. |
| استراتژی کش سمت کلاینت (TanStack Query تنظیمات) | بعد از تثبیت قرارداد API. |
| ~~نحوه‌ی رندر متن فارسی روی canvas~~ | ✅ حل شد — [ADR-023](#adr-023) |
| تماس صوتی/تصویری (SFU) | خارج از فاز ۱ (Q8 در PLAN.md). |
| موتور جستجوی محتوای بورد | فاز ۲. احتمالاً `pg_trgm` کافی است و نیازی به موتور جدا نیست. |
| CDN و anti-DDoS | در زمان دیپلوی، بر اساس امکانات واقعی آروان. |

---

## راهنمای افزودن ADR جدید

```markdown
<a id="adr-0XX"></a>
## ADR-0XX — <عنوان کوتاه و قاطع>

**وضعیت:** پیشنهادی | **جایگزین‌ها:** <چه چیزهایی رد شدند>

**تصمیم:** <در یک تا سه جمله، دقیقاً چه کاری می‌کنیم>

**دلیل:** <یک پاراگراف. چرا این، و چرا نه آن یکی. هزینه‌ی پذیرفته‌شده را صریح بنویس.>

**پیامدها:** <چه چیزی از این به بعد اجباری/ممنوع می‌شود>
```
