# ARCHITECTURE_DECISIONS.md — هم‌بوم

> مرجع تصمیمات فنی پروژه. **هر session جدید Claude Code باید این فایل را قبل از کدنویسی بخواند.**
>
> **قاعده:** اگر تصمیمی اینجا ثبت شده، تغییرش نیاز به تایید صریح مالک پروژه دارد.
> اگر session ای مجبور شد خلاف یک ADR عمل کند، باید یک ADR جدید با وضعیت
> «جایگزین ADR-XXX» اضافه کند — نه اینکه فایل قدیمی را ویرایش کند.
>
> **وضعیت‌ها:** `پذیرفته‌شده` | `پیشنهادی` | `منسوخ` | `نیاز به تایید مالک`
>
> نشان ⚠️ یعنی این تصمیم بر پایه‌ی اطلاعاتی است که باید در زمان پیاده‌سازی
> از منبع رسمی تایید مجدد شود (مثلاً API درگاه یا endpoint آروان).

---

## فهرست

| # | تصمیم | وضعیت |
|---|---|---|
| [ADR-001](#adr-001) | Fastify به‌جای NestJS برای API | پذیرفته‌شده |
| [ADR-002](#adr-002) | pnpm workspaces + Turborepo | پذیرفته‌شده |
| [ADR-003](#adr-003) | مسیر پلکانی Excalidraw: npm → patch → fork | پذیرفته‌شده |
| [ADR-004](#adr-004) | Yjs به‌جای پروتکل اختصاصی excalidraw-room | پذیرفته‌شده |
| [ADR-005](#adr-005) | SQL خام + Kysely، بدون ORM سنگین | پذیرفته‌شده |
| [ADR-006](#adr-006) | مقیاس‌پذیری WebSocket روی K8s آروان | پذیرفته‌شده |
| [ADR-007](#adr-007) | `Y.Map` کلیددار + fractional index برای عناصر | پذیرفته‌شده |
| [ADR-008](#adr-008) | محاسبه‌ی قطعی کانکتور به‌جای همگام‌سازی نقاط | پذیرفته‌شده |
| [ADR-009](#adr-009) | پایداری سند: update log در Postgres + snapshot در S3 | پذیرفته‌شده |
| [ADR-010](#adr-010) | جدایی `type` رندر از `kind` محصولی | پذیرفته‌شده |
| [ADR-011](#adr-011) | احراز هویت: OTP + JWT کوتاه‌عمر + refresh چرخشی | پذیرفته‌شده |
| [ADR-012](#adr-012) | نقش موثر (effective role) به‌جای ACL تخت | پذیرفته‌شده |
| [ADR-013](#adr-013) | abstraction اجباری روی Object Storage | پذیرفته‌شده |
| [ADR-014](#adr-014) | زرین‌پال با آداپتور درگاه + idempotency | پذیرفته‌شده |
| [ADR-015](#adr-015) | پول به‌صورت `BIGINT` ریال | پذیرفته‌شده |
| [ADR-016](#adr-016) | RTL: logical properties + `dir` روی ریشه | پذیرفته‌شده |
| [ADR-017](#adr-017) | فونت Vazirmatn، خودمیزبان | پذیرفته‌شده |
| [ADR-018](#adr-018) | تاریخ: UTC در ذخیره، جلالی در نمایش | پذیرفته‌شده |
| [ADR-019](#adr-019) | Export سمت سرور با Chromium headless | پذیرفته‌شده |
| [ADR-020](#adr-020) | مانیتورینگ و لاگ خودمیزبان | پذیرفته‌شده |
| [ADR-021](#adr-021) | `packages/shared-types` تنها قرارداد بین ماژول‌ها | پذیرفته‌شده |
| [ADR-022](#adr-022) | استروک freedraw از کانال ephemeral | پذیرفته‌شده |
| [ADR-023](#adr-023) | متن فارسی: CSS برای فونت، یک patch برای bidi | پذیرفته‌شده |
| [ADR-024](#adr-024) | تشخیص جهت با اکثریت کاراکترها، نه اولین کاراکتر قوی | پذیرفته‌شده |
| [ADR-025](#adr-025) | wrapper روی `fillText` به‌جای patch — پروژه روی پله‌ی A می‌ماند | پذیرفته‌شده |
| [ADR-026](#adr-026) | گروه‌بندی undo با `captureUpdate` — یک ژست، یک ورودی | پذیرفته‌شده |
| [ADR-027](#adr-027) | جای‌گذاری overlayهای دورِ بوم از یک منبعِ واحد (سیستمِ اسلات) | پذیرفته‌شده |
| [ADR-028](#adr-028) | ناسازگاریِ StrictMode با APIِ امریِ Excalidraw — ریسکِ ثبت‌شده | پذیرفته‌شده |
| [ADR-029](#adr-029) | لایه‌بندیِ M2: `ydoc-schema` خالص + `canvas-sync` برای binder | پذیرفته‌شده |
| [ADR-030](#adr-030) | ترابریِ WebSocket: `ws` پشتِ یک seam، نه `uWebSockets.js` | پذیرفته‌شده |
| [ADR-031](#adr-031) | پورت به‌جای وابستگیِ مستقیم به M3 (auth و snapshot) | پذیرفته‌شده |
| [ADR-032](#adr-032) | الگوی اشتراکِ StrictMode-safe — تاییدِ تجربیِ نامزدِ ADR-028 | پذیرفته‌شده |
| [ADR-033](#adr-033) | `customData` هم `Y.Map` تودرتو می‌شود — بسطِ ADR-007 | پذیرفته‌شده |
| [ADR-034](#adr-034) | متن: `Y.Text` در سند، رشته در قرارداد، دیف در binder | پذیرفته‌شده |
| [ADR-035](#adr-035) | **Yjs** صاحبِ undo است، نه موتورِ رندر | پذیرفته‌شده |
| [ADR-036](#adr-036) | ephemeral کانالِ خودش را دارد، نه یک فیلد در stateِ awareness | پذیرفته‌شده |
| [ADR-037](#adr-037) | تخصیصِ `seq` برای هر بورد سریالی است | پذیرفته‌شده |
| [ADR-038](#adr-038) | `HB_ERROR` همیشه به معنای بستنِ اتصال نیست | پذیرفته‌شده |
| [ADR-039](#adr-039) | اتصالِ مجدد: کدِ بستن سیاست را تعیین می‌کند، نه یک backoffِ یکسان | پذیرفته‌شده |
| [ADR-040](#adr-040) | سازگاریِ نسخه را **خودِ کلاینت** می‌سنجد، نه سرور | پذیرفته‌شده |
| [ADR-041](#adr-041) | اتاق هنگام باز شدن حالتِ تازه‌تر را از نودهای دیگر می‌پرسد | پذیرفته‌شده |
| [ADR-042](#adr-042) | claimهای `rtToken` به `shared-types` — جایگزینِ قیدِ rtTokenِ ADR-031 | پذیرفته‌شده |
| [ADR-043](#adr-043) | `BoardRole` منبعِ حقیقتش به `shared-types` می‌رود (لمسِ `ydoc-schema`) | پذیرفته‌شده |
| [ADR-044](#adr-044) | آپلودِ دارایی با presigned POST + `content-length-range` (نه PUT) | پذیرفته‌شده |
| [ADR-045](#adr-045) | پاسخِ api دقیقاً شکلِ DTOی `shared-types` را می‌دهد (لایه‌ی serialize) | پذیرفته‌شده |
| [ADR-046](#adr-046) | `BoardAccessReader`ِ pg در یک پکیجِ مشترک — api و realtime یک منبع | پذیرفته‌شده |
| [ADR-047](#adr-047) | پیام‌های پروتکلِ کلاینت در `canvas-sync` می‌مانند؛ `apps/web` نمایششان می‌دهد | پذیرفته‌شده |
| [ADR-048](#adr-048) | room affinity (ADR-006 فاز ۳) به M5 موکول، با تریگرِ عددیِ حافظه | پذیرفته‌شده |

---

<a id="adr-001"></a>
## ADR-001 — Fastify به‌جای NestJS برای API

**وضعیت:** پذیرفته‌شده | **جایگزین‌ها:** NestJS، Hono، Express

**تصمیم:** `apps/api` روی **Fastify 5 + TypeScript** ساخته می‌شود، با یک ساختار پوشه‌ای اجباری (هر ماژول: `routes.ts` / `service.ts` / `repo.ts` / `schema.ts`) که نظم NestJS را بدون هزینه‌اش می‌دهد.

**دلیل:** NestJS نظم می‌دهد ولی سه هزینه دارد که برای این پروژه‌ی خاص جدی‌اند. اول، لایه‌ی DI و decorator ها حجم قابل‌توجهی boilerplate تولید می‌کنند که برای یک توسعه‌دهنده‌ی تنها فقط سطح کد را بالا می‌برد بدون اینکه مسئله‌ای حل کند — DI وقتی ارزش دارد که چند تیم روی یک کدبیس کار کنند. دوم، مصرف حافظه و زمان بوت NestJS محسوس بیشتر است و روی آروان‌کلاود مستقیماً به هزینه‌ی ماهانه تبدیل می‌شود؛ Fastify با همان منابع تعداد request بیشتری جواب می‌دهد. سوم و مهم‌تر، هم‌بوم یک API معمولی نیست: باید WebSocket، آپلود stream، و پاسخ باینری (snapshot سند Yjs) بدهد — Fastify در هر سه مستقیم‌تر و کم‌واسطه‌تر است. مزیت اضافه اینکه Fastify از JSON Schema برای اعتبارسنجی و serialization استفاده می‌کند و ما همان schema های zod را (با `zod-to-json-schema`) به OpenAPI تبدیل می‌کنیم، پس مستندسازی رایگان می‌آید. ریسک پذیرفته‌شده: بدون ساختار تحمیلی NestJS، انسجام کد به انضباط ما وابسته است — برای همین ساختار پوشه اجباری و ESLint rule برای جلوگیری از import های خلاف قاعده تعریف می‌شود.

**پیامدها:** هر ماژول API باید دقیقاً الگوی چهارفایلی را رعایت کند. هیچ منطق تجاری در `routes.ts` نوشته نمی‌شود. `buildApp()` بدون `listen` صادر می‌شود تا تست‌ها بدون پورت اجرا شوند.

---

<a id="adr-002"></a>
## ADR-002 — pnpm workspaces + Turborepo

**وضعیت:** پذیرفته‌شده | **جایگزین‌ها:** Nx، Lerna، npm workspaces خالی

**تصمیم:** pnpm workspaces برای مدیریت وابستگی، Turborepo فقط برای orchestration و کش build.

**دلیل:** pnpm با content-addressable store، فضای دیسک و زمان نصب را در یک مونوریپو با چند اپ React به‌شدت کم می‌کند، و مهم‌تر: `node_modules` تخت نمی‌سازد، پس اگر پکیجی وابستگی‌ای را declare نکرده باشد فوراً می‌شکند — این دقیقاً همان چیزی است که در پروژه‌ای با چند session موازی جلوی وابستگی‌های شبح‌وار را می‌گیرد. Turborepo نسبت به Nx سطح مفهومی بسیار کمتری دارد (فقط یک `turbo.json` با تعریف task ها و وابستگی‌شان) و همین برای پروژه‌ی ما کافی است؛ Nx قدرت بیشتری دارد ولی generator ها و plugin هایش لایه‌ی یادگیری اضافه‌ای می‌آورند که ارزشش را ندارد. کش build اهمیت عملی دارد چون هنگام کار روی `apps/api` نباید منتظر build دوباره‌ی `canvas-core` بمانیم.

---

<a id="adr-003"></a>
## ADR-003 — مسیر پلکانی Excalidraw: بسته npm → patch → فورک کامل

**وضعیت:** پذیرفته‌شده | ⚠️ **این یکی از دو تصمیم پرریسک پروژه است**

**تصمیم:** به‌جای فورک کردن Excalidraw از روز اول، یک مسیر سه‌پله‌ای با معیار عبور مشخص:

| پله | روش | معیار عبور به پله بعد |
|---|---|---|
| **A** | مصرف `@excalidraw/excalidraw` از npm + wrapper در `packages/canvas-core` | وقتی تغییری لازم شود که از طریق props / `customData` / `excalidrawAPI` / CSS override ممکن نباشد |
| **B** | همان + `pnpm patch` برای اصلاحات جراحی، هر patch مستند در `patches/README.md` | وقتی تعداد patch از **۸ فایل** یا **۴۰۰ خط** بگذرد، یا وقتی patch ها با هر ارتقای نسخه بشکنند |
| **C** | فورک کامل در `vendor/excalidraw` به‌عنوان git submodule، با فرآیند rebase مستند | — |

**دلیل:** فورک کردن یک کدبیس با ده‌ها هزار خط، برای یک توسعه‌دهنده‌ی تنها، یعنی از آن لحظه به بعد همه‌ی رفع‌باگ‌ها و بهبودهای بالادست را از دست می‌دهی یا باید دستی merge کنی — این هزینه‌ای است که تا سال‌ها پرداخت می‌شود و در ابتدای پروژه بسیار دست‌کم گرفته می‌شود. از طرف دیگر، انکار نمی‌کنم که هم‌بوم تغییرات عمیقی لازم دارد (RTL، نوع عنصر جدید، رفتار sticky) که احتمالاً از props نمی‌گذرند. راه‌حل این است که فورک را **به تعویق بیندازیم تا وقتی که دقیقاً بدانیم چه چیزی را می‌خواهیم تغییر دهیم** — نه اینکه از روز اول کورکورانه فورک کنیم. پله B با `pnpm patch` یک میانه‌ی واقعی است: تغییرات جراحی روی کد کتابخانه بدون از دست دادن ارتباط با بالادست، و هر patch به‌عنوان یک diff قابل‌بازخوانی باقی می‌ماند که در صورت فورک شدن مستقیماً تبدیل به commit می‌شود. اگر پله C اجتناب‌ناپذیر شد، این patch ها نقشه‌ی راه فورک هستند.

**پیامدها:** گام ۲ فایل [TODO.md](TODO.md) یک spike اجباری برای سنجش متن فارسی است. اگر آن spike نشان داد RTL بدون تغییر عمیق ممکن نیست، مستقیماً به پله B (نه C) می‌رویم و نتیجه اینجا به‌عنوان ADR-003b ثبت می‌شود. **نسخه‌ی Excalidraw باید pin شود (نه `^`) تا patch ها تصادفی نشکنند.**

**⚠️ الزام لایسنسی:** قبل از هر vendor کردن، فایل `LICENSE` مخزن Excalidraw و لایسنس جداگانه‌ی فونت‌های همراهش (Excalifont/Virgil و مشابه) باید جداگانه بررسی شوند. فونت‌های همراه استفاده نمی‌شوند — ما Vazirmatn را جایگزین می‌کنیم ([ADR-017](#adr-017)) — ولی نباید ناخواسته در bundle بمانند.

---

<a id="adr-004"></a>
## ADR-004 — Yjs به‌جای پروتکل اختصاصی excalidraw-room

**وضعیت:** پذیرفته‌شده

**تصمیم:** لایه‌ی همکاری با Yjs (CRDT) ساخته می‌شود؛ سرور `excalidraw-room` و مکانیزم reconciliation داخلی Excalidraw استفاده نمی‌شوند.

**دلیل:** `excalidraw-room` برای مدل «جلسه‌ی موقت با چند نفر» طراحی شده: یک relay رمزنگاری‌شده‌ی سرتاسری که کل صحنه را broadcast می‌کند و سرور محتوا را نمی‌فهمد. این برای Excalidraw عالی است ولی برای هم‌بوم سه مشکل دارد. اول، ما **باید** محتوای بورد را سمت سرور بفهمیم — برای thumbnail، جستجو، export، محدودیت اندازه، و بازیابی. دوم، مدل broadcast صحنه‌ی کامل با بوردهای بزرگ و ماندگار (نه جلسه‌ی موقت) به‌سرعت گران می‌شود. سوم، بدون CRDT واقعی، حل تعارض روی متن یک استیکی که دو نفر همزمان تایپ می‌کنند به‌درستی ممکن نیست. Yjs هر سه را حل می‌کند: ادغام قطعی و بدون تعارض، update های تفاضلی و کوچک، پشتیبانی بومی از ویرایش همزمان متن (`Y.Text`)، و کار آفلاین با همگام‌سازی بعدی. هزینه‌اش این است که باید خودمان binder بین Y.Doc و state موتور رندر بنویسیم — که کار دشواری است ولی محدود و یک‌بار.

**پیامدها:** بورد از دید ما هرگز «یک فایل JSON» نیست؛ یک لاگ از update هاست. Export به JSON یعنی materialize کردن سند، نه خواندن مستقیم.

---

<a id="adr-005"></a>
## ADR-005 — SQL خام + Kysely، بدون ORM سنگین

**وضعیت:** پذیرفته‌شده | **جایگزین‌ها:** Prisma، TypeORM، Drizzle

**تصمیم:** migration ها **SQL خام ترتیبی** در `apps/api/migrations/*.sql`. دسترسی به داده با **Kysely** (query builder با تایپ کامل) روی درایور `pg`.

**دلیل:** ORM هایی که schema را از روی مدل تولید می‌کنند (Prisma، TypeORM با synchronize) در پروژه‌ای که چند session مستقل روی آن کار می‌کنند خطرناک‌اند: یک تغییر ناهماهنگ در مدل، migration ای تولید می‌کند که ممکن است داده را نابود کند. با SQL خام، هر تغییر schema یک فایل قابل‌بازبینی است که دقیقاً همان چیزی است که روی دیتابیس اجرا می‌شود — هیچ لایه‌ی جادویی در میان نیست. Kysely برخلاف ORM ها هیچ runtime abstraction ای اضافه نمی‌کند؛ فقط SQL می‌سازد و تایپ‌ها را از یک interface دستی‌نوشته می‌گیرد. این یعنی هر query ای که در PostgreSQL ممکن است (CTE، window function، `FOR UPDATE SKIP LOCKED` برای صف پرداخت، partial index) بدون فرار به raw string نوشته می‌شود. Prisma به‌طور خاص برای این پروژه بد است چون engine باینری‌اش حجم image را بالا می‌برد و کنترل دقیق روی connection pool و تراکنش‌ها را سخت می‌کند — و ما برای پرداخت به تراکنش‌های دقیق نیاز داریم.

**پیامدها:** interface تایپ‌های دیتابیس (`apps/api/src/db/types.ts`) دستی نگهداری می‌شود و باید با هر migration به‌روز شود؛ یک تست که schema واقعی را با interface مقایسه می‌کند این را تضمین می‌کند.

---

<a id="adr-006"></a>
## ADR-006 — مقیاس‌پذیری WebSocket روی K8s آروان

**وضعیت:** پذیرفته‌شده | ⚠️ رفتار دقیق Ingress/LB آروان باید در زمان دیپلوی تایید شود

**تصمیم:** سه‌مرحله‌ای، به ترتیب زمان:

**فاز ۱ — تک‌نود (تا ~۲۰۰ کاربر همزمان):** یک replica از `apps/realtime`. همه‌ی اتاق‌ها در حافظه‌ی همان نود. ساده، بدون هیچ هماهنگی.

**فاز ۲ — چندنود با Redis pub/sub (تا ~۲۰۰۰ کاربر همزمان):** هر نود می‌تواند هر اتاقی را سرو کند. هر update دریافتی روی کانال `hb:board:<boardId>` در Redis منتشر می‌شود؛ نودهای دیگری که همان اتاق را باز دارند آن را روی Y.Doc محلی‌شان اعمال و به کلاینت‌هایشان پخش می‌کنند. awareness هم روی کانال جداگانه‌ی `hb:aware:<boardId>` (بدون پایداری). **این کار درست است چون update های Yjs جابه‌جایی‌پذیر (commutative) و idempotent اند** — ترتیب رسیدن و تکرار مهم نیست. برای جلوگیری از حلقه، هر پیام منتشرشده با `nodeId` مبدأ برچسب می‌خورد و نود مبدأ آن را نادیده می‌گیرد. پایداری در این فاز به یک نود «صاحب» واگذار می‌شود که با یک قفل Redis (`SET hb:owner:<boardId> <nodeId> NX EX 30` با تمدید دوره‌ای) انتخاب می‌شود، تا چند نود همزمان روی `board_updates` ننویسند.

**فاز ۳ — تمایل اتاق (room affinity):** اگر مصرف حافظه مسئله شد (چون در فاز ۲ هر نود نسخه‌ی کاملی از هر سند فعال دارد)، یک لایه‌ی مسیریابی با hashing سازگار (`boardId → node`) اضافه می‌شود تا هر بورد فقط روی یک نود زندگی کند. این کار Redis pub/sub را حذف نمی‌کند، فقط ترافیکش را تقریباً صفر می‌کند.

**دلیل:** ترتیب معکوس (شروع با sharding پیچیده) خطای رایج است. خاصیت CRDT بودن Yjs باعث می‌شود fanout ساده‌ی pub/sub در فاز ۲ **از نظر صحت کاملاً درست** باشد و فقط از نظر حافظه بهینه نباشد — و حافظه مسئله‌ای است که فقط در مقیاس واقعی معنا پیدا می‌کند. تا آن موقع پیچیدگی sharding فقط باگ تولید می‌کند.

**الزامات K8s آروان (⚠️ در زمان دیپلوی تایید شود):**
- Ingress باید WebSocket upgrade را پاس بدهد و timeout اتصال idle آن باید بیشتر از `RT_HEARTBEAT_INTERVAL_MS` (۲۵ثانیه) باشد. اگر لودبالانسر timeout سخت‌گیرانه‌ای دارد، heartbeat کوتاه‌تر شود.
- `sessionAffinity` روی Service برای فاز ۱ و ۲ **لازم نیست** (هر نود هر اتاقی را می‌پذیرد) ولی برای فاز ۳ الزامی می‌شود.
- `terminationGracePeriodSeconds` باید به اندازه‌ی کافی بلند باشد (≥۶۰ ثانیه) تا نود در حال خاموش‌شدن بتواند snapshot بگیرد و کلاینت‌ها را با کد بسته‌شدن مودبانه (`1001 Going Away`) بفرستد تا فوراً به نود دیگر وصل شوند.
- HPA بر اساس تعداد اتصال فعال (متریک سفارشی)، نه CPU — چون WebSocket بیشتر حافظه‌محور است تا CPU-محور.
- شبکه‌ی ایران: کلاینت باید اتصال مجدد با exponential backoff + jitter داشته باشد و update های آفلاین را در IndexedDB (`y-indexeddb`) صف کند.

---

<a id="adr-007"></a>
## ADR-007 — `Y.Map` کلیددار + fractional index برای عناصر

**وضعیت:** پذیرفته‌شده | **جایگزین‌ها:** `Y.Array` از عناصر، یک `Y.Map` تخت با کلید ترکیبی

**تصمیم:** `doc.getMap("elements")` که کلیدش `elementId` و مقدارش خودش یک `Y.Map` از property های آن عنصر است. ترتیب لایه‌ها (z-order) با یک property رشته‌ای `index` بر اساس **fractional indexing** مدیریت می‌شود.

**دلیل:** اگر عناصر در یک `Y.Array` باشند، ترتیب آرایه هم معنای z-order می‌دهد و هم محل عنصر — و این دو با هم تعارض ایجاد می‌کنند: وقتی دو نفر همزمان عنصری را اضافه یا مرتب می‌کنند، ایندکس‌ها جابه‌جا می‌شوند و اشاره‌گرها (مثلاً binding کانکتور) نامعتبر می‌شوند. با `Y.Map` کلیددار، دسترسی به هر عنصر O(1) و پایدار است. جدا کردن z-order به یک property رشته‌ای که با fractional indexing تولید می‌شود، اجازه می‌دهد «این را بین این دو ببر» به یک تغییر تک-property تبدیل شود که هیچ عنصر دیگری را لمس نمی‌کند — دقیقاً چیزی که CRDT در آن قوی است. اینکه هر عنصر خودش یک `Y.Map` است (نه یک آبجکت JSON) اهمیت عملی زیادی دارد: دو نفر می‌توانند همزمان رنگ و موقعیت یک استیکی را عوض کنند و هر دو تغییر بماند؛ با آبجکت ساده، آخرین نویسنده کل عنصر را بازنویسی می‌کرد.

**هزینه‌ی پذیرفته‌شده:** `Y.Map` تودرتو سربار حافظه‌ی بیشتری از آبجکت ساده دارد. برای property های آرایه‌ای که ویرایش همزمانشان بی‌معنی است (`points` یک خط، `groupIds`) مقدار به‌صورت آرایه‌ی ساده ذخیره می‌شود و رفتار LWW دارد — این عمدی است.

---

<a id="adr-008"></a>
## ADR-008 — محاسبه‌ی قطعی کانکتور به‌جای همگام‌سازی نقاط

**وضعیت:** پذیرفته‌شده

**تصمیم:** مسیر یک کانکتور متصل، از روی `startBinding`/`endBinding` و هندسه‌ی دو سر، در **هر کلاینت به‌صورت مستقل و قطعی** محاسبه و رندر می‌شود. آرایه‌ی `points` در Y.Doc فقط توسط کلاینتی نوشته می‌شود که کاربرش عملاً کانکتور یا یکی از دو سرش را دستکاری کرده، و فقط یک‌بار در پایان ژست.

**دلیل:** اگر هر بار که کسی یک استیکی را حرکت می‌دهد، نقاط همه‌ی کانکتورهای متصل هم در CRDT نوشته شوند، حجم ترافیک به‌شدت بالا می‌رود و بدتر: دو کلاینت که همزمان دو سر یک کانکتور را حرکت می‌دهند، `points` را همزمان بازنویسی می‌کنند و نتیجه می‌پرد. با تلقی مسیر به‌عنوان یک **تابع مشتق‌شده از حالت** (derived state) به‌جای حالت ذخیره‌شده، این کلاس از تعارض کاملاً حذف می‌شود. شرط لازم این است که تابع مسیریابی خالص و قطعی باشد: ورودی یکسان در دو مرورگر مختلف باید خروجی بیت‌به‌بیت یکسان بدهد — پس هیچ `Math.random`، هیچ وابستگی به زمان، و دقت اعشاری کنترل‌شده (گرد کردن به ۲ رقم).

---

<a id="adr-009"></a>
## ADR-009 — پایداری سند: update log در Postgres + snapshot در Object Storage

**وضعیت:** پذیرفته‌شده | **جایگزین‌ها:** ذخیره‌ی state کامل در هر تغییر، y-leveldb، y-redis

**تصمیم:** هر update باینری Yjs به‌صورت append در جدول `board_updates` نوشته می‌شود. هر `RT_SNAPSHOT_EVERY_UPDATES` update یا `RT_SNAPSHOT_EVERY_MS` میلی‌ثانیه (هر کدام زودتر)، سند فشرده و به‌عنوان یک شیء در bucket snapshots ذخیره و رکورد آن در `board_snapshots` ثبت می‌شود؛ سپس update های قدیمی‌تر از آن snapshot حذف می‌شوند. بارگذاری اتاق = آخرین snapshot + update های بعد از آن.

**دلیل:** نوشتن state کامل در هر تغییر برای بورد بزرگ فاجعه است (هر جابه‌جایی یک استیکی = نوشتن چند مگابایت). لاگ تفاضلی این را حل می‌کند ولی اگر هرگز فشرده نشود، بارگذاری بورد قدیمی کند می‌شود — پس snapshot لازم است. اینکه لاگ در Postgres و snapshot در Object Storage است عمدی است: لاگ کوچک، پرتراکنش و نیازمند تضمین ترتیب و دوام است (کار Postgres)؛ snapshot بزرگ، کم‌تعداد و فقط-خواندنی است (کار Object Storage، که به‌مراتب ارزان‌تر است). y-leveldb برای استقرار چندنود روی K8s مناسب نیست (فایل محلی) و y-redis داده را در حافظه‌ی گران نگه می‌دارد.

**تضمین دوام:** update قبل از ack به کلاینت نوشته می‌شود، یا حداکثر با ۱ ثانیه تاخیر batch — تصمیم دقیقش در پیاده‌سازی M2 با اندازه‌گیری تاخیر گرفته می‌شود و اینجا به‌روز می‌شود. نشانگر «ذخیره شد» در UI باید حقیقت را نشان دهد، نه خوش‌بینی.

★ **به‌روزرسانیِ گام ۴٫۳ (۱۴۰۵/۰۵/۱۶) — عدد اندازه‌گیری شد و batch لازم نشد.** فاصله‌ی بینِ فرستادنِ یک ژست و رسیدنِ `saved`ِ واقعی روی Postgresِ لوکال **۳۰ تا ۳۵ میلی‌ثانیه** بود (شش اجرا: ۳۳/۳۰/۳۵/۳۱/۳۴/۳۲). این یعنی سقفِ ۱ ثانیه‌ای **مصرف نشد**: نوشتنِ همزمان (بدونِ batch) گران‌تر از آستانه‌ی ادراکِ کاربر نیست، و batch در عوض یک پنجره‌ی از‌دست‌رفتنی می‌ساخت که دقیقاً همان چیزی است که این ADR می‌خواست نبیند. پس **مسیرِ همزمان انتخاب شد** و بندِ «حداکثر ۱ ثانیه» به‌عنوان مجوزِ ذخیره‌ی گروهی **استفاده نمی‌شود**؛ اگر روزی زیرِ بار لازم شد، یک ADRِ جدید می‌خواهد نه تکیه به این جمله.

⚠️ **عدد به محیط گره خورده است:** Postgresِ همان ماشین. با دیتابیسِ شبکه‌ای این عدد بالا می‌رود و باید **دوباره** اندازه‌گیری شود — ادعای «۳۰ms» ادعای معماری نیست، مشاهده‌ی یک محیط است. سنجه‌اش [`scripts/rt-durability.ts`](scripts/rt-durability.ts) است که همان‌جا چاپش می‌کند.

---

<a id="adr-010"></a>
## ADR-010 — جدایی `type` رندر از `kind` محصولی

**وضعیت:** پذیرفته‌شده

**تصمیم:** هر عنصر دو برچسب دارد: `type` که موتور رندر می‌فهمد (`rectangle`, `arrow`, `text`, …) و `customData.hb.kind` که محصول می‌فهمد (`sticky`, `connector`, `frame`, `card`, …).

**دلیل:** افزودن یک نوع عنصر جدید به موتور رندر Excalidraw یعنی دست‌زدن به ده‌ها نقطه از کدبیس بالادست (رندر، hit-test، سریال‌سازی، تبدیل، undo). با این تفکیک، یک استیکی‌نوت از دید موتور فقط یک مستطیل با متن مقید است — یعنی همه‌ی رفتارهای موجود (انتخاب، تغییر اندازه، چرخش، گروه‌بندی، undo) رایگان کار می‌کنند — ولی از دید هم‌بوم یک موجودیت درجه‌یک با ابزار، پالت و رفتار خاص خودش است. این تصمیم مستقیماً هزینه‌ی [ADR-003](#adr-003) را کم می‌کند: هرچه بیشتر بتوانیم معنا را در `customData` بگذاریم، دیرتر مجبور به فورک می‌شویم.

**پیامد:** هیچ کد محصولی نباید مستقیماً روی `type` شرط بگذارد؛ همیشه از `getKind(element)` استفاده شود. یک ESLint rule سفارشی این را چک می‌کند.

---

<a id="adr-011"></a>
## ADR-011 — احراز هویت: OTP + JWT کوتاه‌عمر + refresh چرخشی

**وضعیت:** پذیرفته‌شده

**تصمیم:** ورود با شماره موبایل + کد یک‌بارمصرف پیامکی. خروجی: access token از نوع JWT با عمر ۱۵ دقیقه (در حافظه‌ی جاوااسکریپت، نه localStorage) + refresh token مبهم و تصادفی در cookie با `HttpOnly; Secure; SameSite=Lax`. هر refresh، توکن قبلی را باطل و توکن جدید صادر می‌کند (rotation)؛ استفاده‌ی مجدد از توکن باطل‌شده کل خانواده‌ی session را می‌سوزاند (reuse detection).

**دلیل:** access token کوتاه‌عمر و JWT است چون سرور realtime باید بتواند آن را **بدون تماس با API** اعتبارسنجی کند — این برای مقیاس WebSocket حیاتی است. اما JWT قابل ابطال نیست، پس عمرش باید کوتاه باشد و منبع واقعی session یک refresh token مات در دیتابیس است که هر لحظه قابل ابطال است. نگه‌داشتن access token فقط در حافظه (نه localStorage) سطح حمله‌ی XSS را کم می‌کند. rotation با تشخیص reuse استاندارد صنعتی برای مقابله با سرقت توکن است. OTP پیامکی به‌جای رمز عبور، هم با انتظار کاربر ایرانی هم‌خوان است و هم کلاس کاملی از مسائل (نشت رمز، بازیابی رمز، رمز ضعیف) را حذف می‌کند — به قیمت هزینه‌ی پیامک و وابستگی به درگاه SMS، که با محدودیت نرخ سخت‌گیرانه و OTP ثابت در محیط لوکال مدیریت می‌شود.

**اتصال WebSocket:** توکن اتصال یک JWT جداگانه با عمر ۶۰ ثانیه و scope مخصوص یک `boardId` است ([`GET /boards/:id/rt-token`]) — تا access token اصلی در query string ظاهر نشود (query string در لاگ لودبالانسر می‌ماند).

---

<a id="adr-012"></a>
## ADR-012 — نقش موثر (effective role) به‌جای ACL تخت

**وضعیت:** پذیرفته‌شده

**تصمیم:** دسترسی کاربر به یک بورد یک مقدار **محاسبه‌شده** است، نه یک ردیف ذخیره‌شده. تابع `effectiveBoardRole(user, board, team, link)` نقش نهایی را از ترکیب اینها می‌گیرد و **بیشترین** را برمی‌گرداند: نقش staff پلتفرم → مالک بورد → نقش مستقیم در `board_members` → نقش عضویت در تیم نگاشته‌شده به نقش بورد → دسترسی لینک اشتراک. این تابع در `packages/auth-core` است و **هم API و هم سرور realtime از همان یک پیاده‌سازی استفاده می‌کنند**.

**دلیل:** اگر دسترسی به‌صورت ردیف‌های ACL ذخیره شود، هر تغییر نقش تیمی باید در ده‌ها بورد propagate شود — و هر جایی که یادت برود، یک حفره‌ی امنیتی است. محاسبه در لحظه این کلاس از باگ را کاملاً حذف می‌کند و قیمتش یک query اضافه است که کش‌شدنی است. اینکه API و realtime از یک تابع مشترک استفاده می‌کنند حیاتی است: رایج‌ترین باگ امنیتی در محصولات مشابه این است که REST درست چک می‌کند ولی WebSocket نه، و کسی با نقش viewer می‌تواند مستقیماً update بفرستد. **سرور realtime باید نقش را در هر update بررسی کند، نه فقط هنگام اتصال** — چون نقش ممکن است وسط session عوض شود.

---

<a id="adr-013"></a>
## ADR-013 — abstraction اجباری روی Object Storage

**وضعیت:** پذیرفته‌شده

**تصمیم:** فقط `packages/storage` مجاز به import کردن `@aws-sdk/client-s3` است. رابط صادرشده: `putObject`، `getObject`، `deleteObject`، `presignPut`، `presignGet`، `headObject`، `listPrefix`. هیچ نام سرویسی (`minio`، `arvan`) در امضای توابع نیست.

**دلیل:** این تنها نقطه‌ای است که وعده‌ی «سوییچ از MinIO به آروان فقط با env» عملاً تضمین می‌شود. تجربه نشان می‌دهد که اگر SDK مستقیماً در چند ماژول import شود، تفاوت‌های ریز پیاده‌سازی‌ها (path-style در برابر virtual-host، رفتار presign، پشتیبانی از multipart، header های CORS) در کد نشت می‌کنند و مهاجرت به یک پروژه‌ی چندروزه تبدیل می‌شود. با یک نقطه‌ی تماس، تفاوت‌ها همان‌جا با یک شرط روی config حل می‌شوند. `S3_FORCE_PATH_STYLE` عمداً یک متغیر مستقل است چون همین یک مورد رایج‌ترین علت شکست مهاجرت بین سرویس‌های S3-compatible است.

**تایید شده (۱۴۰۵/۰۴/۳۱، توسط مالک):** آروان از presigned URL پشتیبانی می‌کند، پس مسیر آپلود مستقیم کلاینت→Object Storage قطعی است و مسیر جایگزین (آپلود stream از طریق API) لازم نیست پیاده شود. تنظیمات CORS باکت همچنان در زمان ساخت حساب باید ست شود.

---

<a id="adr-014"></a>
## ADR-014 — زرین‌پال با آداپتور درگاه + idempotency سخت‌گیرانه

**وضعیت:** پذیرفته‌شده | **تایید شده (۱۴۰۵/۰۴/۳۱، توسط مالک):** حالت sandbox زرین‌پال فقط با یک فلگ فعال می‌شود و نیازی به تاییدیه‌ی کسب‌وکار ندارد — پس کل جریان از روز اول قابل ساخت و تست است و `ZARINPAL_MODE` تنها سوییچ لازم است.

**تصمیم:** یک interface به نام `PaymentGateway` با سه متد (`createPayment`، `verifyPayment`، `refund?`) تعریف می‌شود. پیاده‌سازی‌ها: `ZarinpalGateway`، `MockGateway` (برای تست خودکار)، و بعداً `IdpayGateway`. جریان: ساخت رکورد `payments` با وضعیت `pending` و `idempotency_key` → درخواست به درگاه → ذخیره‌ی `authority` → ریدایرکت کاربر → callback → **verify سمت سرور** → فعال‌سازی اشتراک در همان تراکنش دیتابیس.

**دلیل:** جدا کردن آداپتور از منطق تجاری از روز اول (نه بعداً) لازم است چون آیدی‌پی در نقشه‌ی راه هست و چون بدون `MockGateway` نمی‌شود جریان پرداخت را در CI تست کرد. سه قاعده‌ی غیرقابل‌مذاکره در پیاده‌سازی:

1. **هیچ‌گاه به پارامترهای callback اعتماد نکن.** `Status=OK` در URL برگشتی صرفاً یک ادعای مرورگر است. اشتراک فقط پس از پاسخ موفق `verify` مستقیم سرور-به-سرور فعال می‌شود.
2. **verify باید idempotent باشد.** کاربر ممکن است روی callback رفرش بزند. رکورد پرداخت با `SELECT … FOR UPDATE` قفل می‌شود؛ اگر قبلاً `paid` است، همان نتیجه برگردانده می‌شود بدون فعال‌سازی دوباره.
3. **آشتی‌دهی (reconciliation) اجباری است.** کاربر ممکن است پول بدهد و قبل از برگشت، مرورگر را ببندد. یک job دوره‌ای همه‌ی پرداخت‌های `pending` قدیمی‌تر از N دقیقه را دوباره verify می‌کند. بدون این، پول گرفته می‌شود و سرویس داده نمی‌شود — که هم مشکل حقوقی است هم اعتماد را می‌سوزاند.

مبلغ ارسالی به درگاه همیشه از سرور محاسبه می‌شود؛ هیچ مبلغی از کلاینت پذیرفته نمی‌شود.

---

<a id="adr-015"></a>
## ADR-015 — پول به‌صورت `BIGINT` ریال

**وضعیت:** پذیرفته‌شده

**تصمیم:** همه‌ی مبالغ در دیتابیس، API و منطق تجاری، عدد صحیح **ریال** در `bigint` هستند. تبدیل به تومان (تقسیم بر ۱۰) و قالب‌بندی با جداکننده‌ی هزارگان و ارقام فارسی، فقط در لایه‌ی نمایش انجام می‌شود.

**دلیل:** استفاده از اعداد اعشاری برای پول همیشه غلط است، ولی در ایران یک تله‌ی اضافه هم هست: ابهام ریال/تومان. اگر بعضی جاها تومان و بعضی جاها ریال ذخیره شود، دیر یا زود یک ضریب ۱۰ در فاکتور کسی ظاهر می‌شود. انتخاب ریال (نه تومان) به این دلیل است که واحد رسمی و واحد پذیرفته‌شده‌ی درگاه‌های پرداخت است، پس در مرز سیستم تبدیلی لازم نیست. `bigint` نه `integer`، چون مبالغ سالانه‌ی تیم بزرگ به‌راحتی از سقف ۲٫۱ میلیارد `int4` می‌گذرد. در جاوااسکریپت این مقادیر تا `Number.MAX_SAFE_INTEGER` امن‌اند (≈۹ کوادریلیون ریال)، ولی درایور Postgres باید طوری تنظیم شود که `int8` را به `number` تبدیل کند نه `string`، و این در یک جا (`packages/config` یا bootstrap دیتابیس) تنظیم و مستند شود.

---

<a id="adr-016"></a>
## ADR-016 — RTL: logical properties + `dir` روی ریشه

**وضعیت:** پذیرفته‌شده

**تصمیم:** `<html dir="rtl" lang="fa">`. در CSS، **فقط** از logical properties استفاده می‌شود: `margin-inline-start` به‌جای `margin-left`، `padding-inline`، `inset-inline-start`، `border-inline-end`، `text-align: start`. هیچ کلاس `rtl:` شرطی و هیچ stylesheet جداگانه‌ی RTL ساخته نمی‌شود. یک ESLint/Stylelint rule استفاده از property های فیزیکی جهت‌دار را خطا می‌کند.

**دلیل:** الگوی رایج «یک CSS برای LTR و یک نسخه‌ی آینه‌ای برای RTL» دو برابر کد و دو برابر باگ تولید می‌کند، و همیشه یک گوشه‌ای فراموش می‌شود. logical properties در همه‌ی مرورگرهای مدرن پشتیبانی می‌شوند و مسئله را در ریشه حل می‌کنند: کد یک‌بار نوشته می‌شود و مرورگر جهت را اعمال می‌کند. این تصمیم باید **از خط اول** اعمال شود؛ تبدیل بعدی یک بازنویسی کامل است.

**استثنای مهم — بوم:** فضای مختصات بوم ریاضی است و RTL ندارد. `x` همیشه به راست افزایش می‌یابد. RTL فقط به **متن داخل عناصر** و **رابط کاربری اطراف بوم** مربوط می‌شود. این تفکیک باید در `canvas-core` کاملاً صریح باشد وگرنه به یک کلاس گیج‌کننده از باگ‌های هندسی می‌رسیم.

---

<a id="adr-017"></a>
## ADR-017 — فونت Vazirmatn، خودمیزبان

**وضعیت:** پذیرفته‌شده | **نیاز به تایید مالک برای انتخاب نهایی (Q9 در PLAN.md)**

**تصمیم:** فونت اصلی رابط و بوم: **Vazirmatn** با لایسنس SIL Open Font License 1.1. فایل‌ها در `apps/web/public/fonts/` میزبانی می‌شوند، با `font-display: swap` و subset فارسی + لاتین + ارقام. هیچ درخواستی به Google Fonts یا CDN خارجی زده نمی‌شود (اصل P2).

**دلیل:** SIL OFL اجازه‌ی استفاده‌ی تجاری، توزیع و حتی تغییر (با شرط تغییر نام) را می‌دهد، پس با محدودیت لایسنسی پروژه سازگار است. Vazirmatn متغیر (variable font) است و همه‌ی وزن‌ها را در یک فایل می‌دهد که برای حجم bundle خوب است، و ارقام فارسی و لاتین هر دو را با کیفیت دارد. خودمیزبانی کردن نه فقط به‌خاطر اصل P2، بلکه به‌خاطر دسترس‌پذیری از داخل ایران هم ضروری است.

**نکته‌ی فنی مهم برای بوم:** رندر متن روی canvas به اندازه‌گیری دقیق نیاز دارد. فونت باید **قبل از اولین رندر بوم** لود شده باشد (`document.fonts.ready`)، وگرنه عرض‌های اندازه‌گیری‌شده با فونت fallback محاسبه و متن بعداً جابه‌جا می‌شود. `canvas-core` باید یک gate صریح برای این داشته باشد.

---

<a id="adr-018"></a>
## ADR-018 — تاریخ: UTC در ذخیره، جلالی در نمایش

**وضعیت:** پذیرفته‌شده

**تصمیم:** همه‌ی زمان‌ها در دیتابیس `timestamptz` و در UTC. همه‌ی زمان‌ها در API به‌صورت ISO-8601 با `Z`. تبدیل به تقویم جلالی و منطقه‌ی زمانی تهران **فقط** در لایه‌ی نمایش، از طریق `packages/i18n` با `Intl.DateTimeFormat` و کلندر `persian` (بومی مرورگر، بدون کتابخانه‌ی اضافه).

**دلیل:** ذخیره‌ی تاریخ جلالی در دیتابیس، محاسبات بازه‌ای (مثلاً «اشتراک‌هایی که در ۷ روز آینده منقضی می‌شوند») را به کابوس تبدیل می‌کند. `Intl` با کلندر persian در همه‌ی مرورگرهای مدرن و Node پشتیبانی می‌شود، پس نیازی به کتابخانه‌ی تبدیل تاریخ نیست. یک استثنا: **شماره‌ی فاکتور** سال شمسی را در خود دارد (`HB-1405-000123`) — این یک رشته‌ی نمایشی است، نه یک تاریخ، و در زمان صدور از `issued_at` محاسبه و ذخیره می‌شود.

---

<a id="adr-019"></a>
## ADR-019 — Export سمت سرور با Chromium headless

**وضعیت:** پذیرفته‌شده | **جایگزین:** export سمت کلاینت

**تصمیم:** خروجی PNG/PDF در `apps/worker` با Playwright + Chromium تولید می‌شود: worker یک صفحه‌ی مخصوص render را باز می‌کند، سند را از snapshot لود می‌کند، و تصویر می‌گیرد. خروجی SVG و JSON بدون مرورگر و مستقیماً از سند تولید می‌شوند. کلاینت هم برای بوردهای کوچک export فوری سمت مرورگر دارد (مسیر سریع)، ولی مسیر معتبر و مقیاس‌پذیر سرور است.

**دلیل:** export سمت کلاینت برای بورد بزرگ مرورگر کاربر را قفل می‌کند یا حافظه کم می‌آورد، و برای PDF چندصفحه‌ای عملاً کار نمی‌کند. مهم‌تر: export سرور امکان استفاده در thumbnail خودکار، پیش‌نمایش لینک اشتراک، و ایمیل را هم می‌دهد. هزینه‌اش یک image سنگین (Chromium ≈ ۴۰۰MB) است — برای همین worker یک Deployment جدا با منابع خودش است، نه بخشی از API.

---

<a id="adr-020"></a>
## ADR-020 — مانیتورینگ و لاگ خودمیزبان

**وضعیت:** پذیرفته‌شده

**تصمیم:** لاگ ساخت‌یافته با `pino` (JSON، با `requestId` در همه‌ی خطوط). متریک و trace با OpenTelemetry SDK → Prometheus + Grafana + (اختیاری) Tempo، همه خودمیزبان روی همان کلاستر. ردیابی خطا با یک نمونه‌ی **self-hosted** سازگار با Sentry، یا در نبود آن یک جدول ساده‌ی `error_events`.

**دلیل:** اصل P2 هر SaaS خارجی را رد می‌کند، و مستقل از آن، فرستادن لاگ محصول ایرانی به سرور خارجی هم ریسک دسترسی دارد هم ریسک حریم خصوصی کاربران. OpenTelemetry انتخاب شده چون استاندارد و vendor-neutral است؛ اگر بعداً به راهکار دیگری مهاجرت کردیم، instrumentation دوباره نوشته نمی‌شود. اصل P7 (بدون PII در لاگ) با یک redactor در پیکربندی pino اعمال می‌شود که کلیدهای حساس (`phone`, `code`, `token`, `authority`, `password`) را به‌صورت خودکار ماسک می‌کند — و این باید در `packages/config` مرکزی باشد نه در هر اپ.

---

<a id="adr-021"></a>
## ADR-021 — `packages/shared-types` تنها قرارداد بین ماژول‌ها

**وضعیت:** پذیرفته‌شده

**تصمیم:** همه‌ی شکل‌های داده‌ای که از مرز یک ماژول عبور می‌کنند (DTO های API، انواع عناصر، کدهای خطا، enum ها، پیام‌های WebSocket) در `packages/shared-types` با **zod** تعریف می‌شوند و type ها با `z.infer` استخراج می‌شوند. هیچ ماژولی اجازه‌ی تعریف موازی همان شکل را ندارد.

**دلیل:** این پروژه با چند session مستقل (و شاید موازی، در worktree های جدا) ساخته می‌شود. بدون یک قرارداد واحد و اجباری، هر session نسخه‌ی خودش از `Board` را تعریف می‌کند و در نقطه‌ی ادغام همه‌چیز می‌شکند. zod به‌جای interface خالص انتخاب شده چون یک تعریف، هم تایپ زمان کامپایل می‌دهد، هم اعتبارسنجی زمان اجرا، هم schema برای OpenAPI — سه چیز که در غیر این صورت باید جدا نگه داشته می‌شدند و از هم دور می‌افتادند.

**قاعده‌ی فرآیندی:** تغییر `packages/shared-types` باید **دستی توسط مالک پروژه تایید شود**. هیچ session ای نباید بدون اطلاع، این پکیج را تغییر دهد — چون تغییرش همه‌ی ماژول‌های دیگر را می‌شکند. اگر session ای به تغییری نیاز داشت، پیشنهادش را در `PROGRESS.md` بنویسد و منتظر بماند.

---

<a id="adr-022"></a>
## ADR-022 — استروک freedraw و حرکات موقت از کانال ephemeral

**وضعیت:** پذیرفته‌شده

**تصمیم:** هر داده‌ای که «در حال انجام» است و ارزش ماندگاری ندارد — استروک قلم قبل از رها شدن، لیزر پوینتر، ری‌اکشن‌های شناور، پیش‌نمایش کشیدن یک شکل — از کانال `HB_EPHEMERAL` (روی awareness) پخش می‌شود و **هرگز وارد Y.Doc نمی‌شود**. فقط نتیجه‌ی نهایی در `pointerup` به‌عنوان یک عنصر commit می‌شود، پس از ساده‌سازی مسیر.

**دلیل:** یک استروک قلم متوسط ۲۰۰ تا ۵۰۰ نقطه دارد که در طول ۲ ثانیه تولید می‌شوند. اگر هر نقطه یک update در CRDT باشد، یک خط ساده صدها ورودی در تاریخچه، صدها پیام شبکه و رشد دائمی سند تولید می‌کند — و این تاریخچه هرگز جمع نمی‌شود چون CRDT حذف واقعی ندارد. تفکیک «داده‌ی در حال شکل‌گیری» از «داده‌ی نهایی» این را کاملاً حل می‌کند و به‌عنوان یک اصل معماری (نه یک بهینه‌سازی) ثبت می‌شود، چون همین الگو برای هر ابزار آینده‌ای (کشیدن شکل، تغییر اندازه‌ی زنده) هم صادق است.

---

<a id="adr-023"></a>
## ADR-023 — متن فارسی: فونت با CSS، جهت با یک patch

**وضعیت:** پذیرفته‌شده (تایید مالک، ۱۴۰۵/۰۴/۳۱) | **شواهد:** [docs/spike-persian-text.md](docs/spike-persian-text.md)

**تصمیم:** پشتیبانی فارسی روی بوم با سه لایه ساخته می‌شود، به ترتیب کم‌هزینه به پرهزینه:

1. **فونت — بدون patch.** Vazirmatn زیر نام خانواده‌ی خودِ موتور (`Excalifont` و
   بقیه) با `unicode-range` محدود به بازه‌ی عربی ثبت می‌شود. مرورگر برای هر کاراکتر
   face متناسب را برمی‌دارد، پس فارسی با Vazirmatn و لاتین با فونت اصلی رندر می‌شود.
2. **اندازه‌گیری — بدون patch.** مسیر اندازه‌گیری موتور برای فارسی درست است. اگر
   بعداً جایی خطا دیدیم، `setCustomTextMetricsProvider` یک API عمومی برای
   جایگزینی `getLineWidth` است.
3. **جهت (bidi) — یک patch.** موتور `ctx.direction` را هرگز ست نمی‌کند، پس متن
   فارسی با جهت پایه‌ی LTR چیده می‌شود و نویسه‌های خنثی (نشانه‌گذاری، پرانتز، عدد
   لاتین) در دو سر خط جای اشتباه می‌افتند. این تنها موردی است که از طریق props،
   `customData`، `excalidrawAPI` یا CSS قابل حل نیست.

**دلیل:** spike گام ۱٫۳ نشان داد سه ترس اصلی ما بی‌مورد بوده‌اند. شکل‌دهی حروف
کار می‌کند — اثباتش عددی است: رشته‌ی فارسی تا ۳۸٪ باریک‌تر از جمع عرض تک‌تک
کاراکترهایش اندازه‌گیری می‌شود، در حالی که شاهد لاتین دقیقاً برابر است؛ این فقط
وقتی ممکن است که حروف به شکل چسبان رندر شوند. شکست خط روی مرز کلمه می‌افتد و
نیم‌فاصله را نمی‌شکند. اندازه‌گیری عرض هم درست است — انحراف باقی‌مانده روی شاهد
لاتین از فارسی بیشتر است، پس مسئله‌ی فارسی نیست. در مقابل، `ctx.direction` روی
بوم زنده `"ltr"` است و آزمون هش پیکسلی ثابت کرد این مقدار خروجی رندر را واقعاً
عوض می‌کند. ترتیب لایه‌ها عمدی است: هرچه بیشتر بتوانیم با CSS و API عمومی حل کنیم،
سطح تماس با کد بالادست کوچک‌تر می‌ماند و [ADR-003](#adr-003) دیرتر به فورک می‌رسد.

**پیامدها:** پروژه از پله‌ی A به **پله‌ی B** در ADR-003 می‌رود — با دقیقاً یک patch.
`ENGINE_STAGE` در `packages/canvas-core/src/index.ts` باید به `"patch"` برود
(یک تست این مقدار را می‌پاید). هر patch باید در `patches/README.md` ثبت شود.
نسخه‌ی `@excalidraw/excalidraw` از قبل pin شده است، پس ارتقا یک تصمیم آگاهانه
خواهد بود نه اثر جانبی.

**دو محدودیت پذیرفته‌شده:**

- کلمه‌ای که از ظرفش پهن‌تر باشد، وسط کلمه شکسته می‌شود و اتصال حروف پاره می‌شود
  (اثبات عددی: جمع عرض خطوط ۳٫۶٪ از عرض کل کلمه بیشتر می‌شود). فقط در حالت
  مصنوعی رخ می‌دهد؛ رفع نمی‌شود.
- فارسی از فهرست زبان‌های رابط موتور حذف شده (ترجمه‌ی ۸۴٪ در برابر آستانه‌ی ۸۵٪).
  بی‌اهمیت است چون رابط کاربری را در گام ۴٫۲ کاملاً با رابط فارسی خودمان
  جایگزین می‌کنیم.

**⚠️ موردی که هنوز اثبات نشده:** ترتیب bidi تنها بخشی از spike است که چشمی تایید
نشده — امکان اسکرین‌شات در session انجام spike در دسترس نبود. شواهد عددی قطعی‌اند،
ولی تایید بصری روی صفحه‌ی `#spike` با مالک است.

---

<a id="adr-024"></a>
## ADR-024 — تشخیص جهت متن با اکثریت کاراکترها، نه با اولین کاراکتر قوی

**وضعیت:** پذیرفته‌شده | **شواهد:** [spike بخش ۱۰](docs/spike-persian-text.md)
**جایگزین رد شده:** الگوریتم استاندارد `dir="auto"` (اولین کاراکتر قوی)

**تصمیم:** `detectBaseDirection(text)` در `packages/canvas-core/src/text/bidi.ts`
جهت پایه را از **نسبت کاراکترهای قوی RTL به کل کاراکترهای قوی** تعیین می‌کند، نه
از اولین کاراکتر قوی. هر عنصر متنی یک property صریح
`direction: "rtl" | "ltr" | "auto"` دارد (طبق [PLAN بخش ۷٫۳](PLAN.md)) و مقدار
`auto` با همین تابع حل می‌شود — نه با `dir="auto"` مرورگر.

**دلیل:** الگوریتم استاندارد یونیکد برای `dir="auto"` جهت را از اولین کاراکتر قوی
می‌گیرد. spike نشان داد این برای اعداد مشکلی نمی‌سازد (رقم‌ها «قوی» نیستند، پس
«۱۲۳ مورد از ۴۵۶» و «456 مورد باقی مانده» هر دو درست `rtl` می‌شوند) و برای
گیومه‌ی فارسی هم مشکلی ندارد. ولی روی رشته‌ای که با یک **کلمه‌ی لاتین** شروع شود
شکست می‌خورد: «board برای تیم ماست» جهت `ltr` می‌گیرد در حالی که یک جمله‌ی فارسی
است. این الگو در یک بورد فارسی نادر نیست — نام ابزار، اصطلاح فنی یا کلمه‌ی
انگلیسی در ابتدای جمله کاملاً رایج است. شمارش اکثریت این مورد را درست حل می‌کند و
هزینه‌اش یک پیمایش رشته است. جایی که heuristic کافی نیست، کاربر می‌تواند جهت را
صریح روی عنصر ست کند — به همین دلیل property `direction` از ابتدا در مدل داده هست.

**پیامدها:** این تابع در **سه جا** مصرف می‌شود و هر سه باید یک منبع داشته باشند:
مقدار `ctx.direction` در patch رندر (ADR-023)، صفت `dir` روی textarea ویرایشگر
(به‌جای `auto`)، و پیش‌فرض `textAlign` عنصر. اگر این سه از هم جدا شوند، متن هنگام
ورود و خروج از حالت ویرایش می‌پرد.

---

<a id="adr-025"></a>
## ADR-025 — wrapper روی `fillText` به‌جای patch، مشروط به تایید تجربی

**وضعیت:** پذیرفته‌شده (تایید تجربی + تایید مالک، ۱۴۰۵/۰۴/۳۱) | **جایگزین می‌کند:** بخش patch در [ADR-023](#adr-023)

**تصمیم:** به‌جای اعمال patch روی کد minify شده‌ی موتور، متد
`CanvasRenderingContext2D.prototype.fillText` (و `strokeText`، و معادل‌های
`OffscreenCanvas`) در زمان اجرا wrap می‌شوند و `ctx.direction` از روی **خود متنی
که رسم می‌شود** ست می‌گردد. پیاده‌سازی: `packages/canvas-core/src/engine/canvas-direction.ts`.

**دلیل:** تنها کاری که patch تاییدشده (P-1) قرار بود بکند این بود که قبل از
`fillText` مقدار `ctx.direction` را از روی متن ست کند. ولی خودِ `fillText(text, …)`
متن را به‌عنوان آرگومان می‌دهد — پس همان کار از بیرون هم شدنی است. مزیت‌ها:
پروژه روی پله‌ی A از [ADR-003](#adr-003) می‌ماند، با ارتقای نسخه‌ی پکیج نمی‌شکند
(به هیچ نام minify شده‌ای گره نمی‌خورد)، منطق در کد خودمان است و مستقیماً تست
می‌شود، و build های dev و prod موتور یکسان رفتار می‌کنند. هزینه‌اش این است که
روی **همه‌ی** رسم متن canvas در صفحه اثر می‌گذارد — که چون جهت از محتوای خود
رشته می‌آید، رفتار درستی است و نه یک اثر جانبی.

**شواهد تایید:**

- **شمارنده‌ی فراخوانی hook هنگام کار با بوم بالا می‌رود** — یعنی موتور واقعاً از
  همین مسیر متن می‌کشد. (تایید مالک روی صفحه‌ی `#spike`، ۱۴۰۵/۰۴/۳۱.)
  این تنها حلقه‌ی مفقوده بود؛ در session پیاده‌سازی قابل تولید نبود چون
  رویدادهای مصنوعی اشاره‌گر/چرخ را موتور نمی‌پذیرفت و هیچ رندر مجددی رخ نمی‌داد.
- wrapper نصب می‌شود و `direction` را روی هر context درست ست می‌کند (مرورگر واقعی)
- مقدار `ctx.direction` واقعاً خروجی رندر متن مخلوط را عوض می‌کند
  (آزمون هش پیکسلی، [spike بخش ۶](docs/spike-persian-text.md))
- تست واحد: سیم‌کشی، idempotency، حذف، عبور آرگومان‌ها

**پیامدها:**

- **پروژه روی پله‌ی A از [ADR-003](#adr-003) می‌ماند.** `ENGINE_STAGE` همچنان
  `"npm"` است و `patches/` خالی. patch تاییدشده‌ی P-1 اعمال **نمی‌شود** — ولی
  به‌عنوان راه‌حل پشتیبان با متن آماده در `patches/README.md` می‌ماند، چون اگر
  نسخه‌ی بعدی موتور مسیر رندر را عوض کند دوباره لازم می‌شود.
- شمارنده‌ی فراخوانی **در کد می‌ماند**، نه به‌عنوان آمار بلکه به‌عنوان
  تشخیص‌دهنده‌ی رگرسیون: اگر روزی بعد از ارتقای نسخه صفر شد، یعنی موتور دیگر از
  این مسیر رد نمی‌شود و باید به P-1 برگردیم. در چک‌لیست ارتقا ثبت شده است.

---

<a id="adr-026"></a>
## ADR-026 — گروه‌بندی undo با `captureUpdate`

**وضعیت:** پذیرفته‌شده (تایید تجربی، ۱۴۰۵/۰۵/۰۱) | **جایگزین‌ها:** پیاده‌سازی
undo stack خودمان، راه‌حل غیرمستقیم مثل ورود به ویرایش متن

**تصمیم:** هر عملیاتی که باید **یک ژست کاربر** باشد (حرکت فریم با فرزندانش،
تغییر رنگ چند استیکی، گروه‌بندی) در **یک** فراخوانی `updateScene` با همه‌ی
عناصر تغییریافته و `captureUpdate: "IMMEDIATELY"` انجام می‌شود — که موتور آن را
یک increment و در نتیجه یک ورودی undo ثبت می‌کند. تغییراتی که از راه دور
می‌آیند (`applyRemoteChanges` در M2) با `captureUpdate: "NEVER"` نوشته می‌شوند
تا در undo محلی کاربر ظاهر نشوند.

**دلیل:** برخلاف «ورود به حالت ویرایش متن» که API عمومی نداشت و راه‌حل غیرمستقیم
خواست، اینجا یک مسیر عمومی تمیز وجود دارد: ثابت `CaptureUpdateAction` صادر شده و
فیلد `captureUpdate` بخشی از تایپ عمومی `SceneData` است. پس نه undo stack خودمان
را لازم داریم، نه patch. مهم‌تر، granularity دقیقاً همان چیزی است که می‌خواهیم و
**تجربی در مرورگر تایید شد**، نه فقط از روی types: سه عنصر در یک
`updateScene(IMMEDIATELY)` با یک undo همگی برگشتند؛ همان سه عنصر در سه
`updateScene` جدا، هر undo فقط یکی را برگرداند. پس واحد undo برابر یک فراخوانی
capture است، نه یک عنصر.

**پیامدها:**

- سازنده‌های عنصر (`elements/*`) شیء برمی‌گردانند؛ **نوشتن به صحنه و انتخاب
  `captureUpdate` وظیفه‌ی لایه‌ی بالاتر است** (دمو الان، `apps/web` بعداً). یک
  ژست = یک `updateScene`.
- این با [ADR-004](#adr-004)/[ADR-022](#adr-022) هماهنگ است: استروک freedraw و
  داده‌ی ephemeral که نباید در تاریخچه بمانند، اصلاً از مسیر capture نمی‌روند.
- **مکمل نگهبان echo** ([sync/contract](../packages/canvas-core/src/sync/contract.ts)):
  ADR-012 می‌خواست undo محلی کار کاربران دیگر را برنگرداند ولی مکانیزمش معلوم
  نبود. حالا هست — M2 باید `applyRemoteChanges` را با `captureUpdate: "NEVER"`
  اعمال کند. این در `sync/README.md` برای M2 ثبت شده است.
- خودِ «یک undo یک ژست را برمی‌گرداند» رفتار موتور است و در jsdom (که undo
  ندارد) آزمودنی نیست؛ در تست مرورگری گام ۶٫۱ پوشش داده می‌شود.

---

<a id="adr-027"></a>
## ADR-027 — جای‌گذاری overlayهای دورِ بوم از یک منبعِ واحد (سیستمِ اسلات)

**وضعیت:** پذیرفته‌شده (۱۴۰۵/۰۵/۰۷) | **جایگزین‌ها:** hardcode کردنِ گوشه در CSSِ
هر overlay (وضعِ قبلی)، آفستِ دستی روی یکی از دو overlayِ متداخل.

**تصمیم:** موقعیتِ همه‌ی overlayهای chrome دورِ بوم (نوار ابزار، نوار وضعیت، zoom،
آواتارها، پنل استایل، و mini-mapِ آینده) از یک منبعِ واحد می‌آید:
[`ui/overlay-layout.css`](../packages/canvas-core/src/ui/overlay-layout.css) که
کلاس‌های اسلات (`hb-overlay--top-start`، `--top-center`، `--top-end`،
`--bottom-start`، `--bottom-center`، `--bottom-end`) و یک **نقشه‌ی اسلات** دارد.
هر overlay دقیقاً **یک** اسلات می‌گیرد؛ هیچ overlayـی گوشه‌اش را در CSSِ خودش
تعیین نمی‌کند.

**دلیل:** وقتی هر overlay مستقلاً گوشه‌اش را hardcode می‌کرد، هیچ‌جا کلِ چیدمان
دیده نمی‌شد و دو تا (نوار وضعیت و پنل استایل) هر دو `top-start` شدند و روی هم
افتادند — دقیقاً کلاسِ باگی که [ADR-024](#adr-024) هشدارش را می‌دهد (منطقِ واحد،
پراکنده در چند مصرف‌کننده). آفستِ دستی روی یکی، مشکل را با overlayِ بعدی (mini-map)
از نو می‌ساخت. یک نقشه‌ی اسلاتِ متمرکز، تداخل را در **زمانِ افزودن** آشکار می‌کند.
هزینه‌ی پذیرفته‌شده: overlayها حالا به یک فایلِ layoutِ مشترک وابسته‌اند (به‌جای
خودبسندگیِ CSSیِ کامل).

**پیامدها:**

- هر overlayِ جدید **باید** یک اسلات از نقشه بگیرد و آن را به نقشه اضافه کند؛
  گوشه را در CSSِ خودش ننویسد. CSSِ کامپوننت فقط **ظاهر** است، نه موقعیت.
- کلاس‌ها منطقی‌اند (`inset-inline`/`inset-block`) پس در RTL خودشان آینه می‌شوند.
  این با [ADR-016](#adr-016) نمی‌سازد: استثنای بوم فقط مختصاتِ **داخلِ** بوم است،
  نه chromeِ دورش.
- لایه‌های تمام‌بوم (`PeerCursors`/`PeerSelections` با `inset: 0`) اسلات نیستند و
  مستثنایند.
- `apps/web` هم همین را مصرف می‌کند: کامپوننت خودش موقعیت می‌گیرد، پس مصرف‌کننده
  نباید (و نمی‌تواند) دوباره جای‌گذاری کند — همان باگِ تداخل دیگر ساخته نمی‌شود.

---

<a id="adr-028"></a>
## ADR-028 — ناسازگاریِ StrictMode با APIِ امریِ Excalidraw — ریسکِ ثبت‌شده‌ی مصرف‌کننده

**وضعیت:** پذیرفته‌شده — ثبتِ ریسک (۱۴۰۵/۰۵/۰۷) | **جایگزین‌ها:** فقط دور زدن در دمو
و سکوت (رد شد — M2/M3 غافلگیر می‌شدند)؛ ادعای «HamboomCanvas زیر StrictMode درست کار
می‌کند» (خلافش در مرورگر دیده شد).

**تصمیم:** دموی `canvas-core` **بدونِ `<StrictMode>`** اجرا می‌شود
([`dev/main.tsx`](../packages/canvas-core/dev/main.tsx))، **و** این محدودیت به‌عنوان یک
**ریسکِ واقعیِ مصرف‌کننده** ثبت می‌شود، نه صرفاً یک تنظیمِ دمو: هر مصرف‌کننده‌ای که
اشتراک‌های موتور (`onChange`/`onScrollChange`) را داخلِ callbackِ `excalidrawAPI`
(الگوی `onReady`) می‌بندد، زیر StrictMode آن اشتراک‌ها **مرده** می‌مانند و state از
رویدادهای موتور به‌روز نمی‌شود.

**دلیل:** تستِ E2E گام ۶٫۱ کشف کرد که زیر StrictMode مسیرِ `onChange → refreshCounts →
setSnapshot` در دمو مرده است — پنلِ استایل با **هیچ** انتخابِ موتوری (کلیک/کادر/Ctrl+A)
به‌روز نمی‌شد، هرچند `getAppState()` و آرگومانِ `onChange` مقدارِ درست را داشتند. **مکانیزمِ
محتمل:** StrictMode کامپوننت را mount→unmount→remount می‌کند؛ Excalidraw `excalidrawAPI`
را برای نمونه‌ی **اول** (دورانداخته) صدا می‌زند و اشتراک‌هایی که همان‌جا بسته می‌شوند به
`setSnapshot`ِ نمونه‌ی مرده می‌چسبند، در حالی که نمونه‌ی زنده هرگز onReady را دوباره اجرا
نمی‌کند. **برداشتنِ StrictMode کاملاً حلش کرد؛ لغوِ صریحِ listenerها حلش نکرد** — پس یک
leakِ ساده نیست و رفعش پیش‌پاافتاده نبود. چون خودِ HamboomCanvas زیر StrictMode این رفتار
را نشان می‌دهد، این یک ریسکِ محصولی است نه فقط عادتِ دمو. **هزینه‌ی پذیرفته‌شده:** دمو
مزیتِ خطاگیریِ StrictMode را از دست می‌دهد (قابل‌قبول برای یک ابزارِ توسعه که کارش اجرای
موتورِ واقعی است).

**پیامدها:**

- **apps/web (M3) — که به‌احتمالِ زیاد StrictMode دارد (پیش‌فرضِ بیشترِ setupهای React) —
  باید قبل از استفاده‌ی HamboomCanvas این را حل کند.** غافلگیر نشود: پنل‌ها/شمارنده‌هایی
  که به رویدادهای موتور وصل‌اند بی‌صدا از کار می‌افتند.
- **راهِ‌حلِ نامزد (⚠️ هنوز تاییدِ تجربی نشده):** apiِ گرفته‌شده از onReady را در **state**
  بگذار و اشتراک‌ها را در یک `useEffect([api])` با **cleanup** ببند، نه مستقیم در onReady.
  چون تلاشِ اولِ ما (لغوِ listener در onReady) کار نکرد، این فرضیه باید **قبل از M3 در
  مرورگر اثبات شود** (روحیه‌ی [ADR-025](#adr-025): مدعا تا probe نشده، قطعی نیست). گزینه‌ی
  دیگر: خودِ HamboomCanvas اشتراک‌ها را داخلی و StrictMode-safe مدیریت کند و به مصرف‌کننده
  یک API رویدادیِ امن بدهد.
- **نگهبان:** تستِ refreshCounts در [`e2e/selection.spec.ts`](../packages/canvas-core/e2e/selection.spec.ts)
  این کلاسِ باگ را می‌گیرد. M3 وقتی الگوی امن را پیدا کرد، همین تست را زیرِ StrictMode
  تکرار کند تا رفع تایید شود.
- تا آن موقع، دمو StrictMode ندارد و دلیلش در `dev/main.tsx` و [`canvas-core/CLAUDE.md`](../packages/canvas-core/CLAUDE.md)
  (جدولِ تله‌های E2E) ثبت است.

---

<a id="adr-029"></a>
## ADR-029 — لایه‌بندیِ M2: `ydoc-schema` خالص می‌ماند، binder به `canvas-sync` می‌رود

**وضعیت:** پذیرفته‌شده (۱۴۰۵/۰۵/۱۲، تاییدِ مالک) | **جایگزین‌ها:** binder سرِ جای PLAN با
`import type` (رد شد)؛ انتقالِ قراردادِ `CanvasSyncAdapter` به `shared-types` (فعلاً رد شد)

**تصمیم:** M2 سه واحد دارد، نه دو تا:

| واحد | می‌بیند | نمی‌بیند |
|---|---|---|
| `packages/ydoc-schema` | `yjs`، `lib0`، `shared-types` | canvas-core، React، Excalidraw، sdk |
| `packages/canvas-sync` (**جدید**) | ydoc-schema، canvas-core، y-protocols | `apps/*` |
| `apps/realtime` | ydoc-schema، ws، pg، redis | canvas-core، React، Excalidraw، sdk، aws-sdk |

`binder` — یعنی پیاده‌سازیِ `CanvasSyncAdapter` روی Yjs — در `packages/canvas-sync` است،
**نه** در `packages/ydoc-schema/src/binder.ts` که [PLAN بخش ۸](../PLAN.md) گفته بود.

**دلیل:** PLAN با خودش ناسازگار است. بخش ۸ binder را داخلِ `ydoc-schema` می‌گذارد، ولی بخش ۲
صریحاً می‌نویسد `ydoc-schema → canvas-core ❌ (ydoc-schema پایین‌تر است)`. این دو با هم جمع
نمی‌شوند، چون binder **ذاتاً** باید قراردادِ `CanvasSyncAdapter` را ببیند؛ کارش دقیقاً همین است.

ناسازگاری به نفعِ **قاعده‌ی لایه‌بندی** حل شد، نه به نفعِ مسیرِ فایل — چون قاعده دلیل دارد و
مسیر ندارد: `ydoc-schema` تنها چیزی است که **هم کلاینت و هم سرور** مصرفش می‌کنند. اگر
canvas-core را ببیند، سرورِ Node برای typecheck کلِ گرافِ تایپِ Excalidraw و React را می‌کِشد،
و مرزی که قرار بود سرور را از موتورِ رندر جدا نگه دارد عملاً تعارفی می‌شود.

`import type` (که در runtime هیچ هزینه‌ای ندارد) به همین دلیل کافی نبود: هزینه‌اش در runtime
نیست، در **typecheck و در گیت** است. و برای اجازه‌دادنش باید قاعده‌ی مرزی استثنا بخورد —
که درسِ M1 می‌گوید نباید: قاعده‌ای که استثنا یا مثبتِ کاذب دارد دور زده می‌شود، و آن‌وقت موردِ
واقعی را هم نمی‌گیرد (همان اتفاقی که برای `elementKindDiscipline` در گام ۳٫۳ افتاد).

انتقالِ قرارداد به `shared-types` — که از نظر [ADR-021](#adr-021) تمیزترین کار است — فعلاً رد
شد چون به فایل‌های تمام‌شده‌ی M1 دست می‌زند و یک قرارداد وقتی سفت می‌شود که **مصرف‌کننده‌ی
دومش وجود داشته باشد**. بازبینی‌اش جای درستش نقطه‌ی ادغامِ M2/M3 است.

**پیامدها:**

- مسیرِ `packages/ydoc-schema/src/binder.ts` در PLAN بخش ۸ **منسوخ** است؛ هر جا PLAN آن را
  نام می‌برد یعنی `packages/canvas-sync`.
- سه گیتِ ESLint (یکی برای هر واحد) با `RuleTester` خودآزمون + probeِ نقضِ واقعی در پکیج.
- `apps/web` در M3 به `canvas-sync` وصل می‌شود، نه به `ydoc-schema`.
- مزیتِ جانبی: وقتی M3 بخواهد سرور محتوای بورد را «بفهمد» (thumbnail، جستجو، export —
  همان چیزی که [ADR-004](#adr-004) دلیلِ انتخابِ Yjs شمرد)، `ydoc-schema` از قبل خالص و
  قابلِ اجرا در Node است.

---

<a id="adr-030"></a>
## ADR-030 — ترابریِ WebSocket: `ws` پشتِ یک seam، نه `uWebSockets.js`

**وضعیت:** پذیرفته‌شده (۱۴۰۵/۰۵/۱۲، تاییدِ مالک) | **جایگزین‌ها:** `uWebSockets.js` (رد شد
برای فاز ۱)، `socket.io` (رد شد — پروتکلِ خودش را تحمیل می‌کند و y-protocols باینری است)

**تصمیم:** سرورِ `apps/realtime` روی **`ws`** (MIT) ساخته می‌شود، و خودِ ترابری پشتِ یک
interfaceِ نازک قرار می‌گیرد تا تعویضش بازنویسیِ منطقِ اتاق نباشد.

**دلیل:** [PLAN بخش ۲](../PLAN.md) نوشته بود «uWebSockets.js یا ws» و تصمیم را باز گذاشته بود.
دو دلیلِ عملی `ws` را برنده می‌کند و هیچ‌کدام درباره‌ی کارایی نیست:

۱. **`uWebSockets.js` روی npm نیست** — از یک تارِبالِ گیت‌هاب نصب می‌شود. یعنی
   `scripts/license-check.ts` که درختِ `pnpm licenses list --json` را می‌پیماید نمی‌تواند
   تضمینش کند. **P1 یک گیتِ اجرایی است، نه یک نیت**؛ وابستگی‌ای که از زیرِ گیت رد می‌شود
   خودِ گیت را بی‌معنی می‌کند.
۲. **باینریِ نیتیو روی ویندوز** — محیطِ توسعه‌ی این پروژه ویندوز است و **P3 می‌گوید
   `docker compose up && pnpm dev` باید کافی باشد**. یک وابستگیِ نیتیو که ممکن است روی
   ویندوز build نشود، دقیقاً همان اصل را می‌شکند.

درباره‌ی کارایی: [ADR-006](#adr-006) فاز ۱ را «تک‌نود تا ~۲۰۰ کاربر همزمان» تعریف می‌کند. در
آن مقیاس گلوگاه **حافظه‌ی Y.Docهای فعال** است، نه I/O سوکت — و خودِ ADR-006 همین را می‌گوید
(«WebSocket بیشتر حافظه‌محور است تا CPU-محور»، به همین دلیل HPA روی تعداد اتصال است نه CPU).
یعنی انتخابِ `ws` چیزی را که واقعاً محدودکننده است بدتر نمی‌کند.

**پیامدها:**

- منطقِ اتاق/awareness/پایداری هیچ ارجاعِ مستقیمی به `ws` ندارد؛ فقط لایه‌ی ترابری دارد.
- **معیارِ عبور به uWS یک عدد است، نه حس:** بنچمارکِ گام ۶٫۳ (۵۰ کلاینتِ همزمان، تاخیرِ
  p50/p95). اگر گلوگاه واقعاً ترابری بود، آن‌وقت با شواهد بازبینی می‌شود — همان روشی که در M1
  با `docs/perf-baseline.md` یک بهینه‌سازیِ زائد (culling) رد شد.
- اگر روزی uWS آمد، **مسیرِ نصبِ خارج از npm باید جدا حل شود** (vendor کردن یا استثنای
  مکتوب در `scripts/license-exceptions.json`) — نه اینکه بی‌صدا از گیت رد شود.

---

<a id="adr-031"></a>
## ADR-031 — پورت به‌جای وابستگیِ مستقیم به M3 (احراز هویت و snapshot)

**وضعیت:** پذیرفته‌شده (۱۴۰۵/۰۵/۱۲، تاییدِ مالک) | **جایگزین‌ها:** متوقف‌کردنِ M2 تا آماده‌شدنِ
M3 (رد شد)؛ وابستگیِ مستقیم به یک M3 که هنوز شکل ندارد (رد شد)

**تصمیم:** M2 به دو **پورت** تکیه می‌کند، نه به ماژولِ M3:

| پورت | پیاده‌سازیِ امروز | پیاده‌سازیِ M3 |
|---|---|---|
| `BoardAuthority` — اعتبارسنجیِ `rtToken` و نقشِ موثر | `DevBoardAuthority` (JWT HS256 با `RT_DEV_JWT_SECRET` + نگاشتِ نقشِ استاتیک) | `AuthCoreBoardAuthority` روی `packages/auth-core` ([ADR-012](#adr-012)) |
| `SnapshotStore` — نوشتن/خواندنِ snapshot | `FsSnapshotStore` روی `.data/snapshots/` | پیاده‌سازی روی `packages/storage` ([ADR-013](#adr-013)) |

و دو قید:

- **M2 هرگز `@aws-sdk/*` یا `@hamboom/sdk` را import نمی‌کند** — با گیتِ ESLintِ خودآزمون.
- **شکلِ claimهای `rtToken` داخلِ همین پورت تعریف می‌شود، نه در `shared-types`.**

**دلیل:** ترتیبِ واقعیِ ساخت با ترتیبِ PLAN فرق کرد — M2 قبل از M3 و M5 شروع شد. دو راهِ بدیهی
هر دو بد بودند: **صبر کردن** یعنی M2 هفته‌ها معطلِ ماژولی می‌ماند که هنوز شروع نشده؛ و
**وابستگیِ مستقیم** یعنی قراردادی بسازیم که طرفِ دومش وجود ندارد، یعنی با حدس.

پورت راهِ سوم است: **مرز را همین حالا سفت می‌کند و پیاده‌سازی را عقب می‌اندازد.** مهم‌تر از
راحتی، این کار **P4 را با ساخت حفظ می‌کند نه با قول** — وقتی M2 اصلاً SDKِ Object Storage را
نمی‌بیند، «فقط `packages/storage` حق دیدنِ S3 را دارد» دیگر یک قاعده‌ی نانوشته نیست که
یادِ کسی برود؛ یک خطای lint است.

**چرا `rtToken` عمداً در `shared-types` نیست** (قیدِ صریحِ مالک): [ADR-021](#adr-021) می‌گوید
`shared-types` قراردادِ بینِ ماژول‌هاست — و دقیقاً به همین دلیل اینجا زود است. یک قرارداد وقتی
معنا دارد که **هر دو طرفش وجود داشته باشند**؛ الان فقط M2 دارد می‌نویسدش و M3 هنوز نمی‌داند
توکنش چه شکلی است. ثبتش در `shared-types` یعنی قفل‌کردنِ یک **حدس** روی هر شش ماژول، در
حالی که تغییرِ بعدی‌اش گران‌ترین نوعِ تغییر است. جای درستِ این کار همان است که
[PLAN بخش ۸](../PLAN.md) گفته: «بعد از گام ۲، یک session معمولی برای smoke test و
به‌روزرسانیِ `shared-types`».

**پیامدها:**

- M3 دو پیاده‌سازی **اضافه** می‌کند، نه اینکه M2 را بازنویسی کند.
- ★ **خطرِ ثبت‌شده — `DevBoardAuthority` نباید هرگز در production زنده شود.** این یک پیاده‌سازیِ
  توسعه است که هرکسی با دانستنِ `RT_DEV_JWT_SECRET` می‌تواند برای خودش نقشِ owner صادر کند.
  پس یک **گیتِ runtime** لازم است: اگر `APP_ENV=production` و پیاده‌سازیِ dev فعال بود، سرور
  باید **بالا نیاید**، نه اینکه هشدار بدهد. (بدونِ این گیت، خودِ ADR-012 — که کلِ حرفش
  «کسی با نقشِ viewer نتواند مستقیماً update بفرستد» است — از پشت دور زده می‌شود.)
- تا وقتی M3 نیامده، «اعمالِ مجوز در سرور» با نقشِ **ساختگی** آزموده می‌شود؛ پس تستِ مهاجمِ
  گام ۴٫۵ باید **مکانیزمِ رد کردن** را بیازماید، نه صحتِ منبعِ نقش را.

---

<a id="adr-032"></a>
## ADR-032 — الگوی اشتراکِ StrictMode-safe برای رویدادهای موتور

**وضعیت:** پذیرفته‌شده (**تایید تجربی در مرورگر**، ۱۴۰۵/۰۵/۱۲) | **مکملِ**
[ADR-028](#adr-028) که همین را به‌عنوان «راه‌حلِ نامزدِ **تاییدنشده**» ثبت کرده بود.

**تصمیم:** هر مصرف‌کننده‌ای که به رویدادهای موتور (`onChange`، `onScrollChange`، …)
مشترک می‌شود **باید** این الگو را به کار ببرد:

```tsx
const [api, setApi] = useState<CanvasApi | null>(null);
useEffect(() => {
  if (!api) return;
  return api.onChange(handler);        // ← cleanup برمی‌گرداند
}, [api]);
return <HamboomCanvas onReady={setApi} />;
```

و **نباید** اشتراک را مستقیم داخلِ callbackِ `excalidrawAPI`/`onReady` ببندد. این
برای `packages/canvas-sync` (binderِ M2) الزامی است و برای `apps/web` (M3) توصیه‌ی
اکید — با همین الگو، StrictMode دیگر مانع نیست.

**دلیل:** ADR-028 مکانیزم را «محتمل» توصیف کرده بود و راه‌حل را صریحاً
**تاییدنشده** گذاشته بود («مدعا تا probe نشده، قطعی نیست» — روحیه‌ی
[ADR-025](#adr-025)). گام ۱٫۱ آن probe را زد: یک صفحه‌ی کمینه (فقط موتور + یک
شمارنده، بدونِ نوار ابزار و پنلِ ما) که **زیرِ `<StrictMode>`** هر دو الگو را با
**همان** سناریو می‌آزماید — [`packages/canvas-sync/dev/StrictModeProbe.tsx`](../packages/canvas-sync/dev/StrictModeProbe.tsx).

★ **نکته‌ی روشِ آزمایش که نتیجه را قابل‌اتکا کرد:** فقط خواندنِ stateِ ری‌اکت مبهم
است — صفر ماندنش هم می‌تواند «اشتراک مرده» باشد و هم «تعاملِ تست اصلاً چیزی
نکشید». پس یک **شاهدِ کنترل** اضافه شد که حالتِ موتور را **مستقل از ری‌اکت**
می‌خواند. اجرای اولِ probe بدونِ این شاهد گمراه شد (`KeyR` بدونِ focusِ کانتینر به
ابزار نمی‌رسید و درگ یک کادرِ انتخاب می‌کشید، نه شکل) و نزدیک بود یک **باگِ تست**
به‌عنوان یافته‌ی معماری گزارش شود.

**نتیجه‌ی سنجش (Chromiumِ واقعی، دو اجرای پایدار):**

| الگو | موتور | state ری‌اکت |
|---|---|---|
| `onReady` (الگوی M1) | ۱ عنصر، ۱ انتخاب ✓ | `onChange` = **۰** ✗ |
| `useEffect([api])` | ۱ عنصر، ۱ انتخاب ✓ | همه دنبال می‌کنند ✓ — شاملِ **تغییرِ** انتخاب |

یعنی هم فرضِ ADR-028 **بازتولید شد** و هم راه‌حلش **کار می‌کند**.

**آنچه این ADR اثبات نمی‌کند:** *چرا* callbackِ `onReady` به نمونه‌ی دورانداخته
می‌چسبد. علتِ درونیِ Excalidraw کاوش نشد — نشانه و رفعش سنجیده شد، نه مکانیزمش.
اگر روزی لازم شد، این هنوز باز است.

**پیامدها:**

- **`packages/canvas-sync` (فاز ۳)** موظف است همین الگو را به کار ببرد؛ خط قرمز ۳
  در [`CLAUDE.md`](../packages/canvas-sync/CLAUDE.md) همان پکیج.
- **دو نگهبانِ دائمی** در [`e2e/strictmode.spec.ts`](../packages/canvas-sync/e2e/strictmode.spec.ts):
  یکی ثابت می‌کند الگوی درست **زنده است** (اگر قرمز شود، binder بی‌صدا از کار افتاده)،
  و دیگری ثابت می‌کند الگوی غلط **هنوز مرده است**. دومی عمدی است: اگر نسخه‌ی بعدیِ
  Excalidraw این را رفع کند، آن تست **قرمز می‌شود** و می‌فهمیم فرضِ اینجا دیگر لازم
  نیست — چیزی که یک تستِ همیشه‌سبز هرگز به ما نمی‌گفت.
- **دموی `canvas-core` دست‌نخورده می‌مانَد** (هنوز بدونِ StrictMode). تغییرش کارِ M1
  است و خارج از دامنه‌ی M2؛ حالا که الگوی امن معلوم است، M3 می‌تواند تصمیم بگیرد.
- **[ADR-028](#adr-028) منسوخ نمی‌شود** — ثبتِ ریسک و مکانیزمِ محتملش همچنان معتبر
  است؛ این ADR فقط بخشِ «راه‌حلِ نامزد» را از تاییدنشده به **تاییدشده** می‌برد.

---

<a id="adr-033"></a>
## ADR-033 — `customData` هم `Y.Map` تودرتو می‌شود، نه یک مقدارِ ساده

**وضعیت:** پذیرفته‌شده (**تایید تجربی**، ۱۴۰۵/۰۵/۱۲) | **بسطِ**
[ADR-007](#adr-007) به یک لایه پایین‌تر

**تصمیم:** در codecِ سند، **هر آبجکتِ سادهٔ تودرتو داخلِ یک عنصر** به‌صورت `Y.Map`
ذخیره می‌شود — یعنی `customData`، `customData.hb`، و زیرشاخه‌های مخصوصِ هر kind
(`sticky`، `connector`، `frame`، `card`). قاعده **بازگشتی و بدونِ استثنا** است.

آرایه‌ها (`points`، `groupIds`، `tags`) عمداً مقدارِ ساده می‌مانند و LWW‌اند —
همان «هزینه‌ی پذیرفته‌شده»ی خودِ ADR-007.

در مرزِ قرارداد، `Y.Map.toJSON()` کلِ درخت را به آبجکتِ سادهٔ JSON برمی‌گرداند، پس
`shared-types` **دست‌نخورده می‌مانَد** و `hbElement.parse` همچنان پاس می‌شود.

**دلیل:** probeِ گام ۱٫۲ نشان داد `customData` در یک property می‌نشیند و کلش LWW
می‌شود. سوال باز ماند که این محدودیتِ قابل‌پذیرش است یا نه. **با آزمایش حل شد، نه
با نظر** — دقیقاً همان آزمونِ per-property که برای خودِ عنصر زده شده بود:

| مدل | A پالت را عوض می‌کند، B همزمان برچسب می‌زند |
|---|---|
| `customData` به‌صورت آبجکتِ ساده | ❌ **یکی خورده شد** |
| `customData` به‌صورت `Y.Map` تودرتو | ✅ هر دو ماند، `kind` هم دست‌نخورده |

**چرا این یک فیلدِ جانبی نیست:** `customData.hb` قلبِ مدلِ دادهٔ خودِ هم‌بوم است —
`kind` (که کلِ [ADR-010](#adr-010) رویش سوار است)، پالتِ استیکی، جهتِ متن، و
برچسب‌ها. گم‌شدنِ داده اینجا **همان** باگی است که ADR-007 برای سطحِ عنصر حذفش کرد،
فقط یک لایه پایین‌تر و نامرئی‌تر — چون در UI به‌صورت «رنگم برگشت» دیده می‌شود، نه
به‌صورت خطا.

**شدتِ واقعی — سنجیده شد، نه فرض:** `lastEditedBy` فقط هنگامِ **ساخت** نوشته
می‌شود، نه در هر جهش. پس این‌طور نیست که *هر* ویرایشِ همزمان به تعارض بخورد؛ فقط
آن‌هایی که واقعاً `customData` را لمس می‌کنند. این مسئله را کوچک‌تر می‌کند، ولی حذف
نمی‌کند — تغییرِ رنگِ استیکی یکی از رایج‌ترین کارهای کاربر است.

**چرا قاعده‌ی بازگشتیِ بدونِ استثنا:** می‌شد فقط تا عمقِ دو تودرتو کرد و
زیرشاخه‌های kind را ساده گذاشت. ولی هر بُرشی در عمق **دلبخواهی** است و بعداً
کسی باید یادش بماند کجا مرز است — همان کلاسِ باگی که قاعده‌های استثنادار می‌سازند.
یک قاعده‌ی یکنواخت هم پیاده‌سازی‌اش ساده‌تر است و هم توضیحش.

**هزینه‌ی پذیرفته‌شده (اندازه‌گیری‌شده):** ساختِ یک استیکی از **۷۵۸ به ۸۴۹ بایت**
می‌رود (+۱۲٪). سربار است، نه انفجار — و فقط روی **ساخت**؛ ویرایش‌های بعدی
per-property اند و از این هم کوچک‌ترند.

**پیامدها:**

- codecِ گام ۲٫۱ یک تابعِ بازگشتیِ واحد دارد (`toYValue`)، نه شاخه‌بندی به‌ازای نوع.
- خواندن **باید** از `toJSON()` برود، نه `Object.fromEntries(entries())` — وگرنه
  `Y.Map`های تودرتو خام بیرون می‌آیند و `hbElement.parse` می‌افتد.
- نگهبان: تستِ round-trip برای **هر ۱۰ نمونه‌ی واقعی** با customDataِ تودرتو، در
  [`canvas-sync/src/codec-probe.test.ts`](../packages/canvas-sync/src/codec-probe.test.ts).
- سربارِ حافظه‌ی این تودرتویی در بوردِ بزرگ هنوز اندازه‌گیری نشده — کارِ بنچمارکِ
  گام ۶٫۳، همان‌جا که سربارِ خودِ ADR-007 هم سنجیده می‌شود.

---

<a id="adr-034"></a>
## ADR-034 — متن: `Y.Text` در سند، رشته در قرارداد، دیف در binder

**وضعیت:** پذیرفته‌شده (**تایید تجربی**، ۱۴۰۵/۰۵/۱۲) | حل‌کننده‌ی تعارضِ
PLAN بخش ۷٫۳ با `shared-types`

**تصمیم:** سه لایه، با مرزهای صریح:

1. **در `Y.Doc`:** `originalText` یک **`Y.Text`** است.
2. **در قرارداد:** رشته‌ی ساده می‌مانَد. `readElement` با `toString()` تبدیل می‌کند،
   پس **`shared-types` دست‌نخورده می‌مانَد** و `hbElement.parse` پاس می‌شود.
3. **در binder:** رشته‌ی کاملِ نو با **دیفِ پیشوند/پسوندِ مشترک** به delta تبدیل
   می‌شود — چون `ElementChangeSet` عنصرِ کامل می‌دهد، نه delta.

★ **`text` (نسخه‌ی wrap‌شده) `Y.Text` نمی‌شود و ادغام هم نمی‌شود.** یک مقدارِ
**مشتق‌شده** است و باید از `originalText`ِ ادغام‌شده **بازمحاسبه** شود.

**دلیل:** [ADR-004](#adr-004) صریحاً «دو نفر همزمان روی یک استیکی تایپ می‌کنند» را
یکی از سه دلیلِ انتخابِ Yjs شمرده بود، ولی `shared-types` متن را `string` تعریف
کرده. با آزمایش حل شد:

| مدل | دو کاربر همزمان در دو جای متن تایپ می‌کنند |
|---|---|
| رشته‌ی ساده (LWW) | ❌ کاراکترهای یکی پاک می‌شود |
| `Y.Text` + دیف | ✅ هر دو می‌مانند، دو سند همگرا |

**هزینه:** دیفِ یک کاراکتر در متنِ ۱۰۰۰ کاراکتری = **۲۸ بایت** (نه بازنویسیِ کل).
۱۰۰ ویرایشِ پیاپی زیر ۵۰۰ms.

★★ **قیدِ حیاتی — پایه‌ی دیف باید زنده باشد:** دیف **باید** علیهِ
`ytext.toString()`ِ **لحظه‌ی تراکنش** گرفته شود، نه علیهِ عنصرِ کَش‌شده‌ی بوم.

این هم‌کلاسِ باگِ `getAppState`ِ کهنه در M1 است، ولی **پیامدش خیلی بدتر**: با
پایه‌ی کهنه، بازه‌ی `delete` روی متنِ زنده به **ایندکسِ اشتباه** می‌افتد. سنجیده شد —
انتظار «سلام رفیق» بود، نتیجه **«سلام رفیقا»**: هم کاراکترِ همتا رفت و هم یک
کاراکترِ اضافه جا مانْد. **متن مخدوش می‌شود، نه فقط ناقص.**

> ⚠️ اولین آزمونِ من برای همین تله **معتبر نبود** و خودِ تست ردش کرد: با یک ویرایشِ
> **افزایشی** آزمودم و کاراکترِ همتا سالم ماند — چون آن دیف یک درجِ خالص بود و
> اصلاً بازه‌ی حذفی نداشت. خرابی فقط با ویرایشِ **جایگزینی** ظاهر می‌شود.

**پیامدها:**

- codecِ گام ۲٫۱ باید `originalText` را **استثنا** کند: به‌جای مقدارِ ساده، `Y.Text`.
  (بقیه‌ی قاعده‌ی بازگشتیِ [ADR-033](#adr-033) دست‌نخورده می‌مانَد.)
- binder (گام ۳٫۳) دیف را **فقط** با پایه‌ی زنده بگیرد. نگهبان: دو تستِ
  [`text-probe.test.ts`](../packages/ydoc-schema/src/text-probe.test.ts) که حالتِ کهنه
  را قرمز نگه می‌دارند.
- `text` هرگز به‌عنوان CRDT نوشته نشود؛ از `originalText` بازمحاسبه شود.
- **`shared-types` تغییر نمی‌کند** و `textDelta` به قرارداد اضافه **نمی‌شود** —
  مسیرِ ممنوعِ مالک رعایت شد، چون دیف کافی بود.

**دو چیزی که در گام ۱٫۳ اندازه‌گیری نشد — ✅ در گام ۳٫۴ عدد گرفتند**
([`docs/ydoc-baseline.md`](docs/ydoc-baseline.md) بخش ۶):

| اندازه‌گیری | عدد |
|---|---|
| «update روی Y.Doc نشست» → **صحنه** | **۱۱ تا ۱۵ms** (~یک فریم) |
| همان → **textareaِ باز** | ★ **هرگز** (>۳۰۰۰ms) |
| پرشِ مکان‌نما | **۰ کاراکتر** |

★★ **یافته‌ای که از عددها مهم‌تر است:** ویرایشگرِ متنِ موتور تا وقتی باز است
**کاملاً ایزوله** است. پس «پنجره‌ی کهنگی» عددِ میلی‌ثانیه‌ای نیست — به اندازه‌ی
**کلِ مدتِ ویرایش** باز می‌مانَد، و هنگام بستنِ ویرایشگر درجِ همتا **پاک می‌شود**.
این باگِ دیف نیست (دیف علیهِ پایه‌ی زنده درست کار می‌کند)؛ محدودیتِ موتور است و
با تست قفل شده. رفعش کارِ M3 است.

---

## تصمیم‌های معلق (هنوز ADR نشده‌اند)

| موضوع | چرا هنوز تصمیم نگرفته‌ایم |
|---|---|
| کتابخانه‌ی state سمت کلاینت (Zustand در برابر Jotai) | تا وقتی `canvas-core` شکل نگیرد، نمی‌دانیم مدل state چقدر گرانولار است. تصمیم در گام ۳ از TODO. |
| استراتژی کش سمت کلاینت (TanStack Query تنظیمات) | بعد از تثبیت قرارداد API. |
| ~~نحوه‌ی رندر متن فارسی روی canvas~~ | ✅ حل شد — [ADR-023](#adr-023) |
| تماس صوتی/تصویری (SFU) | خارج از فاز ۱ (Q8 در PLAN.md). |
| موتور جستجوی محتوای بورد | فاز ۲. احتمالاً `pg_trgm` کافی است و نیازی به موتور جدا نیست. |
| CDN و anti-DDoS | در زمان دیپلوی، بر اساس امکانات واقعی آروان. |

---

<a id="adr-035"></a>
## ADR-035 — **Yjs** صاحبِ undo است، نه موتورِ رندر

**وضعیت:** پذیرفته‌شده · **جایگزین‌ها:** undoِ خودِ موتور + emit با originِ `"undo"`

**تصمیم:** `Y.UndoManager` با `trackedOrigins: new Set([LocalOrigin])` تنها مرجعِ
undo/redo است. تاریخچه‌ی خودِ موتور **ساکت می‌شود**: `Ctrl+Z`/`Ctrl+Shift+Z`/`Ctrl+Y`
در فازِ **capture** گرفته و به `UndoManager` سپرده می‌شوند
([`canvas-sync/src/undo.ts`](packages/canvas-sync/src/undo.ts)).

**دلیل:** هر دو گزینه در گام ۳٫۴ در مرورگر سنجیده شدند و — برخلافِ انتظار —
undoِ موتور **درست کار می‌کرد**: حتی وقتی همتا روی همان عنصر تغییر داده بود،
`Ctrl+Z` فقط کارِ خودِ کاربر را برگرداند. مشکل جای دیگری بود:

> ⚠️ **undoِ موتور به همتا نمی‌رسد.** کاربرِ ب رنگ را برگرداند، بومِ خودش زرد شد،
> و بومِ الف **بنفش مانْد** — واگرایی، تا وقتی کسی دوباره آن عنصر را لمس کند.
> عددِ سنجیده‌شده: `bcolor: "#FFF9B1"` در برابرِ `acolor: "#D0C6F5"`.

با `Y.UndoManager` این مسئله از ریشه نیست: undo یک تراکنشِ عادیِ Yjs است و از همان
مسیرِ همیشگی به همتا می‌رسد. به‌علاوه انزوا **ساختاری** است نه رفتاری — عملیاتِ
همتا اصلاً در پشته‌ی ما نیستند، نه اینکه فیلتر شوند.

گزینه‌ی رقیب (موتور undo کند و binder نتیجه را با originِ `"undo"` emit کند) شدنی
بود — قرارداد M1 آن origin را دارد و `isEmittableOrigin` مجازش می‌داند — ولی
اسنپ‌شاتِ موتور را روی سند می‌نشاند، و هر property که همتا **در همان پنجره** عوض
کرده باشد به مقدارِ کهنه‌ی موتور برمی‌گشت. **هزینه‌ی پذیرفته‌شده‌ی گزینه‌ی انتخابی:**
اپ باید صریحاً کیبورد را بگیرد؛ اگر یادش برود، undoِ موتور دوباره فعال می‌شود و یک
`Ctrl+Z` دو کار می‌کند.

**پیامدها:**

- ★ اپ **باید** `bindUndoShortcuts` را به کار ببرد. نگهبان:
  [`e2e/undo-two-clients.spec.ts`](packages/canvas-sync/e2e/undo-two-clients.spec.ts).
- **گروه‌بندی با `gestureId`:** ادغامِ ورودی‌های undo فقط **درونِ یک ژستِ شناسه‌دار**
  مجاز است؛ تغییرِ بی‌ژست (ساخت، حذف، استایل) ورودیِ خودش را می‌گیرد. بدونِ این،
  Yjs هرچه در پنجره‌ی `captureTimeout` بیفتد را یکی می‌کند — در گام ۳٫۴ سنجیده شد
  که ساخت و سه ویرایشِ بعدی همگی **یک** `Ctrl+Z` شدند و کلِ بورد پاک شد.
- **دامنه فقط `elements` است.** `appState` وضعیتِ مشترکِ بورد است و برگرداندنش با
  `Ctrl+Z`ِ یک نفر تنظیمِ همه را عوض می‌کند.
- ⚠️ **هر مسیرِ نوشتنی که originش `LocalOrigin` نیست باید بعدش `flushRemote` را صدا
  بزند.** این شکاف در گام ۳٫۴ بیرون آمد: تحویلِ تغییرِ غیرِمحلی به بوم فقط به
  دریافتِ پیامِ ترابری گره خورده بود، پس بوم **undoِ خودش را نمی‌دید**.
- `captureUpdate: "NEVER"` ([ADR-026](#adr-026)) همچنان لازم است — آن تاریخچه‌ی
  **موتور** را می‌بندد و این یکی تاریخچه‌ی **Yjs** را. دو سدِ مستقل (گام ۱٫۴).

---

<a id="adr-036"></a>
## ADR-036 — ephemeral کانالِ خودش را دارد، نه یک فیلد در stateِ awareness

**وضعیت:** پذیرفته‌شده · **جایگزین‌ها:** `PeerState.ephemeral` به‌عنوان یک فیلدِ
معمولیِ stateِ awareness (ساده‌ترین خواندنِ [ADR-022](#adr-022) و قراردادِ M1)

**تصمیم:** حضورِ **ماندگار** (کاربر، مکان‌نما، انتخاب، نما، ابزار) در stateِ
`y-protocols/awareness` می‌نشیند و روی پیامِ `0x01 AWARENESS` می‌رود. داده‌ی
**موقت** (استروکِ در حالِ کشیدن، لیزر، reaction) از پیامِ جداگانه‌ی
`0x13 HB_EPHEMERAL` می‌رود و در `PeerState` فقط **کنارِ** بقیه چیده می‌شود
([`canvas-sync/src/awareness.ts`](packages/canvas-sync/src/awareness.ts)).

**دلیل:** `encodeAwarenessUpdate` هر بار **کلِ state** یک کلاینت را
`JSON.stringify` می‌کند — نه دیف، نه فقط فیلدِ عوض‌شده. پس یک استروک در state
به هر پیامِ awarenessِ بعدی **سنجاق می‌شود**، حتی وقتی خودش اصلاً عوض نشده.
سنجیده شد (گام ۳٫۵، `awareness.test.ts`):

| حالت | یک به‌روزرسانیِ awareness |
|---|---|
| state بدونِ استروک | **۱۹۸ بایت** |
| همان state + استروکِ ۲۰۰نقطه‌ای | **۵۳۶۲ بایت** |
| ★ فقط **مکان‌نما** تکان بخورد (استروک دست‌نخورده) | باز هم **۵۳۶۲ بایت** |

با throttleِ ۴۰ms یعنی یک ثانیه تکانِ ماوس حینِ کشیدن: **۲۱۴KB در برابرِ ۷٫۹KB** —
۲۷ برابر، آن هم روی شبکه‌ای که ADR-030 برایش تصمیم می‌گیرد. دو خاصیتِ دیگرِ
awareness هم با «موقت» ناسازگارند: state تا ۳۰ ثانیه **نگه داشته و تازه می‌شود**،
و به هر همتای **تازه‌رسیده** کامل تحویل می‌شود — یعنی یک استروکِ نیمه‌کشیده برای
کسی پخش می‌شد که اصلاً آنجا نبوده.

**هزینه‌ی پذیرفته‌شده:** دو کانال یعنی دو مسیرِ ادغام و یک `Map`ِ جداگانه برای
ephemeralِ همتاها. عوارضش هم واقعی است: اگر همتا برود و کسی آن `Map` را پاک نکند،
یک استروکِ یخ‌زده تا ابد روی بومِ همه می‌مانَد — نگهبانش یک تستِ صریح است.

**پیامدها:**

- `PeerState.ephemeral` از **حافظه‌ی binder** پر می‌شود، نه از stateِ awareness.
  خروجِ همتا **باید** ephemeralش را هم پاک کند (تستِ «استروکِ نیمه‌کاره»).
- سرور (فاز ۴) `HB_EPHEMERAL` را **باز نمی‌کند** — فقط پخش. همان چیزی که
  PLAN بخش ۵٫۳ می‌گوید و ADR-022 می‌خواهد: این داده هیچ‌جا ذخیره نمی‌شود.
- payloadِ نامفهوم (JSONِ خراب یا `kind`ِ ناشناخته‌ی یک کلاینتِ **جدیدتر**)
  ephemeralِ آن همتا را **پاک می‌کند**، نه اینکه مقدارِ قبلی را نگه دارد.
- ⚠️ `throttle` روی این کانال اعمال **نمی‌شود**: نرخش را خودِ بوم تعیین می‌کند
  (یک نقطه در هر فریم) و throttle کردنش استروکِ همتا را تکه‌تکه نشان می‌دهد.

---

<a id="adr-037"></a>

## ADR-037 — تخصیصِ `seq` برای هر بورد **سریالی** است

**وضعیت:** پذیرفته‌شده · **جایگزین‌ها:** تلاشِ دوباره‌ی بیشتر روی برخورد ·
`pg_advisory_xact_lock` به‌ازای بورد · یک `SEQUENCE`ِ Postgres برای هر بورد ·
`SERIALIZABLE` روی تراکنشِ append

**تصمیم:** نوشتن در `board_updates` برای **هر بورد** از یک صف رد می‌شود
([`postgres-update-log.ts`](apps/realtime/src/persistence/postgres-update-log.ts));
دو appendِ همان بورد هرگز هم‌پوشانی ندارند. بوردهای مختلف کاملاً موازی می‌مانند.
ایندکسِ یکتای `(board_id, seq)` و تلاشِ دوباره **سرِ جایشان می‌مانند** — ولی از
این به بعد نقششان «سدِ آخر برای چند نود» است، نه راهکارِ همزمانی.

**دلیل:** `seq` از `GREATEST(MAX(seq), MAX(seq_upto)) + 1` می‌آید و زیرِ
`READ COMMITTED` دو تراکنشِ همزمان ردیفِ commitنشده‌ی هم را **نمی‌بینند** — پس هر
دو یک عدد می‌گیرند و یکی‌شان به ایندکسِ یکتا می‌خورد. با بارِ واقعی این تصادف
نادر نیست، **قاعده** است: سنجه‌ی ۵۰۰ updateِ گام ۴٫۴ نشان داد **۴۳ تا از ۵۰۰**
append بعد از تمام‌شدنِ پنج تلاش **از بین می‌روند** — بی‌صدا، چون کاربر فقط
`unsaved` می‌بیند. زیاد کردنِ تعدادِ تلاش این را پنهان می‌کند نه رفع: هزینه‌اش با
همزمانی درجه‌دو بالا می‌رود و همیشه یک بارِ بالاتر هست که دوباره ببرد.
`pg_advisory_xact_lock` هم درست است ولی قفلِ صریحِ دیتابیس را وارد مسیرِ داغِ هر
update می‌کند در حالی که — با قفلِ صاحبِ [ADR-006](#adr-006) — هر بورد در هر لحظه
**یک نودِ صاحب** دارد و صفِ درون‌فرایندی همان تضمین را رایگان می‌دهد.

**هزینه‌ی پذیرفته‌شده:** نوشتنِ یک بورد موازی نمی‌شود؛ سقفِ نوشتنِ هر بورد
می‌شود `۱/تاخیرِ نوشتن`. اندازه‌گیری شد: **~۱۵۷۰ append در ثانیه** برای یک بورد
روی Postgresِ لوکال (و دو بوردِ همزمان هر کدام همان). چون یک ژستِ کاربر در
بدترین حالت چند update در ثانیه است، این سقف در عملِ M2 محدودکننده نیست.

**پیامدها:**

- ★ `UpdateLog.append` دیگر تضمین می‌کند `seq`ها **به ترتیبِ صدا زدن** داده
  می‌شوند. `since` با `ORDER BY seq` می‌خواند، پس ترتیبِ اعمالِ بارگذاری هم همان
  ترتیبِ وقوع است.
- ★ و `HB_ROOM_INFO.seq` دیگر **عقب نمی‌رود**؛ پیش‌تر یک appendِ کندترِ با `seq`ِ
  کوچک‌تر می‌توانست عددِ بزرگ‌ترِ قبلی را بازبنویسد و «ذخیره شد» پس برود.
- ⚠️ صف **درون یک فرایند** است. دو نود که همزمان روی یک بورد بنویسند باز هم به
  هم می‌رسند؛ تا آمدنِ قفلِ صاحب (گام ۴٫۷) ایندکسِ یکتا تنها سدِ باقی‌مانده است.
- شکستِ یک نوشتن **نباید** صفِ آن بورد را بخواباند — نگهبانش یک تستِ صریح است.

---

<a id="adr-038"></a>

## ADR-038 — `HB_ERROR` همیشه به معنای بستنِ اتصال نیست

**وضعیت:** پذیرفته‌شده · **جایگزین‌ها:** بستنِ اتصال روی هر `HB_ERROR` (خواندنِ
تحت‌اللفظیِ PLAN بخش ۵٫۳) · یک کدِ پیامِ **تازه** برای ردِ غیرمرگبار · فقط
`HB_PERMISSION` فرستادن و خطا ندادن

**تصمیم:** `0x14 HB_ERROR` دو کاربرد دارد و کلاینت باید از **کد** تشخیصشان بدهد:

| کِی | رفتار |
|---|---|
| ردِ **دستِ‌دادن** (`TOKEN_*`، `FORBIDDEN`ِ اتصال، `SERVER_BUSY`، `DOC_TOO_LARGE`) | پیام، سپس **بستن** با کد ۱۰۰۸ — همان چیزی که گام ۴٫۱ ساخت |
| ردِ **یک پیام** (`FORBIDDEN` روی updateِ بی‌مجوز — گام ۴٫۵) | پیام می‌رود، **اتصال باز می‌مانَد**؛ همراهش `HB_PERMISSION` با نقشِ واقعی |

**دلیل:** تنزلِ نقش وسطِ کار یک حالتِ **عادی** است، نه حمله — و دو دلیل، بستن را
در آن حالت هم *غلط* و هم *بی‌اثر* می‌کند. غلط: کسی که همین الان `viewer` شده
حقِ **دیدن** بورد را دارد؛ پرت کردنش بیرون یعنی تنزلِ دسترسی به قطعِ دسترسی
ترجمه شود. بی‌اثر: نقش داخلِ توکن است و توکن عوض نمی‌شود، پس کلاینتِ بسته‌شده
بلافاصله با همان توکن برمی‌گردد — یک حلقه‌ی بستن و اتصالِ مجدد که هیچ‌چیز را
اعمال نمی‌کند. خودِ [TODO گام ۴٫۵](TODO.md) هم «رد + `HB_ERROR{FORBIDDEN}`» را
برای updateِ `viewer` خواسته، یعنی این کاربردِ غیرمرگبار از اول در نظر بوده.

کدِ پیامِ تازه رد شد چون کلاینت‌های موجود آن را نمی‌شناسند و `decodeMessage`
عمداً پیامِ ناشناخته را **بی‌صدا** دور می‌ریزد — یعنی ردِ مجوز به کلاینتِ قدیمی
اصلاً نمی‌رسید. فقط `HB_PERMISSION` هم کافی نبود: کاربر باید بفهمد **این تغییرِ
مشخص** نوشته نشد، نه فقط اینکه نقشش عوض شده.

**هزینه‌ی پذیرفته‌شده:** «کد را ببین» یک قاعده‌ی رفتاری است و قاعده‌های رفتاری
فراموش می‌شوند. کلاینتی که هر `HB_ERROR` را مرگبار فرض کند، روی تنزلِ نقش
اتصالش را می‌بندد — بد، ولی نه فاجعه (دوباره وصل می‌شود و به‌عنوان تماشاگر
کار می‌کند). گام ۵٫۳ سمتِ کلاینت را درست پیاده می‌کند.

**پیامدها:**

- ردِ **پیام** هرگز `close` صدا نمی‌زند؛ نگهبانش یک تستِ صریح است که ثابت می‌کند
  سوکت باز مانده.
- هر ردِ پیام **باید** `HB_PERMISSION` را هم همراه ببرد — وگرنه کاربر می‌نویسد و
  هر بار بی‌صدا شکست می‌خورد.
- ★ **updateِ تهی «نوشتن» شمرده نمی‌شود.** پروتکلِ sync ایجاب می‌کند کلاینت به
  step1 با step2 جواب بدهد و آن step2 برای تماشاگرِ تازه صفر op دارد؛ رد کردنش
  یعنی یک هشدارِ امنیتیِ **کاذب** به‌ازای هر اتصالِ سالم. سنجه‌ی زنده‌ی
  `rt-permission` همین را نشان داد.

---

<a id="adr-039"></a>

## ADR-039 — اتصالِ مجدد: **کدِ بستن** سیاست را تعیین می‌کند، نه یک backoffِ یکسان

**وضعیت:** پذیرفته‌شده · **جایگزین‌ها:** یک backoffِ واحد برای هر قطعی · تلاشِ
دوباره‌ی بی‌قید و شرط (بدونِ حالتِ مرگبار) · بافر کردنِ پیام‌های خروجی در حالتِ
قطع · jitterِ کامل به‌جای نیمه · کتابخانه‌ی آماده‌ی reconnect

**تصمیم:** ترابریِ کلاینت ([`websocket-transport.ts`](packages/canvas-sync/src/websocket-transport.ts))
بعد از هر بسته‌شدن **سه** واکنشِ متفاوت دارد، و انتخاب از روی کدِ بستن و کدِ
`HB_ERROR` انجام می‌شود:

| کد | واکنش | چرا |
|---|---|---|
| **۱۰۰۱** `Going Away` | **فوری** (فاصله = ۰) | خاموشیِ مودبانه‌ی گام ۴٫۸: نودِ دیگری آماده است و `/readyz`ِ این یکی از قبل قرمز شده |
| **۱۰۰۸** + `TOKEN_EXPIRED` | **فوری** | یک توکنِ تازه دقیقاً همین را رفع می‌کند |
| **۱۰۰۸** + `SERVER_BUSY` · `ROOM_CLOSED` | backoff | خودِ `HB_ERROR_CODES` این دو را «موقتی» تعریف کرده |
| **۱۰۰۸** + بقیه (و **کدِ ناشناخته**) | **مرگبار** — تلاش متوقف | سرور صریحاً ردمان کرده؛ تکرار فقط حلقه است |
| هر چیزِ دیگر (۱۰۰۶، خطای شبکه، ۱۰۱۱) | backoff **نمایی + jitter** | قطعیِ گذرا |

و سه قاعده‌ی جانبیِ اجباری: توکن برای **هر تلاش** دوباره ساخته می‌شود · پیامِ
خروجی روی سوکتِ بسته **بافر نمی‌شود** · «فوری» بیش از دو بارِ پیاپی تکرار
نمی‌شود.

**دلیل:** یک backoffِ یکسان برای هر سه، هر دو سرِ طیف را خراب می‌کند. در خاموشیِ
مودبانه — که در استقرارِ عادی **هر روز** رخ می‌دهد — کاربر چند ثانیه بی‌دلیل قطع
می‌مانَد در حالی که خوشه کاملاً سالم است. در ردِ احراز هویت، کلاینت تا ابد با
توکنی که هرگز قبول نمی‌شود به سرور می‌کوبد. جدولِ بالا اختراع نشده: کامنت‌های
خودِ [`error-codes.ts`](packages/ydoc-schema/src/error-codes.ts) از گام ۲٫۳ صریحاً
نوشته‌اند که `SERVER_BUSY` «موقتی» و `DOC_TOO_LARGE` «دائمی» است و «کارِ کلاینت
در هرکدام فرق می‌کند» — اینجا فقط به کد ترجمه شد.

**jitterِ نیمه** (`delay ∈ [سقف/۲، سقف]`) به‌جای کامل (`[۰، سقف]`) انتخاب شد چون
کامل می‌تواند فاصله را به صفر برساند و نودی را که تازه بالا آمده بلافاصله
بکوبد؛ پخش‌شدگیِ ۲برابری برای مسئله‌ی رگبار کافی است. **بافر نکردنِ خروجی** از
خودِ CRDT می‌آید: اولین کارِ هر اتصال یک دست‌دادنِ کاملِ sync است که **همه‌ی**
حالت را می‌برد، پس بافر یعنی حافظه‌ی بی‌کران برای داده‌ای که همان لحظه تکراری
می‌شود. **سقفِ «فوری»** از یک باگِ واقعیِ همین گام آمد: تامین‌کننده‌ی توکنی که
مقدارِ منقضی را کَش کند، بی‌سقف یک حلقه‌ی تنگ با تمامِ توانِ CPU می‌سازد.

کتابخانه‌ی آماده رد شد چون هیچ‌کدام کدِ بستن را به سیاست ترجمه نمی‌کنند — همان
تنها چیزی که این تصمیم درباره‌اش است — و P1 هم یک وابستگیِ دیگر را گران می‌کند.

**هزینه‌ی پذیرفته‌شده:** «کدِ ناشناخته = مرگبار» یعنی اگر سرورِ آینده کدِ
**گذرای** تازه‌ای اضافه کند، کلاینت‌های قدیمی روی آن تسلیم می‌شوند و کاربر باید
رفرش کند. جهتِ امن همین است: سکوت بهتر از حلقه‌ای است که سرور را می‌کوبد. و
`offline` فقط از `navigator.onLine` می‌آید که در Node وجود ندارد، پس آن مسیر
**در سنجه‌ی زنده آزموده نمی‌شود** و نگهبانش تستِ واحد با محیطِ ساختگی است.

**پیامدها:**

- ★ نگاشتِ وضعیتِ ترابری به `ConnectionState` **فقط در `adapter.ts`** است. هر
  ترجمه‌ی دومی یک منبعِ دومِ حقیقت است.
- ★ **دست‌دادنِ sync روی هر بار باز شدن تکرار می‌شود**، نه فقط اولی — و حضور هم
  با شمارنده‌ی **جلورفته** دوباره معرفی می‌شود. سرور با بسته‌شدنِ سوکت حذفِ ما را
  پخش کرده و پیامی با clockِ مساوی بی‌صدا دور ریخته می‌شود.
- ⚠️ `token()` باید **واقعاً تازه** بسازد. کَش‌کردنش یعنی هر بازگشت پس از قطعیِ
  طولانی‌تر از عمرِ توکن رد می‌شود؛ `pnpm rt:reconnect` با توکنِ ۵ثانیه‌ای همین را
  می‌آزماید.
- سرور از این به بعد `0x10 HB_AUTH_REFRESH` را **می‌فهمد** (پیش‌تر بی‌صدا نادیده
  می‌گرفت). هویت در تازه‌سازی **نباید** عوض شود و ردِ آن اتصال را نمی‌بندد
  ([ADR-038](#adr-038)).

---

<a id="adr-040"></a>

## ADR-040 — سازگاریِ نسخه را **خودِ کلاینت** می‌سنجد، نه سرور

**وضعیت:** پذیرفته‌شده · **جایگزین‌ها:** اعلامِ نسخه‌ی کلاینت در URLِ دست‌دادن
(`&v=`) و ردِ سرور با `HB_ERROR{CLIENT_TOO_OLD}` · یک پیامِ **تازه**ی پروتکل برای
اعلامِ توانایی · نسنجیدن و امید به سازگاریِ رو به عقب

**تصمیم:** کلاینت بعد از هر همگام‌سازی و پیش از اولین رندر، نسخه‌ی
`meta.schemaVersion`ِ سند را با نسخه‌ای که خودش می‌فهمد می‌سنجد
(`checkClientVersion`، [`adapter.ts`](packages/canvas-sync/src/adapter.ts)). اگر
سند جلوتر بود: **ترابری قطع می‌شود**، مجوزها به فقط-خواندنی می‌روند، و
`ConnectionState.error` با پیامِ فارسی و درخواستِ رفرش می‌نشیند. مسیرِ سرور
(`HB_ERROR{CLIENT_TOO_OLD}`) هم اگر روزی فعال شود به **همان** تابع می‌رسد.

**دلیل:** طرحِ اولیه — که در کامنتِ خودِ `checkClientVersion` و در
[`canvas-sync/src/index.ts`](packages/canvas-sync/src/index.ts) نوشته شده بود —
سرور را داور می‌گرفت. ولی سرور **نسخه‌ی کلاینت را نمی‌داند**: نه در URLِ
دست‌دادن جایی برایش هست و نه در پروتکل. برای داوریِ سرور باید یکی از این دو را
پهن می‌کردیم، آن هم برای سنجشی که کلاینت خودش کامل می‌تواند انجام دهد.

★ **و یک دلیلِ قوی‌تر که با گام ۵٫۲ پیدا شد:** از وقتی سند در IndexedDB می‌مانَد،
یک سندِ **جلوتر** می‌تواند اصلاً بدونِ عبور از سرور به کلاینت برسد — تبِ دیگری
با بیلدِ جدیدتر رویش کار کرده و همان مرورگر ذخیره‌اش کرده. داوریِ سرور این حالت
را **اصلاً نمی‌بیند**.

**قطع کردن** (نه فقط هشدار) از هشدارِ صریحِ خودِ `checkClientVersion` می‌آید:
کلاینتِ عقب‌تر ساختاری را که نمی‌فهمد بازنویسی می‌کند و **برای بقیه هم خرابش
می‌کند**. پس ترتیب هم عمدی است: اول قطع، بعد خبر.

**هزینه‌ی پذیرفته‌شده:** کلاینت سندِ جدیدتر را **قبل از** فهمیدن روی `Y.Doc`ِ
خودش اعمال می‌کند. Yjs ساختارِ ناشناخته را سالم نگه می‌دارد و ما پیش از
`replaceDocument` جلویش را می‌گیریم، ولی این یعنی تضمینِ ما «چیزی رندر نمی‌شود»
است، نه «چیزی دریافت نمی‌شود». و چون رد کردن **محلی** است، سرور نمی‌داند چند
کلاینتِ قدیمی پس زده شده‌اند — تا M3 که تله‌متری دارد، این عدد را نداریم.

**پیامدها:**

- ★ `supportedSchemaVersion` یک **ورودیِ** آداپتور است، نه یک ثابتِ سراسری. اپ
  می‌تواند محافظه‌کارتر اعلام کند، و تست می‌تواند کلاینتِ عقب‌تر بسازد.
- ⚠️ پیامِ خطا **باید فارسی باشد**: `StatusBar`ِ M1 متنِ `error` را همان‌طور که
  هست چاپ می‌کند. و چون `packages/i18n` دامنه‌ی M2 نیست، رشته درون‌خطی است —
  همان کاری که `RtProtocolError`ِ سرور می‌کند.
- کلاینتِ **جلوتر** رد نمی‌شود؛ فقط عقب‌تر. سندِ بدونِ مهرِ نسخه هم رد نمی‌شود
  (بوردِ نو).

---

<a id="adr-041"></a>

## ADR-041 — اتاق هنگام باز شدن **حالتِ تازه‌تر را از نودهای دیگر می‌پرسد**

**وضعیت:** پذیرفته‌شده (تاییدِ مالک ۱۴۰۵/۰۵/۲۲) · **جایگزین‌ها:** بافر کردنِ
updateهای پایدارنشده در نودِ غیرِ صاحب · ردِ نوشتن تا وقتی صاحبی نیست · کوتاه
کردنِ اجاره‌ی قفل · هیچ‌کاری (ثبت به‌عنوان محدودیت)

**تصمیم:** نودی که اتاقی را باز می‌کند، **بعد از** اشتراکِ گذرگاه و تخلیه‌ی
بافر، یک `BUS_KINDS.STATE_REQUEST` منتشر می‌کند. هر نودی که همان اتاق را باز
دارد با یک `BUS_KINDS.UPDATE` حاویِ `Y.encodeStateAsUpdate(room.doc)` جواب
می‌دهد. جواب از همان مسیرِ همیشگیِ `UPDATE` اعمال می‌شود.

**دلیل:** خواندن از Postgres فقط چیزی را می‌آورد که **پایدار شده** — و یک پنجره
هست که در آن هیچ‌چیز پایدار نمی‌شود. قفلِ صاحب یک اجاره‌ی ۳۰ثانیه‌ای است
([ADR-006](#adr-006) فاز ۲)؛ وقتی نودِ صاحب می‌میرد، اجاره‌اش **تا انقضا** دستِ
یک نودِ مرده می‌مانَد. در آن فاصله `persistAndFanout` وارد شاخه‌ی `!room.owner`
می‌شود و درست هم هست — ولی یعنی تنها نسخه‌ی آن update بیرون از کلاینت، حافظه‌ی
نودی است که گرفتتش. نودی که آن پیامِ گذرگاه را از دست بدهد **هرگز** به آن
نمی‌رسد: pub/sub گذشته را نگه نمی‌دارد و دیتابیس چیزی ندارد.

⚠️ **و این حدس نبود.** معیارِ پذیرشِ G-1ب (گام ۶٫۱) در ۳ اجرا از ۴ قرمز شد و
بردارِ وضعیت دقیقاً همین را نشان داد: کلاینتِ الف روی `[الف@۹۶، ب@۹۴]` و
کلاینتِ ب برای همیشه روی `[الف@۱، ب@۹۴]`. **ADR-009 فنی نقض نمی‌شد** — نودِ غیرِ
صاحب `saving` می‌فرستد نه `saved` — ولی دو کلاینت برای همیشه واگرا می‌مانند و
هیچ‌کس خبردار نمی‌شود.

**بافر کردن** رد شد چون حافظه‌ی بی‌کران در قطعیِ طولانی می‌خواهد و یک مسیرِ
حالت‌دارِ تازه در `room.ts` می‌سازد. **ردِ نوشتن** رد شد چون کاربر را تا ۳۰
ثانیه فلج می‌کند برای مسئله‌ای که راهِ ارزان‌تری دارد. **کوتاه کردنِ اجاره**
پنجره را کوچک می‌کند ولی حذفش نمی‌کند — و اجاره‌ی کوتاه‌تر یعنی صاحب‌بودن روی
هر کندیِ شبکه از دست می‌رود.

**هزینه‌ی پذیرفته‌شده:** با N نودی که یک بورد را باز دارند، هر بار باز شدنِ اتاق
N−۱ پاسخِ **حالتِ کامل** می‌سازد. برای بورد بزرگ و خوشه‌ی پهن این ترافیک بی‌ربطی
است. امروز اهمیتی ندارد (فاز ۳ ADR-006 — room affinity — هنوز لازم نشده) ولی
اگر روزی شد، پاسخ باید به یک نود محدود شود یا با `state vector` به دیف تبدیل.

**پیامدها:**

- ★ **حلقه ساختاراً ممکن نیست:** پاسخ از نوعِ `UPDATE` است و `UPDATE` هیچ‌وقت
  پرسش تولید نمی‌کند. یک تستِ صریح شمارشِ پرسش‌ها را پین کرده.
- ★ جای انتشار **بعد از** تخلیه‌ی بافرِ اشتراک است، وگرنه پاسخ به اتاقی می‌رسد
  که هنوز `handleBus` ندارد — همان تله‌ای که «اشتراک قبل از خواندن» برای
  جلوگیری‌اش هست.
- ⚠️ `handleBus` کدِ ناشناخته را از قبل بی‌صدا رد می‌کند، پس نودِ قدیمی در یک
  استقرارِ چرخشی فقط جواب نمی‌دهد — چیزی نمی‌شکند.
- **حالتِ کامل فرستاده می‌شود، نه دیف:** پرسنده بردارِ وضعیتش را نمی‌فرستد و
  Yjs opهای تکراری را دور می‌ریزد. بدترین هزینه پهنای باند است، در برابرِ
  گم‌شدنِ کارِ کاربر.

---

<a id="adr-042"></a>

## ADR-042 — claimهای `rtToken` به `shared-types` می‌روند (جایگزینِ قیدِ ADR-031)

**وضعیت:** پذیرفته‌شده (تاییدِ مالک ۱۴۰۵/۰۵/۲۴) · **جایگزینِ** بخشِ «`rtToken` بیرونِ
`shared-types`»ی [ADR-031](#adr-031) · **جایگزین‌های ردشده:** ماندن در پورتِ
`BoardAuthority` (قیدِ M2، حالا منقضی) · تعریفِ موازی در دو ماژول (نقضِ [ADR-021](#adr-021))

**تصمیم:** شکلِ claimهای توکنِ اتصالِ WebSocket — `{ sub, boardId, role, exp }` — به
`packages/shared-types` منتقل می‌شود و هر دو طرف از همان یک تعریفِ zod می‌خوانند: `apps/api`
که آن را **صادر** می‌کند (endpointِ `rt-token`) و `apps/realtime` که **مصرفش** می‌کند
(`AuthCoreBoardAuthority.verify`). هیچ تعریفِ موازی نمی‌مانَد. `role` از enumِ `BoardRole`ِ
همان `shared-types` می‌آید، نه یک رشته‌ی آزاد.

**دلیل:** [ADR-031](#adr-031) عمداً این شکل را **بیرونِ** `shared-types` نگه داشت، با دلیلی که
برای آن زمان درست بود: قرارداد وقتی معنا دارد که **هر دو طرفش وجود داشته باشند**، و در M2 فقط
سرورِ realtime آن را می‌نوشت — صادرکننده (M3) هنوز شکل نداشت، پس ثبتش یعنی قفل‌کردنِ یک **حدس**
روی هر شش ماژول. حالا آن شرط برقرار نیست: M3 هم صادرکننده (`rt-token`) و هم پیاده‌سازیِ
مصرف‌کننده (`auth-core`) را می‌سازد، پس `rtToken` دقیقاً همان چیزی است که [ADR-021](#adr-021)
می‌خواهد در `shared-types` باشد — یک شکلِ داده که از مرزِ دو ماژول عبور می‌کند. نگه‌داشتنش در
پورت از این‌جا به بعد یعنی همان تعریفِ موازی‌ای که ADR-021 منع کرده.

**پیامدها:**

- ★ **قیدِ «`rtToken` بیرونِ `shared-types`»ی [ADR-031](#adr-031) از ۱۴۰۵/۰۵/۲۴ منقضی است**؛ این
  ADR جایگزینش می‌شود. **بقیه‌ی ADR-031 دست‌نخورده می‌مانَد** — پورتِ `SnapshotStore`، ممنوعیتِ
  `@aws-sdk`ِ خام در M2، و گیتِ production (سرور با `APP_ENV=production` و پیاده‌سازیِ dev بالا نیاید).
- افزودنش به `shared-types` طبق [ADR-021](#adr-021) **تاییدِ مالک** خورد (همین ADR). گام مقصد: ۲٫۳.
- ⚠️ **قیدِ گام ۴٫۵/۵٫۱ی M2 هنوز برقرار است:** نقشِ داخلِ توکن حرفِ آخر را نمی‌زند؛ سرور روی هر
  update `currentRole` را می‌سنجد ([ADR-012](#adr-012)). این ADR فقط **شکلِ سیم** را مشترک می‌کند،
  نه اینکه توکن را منبعِ حقیقتِ نقش کند.

---

<a id="adr-043"></a>

## ADR-043 — `BoardRole` منبعِ حقیقتش به `shared-types` می‌رود (لمسِ حداقلیِ `ydoc-schema`)

**وضعیت:** پذیرفته‌شده (تاییدِ مالک ۱۴۰۵/۰۵/۲۴) · **جایگزین‌های ردشده:** تعریفِ دومِ `BoardRole`
در `shared-types` (کپی — نقضِ [ADR-024](#adr-024)/[ADR-021](#adr-021)) · ماندن در `ydoc-schema` و
import از آنجا در DTOها (ناممکن — جهتِ لایه)

**تصمیم:** enumِ `BoardRole` (آرایه‌ی `boardRoles`) منبعِ حقیقتش از `ydoc-schema` به
`packages/shared-types` منتقل می‌شود. `ydoc-schema` آن را **وارد** می‌کند و `BOARD_ROLES`/`BoardRole`
را برای مصرف‌کننده‌های خودش (پروتکلِ سیم، `permission.ts`، auth) **re-export** می‌کند. **ترتیبِ آرایه
دست‌نخورده می‌مانَد** (`owner, editor, commenter, viewer`).

**دلیل:** قراردادِ APIِ گام ۲٫۲ به `BoardRole` نیاز دارد (`Board.myRole`، `boardMember.role`).
`shared-types` پایین‌ترین لایه است و **نمی‌تواند** `ydoc-schema` را import کند (جهتِ لایه، PLAN §۲)،
و [ADR-021](#adr-021) تعریفِ موازی را منع می‌کند. پس تنها راهِ داشتنِ **یک** `BoardRole` بردنش به
پایین‌ترین لایه است — همان الگوی [ADR-042](#adr-042): قرارداد وقتی مصرف‌کننده‌ی دومی پیدا کرد به
`shared-types` می‌رود. در M2 عمداً در `ydoc-schema` ماند چون آن ماژول باید بدونِ تغییرِ `shared-types`
تمام می‌شد؛ آن قید حالا برداشته شده.

**پیامدها:**

- ★ **ترتیبِ `boardRoles` روی سیمِ WS با ایندکس می‌رود** ([`protocol.ts`](../packages/ydoc-schema/src/protocol.ts)) —
  پس فقط می‌شود به **انتها** اضافه کرد؛ جابه‌جایی یعنی کلاینتِ قدیمی نقشِ اشتباه (بدترین باگِ مجوز).
  انتقال ترتیب را حفظ کرد و یک تست در `shared-types` (`api/api.test.ts`) آن را پین می‌کند.
- API عمومیِ `ydoc-schema` عوض نمی‌شود (`BOARD_ROLES`/`BoardRole` هنوز از همان‌جا صادر می‌شوند)، پس
  `permission.ts` و auth دست‌نخورده‌اند. وابستگیِ `ydoc-schema → shared-types` از قبل وجود داشت.
- ⚠️ **این یک لمسِ M2 است** (تغییر در `ydoc-schema`، ماژولِ تحویل‌شده) — با تاییدِ صریحِ مالک، مثلِ B-1.

---

<a id="adr-044"></a>

## ADR-044 — آپلودِ دارایی با presigned POST + `content-length-range` (نه presigned PUT)

**وضعیت:** پذیرفته‌شده (تاییدِ مالک ۱۴۰۵/۰۵/۲۸، بر پایه‌ی probe ۳٫۰ روی MinIO) · **جایگزین‌های ردشده:**
presigned PUT با `Content-Length`ِ امضاشده · اعتبارسنجیِ سقف فقط در `commit`

**تصمیم:** آپلودِ مستقیمِ کلاینت→Object Storage با **presigned POST** انجام می‌شود که policyی با
`content-length-range` (سقفِ اندازه) و `eq $Content-Type` (نوع) دارد. `ObjectStore.presignUpload` و
endpointِ `/assets/presign` شکلِ **`{ url, fields }`** می‌دهند (فرمِ multipart، `file` آخرین فیلد)، نه
`{ uploadUrl, headers }`. `commit` سپس بایت‌های واقعی را **مستقلاً** می‌سنجد.

**دلیل:** probe ۳٫۰ با عدد نشان داد presigned **PUT** سقف را قابل‌اتکا اعمال نمی‌کند: PUTِ بدونِ امضای
`content-length` بدنه‌ی ۵۰۰۰بایتی را با ۲۰۰ پذیرفت (هیچ سقفی)، و امضای `content-length` فقط یک اندازه‌ی
**دقیق** را پین می‌کند نه بازه — آن‌هم اگر یادت بماند امضایش کنی. تنها مکانیزمی که سقف را **سمتِ سرور** و
به‌صورتِ بازه اعمال می‌کند POST-policyِ `content-length-range` است (استانداردِ S3 برای آپلودِ مستقیمِ مرورگر).
هزینه‌ی پذیرفته‌شده: فرمِ multipart از یک PUTِ ساده پیچیده‌تر است، و کلاینت باید `file` را آخرین فیلد بگذارد.

**پیامدها:**

- ★ **`presignPut` عمداً در امضای `ObjectStore` وجود ندارد.** یک متدِ PUTِ بی‌سقف نباید باشد که کسی اشتباهی
  صدایش بزند — «تبدیلِ ممنوع به غیرممکن». آپلود فقط `presignUpload`ِ POST-policy است.
- ★ **`commit` لایه‌ی دومِ اعتماد است، نه اول.** policy فقط نوعِ **اعلامی** و سقف را می‌بندد؛ نوعِ **واقعی**
  (sniffِ magic-bytes)، `sha256`ِ **بازمحاسبه‌شده روی بایت‌های واقعی**، و اندازه‌ی واقعی فقط در commit
  سنجیده می‌شوند — چون آپلودِ مستقیم دورِ سرور را می‌زند. ⚠️ **سرور هرگز به ادعای کلاینت درباره‌ی
  sha256/نوع اعتماد نمی‌کند** (قیدِ مالک ۱۴۰۵/۰۵/۲۸): `validateUploaded` خودش هش می‌گیرد و مقایسه می‌کند.
- منطق در `packages/assets` است — **مصرف‌کننده‌ی `packages/storage`، نه بخشی از آن** ([ADR-029](#adr-029)):
  قاعده‌ی mime/کلید/sniff/sha256 اینجا، دروازه‌ی S3 آنجا نازک. endpointهای HTTP + جدولِ `files` + دی‌دوپِ
  `sha256` در **فاز ۵**.
- ⚠️ **قیدِ [ADR-013](#adr-013) هنوز برقرار:** روی MinIO تایید شد؛ رفتارِ دقیقِ آروان هنگامِ دیپلوی (M5)
  باید تایید شود. `content-length-range` استانداردِ S3 است، پس محتمل‌ترین گزینه‌ی قابل‌حمل.
- **PLAN §۵٫۲ به‌روز شد** (یادداشتِ ۱): پاسخِ `/assets/presign` = `{ fileId, url, fields }`.

---

<a id="adr-045"></a>
## ADR-045 — پاسخِ api دقیقاً شکلِ DTOی `shared-types` را می‌دهد (لایه‌ی serialize)

**وضعیت:** پذیرفته‌شده (تاییدِ مالک ۱۴۰۵/۰۶/۰۸، هنگامِ فاز ۶ — گزینه A) · **جایگزین‌های ردشده:**
باریک‌کردنِ DTOها به snake_case · تعریفِ typeِ موازیِ snake_case در sdk

**تصمیم:** هر endpointِ REST که یک موجودیت (User/Team/Board/Folder/…) برمی‌گرداند، ردیفِ خامِ DB را از یک
**لایه‌ی serialize** ([`apps/api/src/dto.ts`](apps/api/src/dto.ts)) می‌گذراند که آن را به شکلِ **دقیقِ** DTOی
camelCaseِ `@hamboom/shared-types` درمی‌آورد. ردیفِ خامِ snake_case هرگز مستقیم برنمی‌گردد.

**دلیل:** `shared-types` قراردادِ مشترک است ([ADR-021](#adr-021)) و OpenAPI از همان تولید می‌شود ([گام ۵٫۵]).
ولی تا فاز ۶ api ردیفِ خام می‌داد (`team_id`، `display_name`، بدونِ `createdBy`/`memberCount`)، پس **قرارداد
دروغ بود**: کلاینتی که `Board` می‌گرفت `board.teamId === undefined` می‌دید. چرا بی‌صدا مانده بود؟ curlِ فاز ۵
فقط **مقدار** را چک کرده بود نه **شکل**، و گاردِ دریفتِ OpenAPI **مسیر** را می‌سنجد نه **بدنه**. سه گزینه بود؛
تنها «api را به قرارداد برسان» هم sdk را صادق می‌کند هم OpenAPI را، **بی‌آنکه قرارداد را کوچک کند** یا typeِ
موازی بسازد. هزینه‌ی پذیرفته‌شده: یک نگاشتِ per-entity در `dto.ts` و چند JOIN/subquery بیشتر (`createdBy` با
JOINِ سازنده، `memberCount`، `isFavorite`ِ per-user).

**پیامدها:**

- ★ **تنها نقطه‌ی نگاشتِ ردیف→DTO `apps/api/src/dto.ts` است.** endpointِ تازه که موجودیت می‌دهد **باید** از
  mapper رد شود، نه اینکه `rows[0]` را برگرداند. تاریخ‌ها ISO string (نه `Date`)، bigint (docSizeBytes/memberCount) عدد.
- ★ **گیتِ صادق‌بودن: `pnpm sdk:contract`** هر پاسخ را با zodِ `shared-types` **parse** می‌کند — همان چیزی که
  curl نمی‌گرفت. اگر روزی endpointی دوباره ردیفِ خام بدهد، این تست **قرمز** می‌شود.
- envelopeهای api-محور (پاسخِ verify/me/access) خودشان قرارداد جدا نیستند، ولی **موجودیت‌های داخلشان** DTO اند.
- **`Folder` به `shared-types` افزوده شد** (sdk مصرف‌کننده‌اش شد — همان الگوی «قرارداد وقتی مصرف‌کننده پیدا کرد»).
- ⚠️ فیلدهای «آینده»ی DTO در M3 مقدارِ صادقِ پیش‌فرض دارند: `avatarUrl`/`thumbnailUrl`/`templateId` → `null`،
  `linkToken`ِ پاسخِ بورد → `null` (توکنِ خام فقط یک‌بار در `PUT /access`). با آمدنِ آواتار/قالب/worker پُر می‌شوند.

---

<a id="adr-046"></a>
## ADR-046 — `BoardAccessReader`ِ pg در یک پکیجِ مشترک (api و realtime یک منبع)

**وضعیت:** پذیرفته‌شده (تاییدِ مالک ۱۴۰۵/۰۶/۰۸، هنگامِ فاز ۷) · **جایگزین‌های ردشده:** کپیِ مستقل در
`apps/realtime` + تستِ conformance · فراخوانیِ HTTPِ realtime→api برای نقش

**تصمیم:** پیاده‌سازیِ pgِ پورتِ `BoardAccessReader` (کوئریِ JOINداری که ورودیِ `effectiveBoardRole` را
می‌دهد) از `apps/api` به یک پکیجِ کوچکِ مشترک **`packages/board-access-db`** رفت. هم `apps/api` (endpointهای
REST) و هم `apps/realtime` (`currentRole`ِ `AuthCoreBoardAuthority`) از **همان `createPgBoardAccessReader`**
استفاده می‌کنند و `Queryable` (pool/tx) را تزریق می‌کنند.

**دلیل:** فاز ۷ به realtime هم یک reader می‌داد. اگر realtime کپیِ خودش را می‌ساخت، دو نسخه از کوئریِ
دسترسی (با منطقِ DP-4 و گیتینگِ `access_mode`) پیدا می‌شد که می‌توانند **واگرا** شوند — و آن‌وقت api و
realtime سرِ «چه کسی حقِ نوشتن دارد» **اختلاف** پیدا می‌کنند، دقیقاً همان حفره‌ای که [ADR-012](#adr-012)
می‌بندد. تستِ conformance می‌توانست واگرایی را بگیرد، ولی فقط **وقتی کسی اجرایش کند**؛ منبعِ واحد واگرایی را
**غیرممکن** می‌کند. هزینه‌ی پذیرفته‌شده: یک پکیجِ کوچکِ جدید (با `pg`)، و یک لمسِ `apps/api` (انتقالِ یک فایل).

**پیامدها:**

- ★ **`effectiveBoardRole` (منطق) در `auth-core` است، `BoardAccessReader` (داده) در `board-access-db`.** هر دو
  مشترک؛ هیچ‌کدام در اپ کپی نمی‌شود. endpoint/سرورِ تازه که نقش لازم دارد، از همین دو استفاده می‌کند.
- ★ مرزِ پکیج: `board-access-db` **فقط یک کوئری است** — `pg` مجاز، ولی UI/بوم/`sdk`/`storage`/`ws`/`ioredis`/
  `@aws-sdk` ممنوع (گیتِ `boardAccessDbBoundaries`، خودآزمونِ سه‌لایه). `process.env` نمی‌خواند؛ `Queryable` تزریقی.
- realtime دیگر برای نقش به api **زنگ نمی‌زند** (نه HTTP، نه وابستگیِ اپ‌به‌اپ) — همان مدلِ ADR-031: منطق/داده‌ی
  مشترک در پکیج، نه فراخوانیِ سرویس.

---

<a id="adr-047"></a>
## ADR-047 — پیام‌های پروتکلِ کلاینت در `canvas-sync` می‌مانند؛ `apps/web` نمایششان می‌دهد

**وضعیت:** پذیرفته‌شده (M3 فاز ۸٫۴، حلِ §۲ handoff) · **جایگزین‌های ردشده:** بردنِ رشته‌ها به `packages/i18n` ·
رها کردنشان به‌صورت console-only

**تصمیم:** رشته‌های فارسیِ **رو-به-کاربرِ** لایه‌ی سنک (مثلِ `TOO_OLD_MESSAGE` در `adapter.ts` و پیامِ
`RtProtocolError`های سرور) همان‌جا در `canvas-sync`/`apps/realtime` **می‌مانند** — به `i18n` **برده نمی‌شوند**.
`apps/web` مسئولِ **نمایششان** به کاربر است: `onProtocolError` به‌جای `console.warn`ِ تنها، یک نوتیسِ کوتاه
(`error.message` که از قبل فارسیِ آماده است) روی نوارِ بورد نشان می‌دهد. کپیِ **خاصِ وب** (برچسب‌های اتصال/ذخیره)
هم درون‌خطی در خودِ `apps/web` است.

**دلیل:** این پیام‌ها به لایه‌ی سنک **چسبیده‌اند** — `StatusBar`ِ M1 متنِ `connection.error` را همان‌طور که هست
چاپ می‌کند و سرور هم دقیقاً همان قالب را می‌دهد؛ بردنشان به `i18n` یک **churnِ لمسِ M2** است (تاییدِ مالک لازم،
مثلِ گام ۸٫۵) بی هیچ سودِ کاربری. ولی رها کردنشان به‌صورت console-only هم غلط بود: کاربرِ `viewer` که تلاشِ نوشتن
می‌کند باید **بفهمد چرا اعمال نشد**، نه اینکه سکوت ببیند. هزینه‌ی پذیرفته‌شده: کپیِ رو-به-کاربر در دو جا می‌مانَد
(لایه‌ی سنک برای پیام‌های پروتکل، `apps/web` برای برچسب‌های خودش)؛ اگر M4 به i18nِ کامل رفت، آن‌وقت یک‌جا جمعشان می‌کند.

**پیامدها:**

- ★ **`apps/web` هیچ پیامِ پروتکلی را بی‌صدا دور نمی‌ریزد** — هر `onProtocolError` باید به کاربر دیده شود.
- رشته‌های `canvas-sync`/`apps/realtime` رو-به-کاربر **فارسیِ آماده** می‌مانند (نه کلید، نه انگلیسی) تا مصرف‌کننده
  بتواند مستقیم نشانشان دهد؛ این قید همان چیزی است که کامنتِ `TOO_OLD_MESSAGE` از M2 پین کرده بود.
- بردنِ این رشته‌ها به `i18n` در آینده **تصمیم/ADRِ جدا + تاییدِ مالک** می‌خواهد (لمسِ M2).

---

<a id="adr-048"></a>
## ADR-048 — room affinity (ADR-006 فاز ۳) به M5 موکول، با تریگرِ عددیِ حافظه

**وضعیت:** پذیرفته‌شده (M3 گام ۱۱٫۱، تاییدِ مالک ۱۴۰۵/۰۶/۱۳) · **جایگزینِ ردشده:** ساختِ لایه‌ی مسیریابیِ
`boardId→node` همین حالا در M3

**تصمیم:** فازِ ۳ی [ADR-006](#adr-006) (room affinity با hashing سازگار) در M3 **پیاده نمی‌شود** و به **M5**
(زیرساختِ production) موکول می‌شود — ولی برخلافِ «بعداً»، تریگرش با **عدد** ثبت می‌شود:

- **پایه‌ی اندازه‌گیری‌شده ([realtime-baseline.md](docs/realtime-baseline.md)):** بوردِ ۵۰۰۰عنصری = **۳٫۶۶MB سند**
  ولی **~۷۶MB حافظه‌ی اتاق** (Yjs structs + JS + excalidraw + awareness؛ ~۲۰× سند).
- **سقفِ تک‌نود (فاز ۱):** ظرفیتِ هر نود **حافظه‌محور** است، نه تعدادِ اتاق — `RT_MAX_ROOMS_PER_NODE=۵۰۰` گیتِ موثر
  نیست (۵۰۰ بوردِ بزرگ ≈ ۳۸GB). بودجه‌ی تقریبیِ نود = `heap مفید ÷ ۷۶MB`؛ برای heapِ ۴GB
  (`--max-old-space-size=4096` منهای ~۱GB سربار) ≈ **~۴۰ بوردِ ۵۰۰۰عنصریِ همزمان**.
- **تریگرِ فعال‌سازیِ فاز ۳ (affinity):** وقتی **هر دو** برقرار شوند — (۱) `apps/realtime` روی **≥۲ رِپلیکا** اجرا شود
  (فاز ۲ فعال)، **و** (۲) بیشینه‌ی حافظه‌ی مقیمِ اتاق‌ها روی **یک نود از ~۶۰٪ heapِ آن نود بگذرد** (اندازه‌گیری‌شده) —
  یعنی **حافظه** (نه تعدادِ اتصال یا CPU) قیدِ مقیاس شود. در فاز ۲ هر بوردِ بازِ روی N نود، N×۷۶MB می‌گیرد؛ affinity آن N را ۱ می‌کند.
- تا آن آستانه: فاز ۲ (ساخته‌شده در M2، تستِ `rt:cluster`) از نظر **صحت** کافی است (updateهای Yjs جابه‌جایی‌پذیر/idempotent)،
  فقط از نظر حافظه بهینه نیست.

**دلیل:** خودِ ADR-006 می‌گوید «شروع با shardingِ پیچیده خطای رایج است» و حافظه فقط در مقیاسِ واقعی مسئله می‌شود.
M3 تک‌نود و لوکال است (اصل P3): نودِ دومی نیست که چیزی رویش مسیریابی شود، و `sessionAffinity`ِ لازمِ فاز ۳ یک تنظیمِ
**Service/K8s** است که مالکش **M5** است (Dockerfile production/K8s/CI = صریحاً بیرونِ M3). پس پیاده‌سازیِ الانِ affinity
زیرساختی می‌سازد که نه اجرا می‌شود نه end-to-end قابلِ اثبات است — دقیقاً همان «shardingِ زودهنگام». هزینه‌ی پذیرفته‌شده:
در بازه‌ی چندنودیِ فاز ۲، حافظه با ضریبِ تکرارِ N هدر می‌رود تا وقتی تریگر برسد؛ چون **عددش را داریم**، این هدررفت
قابلِ پایش و پیش‌بینی است، نه غافلگیری.

**پیامدها:**

- ★ **معیارِ ظرفیتِ realtime = حافظه، نه تعدادِ اتاق.** HPA/آلارمِ M5 باید روی حافظه‌ی مقیمِ نود (یا مشتقی از آن) باشد، نه CPU/شمارِ اتاق.
- فاز ۳ (affinity) یک **آیتمِ ارثیه‌ی M5** است و در [`docs/m4-handoff.md`](docs/m4-handoff.md) با همین تریگرِ عددی ثبت می‌شود
  (M5 پیاده‌اش می‌کند، نه M4؛ اینجا فقط نقطه‌ی ورودش ثبت است).
- وقتی تریگر رسید: لایه‌ی مسیریابیِ `boardId→node` (hashing سازگار) + `sessionAffinity`ِ Service، **بدونِ** حذفِ
  Redis pub/sub (فقط ترافیکش ~صفر می‌شود).
- تا آن‌وقت اگر گیتِ سختِ ظرفیت لازم شد، باید بر پایه‌ی **بایتِ حافظه** بازتعریف شود (کارِ M5)، نه `RT_MAX_ROOMS_PER_NODE`.

---

## راهنمای افزودن ADR جدید

```markdown
<a id="adr-0XX"></a>
## ADR-0XX — <عنوان کوتاه و قاطع>

**وضعیت:** پیشنهادی | **جایگزین‌ها:** <چه چیزهایی رد شدند>

**تصمیم:** <در یک تا سه جمله، دقیقاً چه کاری می‌کنیم>

**دلیل:** <یک پاراگراف. چرا این، و چرا نه آن یکی. هزینه‌ی پذیرفته‌شده را صریح بنویس.>

**پیامدها:** <چه چیزی از این به بعد اجباری/ممنوع می‌شود>
```
