# PROGRESS-M4-billing.md — دفترِ کارِ ماژول M4 (`billing`)

> **این فایل تاریخِ کار است، نه برنامه.** برنامه در
> [`TODO-M4-billing.md`](TODO-M4-billing.md) است. هر session این‌جا می‌نویسد
> «چه شد / چه تصمیمی گرفتم / قدم بعد» — طبق قاعده ۷ی loop.
>
> بایگانیِ ماژول‌های قبلی: [`PROGRESS-M1-canvas-core.md`](PROGRESS-M1-canvas-core.md) ·
> [`PROGRESS.md`](PROGRESS.md) (M2) · [`PROGRESS-M3-backend-api.md`](PROGRESS-M3-backend-api.md).

---

## ۱۴۰۵/۰۶/۱۴ — برنامه‌ریزی و فاز ۰

### چه شد

**نقشه‌ی M4 نوشته شد** ([`TODO-M4-billing.md`](TODO-M4-billing.md)): ۱۱ فاز (۰ تا ۱۰)،
نُه تصمیمِ مرزی، و شش ADRِ موردانتظار.

⚠️ **برخلافِ M1/M2/M3، برنامه‌ریزیِ M4 با probeِ واقعی شروع شد، نه با خواندنِ سند.**
هفت خواننده‌ی موازی کدِ واقعی را خواندند و یکی‌شان **تماسِ زنده با زرین‌پال** گرفت. دلیلش
درسِ گران‌قیمتِ M3 بود (گام ۱۱٫۲: `redirect:"manual"` در Node کار می‌کرد و در مرورگر نه) —
اگر قرار است روی یک API خارجی سوار شویم، بهتر است قبل از نوشتنِ برنامه بدانیم واقعاً چه
شکلی است، نه بعد از نوشتنِ کد.

**آنچه probeِ زنده ثابت کرد** (جزئیات در جدولِ TODO §«آنچه probeها ثابت کردند»):

- میزبانِ درست **`payment.zarinpal.com`** است، نه `api.zarinpal.com` (که امروز ۴۰۳ داد).
  چند SDKِ شخصِ ثالث و آموزشِ قدیمی هنوز دومی را hardcode می‌کنند.
- ★★ **verifyِ دوم همیشه کدِ ۱۰۱ می‌دهد، نه ۱۰۰.** چکِ طبیعیِ `code === 100` یک مشتریِ
  واقعاً پرداخت‌کرده را «ناموفق» ثبت می‌کند. این تنها مهم‌ترین یافته‌ی کلِ برنامه‌ریزی بود.
- **واحد ریال است** — با تماسِ زنده اثبات شد (`amount:10000` بدونِ `currency` ⇒ صفحه‌ی
  درگاه «۱۰٬۰۰۰ ریال»). ولی `IRT` هم مجاز است، پس باید **صریح** فرستاده شود.
- **ادعای ADR-014 درباره‌ی سندباکس تایید شد** — فقط تعویضِ میزبان + یک UUIDِ دلخواه.
  با merchantِ `11111111-…` تماس گرفته شد و `authority` گرفت. یعنی کلِ جریان از روزِ اول
  ساختنی است و تنها دلتای production تعویضِ میزبان و merchantِ واقعی است.
- شکلِ `errors` بینِ موفق و ناموفق **تغییرِ نوع** می‌دهد (آرایه ⇄ شیء)، و خطاهای عادیِ
  کسب‌وکار **non-2xx** برمی‌گردند (`-51` ⇒ HTTP 401).
- ⛔ `refund` اصلاً در REST نیست — GraphQL + OAuth2 روی `next.zarinpal.com`.

**و پنج بمبِ ساعتی در کدِ خودمان پیدا شد** (B-1 تا B-5 در TODO). مهم‌ترینش **B-1**:
[`apps/api/src/idempotency.ts`](apps/api/src/idempotency.ts) روی هر درخواستِ غیر-POST یا
بدونِ هدرِ `Authorization` بی‌صدا `return` می‌کند — و callbackِ زرین‌پال دقیقاً هر دو است.
یعنی میان‌افزار روی حساس‌ترین مسیرِ کلِ پروژه **اصلاً اجرا نمی‌شود**. خطر این نیست که
حفاظ نداریم؛ این است که حفاظ **به‌نظر می‌رسد** داریم.

**فاز ۰ بسته شد:** هر نُه تصمیم تاییدِ مالک خورد و شش ADR نوشته شد.

### چه تصمیمی گرفتم

| ADR | چه چیزی را بست |
|---|---|
| [ADR-049](ARCHITECTURE_DECISIONS.md#adr-049) | `packages/billing-core` جدا (نه داخلِ `apps/api`)، thresholdِ پوشش **۹۰٪**، پیش‌فرضِ dev = `mock`، استرداد بیرونِ M4 |
| [ADR-050](ARCHITECTURE_DECISIONS.md#adr-050) | idempotencyِ پول = **قفلِ ردیفِ `payments`**، نه میان‌افزارِ موجود |
| [ADR-051](ARCHITECTURE_DECISIONS.md#adr-051) | آشتی‌دهی سه‌تکه، **بدونِ ساختنِ `apps/worker`** |
| [ADR-052](ARCHITECTURE_DECISIONS.md#adr-052) | یک تابع برای هر دو مبلغ (درگاه و فاکتور)، VAT منجمد در فاکتور، پیش‌فرض صفر |
| [ADR-053](ARCHITECTURE_DECISIONS.md#adr-053) | ظرفیت با `count(*)`ِ واقعی؛ `usage_counters` فقط کشِ نمایش؛ downgrade = تنزلِ فقط‌خواندنی |
| [ADR-054](ARCHITECTURE_DECISIONS.md#adr-054) | ریالِ عددِ صحیح روی سیم، دو enumِ وضعیت، کوپن بیرونِ قرارداد |

**سه تصمیمی که ارزشِ ثبتِ دلیل دارند:**

۱. **انحراف از PLAN §۴ درباره‌ی پیش‌فرضِ درگاه** (M4-D5). PLAN نوشته بود
   `PAYMENT_PROVIDER=zarinpal` + `ZARINPAL_MODE=sandbox`. ولی سندباکس یک **سرویسِ خارجی
   روی اینترنت** است؛ پیش‌فرض‌کردنش یعنی ماشینِ آفلاین و CIِ بدونِ egress جریانِ پرداختِ
   مرده دارند — نقضِ مستقیمِ P2 و P3. پیش‌فرض `mock` شد و sandbox حالتِ سومِ دستی ماند.

۲. **`apps/worker` باز هم ساخته نشد** (M4-D4). آشتی‌دهی یک نیازِ **صحت** است، ولی
   ساختنِ یک اپِ چهارم برایش همان خطای «shardingِ زودهنگام»ِ ADR-048 در لباسِ دیگر بود.
   هزینه‌ی پذیرفته‌شده **صریح** ثبت شد: تا M5، آشتی‌دهی در production خودکار نیست.

۳. **کوپن وارد قرارداد نشد** (M4-D2b). برخلافِ `Plan`/`Subscription`/`Invoice`، شکلِ
   `Coupon` در PLAN §۵٫۱ **اصلاً وجود ندارد** — فقط جدولش هست. پس هر DTOی عمومی برایش یک
   قراردادِ نو بود که هیچ سندی تاییدش نکرده. الگو همان M3-D2a/M3-D2b: شکل‌های PLAN-دار
   تاییدِ دسته‌ای می‌گیرند، تصمیم‌های واقعاً نو تک‌تک.

**یک تناقض در خودِ PLAN پیدا و رفع شد** (M4-D2a): `Team.subscriptionStatus` شش مقدار دارد
(با `none`) و `Subscription.status` پنج. دو enumِ جدا شد — `none` وضعیتِ **تیم** است
(«اشتراکی ندارد»)، نه وضعیتِ یک اشتراک.

**یک ابهامِ حقوقی به مالک برگردانده شد** (M4-D6): probe نرخِ عمومیِ VAT را **۱۰٪** یافت
(از ۹٪، با قانونِ بودجه‌ی ۱۴۰۳)، ولی سه چیز از کد قابلِ پاسخ نیست — آیا نرخ برای ۱۴۰۵ هم
همان است، آیا هم‌بوم در نظامِ مالیاتی **ثبت شده** و حق دارد VAT بگیرد، و آیا اشتراکِ SaaS
معافیتی دارد. پیش‌فرض صفر شد و نرخ در خودِ فاکتور منجمد.

### قدم بعد

**فاز ۱ — probeها** (دروازه‌ی کلِ ماژول؛ هیچ کدِ محصولی قبلش نوشته نمی‌شود):

| # | چه چیزی را می‌بندد |
|---|---|
| ۱٫۱ | ★ گذارِ **۱۰۰→۱۰۱** با یک پرداختِ دستیِ سندباکس **با چشم** دیده شود. probeِ برنامه‌ریزی نتوانست غیرتعاملی کاملش کند (صفحه‌ی سندباکس کپچا و OTP دارد) و **کلِ طراحیِ idempotency روی این سوار است** |
| ۱٫۲ | **B-2**: آیا `SUM()` روی `bigint` رشته برمی‌گرداند؟ (فقط OID 20 ثبت شده، نه 1700) |
| ۱٫۳ | **B-1**: اثباتِ اینکه میان‌افزارِ idempotency روی GETِ بدونِ auth اصلاً اجرا نمی‌شود |
| ۱٫۴ | رفتارِ واقعیِ `SELECT … FOR UPDATE` روی دو تراکنشِ هم‌زمان |
| ۱٫۵ | `subtotal − discount + vat === total` دقیقاً، بدونِ اختلافِ یک ریال |

⚠️ **چهار چیز هنوز نامعلوم‌اند و فاز ۱ باید ببندد:** بازتولیدِ گذارِ ۱۰۰→۱۰۱ در
سندباکس · بازه‌ی مجازِ verify قبل از بازگشتِ خودکارِ پول (**هیچ‌جا عدد ندارد** و بازه‌ی
sweep به آن وابسته است) · رفتارِ `metadata.auto_verify` · بزرگیِ بیشینه‌ی `ref_id`
(اگر از ۲^۵۳ بگذرد `JSON.parse` بی‌صدا خرابش می‌کند).

### وضعیتِ گیت

`pnpm verify` — سبز (تغییرِ این session فقط مستندات بود؛ markdown در `.prettierignore` است
و هیچ گیتی را لمس نمی‌کند).

---

## ۱۴۰۵/۰۶/۱۴ (ادامه) — فاز ۱: probeها

### چه شد

فاز ۰ کامیت شد (`6a02f8f`). چهار probe نوشته و اجرا شد؛ **۱٫۲ تا ۱٫۵ سبزند**، و **۱٫۱
نیمه‌تمام است** چون وسطش یک انسان لازم دارد.

| probe | دستور | نتیجه |
|---|---|---|
| ۱٫۱ درگاه | `pnpm billing:probe-gateway -- --request` / `-- --verify` | ◐ چهار چکِ بدونِ پرداخت ✅ · گذارِ ۱۰۰→۱۰۱ ⏳ |
| ۱٫۲ + ۱٫۴ دیتابیس | `pnpm billing:probe-db` | ✅ ۵ چک |
| ۱٫۳ idempotency | `pnpm billing:probe-idem` | ✅ ۴ چک |
| ۱٫۵ ریاضی | `pnpm billing:probe-math` | ✅ ۴ چک |

**همه‌ی فرضیه‌های برنامه‌ریزی تایید شدند** — ولی سه‌تا از آنچه انتظار می‌رفت **دقیق‌تر**
یا **بدتر** درآمدند:

**۱. B-2 و B-3 یک باگ نیستند، دو باگ‌اند — و حالتِ میانی خطرناک‌ترین است.** برنامه‌ریزی
گفته بود «`count(*)` درست برمی‌گردد و همین شباهت خرابی را پنهان می‌کند». probe نشان داد
این **فقط داخلِ `apps/api`** صادق است؛ در یک کلاینتِ خام **همه‌چیز** رشته است. جدولِ سه‌حالته
در TODO ثبت شد. اثرِ واقعی اندازه‌گیری شد: `sum+۱ = «40000001»` و
`sum > ۹٬۰۰۰٬۰۰۰ ⇒ false`.

**۲. مسیرِ دومِ محاسبه فقط «یک ریال» اختلاف نمی‌سازد — مبلغِ منفی می‌سازد.** در موردِ
«کوپنِ بزرگ‌تر از مبلغ» مسیرِ دوم `−۴۴۰٬۰۰۰` داد چون clampِ `Math.min(discount, subtotal)`
را نداشت. یعنی یک کوپنِ سخاوتمندانه می‌توانست مبلغِ منفی به درگاه بفرستد. این حالا یک
قیدِ اجباریِ گام ۳٫۵ است، نه یک نکته‌ی سلیقه‌ای.

**۳. B-1 بدتر از توصیفش است: هر دو گارد به‌تنهایی کافی‌اند.** probe چهار ترکیب را سنجید و
معلوم شد نه افزودنِ auth به callback مسئله را حل می‌کند، نه تبدیلش به POST — چون
گاردِ «غیر-POST» و گاردِ «بدونِ auth» هرکدام **جدا** کافی‌اند که میان‌افزار رد شود. یعنی
هیچ اصلاحِ کوچکی روی callback جواب نمی‌دهد و [ADR-050](ARCHITECTURE_DECISIONS.md#adr-050)
(قفلِ ردیف) تنها راه است.

**و از قراردادِ زرین‌پال، با تماسِ زنده:** `authority` دقیقاً ۳۶ کاراکتر
(`S00000000000000000000000000000p7r8py`) · مبلغِ ۱۰۰ ریال ⇒ **HTTP 422** با
`errors` **شیء** (در حالی که موفق `errors: []` آرایه است) · verifyِ پرداخت‌نشده ⇒
**HTTP 401** با کدِ `-51`. پس قاعده‌ی «هرگز روی `res.ok` شاخه نزن» اثبات شد، نه فرض.

### چه تصمیمی گرفتم

- **probeِ api داخلِ `apps/api/probe/` نشست، نه `scripts/`** — چون `fastify` از ریشه‌ی
  مونوریپو resolve نمی‌شود. الگویش `packages/storage/probe/` است و مثلِ آن بعد از بستنِ
  فاز ۱ پاک می‌شود. لازمه‌اش یک خطِ `ignores` در `apps/api/eslint.config.js` بود، چون
  `no-console` برای `scripts/**` خاموش است ولی برای `probe/`ِ داخلِ پکیج نه.
- **probeِ درگاه دو مرحله‌ای شد** (`--request` / `--verify` با فایلِ حالت در tmp) به‌جای
  انتظارِ تعاملی — چون صفحه‌ی سندباکس کپچا و OTP دارد و پرداخت باید در مرورگر انجام شود.
- **چهار اسکریپتِ `billing:probe-*` به `package.json` اضافه شدند**، هم‌خانواده‌ی `rt:*`.

### ★★ بستنِ ۱٫۱ — گذارِ ۱۰۰→۱۰۱ با پرداختِ واقعی دیده شد

مالک پرداختِ سندباکس را کامل کرد و `--verify` هر سه چک را سبز داد:

```
✔ verifyِ اول   HTTP 200 · code=100 · ref_id=476569601 (typeof number)
✔ verifyِ دوم   HTTP 200 · code=101 · errors=array
✔ خودآزمون      مبلغ +۱ ریال ⇒ HTTP 401 · code=-50
```

**پس ADR-050 دیگر فرض نیست.** و سه چیزِ تازه که در برنامه نبود:

۱. ★ **`101` شکلِ «موفق» دارد، نه «خطا»** — HTTP ۲۰۰ و `errors: []` (آرایه). یعنی
   قاعده‌ی «آرایه ⇒ موفق» درست است و ۱۰۱ در سمتِ موفق می‌نشیند. هرکس ۱۰۱ را «خطا»
   فرض کند، هم HTTP و هم شکلِ پاسخ گمراهش می‌کنند.
۲. ⚠️ **authorityِ پرداخت‌نشده منقضی می‌شود.** تلاشِ اول با «شناسه پرداخت ارسال شده
   منقضی گردیده است» برگشت (~۲۰ دقیقه پس از ساخت) و لینکِ تازه لازم شد. عددِ دقیق را
   نداریم، ولی این **ورودیِ مستقیمِ بازه‌ی sweepِ فاز ۷** است: ردیفِ `pending` پنجره‌ی
   محدودی برای تسویه دارد. و یعنی صفحه‌ی checkout نباید لینکِ درگاه را کَش کند.
۳. **قراردادِ callback زنده تایید شد** — مرورگر به
   `…/billing/callback?Authority=…&Status=OK` رفت، دقیقاً همان دو پارامتر.
   ⊕ `ref_id` عددِ ۹رقمی و `typeof number` بود؛ ابهامِ «بزرگ‌تر از ۲^۵۳» **باز می‌مانَد**
   (یک نمونه اثبات نیست).

⚠️ **خطای «This site can't be reached» روی صفحه‌ی callback طبیعی بود** — dev serverِ وب
بالا نبود. ربطی به درگاه ندارد؛ صفحه‌اش کارِ فاز ۹ است.

### قدم بعد

★★ **دروازه‌ی فاز ۱ باز شد — هر پنج گام سبز.** قدمِ بعد: **فاز ۲**، قرارداد در
`shared-types` (تاییدِ دسته‌ای M4-D2 از قبل گرفته شده).

⚠️ سه ابهامِ باقی‌مانده که probe نمی‌تواند ببندد چون از کد درنمی‌آیند: بازه‌ی مجازِ
verify قبل از بازگشتِ خودکارِ پول · `metadata.auto_verify` (تنظیمِ پنل است) · بیشینه‌ی
`ref_id`. دو موردِ اول ورودیِ **فاز ۷** (بازه‌ی sweep) اند و وقتی حسابِ واقعی گرفته شد
باید از پشتیبانیِ زرین‌پال پرسیده شوند. ⊕ حالا یک ورودیِ چهارم هم دارند: **انقضای
authorityِ پرداخت‌نشده**، که برخلافِ آن سه‌تا **قابلِ اندازه‌گیری** است (ساختِ authority،
صبر، verify تا خطا عوض شود).

### وضعیتِ گیت

`pnpm verify` — **سبز (۹/۹)**. یک‌بار روی `lint` قرمز شد (`no-console` در `probe/`) و با
افزودنِ `probe/**` به `ignores` رفع شد.

---

## ۱۴۰۵/۰۶/۱۴ (ادامه) — فاز ۲: قرارداد

### چه شد

قراردادِ billing به `shared-types` اضافه شد: primitiveِ `rial`، چهار enum،
`Plan`/`Subscription`/`Invoice`/`InvoiceLineItem`، ده کدِ خطای تازه، `checkoutBody` و
`zarinpalCallbackQuery`. `pnpm verify` سبز (۹/۹)، ۶۵ تست.

**پنج گیت با شکستنِ عمدی قرمز شدند** (قاعده ۱۰):

| شکستن | نتیجه |
|---|---|
| `rial` بدونِ `.int()` | ✖ خودآزمون ۱ |
| `invoice.vatRial` → `.optional()` | ✖ خودآزمون ۳ |
| `Status` → `z.enum(["OK","NOK"])` | ✖ تستِ callback |
| افزودنِ `amountRial` به `checkoutBody` | ✖ تستِ ADR-014 |
| `planLimit` → `nonnegative()` | ✖ تستِ سنتینلِ `-1` |

### چه تصمیمی گرفتم

**۱. [!] گام ۲٫۴ (فیلدهای مالیِ `Team`) به فاز ۵ موکول شد — و این تصمیمِ مهمِ این
session است.** افزودنشان به‌صورتِ الزامی، `toTeam`ِ `apps/api/src/dto.ts` را در
**typecheck** می‌شکند. سه راه بود: (الف) پُرکردنشان با مقدارِ ساختگی — یعنی دروغ در
قرارداد؛ (ب) `.optional()` کردنشان — که خودآزمونِ ۳ی خودمان منعش می‌کند و در مرورگر
«NaN ریال» می‌سازد؛ (ج) موکول‌کردن تا وقتی منبعِ داده هست (seedِ پلن‌ها = فاز ۴،
کوئریِ ظرفیت = فاز ۶). (ج) انتخاب شد، چون دقیقاً همان اصلی است که M3 هنگام **حذفشان**
به کار برد: «چیزی بدونِ مصرف‌کننده اضافه نکن» — و اینجا مصرف‌کننده هست ولی **منبعِ داده
نیست**. با `[!]` علامت خورد و به گام ۵٫۵ چسبید.

**۲. `zarinpalCallbackQuery` در `requests.ts` نشست، نه `primitives.ts`** (گام ۲٫۷). یک
query است نه بدنه، ولی همه‌ی ورودی‌هایی که `apps/api` اعتبارسنجی می‌کند باید یک خانه
داشته باشند. docblockِ فایل از «بدنه» به «ورودی» پهن شد. (`pageQuery` در `primitives`
مانْد چون ترکیب‌گرِ **عمومیِ** چنداندپوینتی است.)

**۳. ★ `Status` عمداً `z.string()`ِ آزاد است، نه enum.** این خودش قاعده ۱ی ADR-014 را
در تایپ رمزگذاری می‌کند: به این مقدار **اصلاً اعتماد نمی‌شود**، پس اعتبارسنجیِ
سخت‌گیرانه‌اش بی‌معناست — و اگر enum بود، یک مقدارِ سومِ آینده ۴۰۰ می‌داد و **یک پرداختِ
واقعی را گم می‌کرد**.

**۴. `code`ِ پلن `string` مانْد نه enum** — قفل‌کردنش یعنی افزودنِ یک پلن با یک ردیفِ
دیتابیس ممکن نیست و کاتالوگ در کدِ منتشرشده منجمد می‌شود.

⚠️ **یک خطای روشی که خودم مرتکب شدم و ارزشِ ثبت دارد:** برای شکستن‌های عمدی از
`git checkout -- <dir>` برای بازگردانی استفاده کردم. فایلِ **untracked** (`billing.ts`)
بازنگشت و بقیه‌ی ویرایش‌های tracked **پاک شدند** — پس دو شکستنِ بعدی روی کدِ قدیمی اجرا
شدند و «سبز» نشان دادند، که می‌توانست به‌عنوان «خودآزمون رد شد» تفسیر شود. درست: قبل از
شکستن، فایل‌ها را **کپی** کن و از کپی برگردان. (نشانه‌اش هم بود: تعدادِ تست از ۶۵ به ۵۷
افتاد — یعنی تست‌های تازه اصلاً وجود نداشتند.)

### قدم بعد

**فاز ۳ — `packages/billing-core`**: پکیجِ جدید با کلِ چک‌لیستِ ۹تایی (شاملِ
`test:coverage` با thresholdِ **۹۰٪** و factoryِ boundaries)، پورتِ `PaymentGateway`،
`MockGateway` + `ZarinpalGateway`، و `computeInvoice` با clampِ `Math.min` که probeِ ۱٫۵
اجباری‌اش کرد.

### وضعیتِ گیت

`pnpm verify` — **سبز (۹/۹)**.

---

## ۱۴۰۵/۰۶/۱۴ (ادامه) — فاز ۳: `packages/billing-core`

### چه شد

پکیجِ جدید با **کلِ چک‌لیستِ ۹تایی** ساخته شد: `package.json`/`tsconfig`/`eslint.config.js`،
factoryِ `billingCoreBoundaries()` + **هر سه لایه‌ی** تستش، `vitest.config.ts` با thresholdِ
**۹۰٪**، `test:coverage`، ثبت در `docs/dependencies.md`، و devDependencyِ ریشه.
تست‌های `eslint-config` از ۱۹۳ به ۲۱۰ رفت.

محتوا: پورتِ `PaymentGateway` + `MockGateway` + `ZarinpalGateway` + `computeCharge` +
`formatInvoiceNumber`/`computePeriod`. **۵۴ تست، پوششِ ۹۹٫۲۸٪ خط و ۹۴٫۲۳٪ شاخه.**
`pnpm verify` سبز (۹/۹).

**سه گیتِ تازه با شکستنِ عمدی قرمز شدند:** حذفِ `money.test.ts` ⇒ پوشش ۸۵٪ و سه خطای
threshold · `import pg` در `src` ⇒ `no-restricted-imports` · `ZARINPAL_CURRENCY=IRT` ⇒ رد.

### چه تصمیمی گرفتم

**۱. ★ یک باگِ واقعی حین نوشتنِ تست پیدا شد و رفع شد.** `MockGateway.createPayment` نوعِ
`Promise` اعلام کرده بود ولی `assertGatewayAmount` را **همزمان** صدا می‌زد — پس روی مبلغِ
نامعتبر یک پرتابِ **synchronous** می‌داد که با `.catch()` گرفته نمی‌شود. تست اولش قرمز شد و
من اول فکر کردم تست غلط است؛ ولی مسئله واقعی بود و متدها `async` شدند. (این دقیقاً همان
جنس باگی است که فقط وقتی پیدا می‌شود که تست را **جدی** بگیری، نه اینکه به شکلِ دلخواه
بازنویسی‌اش کنی.)

**۲. `VerifyOutcome` سه حالت دارد، نه دو.** `notPaid` («کاربر پول نداد» ⇒ ردیف `failed`) از
`gatewayError` («نمی‌دانیم» ⇒ ردیف `pending` بماند تا sweep) جدا شد. یکی‌کردنشان یعنی یا
پرداختِ واقعی گم می‌شود یا یک ردیفِ مرده برای همیشه می‌مانَد. قطعیِ شبکه و بدنه‌ی غیر-JSON
هر دو `gatewayError`اند.

**۳. `envIntFromZero` کنارِ `envInt` اضافه شد.** `envInt` عمداً `positive()` است و
`VAT_PERCENT=0` را **رد** می‌کرد — همان تله‌ای که در برنامه‌ریزی پیش‌بینی شده بود.

**۴. `ZARINPAL_CURRENCY` یک `z.literal("IRR")` است، نه enum.** درگاه `IRT` را هم می‌پذیرد و
اشتباهش ضریبِ ۱۰ روی هر تراکنش است؛ این‌طوری با متغیرِ محیطی هم قابلِ تغییر نیست.

**۵. فیلدهای زرین‌پال در schema `optional` ماندند.** اجباری‌کردنشان یعنی `pnpm dev` یک
merchantِ واقعی می‌خواهد و اولین کاری که هرکس می‌کند گذاشتنِ مقدارِ الکی است — «و از آن به
بعد گیت مرده است» (هشدارِ خودِ `sections.ts`). اعتبارسنجیِ ۳۶ کاراکتر داخلِ سازنده‌ی
`ZarinpalGateway` است و فقط وقتی provider واقعاً زرین‌پال باشد اجرا می‌شود.

⚠️ **یک درسِ ابزاری:** ویرایشِ فایل‌های CRLF با anchorهای **چندخطی** در Python بی‌صدا
شکست می‌خورد (چون anchor با `
` نوشته می‌شود و فایل `

` دارد). سه بار خورد به این.
درست: یا `

` را نرمال کن و آخر برگردان، یا anchorِ **تک‌خطی** بگیر و line-based درج کن.

### قدم بعد

**فاز ۴ — migration `0004`**: `CHECK` روی وضعیت‌های billing (B-4)، `coupon_redemptions`،
`activated_by_payment_id` + `unit_price_rial`، اصلاحِ FK/ON DELETE (که probeِ فاز ۱ خودش
به آن خورد)، ستونِ `card_hash`، `vat_percent`ِ منجمد، و seedِ پلن‌ها — که گام ۲٫۴ی موکول‌شده
منتظرش است.

### وضعیتِ گیت

`pnpm verify` — **سبز (۹/۹)**.

---

## ۱۴۰۵/۰۶/۱۴ (ادامه) — فاز ۴: migration `0004`

### چه شد

[`0004_billing_integrity.sql`](apps/api/migrations/0004_billing_integrity.sql) نوشته و اعمال شد،
و **۷ سناریوی billing** به [`db-fk-test`](scripts/db-fk-test.ts) اضافه شد — حالا ۱۰ چک، همه سبز.
`pnpm verify` سبز (۹/۹).

محتوا: `CHECK` روی هر ستونِ وضعیتِ billing (B-4) · جدولِ `coupon_redemptions` ·
`activated_by_payment_id` + `unit_price_rial` روی `subscriptions` · ستونِ `card_hash` و
`vat_percent`ِ منجمد · `invoice_sequences` · FKهای مالیِ **صریحاً** `RESTRICT` · سه ایندکسِ
کاری · و seedِ پلن‌ها.

**خودآزمون:** با حذفِ `invoices_total_ck` و `subscriptions_status_ck` دو چک **قرمز** شدند.

### چه تصمیمی گرفتم

**۱. ★★ رابطه‌ی ADR-052 را در دیتابیس هم گذاشتم، نه فقط در کد.** `computeCharge` تضمینش
می‌کند، ولی probeِ گام ۱٫۵ نشان داد خطرِ واقعی «مسیرِ دومِ محاسبه» است — و مسیرِ دوم را
هیچ تستِ واحدی نمی‌بیند چون کدِ خودش تمیز است. `CHECK` روی `subtotal − discount + vat =
total` یعنی مسیرِ دوم **در لحظه‌ی درج** می‌شکند، هرکجا نوشته شده باشد.

**۲. FKهای مالی `RESTRICT`ِ صریح شدند — رفتار عوض نشد، نانوشتگی عوض شد.** probeِ فاز ۱
خودش به این خورد (حذفِ کاربر با ۲۳۵۰۳ افتاد) و اولش شبیهِ باگ بود؛ ولی رفتار **درست**
است: رکوردِ مالی نباید با حذفِ حساب نابود شود، و مسیرِ حذف در این پروژه نرم است. پس
به‌جای عوض‌کردن، نام‌دار و مستندش کردم تا کسی بعداً از سرِ ناآگاهی `CASCADE` نزند.

**۳. ⚠️ seedِ پلن‌ها عمداً ناقص ماند — و این مرزِ «تصمیمِ کسب‌وکار» است.** `free` زیرساخت
است (هر تیمی باید پلنی داشته باشد وگرنه `Team.planCode` بی‌معناست) و قیمتش صفر، پس هیچ
عددی اختراع نشد. ولی `pro` و `team` با `is_active = false` نشستند: **قیمت‌گذاری کارِ مالک
است، نه این migration.** تا عددِ واقعی نیاید، در فهرستِ عمومی نمی‌آیند و قابلِ خرید نیستند —
همان قاعده‌ی «فقط قابلیتِ واقعی، هیچ به‌زودی».

**۴. `invoices_paid_at_ck`** — `(status = 'paid') = (paid_at IS NOT NULL)`. اضافه‌ی خودم بود:
فاکتورِ «پرداخت‌شده» بدونِ زمانِ پرداخت یعنی گزارشِ درآمد سوراخ می‌شود بدونِ اینکه کسی بفهمد.

### قدم بعد

**فاز ۵ — مسیرهای billingِ `apps/api`**: `GET /billing/plans` · `POST …/checkout` ·
★★ `GET /billing/zarinpal/callback` (قلبِ ADR-050: `FOR UPDATE` + فعال‌سازی در یک تراکنش) ·
`POST /billing/payments/:id/verify` · `GET …/subscription|invoices` · `POST …/cancel`.
و **گام ۲٫۴ی موکول‌شده** (فیلدهای مالیِ `Team`) که حالا منبعِ داده‌اش آماده است.

### وضعیتِ گیت

`pnpm verify` — **سبز (۹/۹)** · `pnpm db:fk-test` — **۱۰ چک سبز**.

---

## ۱۴۰۵/۰۶/۱۴ (ادامه) — فاز ۵: مسیرهای billingِ `apps/api`

### چه شد

هفت مسیرِ billing + صفحه‌ی ساختگیِ پرداخت، سرویسِ `settlePayment`، پلاگینِ درگاه، و
**گام ۲٫۴ی موکول‌شده** (فیلدهای مالیِ `Team`). `pnpm verify` سبز (۹/۹)، ۳۵ تستِ api،
و سنجه‌ی زنده‌ی `billing:settle` سبز.

### چه تصمیمی گرفتم

**۱. ★★ [ADR-055](ARCHITECTURE_DECISIONS.md#adr-055) — یک بندِ ADR-050 غلط بود.** نوشته
بودم «فقط کدِ ۱۰۰ حق دارد فعال کند». برای رفرشِ مرورگر درست است، ولی برای **آشتی‌دهی**
غلط: اگر callback گم شود، ردیفِ ما `pending` می‌مانَد و درگاه در verifyِ بعدی **۱۰۱**
می‌دهد — و با آن بند، آشتی‌دهی می‌گفت «قبلاً تسویه شده» و **هرگز فعال نمی‌کرد**. یعنی
دقیقاً همان «پول گرفته شد، سرویس داده نشد». ریشه‌ی اشتباه: کدِ درگاه را با وضعیتِ **ما**
یکی گرفته بودم. ADR-055 نوشته شد و **منتظرِ تاییدِ توست**.

**۲. ★★ سنجه‌ی همزمانی اولش یک گیتِ دروغین بود.** با `MockGateway`ِ معمولی نوشتمش و سبز
شد؛ ولی وقتی `FOR UPDATE` را عمداً برداشتم **باز هم سبز مانْد** — یعنی دو تسویه هرگز
واقعاً هم‌زمان نبودند (verifyِ mock همزمان برمی‌گشت، اولی commit می‌کرد پیش از SELECTِ
دومی). با یک تاخیرِ ۲۵۰ms داخلِ verify، تراکنشِ دوم مجبور شد وسطِ ناحیه‌ی بحرانی برسد و
حالا **بدونِ قفل قرمز می‌شود**. اگر این را ندیده می‌گرفتم، یک سنجه‌ی بی‌مصرف داشتم که
ادعای بزرگ‌ترین تضمینِ M4 را می‌کرد.

**۳. دو گیتِ خودمان دو باگِ واقعی گرفتند:** `payments_gateway_ck`ِ فاز ۴ جای‌نگهدارِ
`'pending'` را در ستونِ `gateway` ردّ کرد (که خودم گذاشته بودم تا «بعداً» به‌روز شود) —
و مجبورم کرد نامِ واقعیِ درگاه را از اول پاس بدهم. گاردِ دریفتِ OpenAPI هم مسیرِ mockِ
مستندنشده را گرفت.

**۴. کلیدِ idempotency با `teamId` دامنه‌دار شد.** `payments_idem_uq` **سراسری** است، پس
کلیدِ خامِ کلاینت می‌توانست با کلیدِ تیمِ دیگری تصادف کند و ردیفِ آن تیم را برگرداند —
نشتِ بین‌مستاجری. حالا کلید `teamId:clientKey` است.

**۵. `VerifyOutcome.gatewayError` هیچ‌چیز نمی‌نویسد.** قطعیِ شبکه یعنی «نمی‌دانیم»، پس
ردیف `pending` می‌مانَد تا sweepِ فاز ۷ — نه `failed` که پرداختِ واقعی را دور بیندازد.

⚠️ **تله‌ی محیطی:** Node ۲۴ در حالتِ strip-only کلیدواژه‌ی `override` را نمی‌پذیرد
(`ERR_INVALID_TYPESCRIPT_SYNTAX`) — همان خانواده‌ی محدودیتی که `errors.ts` درباره‌ی
parameter properties هشدار داده بود. به‌جای زیرکلاس، یک wrapperِ ساده نوشتم.

### قدم بعد

**فاز ۶ — اعمالِ ظرفیت**: زنده‌کردنِ مسیرِ نوشتنِ `usage_counters` (B-5)، گیتِ سقف با
`count(*)`ِ واقعی در همان تراکنش، سنتینلِ `-1`، گاردِ `is_staff`، و سیاستِ downgradeِ M4-D8.

### وضعیتِ گیت

`pnpm verify` — **سبز (۹/۹)** · `pnpm db:fk-test` — ۱۰ چک · `pnpm billing:settle` — ۳ چک.

---

## ۱۴۰۵/۰۶/۱۵ — فاز ۵٫۹: سخت‌سازی، بعد از یک بازبینیِ خصمانه

### چه شد

قبل از شروعِ فاز ۶، یک بازبینیِ خصمانه روی کدِ فاز ۵ گذاشتم (چون بالاترین ریسکِ مالیِ پروژه
است و سریع نوشته شده بود). **۱۷ نقصِ واقعی پیدا شد**، چند تایشان پول‌سوز. یعنی **فاز ۵
واقعاً تمام نبود** وقتی تیک خورد و کامیت شد.

۱۲ تا رفع شد و **۵ چکِ رگرسیونِ تازه** به سنجه اضافه شد (حالا ۸ چک). `pnpm verify` سبز.

| # | نقص | چرا پول‌سوز بود |
|---|---|---|
| T5 ★★ | `notPaid` را **پایانی** می‌گرفتم و ردیف را `failed` می‌کردم | کدِ `-51`ِ زرین‌پال یعنی «هنوز پرداخت نشده»، نه «نشد». ایندکسِ sweep روی `pending` است ⇒ کاربری که لحظه‌ای بعد پرداخت را کامل می‌کرد **برای همیشه نامرئی** می‌شد |
| T6 ★★ | خروجِ زودهنگام فقط روی `paid` | ردیفِ `refunded` دوباره verify می‌شد، درگاه ۱۰۱ می‌داد، و **اشتراکِ دوم** برای پرداختی که پولش برگشته ساخته می‌شد |
| T7 ★★ | پلن/دوره را از `line_items->0->>` می‌خواندم | اگر ترتیبِ آرایه عوض شود همه `NULL` می‌شوند، و `period === "yearly"` روی `NULL` به شاخه‌ی **ماهانه** می‌افتد: مشتریِ **سالانه** یک ماه می‌گرفت |
| T2 ★★ | کوپن سرِ checkout مصرف می‌شد | یک اختلالِ درگاه کوپن را **برای همیشه** می‌سوزاند |
| T1 ★★ | `createCheckout` فقط INSERT می‌زد | retryِ مشروع با همان کلید ⇒ ۲۳۵۰۵ ⇒ **۵۰۰ی همیشگی** |
| T9/T10 ★★ | Authority در لاگ | `req.url`ِ لاگِ پیش‌فرضِ Fastify کلِ query را می‌نویسد؛ و `err.detail`ِ pg شناسه را می‌آورد. نقضِ P7 روی **همان** مسیری که بیشترین شناسه را دارد |
| T4 ★ | مبلغِ صفر ⇒ ۵۰۰ | پلنِ رایگان یا کوپنِ ۱۰۰٪ به `payments_amount_ck` می‌خورد |
| T3 ★ | `coupons.plan_codes` خوانده و **اعمال نمی‌شد** | کوپنِ پلنِ ارزان روی گران‌ترین پلن می‌نشست |
| T8 ★ | تمدید از `now()` | تمدید در روزِ ۲۰ از ۳۰ ⇒ ۱۰ روزِ پرداخت‌شده دود می‌شد |
| T14 | `assertGatewayAllowed` روی درگاهِ تزریق‌شده اجرا نمی‌شد | ادعای «هر ورودی از یک گیت رد می‌شود» **غلط** بود |
| T11 | جست‌وجوی authority بدونِ `gateway` | ایندکس روی جفت است ⇒ seq scan روی مسیرِ عمومی + امکانِ اشتباهِ ردیف |
| T13 | `seats` بی‌سقف | `unit * seats` می‌توانست از عددِ امن بگذرد و ریالِ **گِردشده** ذخیره شود (نقضِ P5) |
| T15 | شماره‌ی فاکتور روی هر شکست می‌سوخت | دنباله‌ی رسمی سوراخ + فاکتورهای `open`ِ شبح ⇒ حالا `void` می‌شوند |
| T16 | XSSِ بازتابی در صفحه‌ی mock | فقط dev، ولی سرورِ dev کوکیِ نشستِ واقعی دارد |

### چه تصمیمی گرفتم

**۱. `request_payload` جای `line_items` شد.** پارامترهای خرید حالا یک **شیءِ صریحِ
اعتبارسنجی‌شده** روی ردیفِ پرداخت‌اند، نه ایندکسِ یک آرایه‌ی JSONِ نمایشی. `readIntent`
سخت‌گیرانه می‌شکند و تراکنش rollback می‌شود ⇒ ردیف `pending` می‌مانَد و sweep دوباره
می‌آید — به‌جای اینکه بی‌صدا اشتراکِ اشتباه بدهد.

**۲. کوپن: اعتبارسنجی سرِ checkout، مصرف سرِ فعال‌سازی.** تفکیکِ «آیا مجاز است» از «حالا
خرجش کن» — همان تفکیکی که از اول باید می‌بود.

**۳. سه چیز عمداً رفع **نشد** و ثبت می‌شود:**
- **پنجره‌ی سقوط** (T0): بینِ mintِ authority در درگاه و ذخیره‌اش در ردیفِ ما، اگر پروسه
  بمیرد یک لینکِ پرداختیِ بی‌ردیف می‌مانَد. رفعِ واقعی‌اش `unVerified.json`ِ زرین‌پال است
  که **کارِ فاز ۷** است. فعلاً شکستِ آن UPDATE فاکتور را `void` می‌کند.
- **اتصالِ استخر در تراکنش** (T12): تماسِ شبکه داخلِ تراکنش می‌مانَد (ADR-014 اتمیک‌بودن
  می‌خواهد)، ولی `statement_timeout` و کاهشِ timeoutِ درگاه کارِ فاز ۷/M5 است.
- **انقضای خودکارِ `cancel_at_period_end`**: هیچ‌چیز اشتراکِ لغوشده را در پایانِ دوره
  منقضی نمی‌کند — **کارِ sweepِ فاز ۷**.

### درسِ روشی

**تیکِ «فاز ۵ تمام شد» زودهنگام بود.** `pnpm verify` سبز بود، ۳۵ تست سبز بود، سنجه‌ی زنده
سبز بود — و ۱۷ نقص داخلش بود. هیچ‌کدام از آن گیت‌ها دروغ نگفتند؛ فقط **چیزی را که نمی‌سنجیدند
نسنجیدند**. برای کدِ پول، «همه‌ی گیت‌ها سبزند» شرطِ لازم است نه کافی؛ یک بازبینیِ خصمانه‌ی
جدا لازم است. این را برای فازهای بعدیِ پول‌محور (۶ و ۷) قاعده می‌کنم.

### قدم بعد

**فاز ۶ — اعمالِ ظرفیت**، با پنج یافته‌ی تازه‌ی بازبینی که در برنامه نبودند (در TODO ثبت شد):
`restore` و `duplicate` دو مسیرِ **ساختِ بی‌گیت** اند · `count(*)` در تراکنش **اتمیک نیست**
بدونِ قفلِ ردیفِ تیم · `ON CONFLICT DO UPDATE`ِ پذیرشِ دعوت عضو اضافه نمی‌کند · dedupeِ دارایی
`SUM(size_bytes)` را دوبار می‌شمارد · و ⚠️ **فضای شخصی هم سقفِ `free` می‌گیرد** (۳ بورد
برای هر کاربر) — یک تصمیمِ محصولی که باید صریح گرفته شود، نه در production کشف شود.

### وضعیتِ گیت

`pnpm verify` — **سبز (۹/۹)** · `pnpm billing:settle` — **۸ چک** · `pnpm db:fk-test` — ۱۰ چک.

---

## ۱۴۰۵/۰۶/۱۵ (ادامه) — فاز ۶: اعمالِ ظرفیت

### چه شد

گیتِ ظرفیت ساخته و در **پنج** نقطه وصل شد (ساخت/بازیابی/تکثیرِ بورد، پذیرشِ دعوت، commitِ
دارایی). migration ۰۰۰۵ پلنِ `personal` را افزود. سنجه‌ی `billing:quota` با ۶ چک سبز، و
`pnpm verify` سبز.

### چه تصمیمی گرفتم

**۱. ★★ فضای شخصی پلنِ خودش را دارد (تصمیمِ مالک ۱۴۰۵/۰۶/۱۵).** رزولوشنِ پلن حالا
`CASE WHEN t.is_personal THEN 'personal' ELSE 'free' END` است — **در SQL، نه یک `if` در
کد**، تا گیت یک مسیر بماند و سقف‌ها داده باشند نه کد. `personal` بوردِ نامحدود دارد
(`-1`) ولی سقفِ **فضا** (۱GB): سقف روی چیزی که واقعاً هزینه دارد، نه روی شمارشِ بورد که
فقط یک دیوارِ بی‌در می‌ساخت (پلن‌های `pro`/`team` صندلی‌محورند و برای فضای شخصی مسیرِ
خریدی وجود ندارد).

**۲. ⚠️ `usage_counters` را **نه** نوشتم و **نه** خواندم — انحراف از گام ۶٫۱.** ADR-053
آن را «کشِ نمایش» خوانده بود، ولی B-5 دقیقاً نشان داد کشی که drift کند چه می‌کند: سه سال
عددِ صفر بدونِ اینکه کسی بفهمد. در دامنه‌ی پول، کشِ اشتباه یا مشتری را بی‌جهت می‌بندد یا
سرویسِ پولی را رایگان بیرون می‌دهد. همه‌جا شمارشِ زنده شد؛ جدول بلااستفاده ماند.

**۳. `guest` هم عضو شمرده می‌شود — عمداً.** نه چون درست‌ترین مدلِ کسب‌وکار است، بلکه چون
`memberCount`ِ همان DTO هم می‌شمردش. اگر گیت و نمایش دو تعریف داشته باشند، کاربر عددی
می‌بیند که با خطایی که می‌گیرد جور درنمی‌آید. اگر روزی مدلِ صندلی عوض شود، **هر دو** با هم.

**۴. گیت فقط `teamId` می‌گیرد و هرگز نقش را حل نمی‌کند.** `effectiveBoardRole` به
`is_staff` روی هر بورد `owner` می‌دهد؛ گیتی که «نقش را حل کن، owner رد شود» بنویسد به هر
حسابِ staff ظرفیتِ نامحدودِ رایگان می‌داد. چکِ ۶ی سنجه همین را می‌سنجد.

⚠️ **و باز هم همان درس، بارِ سوم:** چکِ همزمانی اولش سبز بود **و با برداشتنِ `FOR UPDATE`
هم سبز مانْد**. شمارش آن‌قدر سریع است که دو تراکنش هرگز هم‌پوشانی نمی‌کنند. با یک
`pg_sleep(0.25)` بینِ شمردن و درج، پنجره‌ی واقعی ساخته شد و حالا بدونِ قفل **قرمز** می‌شود.
سه بار در این ماژول: سنجه‌ی تسویه، و حالا دو بار اینجا. **پیش‌فرض این است که یک چکِ
همزمانی هم‌زمان نیست، مگر ثابت شود.**

⊕ ضمناً سنجه طوری بازنویسی شد که خطای غیرمنتظره در یک چک، **همان چک** را قرمز کند نه اینکه
کلِ اسکریپت crash کند — وگرنه شکستِ خودِ سنجه از شکستِ چیزی که می‌سنجد قابلِ تفکیک نیست.

### قدم بعد

**فاز ۷ — آشتی‌دهی**، که سه ارثیه‌ی ثبت‌شده‌ی فاز ۵٫۹ را هم برمی‌دارد: پنجره‌ی سقوطِ
authority (`unVerified.json`)، `statement_timeout` برای اتصالِ استخر، و انقضای
`cancel_at_period_end`.

### وضعیتِ گیت

`pnpm verify` — **سبز (۹/۹)** · `billing:quota` ۶ چک · `billing:settle` ۸ چک ·
`db:fk-test` ۱۰ چک.

---

## ۱۴۰۵/۰۶/۱۵ (ادامه) — فاز ۷: آشتی‌دهی

**چه شد:** پرداختِ گم‌شده حالا پیدا و تسویه می‌شود — و سه ارثیه‌ی ثبت‌شده‌ی فاز ۵٫۹ هم
همین‌جا بسته شدند. دو ADRِ تازه: [ADR-056](ARCHITECTURE_DECISIONS.md#adr-056) (نردبانِ
تصمیم) و [ADR-057](ARCHITECTURE_DECISIONS.md#adr-057) (سقفِ زمانیِ اتصال) — **هر دو
منتظرِ تاییدِ مالک**.

| گام | چه ساخته شد |
|---|---|
| ۷٫۱ | [`billing-core/reconcile.ts`](packages/billing-core/src/reconcile.ts) — `planSweep` + `matchOrphans`، **خالص**؛ ۲۱ تستِ واحد. پوششِ پکیج: ۹۹٫۴۶٪ خط / ۹۵٫۰۹٪ شاخه |
| ۷٫۲ | [`services/reconcile.ts`](apps/api/src/services/reconcile.ts) (اجرا) + [`scripts/billing-reconcile.ts`](scripts/billing-reconcile.ts) (`pnpm billing:reconcile`) |
| ۷٫۳ | [`billing:probe-reconcile`](scripts/billing-probe-reconcile.ts) — **۹ چک روی Postgresِ زنده** |
| ۷٫۴ | [`plugins/reconcile.ts`](apps/api/src/plugins/reconcile.ts) — تایمر پشتِ `BILLING_RECONCILE_ENABLED` (پیش‌فرض **خاموش**) |

### تصمیم‌هایی که گرفتم

**۱. ★★ هیچ ردیفی پیش از دستِ‌کم یک پرسش باطل نمی‌شود.** این قلبِ ADR-056 است و از یک
خرابیِ مشاهده‌شده می‌آید: ایندکسِ sweep روی `status='pending'` است، پس هر ردیفی که
`failed` شود **از دیدِ آشتی‌دهی برای همیشه ناپدید می‌شود**. اگر پولش گرفته شده باشد،
هیچ‌کس دیگر پیدایش نمی‌کند. پس «کهنه» به‌تنهایی مجوزِ باطل‌کردن نیست؛ لازم است درگاه
دستِ‌کم یک‌بار صریح گفته باشد «پرداخت نشد» (`failure_code IS NOT NULL`). یک ردیفِ کهنه‌ی
بی‌ضرر بهتر از یک پرداختِ گم‌شده است.

**۲. ردیفِ بدونِ `authority` یتیم است، نه شکست‌خورده.** قابلِ پرسیدن نیست (چیزی برای
پرسیدن نداریم) ولی ممکن است پولش گرفته شده باشد — همان پنجره‌ی سقوطِ بینِ ساختِ authority
در درگاه و ذخیره‌اش در ردیف. پس گزارش می‌شود و **هرگز خودکار بسته نمی‌شود**.

**۳. فرزندخواندگی حدس می‌زند، پس قاعده‌اش سخت است.** `listUnverified` به پورت اضافه شد
(mock + زرین‌پال). ولی آن فهرست **شناسه‌ی سفارشِ ما را ندارد** — فقط authority و مبلغ. پس
`matchOrphans` فقط وقتی می‌پذیرد که **دقیقاً یک یتیم با آن مبلغ و دقیقاً یک نامزدِ
بی‌صاحب با همان مبلغ** باشد؛ هر ابهامی رد می‌شود و به دستِ آدم می‌رسد. یک تطبیقِ اشتباه
یعنی اشتراکِ تیمِ الف با پولِ تیمِ ب فعال شود — خطایی که هیچ لاگی بعداً پیدایش نمی‌کند.
⚠️ به همین دلیل **پیش‌فرض خاموش** است و متدِ زرین‌پالش **با تماسِ زنده اثبات نشده** (به
یک تراکنشِ پرداخت‌شده‌ی verify‌نشده روی حسابِ واقعی نیاز دارد).

**۴. `statement_timeout` نگهبانِ این مسئله نبود.** ارثیه را «`statement_timeout` برای
اتصالِ استخر» ثبت کرده بودم و سرِ پیاده‌سازی معلوم شد **اشتباه** است: اتصالی که منتظرِ
پاسخِ درگاه است هیچ دستوری اجرا نمی‌کند، پس `statement_timeout` اصلاً نمی‌بیندش. نگهبانِ
درست `idle_in_transaction_session_timeout` است. هر دو گذاشته شدند (۱۵s و ۳۰s)، و عدد
باید از سقفِ خودِ آداپتور (۱۵s) بزرگ‌تر باشد وگرنه پرداختِ کُندِ سالم را می‌کُشد.

**۵. `cancel_at_period_end` بالاخره معنا پیدا کرد.** تا امروز نوشته می‌شد و هیچ‌کس در
پایانِ دوره نگاهش نمی‌کرد — یعنی مشتریِ لغوکرده **برای همیشه** سرویس می‌گرفت. حالا همان
sweep می‌بندد، با **دو** وضعیتِ متفاوت چون دو چیزِ متفاوت‌اند: `canceled` (خودش لغو کرد)
و `expired` (تمدید نکرد). تمدیدِ خودکار در M4 وجود ندارد، پس امروز تنها اثرِ کاربردیِ آن
فلگ همین تفکیک است.

**۶. sweep فقط ردیف‌های درگاهِ خودش را می‌بیند.** verify کردنِ ردیفِ `mock` با درگاهِ
زرین‌پال یعنی پرسیدنِ authorityِ بیگانه از درگاهِ واقعی؛ و برعکس، `MockGateway` به **هر**
authority «پرداخت شد» می‌گوید.

### ★★ چیزی که خودِ سنجه اصلاح کرد — ادعای B-3 دقیق نبود

چکِ B-3 اولش این بود: «`runReconcile` روی یک `new pg.Pool` بالا نمی‌آید». **قرمز شد** —
چون استخرِ خام در همان پروسه هم عدد می‌داد. علتش این است که `registerInt8Parser` روی
`pg.types`ِ **سراسری** می‌نشیند، نه روی استخر؛ پس به‌محضِ اینکه یک `createDbPool` جایی
اجرا شود، همه‌ی استخرهای آن پروسه کوئرس می‌گیرند.

یعنی **B-3 خطرِ پروسه است، نه خطرِ استخر** — اسکریپت یا workerی که هرگز `createDbPool` را
صدا نمی‌زند. چک در یک پروسه‌ی **فرزند** بازنویسی شد و همان‌جا اثبات شد: `bigint` رشته
می‌آید و `assertBigintCoercion` اجرا را رد می‌کند. کامنتِ خودِ تابع هم اصلاح شد.

### ⚠️ یک گیتِ کهنه که این فاز پیدایش کرد

`db:fk-test` از فاز ۴ می‌خواست مجموعه‌ی پلن‌های غیرفعال **دقیقاً** `pro,team` باشد. فاز ۶
با migrationِ `0005` پلنِ `personal` را اضافه کرد که آن هم `is_active=false` است — نه
به‌خاطرِ قیمت، بلکه چون اصلاً **فروختنی نیست**. گیت از آن لحظه کهنه بود و اولین اجرای
بعدش (همین فاز) قرمزش کرد. یعنی «۱۰ چکِ سبز»ِ ثبت‌شده‌ی فاز ۶ روی دیتابیسی بود که هنوز
`0005` را نگرفته بود. اصلاح شد و مجموعه حالا **صریح** است تا پلنِ غیرفعالِ بعدی هم دیده شود.

### دو شکستنِ عمدی (وگرنه سنجه گیت نیست)

| شکستن | نتیجه |
|---|---|
| برداشتنِ `FOR UPDATE` از `settlePayment` | ✖ چکِ همزمانی: `total=2` اشتراک و `activations=2` از **یک** پرداخت |
| تخت‌کردنِ `canceled\|expired` به یک وضعیت | ✖ چکِ پایانِ اشتراک: `expired/expired` به‌جای `expired/canceled` |

★ و چکِ همزمانی از اول با درگاهِ **کُند** (۲۵۰ms) نوشته شد — چون همین ماژول سه بار نشان
داده بود که یک چکِ «همزمانی» بدونِ تاخیرِ واقعی، با برداشتنِ قفل هم سبز می‌مانَد.

### ⚠️ تله‌ی محیطی — پورتِ دیتابیس دوباره جابه‌جا شد

`pnpm db:up` با «socket... forbidden» افتاد: رنجِ excludedِ ویندوز بعد از ری‌استارت
جابه‌جا شده و حالا **۵۴۳۳–۵۵۳۲** را گرفته. پورتِ DB در `.env`ِ محلی به **۵۵۴۴** منتقل شد
(بیرونِ همه‌ی رنج‌ها). ⚠️ رنج **۳۰۰۱–۳۴۰۰** هم گرفته شده، یعنی `RT_PORT=3001` و
`PORT=3002` دفعه‌ی بعد که سرور بالا بیاید همین مشکل را خواهند داشت.

### قدم بعد

**فاز ۸ — `packages/sdk`**: فضای‌نامِ `billing` در کلاینت، پشتیبانیِ `Idempotency-Key`
(که امروز اصلاً وجود ندارد)، و گسترشِ `scripts/sdk-contract.ts`.

⚠️ **دو ADRِ این فاز (۰۵۶ و ۰۵۷) هنوز «پیشنهادی»اند و تاییدِ مالک می‌خواهند.** ADR-057
مخصوصاً، چون به **کلِ** `apps/api` دست می‌زند نه فقط billing.

### وضعیتِ گیت

`pnpm verify` — **سبز (هر ۸ گیت)** ⊕ عددِ «۹/۹»ی ورودی‌های قبلی اشتباه بود؛ اسکریپت ۸ گیت دارد · `billing:probe-reconcile` ۹ چک · `billing:settle` ۸ چک ·
`billing:quota` ۶ چک · `db:fk-test` ۱۰ چک (یکی‌شان اصلاح‌شده).
