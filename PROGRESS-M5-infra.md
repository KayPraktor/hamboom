# PROGRESS-M5-infra.md — دفترِ کارِ ماژول M5 (`infra`)

> «چه شد / چه تصمیمی گرفتم / قدم بعد» در پایانِ هر session.
> TODO در [`TODO-M5-infra.md`](TODO-M5-infra.md) · نقطه‌ی ورود [`docs/m5-handoff.md`](docs/m5-handoff.md).

---

## ۱۴۰۵/۰۶/۱۷ — فاز ۰: تصمیم‌های مرزی

**چه شد:** نقشه‌ی M5 تایید شد، نُه تصمیمِ مرزی بسته شد، و پنجِ‌شان به ADR تبدیل شد
(۵۷ → **۶۲**). صفر خط کد — عمداً، مثلِ فاز ۰ی M4.

### ★★ سه یافته‌ی بررسی که کلِ نقشه را شکل دادند

**۱. هیچ پکیجی build ندارد، جز `apps/web`.** اندازه‌گیری‌شده: `apps/api`،
`apps/realtime`، `packages/{shared-types,billing-core,config}` هیچ اسکریپتِ `build` ندارند و
همه مستقیماً `./src/index.ts` را export می‌کنند. یعنی سوالِ «Dockerfile چه بسازد» جوابِ
از-پیش-تعیین‌شده دارد: **چیزی برای ساختن نیست، سورس می‌رود** → [ADR-058](ARCHITECTURE_DECISIONS.md#adr-058).
این را حدس نزدم؛ از `package.json`ها خواندم.

**۲. ★★ تریگرِ ADR-048 امروز اندازه‌ناپذیر است.** آن ADR شرطِ فعال‌سازیِ room affinity را با
عدد نوشته — «≥۲ رپلیکا **و** حافظه‌ی مقیمِ اتاق‌ها روی یک نود از ~۶۰٪ heap بگذرد» — ولی
هیچ‌جای سیستم حافظه‌ی اتاق را **گزارش نمی‌کند**. یعنی معیارِ تصمیمِ خودمان قابلِ ارزیابی
نیست. **تریگری که قابلِ اندازه‌گیری نباشد تریگر نیست، آرزوست** — و این تنها دلیلی است که
فاز ۵ (رصدپذیری) پیش از فاز ۶ (چندنودی) می‌آید، نه بعدش.

**۳. کلِ `apps/api/src/plugins` فقط یک گاردِ بوت دارد** (`merchantId`ِ زرین‌پال)، به‌علاوه‌ی
`assertGatewayAllowed` و گاردِ مسیرِ callback که فاز ۹ی M4 اضافه کرد. `JWT_SECRET`،
`DATABASE_SSL` و `RT_DEV_JWT_SECRET` **هیچ گاردی ندارند** — یعنی یک اپِ production با رازِ
پیش‌فرض بالا می‌آید و چیزی نمی‌گوید. کارِ فاز ۴.

### تصمیم‌هایی که گرفتم

**M5-D2 (بی‌build) سخت‌ترینشان بود** چون وسوسه‌ی «درست»ش این است که کامپایل اضافه کنیم. ولی
هزینه‌اش نقشه‌ی `exports`ِ هر پکیج، هر importِ نسبیِ `.ts`، و **کلِ ابزارِ سنجه‌ها**ست — همان
اسکریپت‌هایی که مبنای اثباتِ M2/M3/M4 بودند. هزینه‌ی پذیرفته‌شده صریح ثبت شد: حجمِ ایمیج،
strippingِ هر بوت (**فاز ۱ اندازه‌اش می‌گیرد**)، و محدودیتِ زبانی (`enum`/`namespace`/decorator
اجراناپذیرند — در M4 با `override` یک‌بار خورده‌ایم).

**M5-D1 (Compose نه K8s) یک انحرافِ ثبت‌شده از PLAN است.** PLAN آن بند را **پیش از**
اندازه‌گیری‌های M2 نوشت؛ حالا می‌دانیم ظرفیت حافظه‌محور است و یک نودِ ۴گیگی ~۴۰ بوردِ بزرگ
می‌گیرد. مسیرِ ارتقا هم بسته نشده — با عددِ ADR-048 باز می‌شود.

**M5-D3: advisory lock برای اتلاف است، نه درستی.** این تفکیک را در خودِ ADR پررنگ کردم چون
اشتباه‌گرفتنش خطرناک است: درستیِ «یک اشتراک از یک پرداخت» از قفلِ **ردیف** می‌آید و سنجه‌اش
با برداشتنِ `FOR UPDATE` قرمز می‌شود. اگر کسی فکر کند advisory lock ضامنِ درستی است، روزی
برش می‌دارد و خیال می‌کند فقط کمی اتلاف اضافه شده.

**M5-D5: سومین تعویقِ `apps/worker` — این‌بار با قاعده.** M3 و M4 هر دو موکولش کردند بدونِ
اینکه دلیل نوشته شود. حالا نوشته شد: worker وقتی لازم است که کار **مقیم** باشد. هر چیزِ
دوره‌ای و idempotent با یک کانتینرِ یک‌بارمصرف از **همان ایمیج** حل می‌شود — الگویی که
`billing:reconcile` از قبل اثباتش کرده.

### آنچه عمداً باز ماند

- **M5-D9 (عددهای نگهداشتِ داده)** — فقط فاز ۸ را بلاک می‌کند.
- **حسابِ آروان/دامنه و تاییدِ زرین‌پالِ production** — فاز ۱ و ۲ کاملاً لوکال اثبات می‌شوند،
  پس هیچ‌کدام امروز بلاک نیستند. ثبت شد تا وقتی رسیدیم پرسیده شود.

### قدم بعد

**فاز ۱ — probe.** پنج اندازه‌گیری، همه لوکال: ایمیجِ دستیِ api که واقعاً بوت شود · زمانِ
بوتِ سرد و حجمِ ایمیج · دسترسیِ رجیستری · realtime در کانتینر با WSِ واقعی · و
**advisory lock با دو پروسه‌ی واقعی** (ورودیِ فاز ۶).

⚠️ اگر ۱٫۲ عددِ بدی بدهد، [ADR-058](ARCHITECTURE_DECISIONS.md#adr-058) بازبینی می‌شود — نه
اینکه با آن سر کنیم. برای همین probe اول است.

### وضعیتِ گیت

`pnpm verify` سبز (فاز ۰ فقط سند بود).

---

## ۱۴۰۵/۰۶/۱۷ (ادامه) — فاز ۱: probe

**چه شد:** پنج اندازه‌گیری، همه لوکال. [ADR-058](ARCHITECTURE_DECISIONS.md#adr-058) پابرجا
ماند، و یک **نقصِ بسته‌بندی** پیدا شد که فقط نصبِ production نشانش می‌دهد.

| اندازه‌گیری | عدد |
|---|---|
| حجمِ ایمیجِ api / realtime | **۴۹۶MB** / **۴۹۱MB** |
| بوتِ سرد (start → listening) | **۴۸۳ms** |
| `/readyz` با دیتابیسِ سالم | **۲۰۰ در ۱۷ms** |
| pullِ `node:24-bookworm-slim` | **۴۳ ثانیه** (⚠️ از ماشینِ توسعه، نه آروان) |
| buildِ ایمیج | ۳۵s (api) · ۲۱s (realtime) |

### ★★ نقصی که فقط نصبِ `--prod` پیدایش کرد

`apps/realtime` از `@hamboom/config` استفاده می‌کند ولی آن را **هیچ‌جا اعلام نکرده بود** —
نه `dependencies`، نه `devDependencies`. کانتینر با `ERR_MODULE_NOT_FOUND` مرد.

چرا هیچ گیتی نگرفتش: در dev و در `pnpm verify` ماژول از راهِ ورک‌اسپیس resolve می‌شود، پس
typecheck و تست و لینت همه سبزند. **تنها چیزی که این کلاسِ نقص را نشان می‌دهد یک نصبِ
واقعیِ production است.** اعلام شد، lockfile به‌روز شد، ایمیج بالا آمد.

⊕ یک ممیزیِ سرتاسری هم زدم (هر `@hamboom/*`ی که در `src` استفاده می‌شود در برابرِ
`package.json`): هیچ پکیجِ دیگری این نقص را نداشت. ولی **هیچ گیتی هم نگهبانش نیست** —
به‌عنوانِ گامِ ۳٫۶ ثبت شد.

### آنچه خودآزمون‌ها ثابت کردند

**۱. ADR-058 پابرجاست.** بوتِ سرد ۴۸۳ms است — type-stripping هزینه‌ی محسوسی ندارد. اگر عدد
بد بود، ADR بازبینی می‌شد؛ نبود.

**۲. ★★ ادعای «strip-only خطرناک است» حالا دقیق شد.** خودآزمون: یک `enum` داخلِ کانتینر ⇒
`ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` و خطِ بعدش **هرگز اجرا نشد** — یعنی شکست در
**بارگذاریِ ماژول** است. و چون گرپ نشان داد سرورها **هیچ importِ پویا ندارند**، کلِ گراف در
بوت بارگذاری می‌شود. نتیجه‌ی دقیق: **هر نحوِ strip‌ناپذیری در بوت می‌شکند، نه وسطِ درخواستِ
مشتری.** ریسکِ ADR-058 حالا کران‌دار است.

**۳. fail-closed در کانتینر هم سالم است.** اتصالِ WS بدونِ توکنِ معتبر از بیرونِ کانتینر ⇒
`1008 TOKEN_INVALID`.

**۴. advisory lock همان‌طور که ADR-060 فرض کرده رفتار می‌کند** — ولی دو چیزش اثبات لازم داشت:
**مرگِ نشست قفل را خودکار آزاد می‌کند** (پس crashِ یک نود، آشتی‌دهی را تا ری‌استارتِ دیتابیس
نمی‌خواباند)، و **آزادسازی روی اتصالِ دیگرِ استخر `false` می‌دهد و قفل باقی می‌مانَد** — یعنی
قیدِ «اتصالِ اختصاصی»ِ ADR-060 حدس نبود.

### ⚠️ دو یافته‌ی جانبی که به فاز ۵ می‌روند

- **`apps/realtime` هیچ health endpointی ندارد.** پروکسی و ارکستراتور چیزی برای probe کردن
  ندارند.
- **`/readyz`ِ api وقتی دیتابیس نیست ۸ ثانیه طول می‌کشد و هیچ لاگی از علت نمی‌دهد.** یعنی
  اپراتور فقط «not_ready» می‌بیند، بی‌هیچ سرنخی.

### ⚠️ تله‌ی پورت، بارِ چهارم

ماشین بین دو session ری‌استارت شده بود و کانتینرها ۴۰ ساعت خاموش بودند؛ رنجِ excludedِ ویندوز
هم **دوباره جابه‌جا شده** و این‌بار **۷۳۷۳–۷۴۷۲** را گرفته — یعنی Redisِ ما (۷۳۷۹) داخلش
افتاد. به **۷۶۰۰** منتقل شد. ⊕ و اولین ۵۰۳ی `/readyz` واقعاً **درست** بود: دیتابیس بالا نبود.

### قدم بعد

**فاز ۲ — ایمیج‌های production.** ورودی‌هایی که فاز ۱ داد: الگوی Dockerfile کار می‌کند و
عددهایش قابلِ قبول است؛ حجمِ ~۵۰۰MB جای بهینه‌سازی دارد (لایه‌بندی و کپیِ گزینشیِ سورس)؛ و
⚠️ تله‌ی reverse proxy (هم‌مبدأبودنِ `API_PREFIXES`) هنوز پیشِ روست.

### وضعیتِ گیت

`pnpm verify` سبز · `infra:probe-lock` **۴ چک** · ایمیجِ api و realtime هر دو **بوت‌شده و
اثبات‌شده** روی زیرساختِ واقعیِ لوکال.

---

## ۱۴۰۵/۰۶/۱۷ (ادامه) — پاسخِ مالک درباره‌ی حساب‌ها

**آروان ✅ · دامنه ✅ · زرین‌پال ⏳** (هنوز تایید نکرده‌اند).

⊕ و یک ابهامِ زبانی رفع شد: منظورم از «تاییدِ زرین‌پال» **تاییدِ حسابِ مالک توسط زرین‌پال**
بود (احراز هویت و گرفتنِ `merchant_id`ِ واقعی)، نه تاییدِ من.

### چه چیزی واقعاً بلاک است

★★ **فقط بالاآمدنِ `APP_ENV=production`** — و این رفتارِ **عمدیِ** کدِ خودمان است:
`assertGatewayAllowed` نمی‌گذارد `MockGateway` در production بالا بیاید، و `createZarinpal`
بدونِ merchant idِ ۳۶کاراکتری در بوت می‌شکند. یعنی بدونِ درگاهِ واقعی، production اصلاً
استارت نمی‌خورد — که رفتارِ درستی است.

★ **ولی `APP_ENV=staging` با mock بالا می‌آید** (enumِ `APP_ENV` سه مقدار دارد و گیت فقط
روی `production` است). پس **فاز ۲ تا ۹ کاملاً روی staging جلو می‌روند** و هیچ‌کدام منتظرِ
زرین‌پال نمی‌مانند. این را در TODO ثبت کردم تا دوباره کشف نشود.

### آنچه به مالک گفته شد و در TODO نشست

فهرستِ زیرساختِ آروان (VMِ ۸GB با استدلالِ حافظه‌محور، **سه** باکت — سومی جدا برای پشتیبان،
DNS)، دو چیزی که باید probe شوند نه فرض (**CDN جلوی WebSocket** و **مسیرِ گواهیِ TLS**)، و
سه سوالی که وقتی زرین‌پال تایید کرد باید از پشتیبانی پرسیده شود (بازه‌ی مجازِ verify،
`metadata.auto_verify`، بیشینه‌ی `ref_id`) — هر سه ورودیِ مستقیمِ بازه‌ی sweep.

⚠️ **PLAN Q4 (حقیقی یا حقوقی) حالا واقعاً لازم شده** — نوعِ حسابِ زرین‌پال به آن بسته است.

### قدم بعد

**فاز ۲ — ایمیج‌های production.** شروع نشده (به‌خواستِ مالک، پیش از کامپکت).

---

## ۱۴۰۵/۰۶/۱۸ — فاز ۲: ایمیج‌های production ✅

**چه شد:** هر سه ایمیج ساخته شدند، کلِ استک با `APP_ENV=production` روی یک شبکه‌ی تمیز
بالا آمد، و جریانِ واقعیِ کاربر از پشتِ reverse proxy اثبات شد. **پنج نقص پیدا شد که
هیچ‌کدام در dev دیده نمی‌شدند** — سه‌تا رفع شد، یکی به فاز ۴ رفت، و یکی تصمیمِ مالک می‌خواهد.

| گام | چه شد |
|---|---|
| ۲٫۱ | [`api.Dockerfile`](infra/docker/api.Dockerfile) و [`realtime.Dockerfile`](infra/docker/realtime.Dockerfile) سخت شدند: HEALTHCHECK، اسکریپت‌های اپراتور، نصبِ ریشه |
| ۲٫۲ | [`web.Dockerfile`](infra/docker/web.Dockerfile) — buildِ Vite → nginx (**۱۴۲MB**) |
| ۲٫۳ | [`nginx/`](infra/nginx/) + گیتِ [`infra-check-proxy`](scripts/infra-check-proxy.ts) — **داخلِ `pnpm verify`** |
| ۲٫۴ | [`docker-compose.prod.yml`](infra/docker/docker-compose.prod.yml) + [`infra/README`](infra/README.md) |
| ۲٫۵ | آشتی‌دهی به‌عنوانِ کانتینرِ یک‌بارمصرفِ **همان ایمیجِ api** ✅ |

### ★★ گیتِ پروکسی — همان لحظه‌ی ساخته‌شدن، دو دریفتِ واقعی گرفت

فهرستِ پیشوندهای api از `vite.config.ts` به [`apps/web/src/api-prefixes.ts`](apps/web/src/api-prefixes.ts)
منتقل شد و **سه** مصرف‌کننده پیدا کرد: پروکسیِ dev، فایلِ **تولیدشده‌ی** nginx، و گیتی که
هر دو را با مسیرهای **واقعیِ ثبت‌شده‌ی** `buildApp()` می‌سنجد.

اولین اجرا قرمز شد و دو نقصِ از-M3-مانده را نشان داد:

- **`/public` اصلاً در فهرست نبود** — یعنی `POST /public/boards/resolve` (که
  `sdk.links.resolve` صدایش می‌زند) در dev به SPA می‌رفت و `index.html` می‌گرفت.
- **`/links` در فهرست بود ولی هیچ مسیری ندارد** — نامِ فضای‌نامِ sdk بود، نه پیشوندِ URL.
- (+ `/api/v1/docs` هم پوشیده نبود.)

★ **خودآزمون دو لایه دارد:** `--self-test` هر دو مقایسه را با ورودیِ ساختگیِ خراب قرمز
می‌کند، **و** خودِ گیت با یک بلوکِ اضافیِ nginx داخلِ `pnpm verify` قرمز شد (`exit 1`).
گیت‌ها ۸ → **۱۰**.

### ★★ نقصی که فقط ایمیجِ production نشانش داد: `/assets`

خروجیِ پیش‌فرضِ Vite در `dist/assets/` می‌نشیند ⇒ مرورگر باندل را از
`/assets/index-<hash>.js` می‌خواهد. ولی **`/assets` یکی از پیشوندهای api است**
(`GET /assets/:fileId`) ⇒ reverse proxy کلِ JS/CSS را به api می‌فرستاد و اپ **هرگز بالا
نمی‌آمد**. در dev هرگز دیده نمی‌شود چون سرورِ dev اصلاً `/assets/*` تولید نمی‌کند.

رفع: `build.assetsDir = "static"`. اثبات: `index.html` به `/static/…` اشاره می‌کند و در
مرورگرِ واقعی هر پنج دارایی (JS/CSS/دو فونتِ Vazirmatn) ۲۰۰ گرفتند.

### ★★ نقصِ دوم: اسکریپت‌های اپراتور در ایمیج resolve نمی‌شدند

`docker compose run migrate` با `ERR_MODULE_NOT_FOUND: @hamboom/config` می‌مُرد. علتش
ساده و ظریف است: Node شناسه‌ی bare را از **محلِ خودِ فایل** به بالا resolve می‌کند، پس
`/app/scripts/migrate.ts` سراغِ `/app/node_modules` می‌رود — و نصبِ
`--prod --filter @hamboom/api...` اصلاً پروژه‌ی ریشه را نصب نمی‌کرد.

⚠️ **این همان کلاسِ نقصِ فاز ۱ است، یک طبقه بالاتر:** پروژه‌ی ریشه **وابستگیِ runtime دارد**
(اسکریپت‌های اپراتور به production می‌روند) ولی manifest هرگز نگفته بود. رفع: `@hamboom/config`
و `pg` از `devDependencies`ِ ریشه به `dependencies` رفتند و Dockerfile
`--filter hamboom` را هم انتخاب می‌کند.

⊕ `billing-reconcile.ts` از اول کار می‌کرد — چون همه‌ی importهایش **نسبی** به
`apps/api/src` اند. یعنی تفاوت را فقط اجرا نشان می‌داد، نه خواندنِ کد.

### ★ نقصِ سوم: کانتینرِ آشتی‌دهی کلِ configِ api را می‌خواهد

`scripts/billing-reconcile.ts` همان `loadApiConfig` را صدا می‌زند ⇒ بدونِ `JWT_SECRET` و
`S3_*` بالا نمی‌آید، با اینکه هیچ‌کدام را مصرف نمی‌کند. با یک بلوکِ envِ جدا، اولین متغیرِ
تازه‌ی api این کانتینر را **بی‌صدا** می‌شکست؛ پس api و reconcile یک anchorِ مشترک دارند.
⊕ **باریک‌کردنِ خودِ schema** (تا ابزارِ اپراتور رازِ JWT را نبیند) به **فاز ۴** رفت.

### ⚠️⚠️ یافته‌ای که تصمیمِ مالک می‌خواهد: پیامک در production

در همین مشق، کدِ OTP از **لاگِ سرور** خوانده شد در حالی که `APP_ENV=production` بود —
`app.ts` بی‌قیدوشرط `createMockSmsProvider` می‌سازد. یعنی یک نقضِ [ADR-031](ARCHITECTURE_DECISIONS.md#adr-031)
که تا امروز کسی ندیده بودش، و **بلاک‌کننده‌ی launch است**: با آن، هیچ کاربرِ واقعی‌ای
نمی‌تواند وارد شود (پیامکی فرستاده نمی‌شود) و کد در لاگ می‌نشیند.

⚠️ رفعش کدِ صرف نیست — **یک حسابِ سرویسِ پیامکِ ایرانی می‌خواهد**، مثلِ زرین‌پال. گاردِ
بوتش در گامِ ۴٫۱ می‌نشیند؛ انتخابِ سرویس تصمیمِ مالک است.

### RT از دامنه‌ی پخته‌شده آزاد شد

`VITE_RT_URL` در build تزریق می‌شد ⇒ هر محیط ایمیجِ خودش را می‌خواست و آنچه آزموده‌ایم
دقیقاً آنچه مستقر می‌شود نبود. حالا پیش‌فرضِ **production هم‌مبدأ** است (`wss://<host>/rt`
از راهِ پروکسی) و dev بی‌تغییر مستقیم به `RT_PORT` می‌رود.

### آنچه واقعاً اجرا شد (نه ادعا)

| چه چیزی | نتیجه |
|---|---|
| کلِ استک با `APP_ENV=production` | هر ۵ سرویس **healthy**؛ `migrate` با exit 0 و ۷ migration |
| `/` · `/static/*.js` · فونت‌ها | ۲۰۰ — و در **مرورگرِ واقعی** صفحه‌ی ورودِ فارسی/RTL رندر شد |
| `/healthz` · `/openapi.json` · `/api/v1/docs` | ۲۰۰ از پشتِ پروکسی |
| `/billing/plans` | ۲۰۰ با پلن‌های واقعیِ migration `0006` |
| `POST /public/boards/resolve` | **۴۰۱ JSON** — پیشوندِ تازه‌افزوده واقعاً به api می‌رسد |
| WebSocket روی `/rt` | upgrade رد شد · `1008 TOKEN_MISSING` ⇒ fail-closed سالم |
| ★ چرخه‌ی کاملِ ورود | OTP → verify → **کوکیِ HttpOnly با `path=/auth`** → `/me` → `/auth/refresh` |
| ★★ گیتِ production + `mock` | **exit 1** (`GatewayNotAllowedError`) |
| ★★ گاردِ مسیرِ callback | **exit 1** با `/api/v1/…`ِ غلط |
| ۲٫۵ آشتی‌دهیِ یک‌بارمصرف | exit 0، با `--dry-run` هم |

### عددها

| چه چیزی | فاز ۱ | حالا |
|---|---|---|
| ایمیجِ api | ۴۹۶MB | **۴۷۰MB** |
| ایمیجِ realtime | ۴۹۱MB | **۴۶۵MB** |
| ایمیجِ web | — | **۱۴۲MB** |
| buildِ web (سرد) | — | ~۸۹s |

⚠️ حجم هنوز زیرِ سلطه‌ی ایمیجِ پایه (`node:24-bookworm-slim`) و `@aws-sdk/client-s3` است؛
`pnpm deploy` بررسی شد و **صرف نمی‌کرد** (سورس چند مگابایت است، نه چند صد).

### قدم بعد

**فاز ۳ — CI.** گامِ ۳٫۶ (وابستگیِ اعلام‌نشده) حالا **دو** نمونه‌ی واقعی دارد: `apps/realtime`
از فاز ۱ و پروژه‌ی ریشه از همین فاز.

---

## ۱۴۰۵/۰۶/۱۸ (ادامه) — فاز ۳: CI ◑ (نوشته و محلی مشق‌شده)

**چه شد:** دو workflow نوشته شد، **سه** گیتِ نو ساخته و خودآزمون شد، و کلِ فهرستِ
دستوراتِ jobِ سرویس‌ها محلی اجرا شد — که همان‌جا یک نقصِ ماه‌هاقدیمی را نشان داد.

⚠️⚠️ **و سه گام عمداً تیک نخوردند.** معیارِ پذیرشِ ۳٫۱، ۳٫۲ و ۳٫۵ «سبز روی PR» است و
**هیچ کامیتی هنوز push نشده** (`origin/main` هجده کامیت عقب است، روی یک کامیتِ M3).
یعنی workflow هرگز روی یک runner اجرا نشده. نوشتنِ «✅» برایشان دقیقاً همان سبزِ دروغینی
است که کلِ `verify.mjs` برای جلوگیری از آن نوشته شده.

| گام | وضعیت |
|---|---|
| ◑ ۳٫۱ · ۳٫۲ | [`ci.yml`](.github/workflows/ci.yml) — دو job؛ ۵ + ۱۸ مرحله. همه‌ی مراحل محلی سبز |
| ✅ ۳٫۳ | [`gen-openapi --check`](scripts/gen-openapi.ts) — داخلِ `pnpm verify` |
| ✅ ۳٫۴ | جدولِ گیت‌های اجراناپذیر، در صدرِ `ci.yml` |
| ◑ ۳٫۵ | [`images.yml`](.github/workflows/images.yml) — build بدونِ push (**M5-D10**) |
| ✅ ۳٫۶ | [`deps:check`](scripts/check-workspace-deps.ts) — داخلِ `pnpm verify` |

### ★★ مشقِ محلی یک نقصِ واقعی پیدا کرد: `pnpm db:smoke` از M3 شکسته بود

`0002_board_fks.sql` روی `board_updates.board_id` یک FK به `boards` گذاشت، ولی اسکریپتِ
تستِ دود هنوز یک UUIDِ **تصادفی** درج می‌کرد ⇒ `23503 board_updates_board_fk`.

★ **این دقیقاً همان چیزی است که فاز ۳ برایش وجود دارد.** اسکریپت ماه‌ها قرمز بود و
هیچ‌کس ندید، چون هیچ‌چیز خودکار اجرایش نمی‌کرد — همان درسی که `lint:css` و `coverage`
قبلاً داده بودند، این‌بار روی یک سنجه‌ی زیرساخت. رفع: ردیف‌های پیش‌نیازِ user/team/board
داخلِ **همان** تراکنشی که آخرش rollback می‌شود، پس هیچ‌چیز در دیتابیس نمی‌مانَد.

### دو گیتِ تازه‌ای که به `verify` رفتند (گیت‌ها ۱۰ → ۱۳)

**۱. `docs (openapi)` (گام ۳٫۳).** یافته‌ی فاز ۱۰ی M4 بسته شد: `docs/openapi.json`
۶۶۴ خط کهنه بود و هیچ گیتی نگرفت، چون گاردِ دریفتِ داخلِ اپ **تعریف** را می‌سنجد نه
**فایلِ تولیدشده**. حالا `--check` هر دو فایل را بازتولید و مقایسه می‌کند.

**۲. `deps` (گام ۳٫۶).** دو ادعا: (الف) هر شناسه‌ی bare در سورسِ هر پکیج باید در منیفستِ
**همان** پکیج اعلام شده باشد؛ (ب) ★ importهای دو اسکریپتی که واقعاً به production می‌روند
باید در **`dependencies`**ِ ریشه باشند، نه `devDependencies`. **ادعای الف به‌تنهایی نقصِ
فاز ۲ را نمی‌گرفت** — به همین دلیل ادعای ب جدا هست.

★★ **و با بازساختنِ هر دو نقصِ تاریخی آزموده شد، نه با نمونه‌ی ساختگی:**
`@hamboom/config` از منیفستِ `apps/realtime` برداشته شد و `pg` به devِ ریشه برگردانده شد
⇒ گیت هر دو را همان لحظه گزارش کرد.

⚠️ و خودِ گیت یک **منفیِ کاذبِ واقعی** داشت که اجرای اول نشانش داد: یک مسیرِ مطلقِ ویندوز
داخلِ یک **کامنت** در `apps/realtime/src/index.test.ts` به‌عنوانِ «وابستگیِ اعلام‌نشده‌ی
`G:\…`» گزارش شد ⇒ حالا کامنت‌ها پیش از اسکن حذف می‌شوند و شناسه‌های دارای `:`/`\` رد
می‌شوند. (و فیکسچرِ خودآزمون کلمه‌ی `import` را از یک **ثابت** می‌سازد، وگرنه اسکنر
فیکسچرِ خودش را می‌شمرد — که خودش یادآورِ محدودیتِ regex است.)

### دو تصمیمِ ساختاریِ CI

**۱. jobِ سرویس‌ها از `docker-compose.yml`ِ خودِ ریپو استفاده می‌کند**، نه از `services:`ِ
گیت‌هاب. دلیلش P3 است: «`docker compose up` باید کافی باشد» ⇒ CI باید همان مسیری را برود
که توسعه‌دهنده می‌رود. (⊕ سودِ جانبی: minio در `services:`ِ گیت‌هاب اصلاً کار نمی‌کند،
چون آن‌جا نمی‌شود `command` داد.)

**۲. `.env` از `.env.example` ساخته می‌شود** ⇒ **`.env.example` هم آزموده می‌شود**: اگر
کسی متغیری اضافه کند و آن‌جا ننویسدش، jobِ سرویس‌ها قرمز می‌شود. قاعده‌اش از M2 نوشته
شده بود ولی تا امروز هیچ گیتی نداشت.

★ و [`ci-wait-services.mjs`](scripts/ci-wait-services.mjs) به‌جای `--wait`: minio عمداً
healthcheck ندارد، و **«پورت باز است» با «آماده است» یکی نیست**. هر سرویس با کارِ واقعیِ
خودش سنجیده می‌شود (`SELECT 1`، `PING`، health/live) **به‌علاوه‌ی وجودِ باکت‌ها** — که
`minio-init` می‌سازد و خودش خارج می‌شود. خودش هم آزموده شد (آدرسِ غلط ⇒ exit 1).

### ⏳ M5-D10 — مقصدِ رجیستری، عمداً باز

`images.yml` build می‌کند و **push نمی‌کند**. انتخابِ ghcr.io یا Docker Hub بدونِ دانستنِ
اینکه **VMِ آروان از کجا می‌تواند pull کند** همان «فرض به‌جای probe» است. فاز ۱ کشیدنِ
ایمیجِ پایه را ۴۳ ثانیه اندازه گرفت — ولی از ماشینِ توسعه، نه از داخلِ ایران.

⊕ ولی یک ادعای واقعی دارد: **گاردِ بوت داخلِ ایمیجِ ساخته‌شده زنده است** (production +
`mock` ⇒ exit 1 با `GatewayNotAllowedError`؛ محلی اثبات شد، بدونِ دیتابیس).

### آنچه محلی اجرا شد (همان فهرستِ jobِ سرویس‌ها)

`db:migrate` · `db:smoke` (بعد از رفع) · `db:fk-test` · `db:store-test` ·
`storage:smoke` · `assets:smoke` · `billing:probe-db` · `billing:probe-idem` ·
`billing:probe-math` · `billing:settle` · `billing:quota` · `billing:probe-reconcile` ·
`sdk:contract` · `infra:probe-lock` · **هر ۷ سنجه‌ی realtime** — همه ✔.

⚠️ YAMLِ هر دو workflow با `yq` پارس شد (ساختار و شمارِ مراحل درست) — ولی **پارس‌شدن با
اجراشدن یکی نیست** و این تنها چیزی است که از این‌جا قابلِ اندازه‌گیری بود.

### قدم بعد

**فاز ۴ — سخت‌سازیِ پیکربندی و راز.** ورودی‌هایش از فاز ۲ آماده است: گاردِ
`createMockSmsProvider` در production (بلاک‌کننده‌ی launch)، و باریک‌کردنِ schemaی config.

⊕ و یک تصمیمِ مالک: **آیا این ۱۸ کامیت push شوند** تا CI برای اولین بار واقعاً اجرا شود؟

---

## ۱۴۰۵/۰۶/۱۸ (ادامه) — فاز ۳ تیک خورد: CI روی runnerِ واقعی سبز شد

مالک اجازه‌ی push داد؛ هجده کامیت رفتند و **اولین اجرای واقعیِ CI** انجام شد.

### اجرای اول (`d27fd32`) — `verify` سبز، دو job قرمز

★★ **و هر دو شکست روی یک خط بود که هیچ مشقِ محلی‌ای اجرایش نمی‌کرد:**

۱. مرحله‌ی انتظار را `node scripts/ci-wait-services.mjs` صدا زده بودم، **بدونِ
   `--env-file`** ⇒ `.env` ساخته می‌شد و هیچ‌کس نمی‌خواندش. محلی همیشه با
   `--env-file-if-exists` اجرا شده بود، پس این حالت اصلاً رخ نداده بود. ⊕ و پیامِ خطای
   خودِ اسکریپت هم گمراه‌کننده بود («در CI با `cp` ساخته می‌شود») — فایل **بود**.
۲. `docker images` **حداکثر یک آرگومان** می‌گیرد و من هر سه تگ را با هم داده بودم ⇒
   جدولِ حجم افتاد، **بعد از** اینکه هر سه ایمیج build شده و گاردِ بوت پاس شده بود.

هر دو محلی بازتولید شدند، رفع شدند (`93d3195`)، و مرحله‌ی انتظار حالا `pnpm ci:wait` است
— مثلِ بقیه‌ی مراحل، یک اسکریپتِ `package.json`، نه یک دستورِ مخصوصِ YAML.

### اجرای دوم (`93d3195`) — هر سه job سبز

| job | نتیجه | زمان | کندترین مرحله |
|---|---|---|---|
| `ci / pnpm verify` | ✅ | ۲m۱۵s | `pnpm verify` ۱۲۴s |
| `ci / سنجه‌های زیرساختِ زنده` | ✅ | ۲m۰۰s | هفت سنجه‌ی realtime ۸۳s |
| `images / build` | ✅ | ۱m۰۴s | web ۳۱s · api ۲۱s · realtime ۸s |

⇒ **۳٫۱، ۳٫۲ و ۳٫۵ حالا معیارِ پذیرششان محقق است** و تیک خوردند. کلِ فاز ۳ تمام.

### ⚠️ دو چیزی که ثبت می‌شوند

**۱. «محلی سبز شد» برای CI شرطِ لازم است، نه کافی** — همان رابطه‌ای که «همه‌ی گیت‌ها
سبزند» با کدِ پول دارد (درسِ M4 فاز ۵). مشقِ محلی **همه‌ی ۱۸ مرحله** را سبز کرده بود و
باز هم دو نقص ماند، چون مشق دستورها را طورِ **دیگری** صدا می‌زد.

**۲. لاگِ Actions از API خوانده نمی‌شود** — `403 Must have admin rights` حتی روی ریپوی
عمومی. تشخیصِ هر دو نقص از روی **نامِ مرحله‌ی افتاده** + بازتولیدِ محلی انجام شد. اگر
روزی شکستی از نامِ مرحله معلوم نشد، متنِ لاگ از مالک لازم است.

### قدم بعد

**فاز ۴ — سخت‌سازیِ پیکربندی و راز.** ورودی‌هایش آماده‌اند: گاردِ
`createMockSmsProvider` در production (بلاک‌کننده‌ی launch، یافته‌ی فاز ۲) ·
`JWT_SECRET`ِ ضعیف · `DATABASE_SSL=false` · `RT_DEV_JWT_SECRET` · و باریک‌کردنِ
schemaی config تا کانتینرِ آشتی‌دهی رازِ JWT را نبیند.

---

## ۱۴۰۵/۰۶/۱۸ (ادامه) — فاز ۴: سخت‌سازیِ پیکربندی و راز ✅

**چه شد:** چهار گاردِ بوت، یک نمونه‌ی محیطِ استقرار، باریک‌کردنِ دسترسیِ کانتینرِ اپراتور،
و ممیزیِ ADR-031 روی `apps/realtime` که دو یافته داد.

⛔ **و مهم‌ترین پیامدش:** از حالا **`APP_ENV=production` عمداً بالا نمی‌آید** تا یک سرویسِ
پیامکِ واقعی سیم‌کشی شود.

| گام | نتیجه |
|---|---|
| ✅ ۴٫۱ | چهار گارد در [`production-guards.ts`](packages/config/src/production-guards.ts) + [`assertSmsProviderAllowed`](packages/auth-core/src/otp.ts) |
| ✅ ۴٫۲ | [`.env.production.example`](.env.production.example) + `loadReconcileConfig`ِ باریک |
| ✅ ۴٫۳ | staging روی همان compose — هر ۵ سرویس healthy |
| ✅ ۴٫۴ | ممیزیِ ADR-031 — دو یافته، هر دو بسته |

### ★★ گیتِ پیامک — و چرا عمداً production را می‌بندد

`assertSmsProviderAllowed` قرینه‌ی دقیقِ `assertGatewayAllowed`ِ M4 است. با `production`،
`MockSmsProvider` بالا نمی‌آید.

**چرا این درست است و نه سخت‌گیریِ بی‌جا:** با mock هیچ پیامکی فرستاده نمی‌شود، پس هیچ کاربرِ
واقعی نمی‌تواند وارد شود — و کدِ ورود در **لاگِ سرور** می‌نشیند. در فاز ۲ همین اتفاق افتاد و
من کدِ OTP را از لاگ خواندم در حالی که `APP_ENV=production` بود. یعنی نبودِ گیت، یک
«production»ی می‌ساخت که هم ناامن بود هم بی‌فایده.

⏳ **انتخابِ سرویسِ پیامکِ ایرانی تصمیمِ مالک است** — دومین بلاک‌کننده‌ی launch کنارِ زرین‌پال.
تا آن‌وقت `APP_ENV=staging` کاملاً کار می‌کند و کلِ فاز ۵ تا ۹ رویش جلو می‌رود.

### ⚠️ گاردِ SSL: چرا «همیشه true» غلط بود

نگارشِ اولِ ذهنی‌ام «در production حتماً `DATABASE_SSL=true`» بود — و **استقرارِ خودمان را
روزِ اول می‌شکست**: [ADR-059](ARCHITECTURE_DECISIONS.md#adr-059) یعنی Postgres در همان شبکه‌ی
compose است، بدونِ پورتِ هاست، و ترافیک اصلاً از ماشین بیرون نمی‌رود.

قاعده‌ی نهایی: **loopback یا نامِ تک‌بخشی (نامِ سرویسِ compose) محلی است؛ نامِ نقطه‌دار یا IP
یعنی اتصال بیرون می‌رود ⇒ SSL اجباری.** پس حالتِ خطرناکِ واقعی — بردنِ `DATABASE_URL` به
PostgreSQLِ Managedِ آروان و یادرفتنِ SSL — می‌شکند.

⚠️ **و در خودِ کد نوشته شده که این یک اثبات نیست، یک گارد است:** یک نامِ تک‌بخشی هم می‌تواند
دور باشد. ادعای بزرگ‌تر از این، همان سبزِ دروغینی است که این ریپو از آن می‌ترسد.

### ★ کمترین دسترسی: کانتینرِ اپراتور دیگر رازِ توکن را نمی‌بیند

یافته‌ی فاز ۲ بسته شد. `scripts/billing-reconcile.ts` حالا `loadReconcileConfig` را صدا
می‌زند (appEnv + database + apiServer + payment)، و `createPaymentGateway` به‌جای کلِ
`ApiConfig` یک `PaymentGatewayConfig`ِ `Pick`شده می‌گیرد.

**اندازه‌گیری، نه ادعا:** داخلِ کانتینرِ آشتی‌دهی

```
env | grep -c "JWT_SECRET\|S3_"   →   0
```

### ★ دو یافته‌ی ممیزیِ ۴٫۴ (ADR-031 روی realtime)

**۱. `devAuthEnvSchema` مرده بود.** `RT_DEV_JWT_SECRET` برای `DevBoardAuthority` بود که در
**M3 فاز ۷** حذف شد. از آن روز این بخش هیچ مصرف‌کننده‌ای نداشت — خلافِ قاعده‌ی خودِ
`packages/config` («فقط بخشِ دارای مصرف‌کننده»). حذف شد، و تستش به `authEnvSchema`ِ **زنده**
منتقل شد. ★ ولی **وجودِ** آن متغیر هنوز معنا دارد: نشانه‌ی `.env`ی که از dev کپی شده — پس
حالا یک تخلفِ گاردِ production است.

**۲. ★★ `BoardAuthority.developmentOnly` اختیاری بود.** همان پرچم در `PaymentGateway` و
`SmsProvider` **اجباری** است. اختیاری‌بودنش یعنی یک پیاده‌سازیِ آینده که فراموشش کند بی‌صدا
`undefined` می‌شود و **از گیت رد می‌شود** — یک گاردِ fail-open، گارد نیست.

⚠️ **و این فرضی نبود:** بدلِ تستِ «یک پیاده‌سازیِ واقعی در production مشکلی ندارد» در
`server.test.ts` دقیقاً همین کار را می‌کرد — پرچم را ننوشته بود و از گیت رد می‌شد. اجباری شد.

### آنچه واقعاً اجرا شد

| چه چیزی | نتیجه |
|---|---|
| ایمیجِ api · `APP_ENV=production` | **exit 1** — `SmsProviderNotAllowedError` |
| ایمیجِ api · رازِ ضعیف (پیش‌فرضِ واقعیِ `.env.example`) | **exit 1** — نشانه‌ی «change_me» |
| ایمیجِ api · دیتابیسِ دور بدونِ SSL | **exit 1** |
| ایمیجِ api · `RT_DEV_JWT_SECRET` تعریف‌شده | **exit 1** |
| ایمیجِ realtime · هر سه موردِ بالا | **exit 1** (گاردها مشترک‌اند) |
| کلِ استک با `APP_ENV=staging` | هر ۵ سرویس **healthy** · `/` · `/healthz` · `/billing/plans` همه ۲۰۰ |
| کانتینرِ آشتی‌دهیِ باریک | exit 0 · **صفر** متغیرِ `JWT_SECRET`/`S3_*` |
| تست‌ها | ۱۶ تستِ گارد در config · ۴ تستِ بوت در api · `pnpm verify` سبز (۱۳ گیت) |

### قدم بعد

**فاز ۵ — رصدپذیریِ حداقلی** ([ADR-061](ARCHITECTURE_DECISIONS.md#adr-061)). دو یافته‌ی فاز
۱ همان‌جا بسته می‌شوند: `apps/realtime` **هیچ health endpointی ندارد** (healthcheckِ امروزِ
کانتینرش فقط listen بودنِ پورت را می‌سنجد) · و `/readyz`ِ api با دیتابیسِ خاموش **۸ ثانیه**
طول می‌کشد و هیچ لاگی از علت نمی‌دهد. ★★ و گامِ ۵٫۱: **گیجِ حافظه‌ی اتاق**، که بدونش تریگرِ
عددیِ ADR-048 اندازه‌ناپذیر است.

---

## ۱۴۰۵/۰۶/۱۸ (ادامه) — CIِ فاز ۴ و یک قرمزِ **درست**

هجده کامیت push شد و CI روی `cce1322` اجرا شد.

| job | نتیجه | زمان |
|---|---|---|
| `ci / pnpm verify` | ✅ | ۱۳۲s |
| `ci / سنجه‌های زیرساختِ زنده` | ✅ | ۱۲۳s (هر ۱۸ مرحله) |
| `images / build` | ✖ → ✅ بعد از رفع | ۶۰s |

★★ **و شکستِ `images` دقیقاً همان چیزی بود که آن چک برایش نوشته شده بود.**

مرحله‌ی «گاردِ بوت داخلِ ایمیج زنده است» ادعا می‌کرد ایمیج باید با
`GatewayNotAllowedError` رد شود. فاز ۴ گیتِ **پیامک** را در `buildApp` **جلوتر** از گیتِ
درگاه گذاشت، پس ایمیج با `SmsProviderNotAllowedError` رد شد — یعنی هنوز رد می‌شد، ولی به
دلیلِ **دیگری**. چک همان را گفت: «بالا نیامد، ولی به دلیلِ دیگری. گیت اثبات نشد.»

⚠️ **این دقیقاً رفتارِ درست است.** یک چکِ سست که هر شکستی را «اثبات» بشمارد، روزی که گیتِ
واقعی حذف شود و اپ به دلیلِ یک تایپو بالا نیاید هم سبز می‌مانَد. حساس‌بودن به **نامِ خطا**
همان چیزی است که این کلاسِ سبزِ دروغین را می‌گیرد.

**رفع:** دو ادعای صریح به‌جای یکی، هر دو با اجرای واقعیِ ایمیج محلی اثبات‌شده —
`SmsProviderNotAllowedError` (اولین گیتی که در production می‌ایستد) و
`ProductionConfigError` با رازِ ضعیف (که **پیش از** همه می‌شکند). ★ و خودِ چک با یک
انتظارِ عمداً غلط قرمز شد.

⊕ **یک واقعیتِ ترتیبی که ثبت می‌شود:** در `APP_ENV=production` گیتِ درگاه دیگر هرگز
**اول** نمی‌شکند، چون پیامک قبلش می‌ایستد. اثباتِ گیتِ درگاه در
[`app.test.ts`](apps/api/src/app.test.ts) است، نه در CIِ ایمیج.

---

## ۱۴۰۵/۰۶/۱۸ (ادامه) — فاز ۵: رصدپذیریِ حداقلی ✅

**چه شد:** `/metrics` روی هر دو سرور، گیجِ حافظه‌ی اتاق، سندِ آستانه‌ها — و **یک یافته‌ی
فاز ۱ که غلط از آب درآمد**.

### ★★ تریگرِ ADR-048 حالا اندازه‌پذیر است

ADR-048 تریگرِ room affinity را با عدد نوشته بود («بیشینه‌ی حافظه‌ی مقیم از ~۶۰٪ heap
بگذرد») ولی آن عدد **هیچ‌جا گزارش نمی‌شد**. حالا `hamboom_rt_adr048_trigger_ratio`
مستقیم منتشر می‌شود — **خودِ نسبت، از قبل حساب‌شده**، نه دو عددِ خام که کسی جایی تقسیمشان
کند. دلیلش: وقتی معیارِ تصمیم یک فرمول در داشبوردِ دیگری باشد، همان فرمول کهنه می‌شود.

### ⚠️ چه چیزی اندازه‌گیری است و چه چیزی تخمین

**دقیق:** heap، سقفِ heap، شمارِ اتاق/نشست/اتصال، و **حجمِ سندِ** هر اتاق.
**تخمین:** حافظه‌ی **مقیمِ** اتاق = حجمِ سند × **۲۰×**.

اندازه‌گیریِ دقیقش heap snapshot می‌خواهد که در سرویسِ زنده مکثِ محسوس دارد. ضریب از
`realtime-baseline` آمد که در چهار مقیاسِ اتاقِ **واقعی** بین ۲۰٫۳ و ۲۰٫۸ ماند.

★★ **و probe مستقلاً سنجیدش: ~۱۱–۱۳×** (۳٫۵–۴٫۱KB/عنصر). ⚠️ **این تناقض نیست و پنهانش
نکردم:** probe سند را در **یک تراکنش** می‌سازد، در حالی که اتاقِ واقعی updateها را یکی‌یکی
از پروتکل می‌گیرد و ساختارهای داخلیِ بیشتری دارد. یعنی **۲۰× دست‌بالا می‌گیرد** — و برای
یک تریگرِ ظرفیت این جهتِ **امن** است: زودتر هشدار می‌دهد، نه دیرتر. ضریب خودش هم منتشر
می‌شود (`hamboom_rt_room_memory_ratio`) تا اگر با ارتقای Yjs عوض شد، دیده شود.

⚠️ و `encodeStateAsUpdate` **کش می‌شود** (۳۰ ثانیه): انجامش در هر scrape یعنی ابزارِ
اندازه‌گیری خودش به منبعِ فشارِ حافظه تبدیل شود.

### ⚠️⚠️ یک یافته‌ی فاز ۱ **غلط** بود

فاز ۱ ثبت کرده بود «`apps/realtime` هیچ health endpointی ندارد» و من در فاز ۲ بر همان
اساس یک healthcheckِ **TCPی** به کانتینرش دادم. فاز ۵ با درخواستِ واقعی سنجید:

```
/healthz → 200 ok      /readyz → 200 ready      (هر دو از M2)
```

⇒ healthcheck به `/healthz` عوض شد. ★ فرقش واقعی است: چکِ TCPی یک فرایندِ **قفل‌شده** را
سالم می‌دید، چون سوکتِ listen هنوز باز بود.

**درسش:** یافته‌ی ثبت‌شده هم باید مثلِ هر ادعای دیگری آزموده شود. من آن را از فاز ۱ به فاز
۲ و بعد به CLAUDE.md بردم بدونِ اینکه یک‌بار `curl` بزنم.

### ★★ ولی دو نقصِ واقعی در `/readyz`ِ api پیدا شد

**۱. علت را می‌بلعید.** `catch` خطا را کاملاً دور می‌ریخت: ۵۰۳ بدونِ هیچ سرنخی. حالا لاگ
می‌شود — ⚠️ فقط `message`، نه خودِ خطا: رشته‌ی اتصال **رمزِ عبور** دارد (P7).

**۲. سقفِ زمانی نداشت.** و عددِ ثبت‌شده‌ی فاز ۱ (۸ ثانیه) هم درست نبود. اندازه‌گیری:

| حالت | `/readyz` |
|---|---|
| اتصالِ **refused** (پورتِ بسته) | **۸ میلی‌ثانیه** |
| میزبانِ **غیرقابلِ دسترس** | **۲۱ ثانیه** |

⇒ `connectionTimeoutMillis = 5s` روی استخر. یک probeِ استقرار که ۲۱ ثانیه جواب ندهد،
نودِ سالم را هم unhealthy علامت می‌زند.

### ★ الگویی که دو بار لازم شد: **استثنای صریح**

`GET /metrics` دو گیت را قرمز کرد و **هر دو حق داشتند**: گیتِ پروکسی («مسیری که پروکسی
نمی‌بیندش») و گاردِ دریفتِ OpenAPI («مسیرِ بی‌سند»).

⚠️ ولی هر دو راهِ آسان بد بود — بردنش به `API_PREFIXES` یعنی nginx آن را **به دنیا** سرو
کند، و مستندکردنش در `openapi.json`ِ **عمومی** یعنی تبلیغِ همان چیزی که ADR-061 گفته نباید
عمومی باشد. راهِ سوم: استثنای **نام‌دار با دلیل** — `NEVER_PROXIED` و `NOT_IN_PUBLIC_SPEC`.

★★ **و خودِ استثناها آزموده می‌شوند:** اگر مسیرشان حذف شود، «استثنای مرده» قرمز می‌شود.
بدونِ آن، یک استثنا فقط سوراخی است که کسی یادش می‌رود ببندد.

### آنچه واقعاً اجرا شد

| چه چیزی | نتیجه |
|---|---|
| `/metrics`ِ realtime (سرورِ زنده) | ۱۲ متریک |
| `/metrics`ِ api (سرورِ زنده) | ۱۵ متریک |
| `pnpm infra:probe-metrics` | ✔ ۳ چک |
| `/healthz` و `/readyz`ِ realtime | ۲۰۰/۲۰۰ |
| `/readyz` با دیتابیسِ مرده | ۵۰۳ + **لاگِ علت** |
| تست‌ها | realtime ۱۴۱ · api ۴۱ · `pnpm verify` سبز (۱۳ گیت) |

⚠️ **تله‌ی محیطی که دوباره خورد:** `RT_PORT=3001` حالا داخلِ رنجِ excludedِ ویندوز است
(**۳۰۰۱–۳۲۰۰**) و سرور با `EACCES` بالا نمی‌آمد — دقیقاً همان چیزی که CLAUDE.md پیش‌بینی
کرده بود. برای اجرای دستی از ۱۵۴۰۱/۱۵۴۰۲ استفاده شد.

### قدم بعد

**فاز ۶ — انتخابِ رهبر و درستیِ چندنودی.** ورودی‌هایش آماده‌اند: `infra:probe-lock` از
فاز ۱ رفتارِ advisory lock را اثبات کرده، و حالا `/metrics` شمارنده‌های آشتی‌دهی را دارد
تا اثرِ روشن‌کردنِ `BILLING_RECONCILE_ENABLED` **دیده** شود.

---

## ۱۴۰۵/۰۶/۱۸ (ادامه) — فاز ۶: انتخابِ رهبر و درستیِ چندنودی ✅

**چه شد:** advisory lock روی کارِ آشتی‌دهی، روشن‌کردنش در configِ استقرار، و ممیزیِ
چیزهایی که هنوز تک‌نودی‌اند — که **یک نقصِ واقعی** بیرون داد.

### ★★ سنجه‌ای که می‌توانست دروغ بگوید

درسِ M4 این‌جا مستقیم به کار آمد: **یک چکِ همزمانی هم‌زمان نیست، مگر ثابت شود.** اگر کارِ
داخلِ قفل لحظه‌ای بود، نودِ اول قفل را می‌گرفت و آزاد می‌کرد **پیش از** اینکه نودِ دوم
اصلاً تلاش کند — و سنجه بی‌آنکه چیزی اثبات کند سبز می‌شد.

⇒ کارِ داخلِ ناحیه‌ی بحرانی یک `pg_sleep(1)`ِ **واقعی** است و هر دو نود با `Promise.all`
شروع می‌شوند. ★ و **دو** خودآزمون:

| خودآزمون | نتیجه |
|---|---|
| بدونِ قفل، هر دو نود کار می‌کنند | ✔ ⇒ سبزِ چکِ اول از **خودِ قفل** می‌آید، نه از سریالی‌شدنِ تصادفی |
| پیاده‌سازیِ عمداً شکسته (`held = true`) | ✖ «۲ نود کار کردند» ⇒ سنجه واقعاً می‌بیند |

### ★★ ممیزیِ ۶٫۳ — خواندنِ کد کافی نبود

سوال: `idempotency.ts` **حافظه‌ای** است؛ با دو نود چه می‌شود؟

**خواندنِ کد می‌گفت «امن است»** و درست هم می‌گفت: `createCheckout` اول با
`idempotency_key` در **دیتابیس** جست‌وجو می‌کند و `payments_idem_uq` یکتاست. پس یک retryِ
**پشتِ سرِ هم** — که حالتِ رایج است (double-click، تلاشِ دوباره بعد از timeout) — حتی بینِ
دو نود هم درست replay می‌شود. یک چکِ ساده هم همین را نشان داد: دو استخرِ جدا، یک کلید،
**یک ردیف**، و هر دو همان `paymentId`.

⚠️ **ولی آن چک اثبات نمی‌کرد که دو تراکنش هم‌پوشانی داشته‌اند** — دقیقاً همان تله. پس
شکلِ `SELECT → INSERT` با SQLِ خام بازسازی شد و یک `pg_sleep(0.4)` وسطش نشست:

```
نودِ ۱ ⇒ inserted   ·   نودِ ۲ ⇒ خطا: 23505   ·   ردیف: 1
```

★ **حکم: داده هرگز خراب نمی‌شود** (ردیف همیشه یکی) **ولی بازنده ۵۰۰ می‌بیند.**

**رفع:** [`createCheckoutIdempotent`](apps/api/src/services/billing.ts) یک بار دوباره
تلاش می‌کند. ⚠️ و **بیرونِ** تراکنش، چون بعد از `23505` کلِ تراکنش abort شده و هر کوئریِ
بعدی روی همان `tx` با «current transaction is aborted» می‌شکند — تراکنشِ **تازه** باید باز
شود تا SELECTش ردیفِ commit‌شده‌ی برنده را ببیند. دو تستِ واحد شاخه‌ی تازه را قفل کردند
(retry روی ۲۳۵۰۵ · و **عدمِ** retry روی هر کدِ دیگر، تا خطای واقعی پنهان نشود).

### جدولِ «پیش از افزودنِ نودِ دوم»

در [`infra/README`](infra/README.md) نشست. خلاصه:

| چه چیزی | حکم |
|---|---|
| آشتی‌دهیِ بازه‌ای | ✅ حل شد (advisory lock) |
| `Idempotency-Key`ِ پرداخت | ✅ درستی از قبل در **دیتابیس** بود؛ شکافِ ۵۰۰ هم بسته شد |
| `Idempotency-Key`ِ POSTهای غیرمالی | ⚠️ **باگِ کاربری، نه فسادِ داده** — دو کلیکِ هم‌زمان ⇒ دو بورد |
| سقفِ نرخِ حافظه‌ای | ⚠️ **تضعیفِ حفاظت** — سقفِ موثر N برابر؛ `RATE_LIMIT_OTP_MAX=5` با ۳ نود = ۱۵ پیامک |

⇒ هر دو موردِ ⚠️ **شرطِ نودِ دوم**اند، نه بلاک‌کننده‌ی امروز. و نودِ دوم تریگرِ عددی دارد
که از فاز ۵ اندازه‌پذیر است.

### گیت‌ها

`pnpm verify` سبز (۱۳ گیت، ۴۳ تستِ api) · `billing:settle` **۹ چک** (یکی نو) ·
`billing:probe-reconcile` ۹ · `billing:quota` ۶ · `infra:probe-lock` ۴ ·
`infra:probe-leader` **۳ (نو)** · `sdk:contract` ۱۶.

### قدم بعد

**فاز ۷ — دوامِ داده.** ★★ و قاعده‌ی خودِ TODO آن‌جا حاکم است: **یک پشتیبانِ بازیابی‌نشده
پشتیبان نیست** — مشقِ بازیابی باید گیت شود، نه یک اسکریپتِ `pg_dump`ِ تنها.

---

## ۱۴۰۵/۰۶/۱۸ (ادامه) — CIِ فاز ۶، و وضعیتِ پیامک

### CI روی `6c98f30` — هر سه سبز

| job | نتیجه | زمان |
|---|---|---|
| `ci / pnpm verify` | ✅ | ۱۳۹s |
| `ci / سنجه‌های زیرساختِ زنده` | ✅ | ۱۲۳s |
| `images / build` | ✅ | ۶۳s |

⊕ معنادار: jobِ سرویس‌ها `billing:settle` را با **چکِ نهمِ تازه** اجرا کرد — یعنی همان
هم‌پوشانیِ اجباری که نقصِ `23505` را پیدا کرد، حالا در **هر PR** روی Postgresِ واقعی تکرار
می‌شود، نه فقط یک‌بار روی ماشینِ توسعه.

### ⏳ پیامک — تصمیمِ مالک آمد، ولی بلاک شد

**سرویس: sms.ir**، حساب آماده. ⛔ **قالب نمی‌دهند چون «سایت بالا نیست»** — و سایت بالا نیست
چون لانچ نشده‌ایم.

★★ **قوی‌ترین راه، با کارِ فاز ۲ ساختنی است:** یک صفحه‌ی معرفی روی دامنه‌ی واقعی. دامنه ✅،
VMِ آروان ✅، ایمیجِ nginx+SPA ✅ — و فاز ۹ به‌هرحال همان VM را می‌خواهد. پنج راه و **سه
پیش‌نیازِ فازِ ۴٫۵** در [`TODO-M5-infra.md`](TODO-M5-infra.md) ثبت شد.

⚠️⚠️ **و یک کارِ امنیتیِ معلق:** یک کلیدِ APIِ sms.ir در گفت‌وگو فرستاده شد. **باید باطل
شود**؛ در هیچ فایلی ننوشتمش و کلیدِ تازه هم نباید در چت بیاید.

★ **قیدِ طراحی:** چون معلوم نیست کدام سرویس می‌مانَد، شکلِ قالب وارد لایه‌ی دامنه نمی‌شود —
`templateId` و نامِ پارامتر در آداپتور و config می‌مانند.

### ⚠️ و یک تله‌ی محیطی که پیش‌بینی‌اش درست درآمد

رنجِ excludedِ ویندوز به **۳۰۰۱–۳۲۰۰** رسید و `RT_PORT=3001` با `EACCES` بالا نیامد —
دقیقاً همان چیزی که CLAUDE.md از M4 هشدارش را داده بود. اجرای دستی به **۱۵۴۰۱/۱۵۴۰۲** رفت.

---

## ۱۴۰۵/۰۶/۱۸ — فاز ۷: دوامِ داده

### چه شد

پشتیبانِ دیتابیس، آینه‌ی Object Storage، **مشقِ بازیابی** به‌عنوانِ گیت، و داستانِ
migration در استقرار. سه اسکریپتِ تازه، همه داخلِ **همان ایمیجِ api** اجرا می‌شوند
(ADR-062: بدونِ ایمیجِ دوم و بدونِ `apps/worker`) و همه در **CI** هم اجرا می‌شوند.

| گام | خروجی |
|---|---|
| ۷٫۱ | [`backup-db.ts`](scripts/backup-db.ts) → `pg/<db>-<زمان>.dump` + `.json`ِ مانیفست در **باکتِ سوم** |
| ۷٫۲ | ★★ [`restore-drill.ts`](scripts/restore-drill.ts) — ۵ چک + خودآزمونِ ۴-سناریویی |
| ۷٫۳ | advisory lock در [`migrate.ts`](scripts/migrate.ts) + [`docs/backup-restore.md`](docs/backup-restore.md) |
| ۷٫۴ | [`backup-storage.ts`](scripts/backup-storage.ts) — آینه‌ی افزایشی + مانیفستِ تاریخ‌دار |

### ★★ یافته‌ای که پیش از نوشتنِ گیت، شکلِ گیت را عوض کرد

قبل از کدنویسی، یک dumpِ واقعی عمداً **بریده** شد و بازیابی امتحان شد. نتیجه:

```
pg_restore: error: could not read from input file: end of file
truncated-restore-exit=1
جدول‌های ساخته‌شده: 31        ← کاملِ schema
```

⇒ **گیتی که فقط `db:fk-test` را روی نسخه‌ی بازیابی‌شده بزند، روی این پشتیبان سبز
می‌شود** — چون آن تست ردیف‌های خودش را می‌سازد و rollback می‌کند و به داده‌ی موجود کاری
ندارد. پس مشق **پنج** چک دارد: `sha256` · کدِ خروجِ `pg_restore` · `schema_migrations` ·
★★ **شمارشِ ردیف در بازه** · `db:fk-test`.

★ **و شمارش یک بازه است، نه عدد:** `pg_dump` snapshotش را از لحظه‌ای وسطِ کار می‌گیرد، پس
مانیفست شمارشِ «قبل» و «بعد» را ثبت می‌کند و مشق می‌سنجد عدد **داخلِ بازه** باشد. روی
دیتابیسِ آرام بازه صفر-عرض است ⇒ برابریِ دقیق.

### ⚠️ انتظارِ خودم در خودآزمون غلط بود — و خودآزمون گرفتش

سناریوی چهارم قرار بود `--schema-only` باشد با این ادعا: «`pg_restore` سبز، `db:fk-test`
سبز، فقط `rows` قرمز». خودآزمون **قرمز شد**: `fkسبز=false`.

علتش اتفاقی نبود — `db-fk-test.ts` یک چکِ صریح دارد که `free`/`pro`/`team` باید فعال
باشند، یعنی به **seedِ جدولِ `plans`** تکیه می‌کند. پس تصادفاً یک چکِ داده هم هست، ولی
**فقط برای یک جدول**؛ خالی‌بودنِ `boards`/`payments` را نمی‌بیند.

⇒ سناریو عوض شد به **«داده‌ی یک جدولِ برگ جا مانده»** (`--exclude-table-data=otp_challenges`،
با یک ردیفِ سنتینل که خودِ خودآزمون می‌کارد و پاک می‌کند). آن جدول عمداً **برگ** است — نه
FKی به آن نه از آن — وگرنه نبودِ داده‌اش ساختِ FK را می‌شکست و چکِ `restore` قرمز می‌شد،
یعنی سناریو دیگر «فقط rows» را اثبات نمی‌کرد. حالا هر چهار چکِ دیگر **سبز** می‌مانند و
تنها `rows` قرمز است.

### ★★ قفلِ migration — با هم‌پوشانیِ **واقعی**، نه ادعا

تا امروز هیچ چیزی جلوی دو `migrate`ِ هم‌زمان را نمی‌گرفت (یک `docker compose up` هم‌زمان
با یک اجرای دستی). advisory lockِ `811_451_002` اضافه شد و **سنجیده شد**:

| حالت | زمان |
|---|---|
| بدونِ رقیب | **۲۳۳ms** |
| با نشستی که همان قفل را ۶ ثانیه نگه داشته | **۶ ثانیه**، بعد موفق |

⚠️ `lock_timeout = 60s` عمدی است: انتظارِ بی‌پایان پشتِ یک migrateِ گیرکرده، از شکستِ
صریح بدتر است.

### گیت‌ها و اعداد

- `pnpm verify` **سبز، هر ۱۳ گیت**.
- `pnpm deps:check` نقصِ واقعی گرفت: اسکریپت‌های تازه در ایمیج اجرا می‌شوند ولی
  `@hamboom/storage` در `devDependencies` بود ⇒ به `dependencies` رفت. ★ با برگرداندنِ
  عمدی **قرمز** شد.
- ایمیجِ api: **۴۷۰MB → ۵۶۲MB** (کلاینتِ Postgres 16). گیتِ `images` حالا ادعا می‌کند
  هر سه ابزار هم‌نسخه‌ی سرورِ compose‌اند — با انتظارِ اشتباه (۱۵) **قرمز** شد.
- مشق روی پشتیبانِ واقعی: `301076` بایت · ۷ migration · ۳۱ جدول · ۲۱ جدولِ دارای داده،
  همه در بازه · هر ۱۰ چکِ FK سبز.
- `db:smoke` · `db:store-test` · `infra:probe-lock` · `infra:probe-leader` سبز بعد از
  تغییرِ `migrate.ts`.

### ⚠️ دو چیزی که ثبت شد، نه پنهان

**۱. سقفِ حافظه:** پورتِ `ObjectStore` نه stream دارد نه کپیِ سمتِ سرور ⇒ هر شیء کامل در
حافظه بارگذاری می‌شود. بالای ۵۱۲MB هشدارِ صریح می‌دهد؛ تغییرِ پورت کارِ **M6**.

**۲. مبدأ شبکه‌ایِ تازه در build:** کلاینتِ Postgres 16 از `apt.postgresql.org` می‌آید
(bookworm فقط ۱۵ دارد، و کلاینتِ قدیمی‌تر از سرور اصلاً کار نمی‌کند). این هم مثلِ
**M5-D10** باید از VMِ آروان probe شود.

### ⚠️ و باز هم همان تله‌ی محیطی

رنجِ excludedِ ویندوز دوباره جابه‌جا شد و این‌بار **۹۵۷۴–۹۶۷۳** را گرفت ⇒ MinIO روی
۹۶۰۰ bind نشد. به **۱۵۹۰۰/۱۵۹۰۱** رفت (`.env`ِ محلی). ⊕ و همان لحظه ۳۰۰۱–۳۲۰۰ **آزاد**
شده بود — یعنی این رنج‌ها نه فقط جابه‌جا می‌شوند، که آزاد هم می‌شوند؛ هیچ پورتی «امنِ
همیشگی» نیست.

---

## ۱۴۰۵/۰۶/۱۸ — فازِ ۴٫۵: پیامکِ واقعی (sms.ir)

### چه شد

مالک قالبِ sms.ir را گرفت (`#CODE#`، شناسه `265541`) و بلاک برداشته شد. سیم‌کشی دقیقاً
هم‌شکلِ زرین‌پال در M4 انجام شد: پورت از قبل بود، این پیاده‌سازیِ واقعی است.

| فایل | چیست |
|---|---|
| [`sms-ir.ts`](packages/auth-core/src/sms-ir.ts) | فرستنده — جفتِ `ZarinpalGateway` |
| [`apps/api/src/sms.ts`](apps/api/src/sms.ts) | انتخاب از روی env — جفتِ `plugins/payment.ts` |
| [`sms-probe.ts`](scripts/sms-probe.ts) | ارسالِ واقعی + چاپِ **بدنه‌ی خام** |

### ★★ گیتی که از فاز ۲ بسته بود، باز شد

چهار حالتِ بوت روی همین ماشین سنجیده شد:

| حالت | نتیجه |
|---|---|
| `staging` + `smsir` | ✔ بالا می‌آید |
| `smsir` بدونِ کلید | ✔ `SmsIrConfigError` **در بوت** |
| `production` + `mock` | ✔ `SmsProviderNotAllowedError` (گیتِ فاز ۴ زنده است) |
| ★★ `production` + `smsir` | ✔ **`GatewayNotAllowedError`** |

آخری مهم است: پیامک دیگر جلوی production را نمی‌گیرد ⇒ **تنها بلاک‌کننده‌ی باقی‌مانده
زرین‌پال است**.

⚠️ و همان اجرا یک چیزِ دیگر را هم نشان داد: دو حالتِ production **اول** با
`ProductionConfigError` می‌شکستند، چون `.env`ِ این ماشین `RT_DEV_JWT_SECRET` دارد —
یعنی گاردِ «`.env`ِ کپی‌شده از dev»ِ فاز ۴ دقیقاً همان کاری را کرد که برایش ساخته شد.

### ★★ یک نقصِ واقعی که **تا امروز غیرقابلِ‌رسیدن بود**

`requestOtp` چالش را **پیش از** ارسال ذخیره می‌کند. با `MockSmsProvider` که هرگز شکست
نمی‌خورد، این بی‌ضرر بود. با فرستنده‌ی واقعی مسیرش باز می‌شود و شکلش بد است: یک شکستِ
گذرا (اعتبارِ تمام‌شده، timeout) کاربر را برای کلِ `cooldownSeconds` در حالتی می‌گذاشت
که **هر تلاشِ دوباره بی‌صدا رد می‌شود** و هیچ پیامکی هم نیامده — یعنی «کد نیامد، دوباره
بزن» هم جواب نمی‌داد.

رفع: شکستِ ارسال ⇒ چالش پس گرفته می‌شود. ★ با برداشتنِ عمدیِ همان خط، تست **قرمز** شد.

### آنچه از پیش گرفته شد

**P7:** پیامِ خطای فرستنده نه کد دارد نه شماره‌ی خام (تستِ صریح) · **نرمال‌سازیِ شماره**
شاملِ **ارقامِ فارسی و عربی** — کاربرِ ایرانی `۰۹۱۲…` تایپ می‌کند و آن رشته با `09…`
برابر نیست · **`res.ok` معیار نیست** (درسِ زرین‌پال): تصمیم از `status`ِ بدنه می‌آید ·
**نامِ پارامتر متغیر است، نه ثابتِ کد** · و گاردِ production حالا کلیدِ **جای‌نگه‌دار**
را هم رد می‌کند (وگرنه سرور بالا می‌آید و هیچ پیامکی نمی‌رود).

### ✅ تاییدِ روی گوشیِ واقعی — انجام شد (مالک، ۱۴۰۵/۰۶/۱۸)

`pnpm sms:probe` روی یک شماره‌ی واقعی اجرا شد و **هر دو** چیزی که هیچ تستی نمی‌گیردشان
اثبات شدند: پیامک **رسید** (نه فقط «صف شد») و **عددِ داخلِ پیامک با کدِ probe یکی بود**
⇒ `SMS_IR_PARAM_NAME=CODE` با متنِ قالب می‌خواند.

⇒ **زنجیره‌ی ورودِ کاربرِ واقعی کامل است.** فرضِ آخری که از روی مستندات مانده بود
(شکلِ `/v1/send/verify` و `status=1`) هم با همین اجرا قفل شد.

⚠️ متنِ قالبِ فعلی با «این یک پیام تست از هم بوم است» شروع می‌شود — کاربرِ واقعی همین را
می‌بیند. نسخه‌ی تولیدی باید جدا تایید شود.

⚠️⚠️ کلیدِ API دو بار در گفت‌وگو آمد و **باید باطل شود**. در هیچ فایلی نوشته نشد.

---

## ۱۴۰۵/۰۶/۱۸ — M5-D10 بسته شد ([ADR-063](ARCHITECTURE_DECISIONS.md#adr-063))

### ★★ یک عددِ غلط، سه ماه استدلال را نگه داشته بود

M5-D10 از فاز ۳ باز مانده بود با این استدلال که «انتقالِ دستی ~۱٫۲GB در هر استقرار است، پس
رجیستری لازم می‌شود». آن عدد از `docker images` خوانده شده بود، که اندازه‌ی هر ایمیج را
**با تمامِ لایه‌های مشترکش** جدا می‌شمارد. اندازه‌گیریِ واقعی:

| چه چیزی | `docker images` | ★ `docker save`ِ واقعی |
|---|---|---|
| هر سه ایمیج | ۱۱۶۸MB | **۱۸۵MB** |
| فقط api | ۵۶۱MB | **۱۱۹MB** |

★ و ادعا نماند: آرشیو با `docker load` بازگردانده شد و هر سه تگ برگشتند.

⇒ با این اعداد استدلال فرو ریخت. رجیستری **دو** وابستگیِ شبکه‌ای می‌آورد که هیچ‌کدام در
کنترلِ ما نیستند (CIِ بیرونِ ایران بتواند push کند · VMِ داخلِ ایران بتواند pull کند) تا
چند دقیقه صرفه‌جویی کند. `docker save` روی ssh به هیچ سرویسِ ثالثی وابسته نیست.

⊕ **و یک پیامدِ امنیتیِ رایگان:** CI هرگز رازِ رجیستری نمی‌خواهد، چون push وجود ندارد.

### [`infra:ship`](scripts/ship-images.ts) — و سه چک

`docker save` → انتقال → `docker load` → و بعد **راستی‌آزمایی در مقصد**: هر تگ `inspect`
می‌شود و **شناسه‌اش با مبدأ سنجیده می‌شود**. بدونِ سومی، یک ایمیجِ هم‌نامِ **قدیمی** در مقصد
«موفق» دیده می‌شد.

⚠️ **تله‌ی اندازه‌گیری‌شده:** روی این محیط `docker save -o <file> <یک ایمیج>` فایلِ **صفر
بایتی** می‌سازد، در حالی که همان دستور با سه ایمیج ۱۸۵MB می‌نویسد. علتش ایزوله نشد و
ادعایی درباره‌اش نمی‌کنیم — اسکریپت از **stdout** می‌نویسد، نه `-o`.

### ⚠️ و خودآزمون، طراحیِ خودم را غلط درآورد

سناریوی سوم قرار بود دو ایمیج از سه تا را بفرستد و انتظار داشت سومی «غایب» دیده شود.
**سبز مانْد** — چون در تمرینِ محلی مبدأ و مقصد **یک daemon**اند و سومی از سناریوی قبل
همان‌جا بود. یعنی «ایمیجِ واقعاً غایب» در تمرینِ محلی **ذاتاً غیرقابلِ ساخت** است.

⇒ ادعای واقعیِ زیرِ آزمون این است: «اگر تگی در مقصد نباشد، چک قرمز می‌شود» — پس با تگی
سنجیده شد که **هرگز ساخته نشده**. حالا `docker load` کدِ صفر می‌دهد و **فقط** چکِ تگ‌ها
قرمز می‌شود.

### ⏳ و آنچه آگاهانه پذیرفته شد

**VPS فعلاً خریده نمی‌شود.** هیچ فازی به آن بسته نیست: ۸ و ۹٫۱ و ۹٫۳ و ۱۰ کاملاً محلی‌اند،
و ۹٫۲ نیمه (پیکربندی لوکال اثبات می‌شود، صدورِ گواهی روزِ استقرار). ★★ و اولین استقرارِ
واقعی به‌هرحال منتظرِ زرین‌پال است، چون `APP_ENV=production` بدونِ merchant id در بوت
می‌شکند ⇒ **VM روی مسیرِ بحرانی نیست**.

⚠️ **ریسکِ پذیرفته‌شده، صریح:** این چیدمان هرگز روی ماشینِ مقصد اجرا نشده. هر یافته‌ی این
ماژول از اجرای واقعی آمده، پس روزِ اولِ استقرار چیزهایی پیدا می‌شود. کاهش‌دهنده‌اش این است
که کلِ استک با `APP_ENV=staging` از پشتِ nginx روی همین ماشین اثبات شده.

---

## ۱۴۰۵/۰۶/۱۸ — فاز ۸: نگهداشت و پاک‌سازی

### ★★ جاروبِ بلابِ یتیم، و نشتی‌ای که همان اجرای اول اندازه گرفت

نشتیِ ثبت‌شده از M3: حذفِ بورد ردیف‌ها را CASCADE می‌کند ولی **بلابِ S3 را پاک نمی‌کند**.
اولین اجرای [`infra:sweep-orphans`](scripts/sweep-orphans.ts) روی دیتابیسِ توسعه:

```
── hamboom-snapshots ──
  دارای مرجع : 9
  ★ یتیم     : 13  (3759KB)
```

⊕ و همان اعداد **درستیِ تطبیق را هم اثبات می‌کنند**: اگر شکلِ کلید غلط بود، صفر تا
تطبیق می‌خورد؛ ۹ تا خورد و ۱۳ تا نه.

### ★★ قیدی که شکلِ کلِ فاز را تعیین کرد

یک شیء بدونِ ردیف دو چیزِ کاملاً متفاوت می‌تواند باشد: زباله‌ی ماه‌ها پیش، یا آپلودی که
**همین حالا** تمام شده و ردیفش هنوز commit نشده. تنها چیزی که این دو را جدا می‌کند سنِ
شیء است — و پورتِ `ObjectStore` آن را **نداشت**.

⇒ `ObjectHead.lastModified` به پورت اضافه شد (هر دو پیاده‌سازی + تست). بدونِ آن، یک
جاروبِ ایمن **قابلِ نوشتن نبود** — نه اینکه سخت باشد، ممکن نبود.

### چهار محافظ، و اثباتِ اینکه هرکدام **به‌تنهایی** لازم است

پیش‌فرض هیچ چیزی پاک نمی‌شود · مهلتِ ۲۴ ساعته · ابهام ⇒ دست‌نزدن ([ADR-056](ARCHITECTURE_DECISIONS.md#adr-056)) ·
و **اول مرجع، بعد فهرستِ اشیاء** (برعکسش یتیمِ کاذب می‌سازد).

★ شش سناریوی خودآزمون، و مهم‌ترینشان دو تا: شیءِ **دارای مرجع با سنِ یک‌ساله** هرگز
انتخاب نمی‌شود · و همان شیءِ جوان **با مهلتِ صفر** یتیم می‌شود ⇒ پس مهلت بود که نگهش
داشت، نه یک تصادف.

⚠️ **و مسیرِ حذف هم آزمون‌پذیر شد.** اولش `deleteObject` داخلِ حلقه‌ی `main` بود، یعنی
خطرناک‌ترین چند خطِ ریپو تنها بخشِ **آزمون‌ناپذیر**ش می‌ماند — اثباتش یعنی پاک‌کردنِ
داده‌ی واقعی. حالا `deleteOrphans` جداست و با انبارِ ساختگی اثبات می‌شود که **دقیقاً
یک** کلید به آن می‌رسد.

⊕ به همین دلیل ۱۳ بلابِ یتیمِ واقعی هم **پاک نشدند**: اثباتِ مسیرِ حذف به قربانی‌کردنِ
داده نیاز نداشت.

### ✅ گام ۸٫۳ — جدولِ مرده رفت

`usage_counters` از M3 بود و **هرگز به‌روز نمی‌شد** (B-5)؛ M4 با
[ADR-053](ARCHITECTURE_DECISIONS.md#adr-053) ظرفیت را به `count(*)`ِ زنده برد و از آن روز
این جدول نه نوشته می‌شد نه خوانده. دو نوشتنِ باقی‌مانده برداشته شد و
[`0007`](apps/api/migrations/0007_drop_usage_counters.sql) حذفش کرد.

★ اثباتش هم اجرای واقعی بود، نه grep: بعد از drop، `billing:quota` (که دقیقاً همان چیزی
است که اگر به این جدول تکیه می‌کرد می‌شکست)، `db:fk-test` و ۴۷ تستِ api سبز ماندند.

⛔ **و `audit_logs` عمداً دست‌نخورده مانْد.** آن هم امروز نویسنده‌ای ندارد، ولی تفاوت
اساسی است: `usage_counters` **جایگزین شده**، در حالی که audit هنوز **پیاده نشده** و
مصرف‌کننده‌اش پنلِ ادمینِ M6 است.

### ◑ گام ۸٫۱ — سازوکار آماده، **عدد نه**

[`purge-deleted.ts`](scripts/purge-deleted.ts) ساخته و آزموده شد، ولی `--days` **پیش‌فرض
ندارد** و بدونش اصلاً بالا نمی‌آید. «چند روز بعد از حذف، داده واقعاً برود» تصمیمِ
**سیاستی** است (M5-D9)، و یک پیش‌فرضِ حدسی این‌جا یعنی داده‌ی کسی طبقِ حدسِ من پاک شود.

★ خودآزمونش روی **SQLِ واقعی** داخلِ تراکنشِ rollback‌شونده اجرا می‌شود (الگوی
`db:fk-test`): بوردِ ۴۰روزه می‌رود، ۱روزه و زنده می‌مانند، و با `N=0` همان ۱روزه هم
نامزد می‌شود ⇒ معیارِ سن واقعاً معیار است.

### ◑ گام ۸٫۴ — دو چیزِ متفاوت که یک اسم داشتند

**چرخشِ لاگِ کانتینر ✅** — درایورِ پیش‌فرضِ `json-file` **هیچ سقفی ندارد**، یعنی لاگِ یک
سرویسِ پرترافیک تا پرشدنِ دیسکِ VM بزرگ می‌شود و بعد **همه‌چیز** می‌ایستد. سقفِ ۱۰MB×۳
برای هر سرویس در compose.

⚠️ **و این با M5-D9 قاطی نمی‌شود:** آن تصمیمِ سیاستی درباره‌ی داده‌ی **کاربر** است؛ این
بهداشتِ دیسک است و عددش فنی. **نگهداشتِ audit** فعلاً موضوعیت ندارد چون رکوردی وجود ندارد.

### ✅ تاییدِ مالک (۱۴۰۵/۰۶/۱۹) و نقطه‌ی کامپکت

فاز ۸ تایید شد. **وضعیت در لحظه‌ی کامپکت:** M5 فاز ۰ تا ۸ + ۴٫۵ · `pnpm verify` سبز (۱۳ گیت)
· CI هر سه job سبز روی `37a47d3` · working tree تمیز · ریموت هم‌گام.

**سنجه‌های زنده (بیرونِ verify) که همه سبزند:** `billing:settle` ۱۰ · `billing:probe-reconcile` ۹ ·
`billing:quota` ۶ · `db:fk-test` ۱۰ · `infra:probe-lock` ۴ · `infra:probe-leader` ۳ ·
`infra:probe-metrics` ۳ · `infra:restore-drill` ۵ چک + خودآزمونِ ۵ سناریو ·
`infra:ship --self-test` ۳ · `infra:sweep-orphans --self-test` ۶ · `infra:purge --self-test` ۳ ·
`sdk:contract` ۱۶ · هر ۷ سنجه‌ی realtime.

**پورت‌های این ماشین:** DB ۵۵۴۴ · api ۳۴۱۰ · Redis ۷۶۰۰ · **MinIO ۱۵۹۰۰/۱۵۹۰۱** · web ۱۵۳۸۰ ·
اجرای دستیِ realtime/api روی ۱۵۴۰۱/۱۵۴۰۲.

**بازها، هیچ‌کدام بلاک‌کننده‌ی فاز ۹:** زرین‌پال (تنها بلاک‌کننده‌ی production) · M5-D9 · قالبِ
تولیدیِ sms.ir · PLAN Q4 · VPS (آگاهانه موکول؛ [ADR-063](ARCHITECTURE_DECISIONS.md#adr-063)).

⚠️⚠️ **کلیدِ APIِ sms.ir که در گفت‌وگو آمد باید باطل شود** — در هیچ فایلی نوشته نشد.

### قدم بعد

**فاز ۹ — سخت‌سازیِ ایران** (شروع نشده، به‌خواستِ مالک پیش از کامپکت):
۹٫۱ بازبینیِ heartbeat/timeoutِ WS با `rt:reconnect` (نه حدس) · ۹٫۲ TLS/ingress + سقفِ نرخِ لبه
(پیکربندی لوکال اثبات می‌شود؛ صدورِ گواهی روزِ استقرار — تصمیمِ «آروان یا ACME» عمداً باز) ·
۹٫۳ ممیزیِ نهاییِ P2 با grep. هیچ‌کدام VM نمی‌خواهند. ⏳ بلاکِ نرم: عددهای **M5-D9** از مالک نیامده. گام ۸٫۲
(جاروبِ بلابِ یتیم) و ۸٫۳ (حذفِ `usage_counters`) به آن عددها وابسته **نیستند** و
می‌توانند جلو بروند.
