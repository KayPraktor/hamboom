# PROGRESS-M5-infra.md — دفترِ کارِ ماژول M5 (`infra`)

> «چه شد / چه تصمیمی گرفتم / قدم بعد» در پایانِ هر session.
> TODO در [`TODO-M5-infra.md`](TODO-M5-infra.md) · نقطه‌ی ورود [`docs/m5-handoff.md`](docs/m5-handoff.md).

---

## ۱۴۰۵/۰۶/۱۷ — فاز ۰: تصمیم‌های مرزی

**چه شد:** نقشه‌ی M5 تایید شد، نُه تصمیمِ مرزی بسته شد، و پنجِ‌شان به ADR تبدیل شد
(۵۷ → **۶۲**). صفر خط کد — عمداً، مثلِ فاز ۰ی M4.

### ★★ سه یافته‌ی بررسی که کلِ نقشه را شکل دادند

**۱. هیچ پکیجی build ندارد، جز `apps/web`.** اندازه‌گیری‌شده: `apps/api`،
`apps/realtime`، `packages/{shared-types,billing-core,config}` هیچ اسکریپتِ `build` ندارند و
همه مستقیماً `./src/index.ts` را export می‌کنند. یعنی سوالِ «Dockerfile چه بسازد» جوابِ
از-پیش-تعیین‌شده دارد: **چیزی برای ساختن نیست، سورس می‌رود** → [ADR-058](ARCHITECTURE_DECISIONS.md#adr-058).
این را حدس نزدم؛ از `package.json`ها خواندم.

**۲. ★★ تریگرِ ADR-048 امروز اندازه‌ناپذیر است.** آن ADR شرطِ فعال‌سازیِ room affinity را با
عدد نوشته — «≥۲ رپلیکا **و** حافظه‌ی مقیمِ اتاق‌ها روی یک نود از ~۶۰٪ heap بگذرد» — ولی
هیچ‌جای سیستم حافظه‌ی اتاق را **گزارش نمی‌کند**. یعنی معیارِ تصمیمِ خودمان قابلِ ارزیابی
نیست. **تریگری که قابلِ اندازه‌گیری نباشد تریگر نیست، آرزوست** — و این تنها دلیلی است که
فاز ۵ (رصدپذیری) پیش از فاز ۶ (چندنودی) می‌آید، نه بعدش.

**۳. کلِ `apps/api/src/plugins` فقط یک گاردِ بوت دارد** (`merchantId`ِ زرین‌پال)، به‌علاوه‌ی
`assertGatewayAllowed` و گاردِ مسیرِ callback که فاز ۹ی M4 اضافه کرد. `JWT_SECRET`،
`DATABASE_SSL` و `RT_DEV_JWT_SECRET` **هیچ گاردی ندارند** — یعنی یک اپِ production با رازِ
پیش‌فرض بالا می‌آید و چیزی نمی‌گوید. کارِ فاز ۴.

### تصمیم‌هایی که گرفتم

**M5-D2 (بی‌build) سخت‌ترینشان بود** چون وسوسه‌ی «درست»ش این است که کامپایل اضافه کنیم. ولی
هزینه‌اش نقشه‌ی `exports`ِ هر پکیج، هر importِ نسبیِ `.ts`، و **کلِ ابزارِ سنجه‌ها**ست — همان
اسکریپت‌هایی که مبنای اثباتِ M2/M3/M4 بودند. هزینه‌ی پذیرفته‌شده صریح ثبت شد: حجمِ ایمیج،
strippingِ هر بوت (**فاز ۱ اندازه‌اش می‌گیرد**)، و محدودیتِ زبانی (`enum`/`namespace`/decorator
اجراناپذیرند — در M4 با `override` یک‌بار خورده‌ایم).

**M5-D1 (Compose نه K8s) یک انحرافِ ثبت‌شده از PLAN است.** PLAN آن بند را **پیش از**
اندازه‌گیری‌های M2 نوشت؛ حالا می‌دانیم ظرفیت حافظه‌محور است و یک نودِ ۴گیگی ~۴۰ بوردِ بزرگ
می‌گیرد. مسیرِ ارتقا هم بسته نشده — با عددِ ADR-048 باز می‌شود.

**M5-D3: advisory lock برای اتلاف است، نه درستی.** این تفکیک را در خودِ ADR پررنگ کردم چون
اشتباه‌گرفتنش خطرناک است: درستیِ «یک اشتراک از یک پرداخت» از قفلِ **ردیف** می‌آید و سنجه‌اش
با برداشتنِ `FOR UPDATE` قرمز می‌شود. اگر کسی فکر کند advisory lock ضامنِ درستی است، روزی
برش می‌دارد و خیال می‌کند فقط کمی اتلاف اضافه شده.

**M5-D5: سومین تعویقِ `apps/worker` — این‌بار با قاعده.** M3 و M4 هر دو موکولش کردند بدونِ
اینکه دلیل نوشته شود. حالا نوشته شد: worker وقتی لازم است که کار **مقیم** باشد. هر چیزِ
دوره‌ای و idempotent با یک کانتینرِ یک‌بارمصرف از **همان ایمیج** حل می‌شود — الگویی که
`billing:reconcile` از قبل اثباتش کرده.

### آنچه عمداً باز ماند

- **M5-D9 (عددهای نگهداشتِ داده)** — فقط فاز ۸ را بلاک می‌کند.
- **حسابِ آروان/دامنه و تاییدِ زرین‌پالِ production** — فاز ۱ و ۲ کاملاً لوکال اثبات می‌شوند،
  پس هیچ‌کدام امروز بلاک نیستند. ثبت شد تا وقتی رسیدیم پرسیده شود.

### قدم بعد

**فاز ۱ — probe.** پنج اندازه‌گیری، همه لوکال: ایمیجِ دستیِ api که واقعاً بوت شود · زمانِ
بوتِ سرد و حجمِ ایمیج · دسترسیِ رجیستری · realtime در کانتینر با WSِ واقعی · و
**advisory lock با دو پروسه‌ی واقعی** (ورودیِ فاز ۶).

⚠️ اگر ۱٫۲ عددِ بدی بدهد، [ADR-058](ARCHITECTURE_DECISIONS.md#adr-058) بازبینی می‌شود — نه
اینکه با آن سر کنیم. برای همین probe اول است.

### وضعیتِ گیت

`pnpm verify` سبز (فاز ۰ فقط سند بود).

---

## ۱۴۰۵/۰۶/۱۷ (ادامه) — فاز ۱: probe

**چه شد:** پنج اندازه‌گیری، همه لوکال. [ADR-058](ARCHITECTURE_DECISIONS.md#adr-058) پابرجا
ماند، و یک **نقصِ بسته‌بندی** پیدا شد که فقط نصبِ production نشانش می‌دهد.

| اندازه‌گیری | عدد |
|---|---|
| حجمِ ایمیجِ api / realtime | **۴۹۶MB** / **۴۹۱MB** |
| بوتِ سرد (start → listening) | **۴۸۳ms** |
| `/readyz` با دیتابیسِ سالم | **۲۰۰ در ۱۷ms** |
| pullِ `node:24-bookworm-slim` | **۴۳ ثانیه** (⚠️ از ماشینِ توسعه، نه آروان) |
| buildِ ایمیج | ۳۵s (api) · ۲۱s (realtime) |

### ★★ نقصی که فقط نصبِ `--prod` پیدایش کرد

`apps/realtime` از `@hamboom/config` استفاده می‌کند ولی آن را **هیچ‌جا اعلام نکرده بود** —
نه `dependencies`، نه `devDependencies`. کانتینر با `ERR_MODULE_NOT_FOUND` مرد.

چرا هیچ گیتی نگرفتش: در dev و در `pnpm verify` ماژول از راهِ ورک‌اسپیس resolve می‌شود، پس
typecheck و تست و لینت همه سبزند. **تنها چیزی که این کلاسِ نقص را نشان می‌دهد یک نصبِ
واقعیِ production است.** اعلام شد، lockfile به‌روز شد، ایمیج بالا آمد.

⊕ یک ممیزیِ سرتاسری هم زدم (هر `@hamboom/*`ی که در `src` استفاده می‌شود در برابرِ
`package.json`): هیچ پکیجِ دیگری این نقص را نداشت. ولی **هیچ گیتی هم نگهبانش نیست** —
به‌عنوانِ گامِ ۳٫۶ ثبت شد.

### آنچه خودآزمون‌ها ثابت کردند

**۱. ADR-058 پابرجاست.** بوتِ سرد ۴۸۳ms است — type-stripping هزینه‌ی محسوسی ندارد. اگر عدد
بد بود، ADR بازبینی می‌شد؛ نبود.

**۲. ★★ ادعای «strip-only خطرناک است» حالا دقیق شد.** خودآزمون: یک `enum` داخلِ کانتینر ⇒
`ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` و خطِ بعدش **هرگز اجرا نشد** — یعنی شکست در
**بارگذاریِ ماژول** است. و چون گرپ نشان داد سرورها **هیچ importِ پویا ندارند**، کلِ گراف در
بوت بارگذاری می‌شود. نتیجه‌ی دقیق: **هر نحوِ strip‌ناپذیری در بوت می‌شکند، نه وسطِ درخواستِ
مشتری.** ریسکِ ADR-058 حالا کران‌دار است.

**۳. fail-closed در کانتینر هم سالم است.** اتصالِ WS بدونِ توکنِ معتبر از بیرونِ کانتینر ⇒
`1008 TOKEN_INVALID`.

**۴. advisory lock همان‌طور که ADR-060 فرض کرده رفتار می‌کند** — ولی دو چیزش اثبات لازم داشت:
**مرگِ نشست قفل را خودکار آزاد می‌کند** (پس crashِ یک نود، آشتی‌دهی را تا ری‌استارتِ دیتابیس
نمی‌خواباند)، و **آزادسازی روی اتصالِ دیگرِ استخر `false` می‌دهد و قفل باقی می‌مانَد** — یعنی
قیدِ «اتصالِ اختصاصی»ِ ADR-060 حدس نبود.

### ⚠️ دو یافته‌ی جانبی که به فاز ۵ می‌روند

- **`apps/realtime` هیچ health endpointی ندارد.** پروکسی و ارکستراتور چیزی برای probe کردن
  ندارند.
- **`/readyz`ِ api وقتی دیتابیس نیست ۸ ثانیه طول می‌کشد و هیچ لاگی از علت نمی‌دهد.** یعنی
  اپراتور فقط «not_ready» می‌بیند، بی‌هیچ سرنخی.

### ⚠️ تله‌ی پورت، بارِ چهارم

ماشین بین دو session ری‌استارت شده بود و کانتینرها ۴۰ ساعت خاموش بودند؛ رنجِ excludedِ ویندوز
هم **دوباره جابه‌جا شده** و این‌بار **۷۳۷۳–۷۴۷۲** را گرفته — یعنی Redisِ ما (۷۳۷۹) داخلش
افتاد. به **۷۶۰۰** منتقل شد. ⊕ و اولین ۵۰۳ی `/readyz` واقعاً **درست** بود: دیتابیس بالا نبود.

### قدم بعد

**فاز ۲ — ایمیج‌های production.** ورودی‌هایی که فاز ۱ داد: الگوی Dockerfile کار می‌کند و
عددهایش قابلِ قبول است؛ حجمِ ~۵۰۰MB جای بهینه‌سازی دارد (لایه‌بندی و کپیِ گزینشیِ سورس)؛ و
⚠️ تله‌ی reverse proxy (هم‌مبدأبودنِ `API_PREFIXES`) هنوز پیشِ روست.

### وضعیتِ گیت

`pnpm verify` سبز · `infra:probe-lock` **۴ چک** · ایمیجِ api و realtime هر دو **بوت‌شده و
اثبات‌شده** روی زیرساختِ واقعیِ لوکال.
