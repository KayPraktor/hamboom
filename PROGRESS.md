# PROGRESS — canvas-core

تاریخ آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۰۷ (2026-07-29)
**گام فعلی: فاز ۵ (تعامل و صیقل) — گام ۵٫۱ در جریان.** z-order (لایه‌بندی) تمام و
تایید شد؛ بقیه‌ی ۵٫۱ (انتخاب/گروه بومی، روشن‌کردنِ snap، ابزارِ align) مانده.
(فاز ۴ کامل — mini-map موکول.)
پله‌ی [ADR-003](ARCHITECTURE_DECISIONS.md#adr-003): **A** (بسته npm، بدون patch)

**۵۰۵ تست سبز** (۴۴ shared-types، ۴۱۹ canvas-core، ۴۲ i18n). `typecheck` · `lint` ·
`lint:css` · `format:check` · `license:check` (self-test ۱۷/۱۷ + ۶۵۵ پکیج) — همه سبز.
درخت git تمیز، همه‌چیز کامیت شده.

---

## گام‌های تمام‌شده

| گام | چه شد | commit |
|---|---|---|
| ۰٫۱ | اسکلت مونوریپو، گیت لایسنس سه‌سطحی با self-test | `d697903` |
| — | `.gitattributes` (eol=lf) | `767b15c` |
| ۰٫۲ | پکیج canvas-core + دموی Vite + Vitest | `b83f821` |
| ۱٫۱–۱٫۳ | نصب موتور، spike متن فارسی | `f8aba4f` |
| ۱٫۳ب | تکمیل spike: ویرایش inline، کلیپ‌بورد، اعداد | `b32151f` |
| ۱٫۴ | لایه‌ی متن فارسی — جهت، نرمال‌سازی، wrapper `fillText` (بدون patch) | `60e5bbf`, `085938f` |
| ۲٫۱–۲٫۲ | `shared-types` (انواع عنصر + zod)، قرارداد `CanvasSyncAdapter` | `77e988c` |
| ۲٫۳ | نگاشت دوطرفه `toExcalidraw`/`fromExcalidraw`/`getKind` + قاعده‌ی ADR-010 | `38af038` |
| — | تک‌منبعی کردن واقعی `direction` (فیکس باگ round-trip) | `eafc07a` |
| ۳٫۱ | پالت استیکی، توکن‌ها، گیت کنتراست WCAG خودآزموده | `e6be078` |
| ۳٫۲ | استیکی‌نوت — سازنده، autoFit، تغییر رنگ، ابزار، میانبر | `592f1a6` |
| ۳٫۳ | شکل، متن آزاد، پنل استایل RTL، `factory.ts` مشترک | `7f924e7` |
| ۳٫۴ | کانکتور — مسیریابی قطعی (ADR-008)، reroute هنگام حرکت | `1a8b0f2` |
| ۳٫۵ | فریم — عضویت، حرکت یک‌ژستی، undo یک‌باره (ADR-026)، `bumpVersion` | `f43cfba` |
| — | فیکس دمو: تغییر رنگ باید یک ورودی undo باشد (تایید مرورگر) | `b0a4923` |
| ۳٫۶ | تصویر — `createImage`/اعتبارسنجی/`fitImageBox`، ابزار drag&drop+paste، جریان placeholder→saved | `692798e` |
| ۳٫۷ | قلم آزاد — RDP، کانال ephemeral، یک commit به‌ازای استروک (ADR-022)، overlay | `1c93306` |
| نگهبان | لینتِ `require-capture-update` + چوک‌پوینت‌ها (باگِ نهفته‌ی sticky-tool را گرفت) | `1fc540f` |
| ۴٫۱ | `packages/i18n` (اعداد/تاریخ جلالی/`t`) + گیتِ Stylelint ADR-016 | `c703831`, `cd79c4a` |
| — | سخت‌سازیِ i18n: self-testِ پول + مرزهای کبیسه‌ی جلالی (ممیزیِ مالک) | `7f501e3` |
| ۴٫۲ | نوار ابزارِ شناورِ پایین-وسط (۱۱ ابزار، tooltip فارسی، میانبر) | `2145af1` |
| ۴٫۳ (۱) | نوار وضعیتِ RTL (اتصال/ذخیره، فارسی) | `7b64d5c` |
| ۴٫۳ (۲) | منوی راست‌کلیکِ RTL — preemptِ منوی موتور (تایید مرورگر) | `82846a8` |
| — | تک‌منبعیِ قفل/حذف در `elements/operations.ts`؛ لایه → ۵٫۱ | `37dc180` |
| ۴٫۳ (۳) | پنل استایل: قفل (از `toggleLock`ِ مشترک) + لایه coming-soon | `b50cabd` |
| ۴٫۳ (۴) | کنترلِ zoom + برازش + درصدِ فارسی (zoom حول مرکز) | `e365d0a` |
| ۴٫۴ | حضور — مکان‌نما/هاله/آواتار/follow/read-only (BroadcastChannel نمایشی) | `dcc5737`, `5705b82` |

---

## ✅ تایید بسته‌شده — تغییر رنگ استیکی → undo (تعامل واقعی مرورگر)

**تایید شد و یک باگ واقعی بیرون کشید (۱۴۰۵/۰۵/۰۲).** سناریو با تعامل واقعی
مرورگر روی دموی زنده اجرا شد: ساخت فریم+دو استیکی → انتخاب استیکی زرد →
کلیک روی پالت «بنفش» (مسیر واقعی `recolorSelection`) → دکمه‌ی Undo موتور.

- **باگ:** `recolorSelection` در `dev/App.tsx` هنگام `updateScene`، **`captureUpdate:
  "IMMEDIATELY"` نداشت** (برخلاف `moveSelectedFrame`/`addFrameWithChildren` و
  برخلاف صریحِ [ADR-026](ARCHITECTURE_DECISIONS.md#adr-026) که «تغییر رنگ چند
  استیکی» را یک ژست IMMEDIATELY می‌داند). نتیجه: تغییر رنگ ورودی undo جدا
  **نمی‌ساخت**؛ اولین Undo به‌جای برگرداندن رنگ، **کل ژست قبلی را پاک می‌کرد**
  (۵ عنصر → ۰). دقیقاً همان کلاس باگی که `bumpVersion` تنها نیمی‌اش را می‌پوشاند:
  `bumpVersion` باعث می‌شود موتور تغییر را **ببیند**، ولی گروه‌بندی undo به
  `captureUpdate` بستگی دارد، نه به nonce.
- **فیکس:** افزودن `captureUpdate: "IMMEDIATELY"` به همان `updateScene`.
- **تایید بعد از فیکس (همه با دکمه‌های واقعی موتور):** Undo → رنگ به زرد
  `#FFF9B1` برگشت و **هر ۵ عنصر ماندند**؛ Redo → دوباره بنفش `#D0C6F5`؛ دو
  Undo پشت‌سرهم → اول فقط رنگ، بعد کل ساخت (۵ → ۰). یعنی «ساخت» و «تغییر رنگ»
  دو ورودی مجزای undo اند — granularity درست است.

## ⚠️ تاییدهای معلق (تعامل واقعی مرورگر لازم است)

این‌ها منطقاً درست‌اند و تست واحد دارند، ولی با **تعامل واقعی کاربر در مرورگر**
هنوز تایید نشده‌اند. jsdom هیچ‌کدام را نمی‌گیرد.

- [ ] تایید بصری ترتیب bidi روی `#spike` (از گام ۱٫۳) — تنها موردی که عددی
      اثبات شد ولی چشمی نه.
- [ ] تایید بصری هماهنگی رنگ‌های پالت (زیبایی‌شناختی، گام ۳٫۱).
- [ ] تایید بصری **پیکسلِ تصویر روی بوم** (گام ۳٫۶) — اسکرین‌شات در session ممکن
      نبود (پنل مرورگر نمایش داده نمی‌شد). ولی رفتاری‌ها همه تایید شدند: درج/undo/
      redo، preempt کردن drop/paste موتور، و رد فرمت غیرمجاز؛ blob رمزگشایی و فایل
      بدون خطا در موتور ثبت می‌شود (مسیر رندر استاندارد موتور).
- [ ] تایید بصری **خودِ استروکِ قلم روی بوم و overlay** (گام ۳٫۷) — همان محدودیتِ
      اسکرین‌شات. مسیر داده تایید شد: ۲۰۱ نقطه → یک `emitElementChanges`، RDP
      ۲۰۱→۲۹، یک عنصر freedraw، undo/redo درست، بدون خطای کنسول.

### ✅ دو سوالِ بازِ گام ۴٫۴ (حضور) — جواب داده شد (۱۴۰۵/۰۵/۰۷، تاییدِ مرورگر)

- [x] **Q1 — re-project شدنِ مکان‌نما/هاله‌ی همتا هنگام zoom/panِ محلی.** در مرورگر با
      یک همتای تزریق‌شده در مختصاتِ صحنه‌ی ثابت (۵۰۰،۳۰۰) و مقایسه با پروجکشنِ
      **دست‌محاسبه** آزموده شد:
  - **zoom:** درست re-project می‌شد — بعد از zoom به ۱٫۱، مکان‌نما دقیقاً روی
    `(۵۰۶، ۲۵۷٫۷۵)` = `(۵۰۰−۱۳۲٫۶۴)×۱٫۱ , (۳۰۰−۱۱۶٫۵)×۱٫۱`.
  - **panِ خالص:** ❌ **re-project نمی‌شد** — بعد از pan به `(−۶۰،−۱۲۰)` مکان‌نما سرِ
    پیکسلِ `(۵۰۰،۳۰۰)` می‌ماند به‌جای `(۴۴۰،۱۸۰)`ِ درست.
  - **علتِ واقعی (فرضیه‌ی قبلی غلط بود):** فرضیه گفته بود «panِ خالص اصلاً re-render
    نمی‌دهد» — ولی `onChange` روی pan **fire می‌شد** (شمارنده=۱) و `setSnapshot` هم
    re-render می‌داد. مشکل: آن re-render مقدارِ scroll را از `api.getAppState()`
    می‌خواند که **یک فریمْ کهنه** است (تله‌ی ثبت‌شده)، و چون `setZoom(همان‌مقدار)` روی
    panِ خالص bail می‌کند، re-renderِ دومی نبود تا تصحیح کند. (خودِ پروجکشن درست بود:
    با یک re-renderِ اجباری، مکان‌نما به `(۴۴۰،۱۸۰)`ِ درست می‌پرید.)
  - **فیکس ([dev/App.tsx](packages/canvas-core/dev/App.tsx)):** یک state واحدِ
    `viewport{scrollX,scrollY,zoom}` که از **`onScrollChange`** (مقادیرِ معتبر، نه
    getAppState) پر می‌شود و همه‌ی مسیرها (چرخ، دکمه‌ی zoom، برازش، follow) به‌روزش
    می‌کنند؛ `projectPeer` از همین state پروجکت می‌کند (فقط offsetLeft/Top — که با
    pan/zoom عوض نمی‌شوند — از getAppState). وابستگیِ `[viewport]` تضمین می‌کند لایه‌ی
    حضور با **هر** جابه‌جاییِ نما re-render شود.
  - **تاییدِ بعد از فیکس (مرورگر):** مکان‌نما روی pan `(۵۰۰،۳۰۰)→(۴۴۰،۱۸۰)`، **هاله**
    روی pan `(۶۵،۶۲)→(۲۵،−۱۸)`، و مکان‌نما روی zoom `(۵۰۶،۲۵۷٫۷۵)` — هر سه دقیقاً
    برابرِ پروجکشنِ دست‌محاسبه. gate سبز (typecheck/lint/۴۰۸ تست).
  - **درسِ M2:** binder باید لایه‌ی حضور را از نمای معتبرِ callbackِ موتور پروجکت کند و
    با **هر** تغییرِ viewport (شاملِ panِ خالص) دوباره رندر بدهد — ذیلِ G-1 ثبت شد.
- [x] **Q2 — تاییدِ حضور با کانالِ دومِ `BroadcastChannel` بود، نه دو نمونه‌ی واقعی.**
      **تصمیم: ذیلِ G-1 می‌مانَد، نه ورودیِ جدا.** رفتارِ دو موتورِ واقعی (viewportِ
      مستقل، re-project، تعاملِ همزمان) همان گپِ «دو-نمونه‌ای با binder واقعی» است؛
      جدا کردنش دو تکه‌اش می‌کند. حالا G-1 در [TODO گام ۶٫۱](TODO.md) و
      [`sync/README.md`](packages/canvas-core/src/sync/README.md) زیرموردهای صریحِ
      **رندرِ حضور** (مکان‌نما/هاله/follow + re-projectِ viewport — Q1) را دارد تا گم نشود.

**گپ‌های ثبت‌شده برای M2/6٫۱** (در `sync/README.md` و TODO گام ۶٫۱):
G-1 تست دو-نمونه‌ای با binder واقعی **+ رندرِ حضور (مکان‌نما/هاله/follow و re-projectِ
viewport هنگام pan/zoom — Q1/Q2)** · G-2 تست رگرسیون هش پیکسلی جهت متن.

---

## تصمیم‌های کلیدی که باید یادت بماند

- **`type` رندر ≠ `kind` محصولی** (ADR-010). استیکی و شکل هر دو `rectangle` اند.
  هیچ‌جا بیرون از `elements/mapping.ts` روی `element.type` شرط نگذار — قاعده‌ی
  ESLint می‌گیرد. همیشه `getKind(element)`.
- **`direction` تک‌منبعی است** — فقط در `customData.hb`، نه سطح بالای عنصر موتور.
- **هر جهش عنصر باید `bumpVersion()` بزند** (هم `version` هم `versionNonce`)،
  وگرنه موتور تغییر را برای undo ثبت نمی‌کند (ADR-026). باگ خاموش.
- **یک ژست = یک `updateScene({ captureUpdate: "IMMEDIATELY" })`** (ADR-026).
  `applyRemoteChanges` در M2 باید `"NEVER"` بدهد (مکمل نگهبان echo).
- **ترتیبِ capture در جریان چندمرحله‌ای مهم است** (گام ۳٫۶): `NEVER`/`EVENTUALLY`
  خطِ پایه‌ی تاریخچه را جلو می‌برند. برای اینکه یک ساختِ چندمرحله‌ای (pending→saved)
  با یک undo کامل برگردد، **creation باید در اولین `updateScene` با `IMMEDIATELY`**
  باشد و به‌روزرسانی‌های بعدیِ همان ژست `NEVER`.
- **مسیر کانکتور حالت مشتق‌شده است** (ADR-008) — قطعی، فقط `+−×÷` و `Math.round`،
  بدون `Math.hypot`/`atan2`/`toFixed`.
- **پنج تله‌ی موتوری** در جدول `packages/canvas-core/CLAUDE.md` — همه فقط در
  مرورگر قابل کشف، نه jsdom.
- **`normalizePersian` در `shared-types` است**، نه canvas-core — قرارداد
  بین‌ماژولی. `normalizePersianPreservingLength` برای زمان تایپ (حفظ طول مکان‌نما).

---

## ✅ نگهبان خودکار خانواده‌ی `captureUpdate` — گزینه‌های ۱ و ۲ ساخته شد

سه باگِ جدا از این خانواده دیده شده بود؛ حالا نگهبان دارد:

1. **✅ قاعده‌ی ESLint `require-capture-update`** ([boundaries.js](packages/eslint-config/boundaries.js)):
   هر `api.updateScene({…})` باید `captureUpdate` **صریح** داشته باشد. یک قاعده‌ی
   واقعی با `create()` (چون `:has` در esquery نیست)، با **خودآزمونِ RuleTester**
   (۱۰ مورد). **بلافاصله یک باگِ نهفته گرفت:** `sticky-tool` هنگام ساختِ استیکی
   `captureUpdate` نداشت — یعنی undoِ ساختِ استیکی هم همان کلاسِ باگ را داشت. در
   مرورگر بعد از فیکس تایید شد: دو استیکی، هر undo فقط یکی را برمی‌گرداند.
2. **✅ چوک‌پوینتِ نوشتن** ([engine/scene-commit.ts](packages/canvas-core/src/engine/scene-commit.ts)):
   `commitGesture` (IMMEDIATELY) و `commitSystemUpdate` (NEVER). همه‌ی ابزارها
   (sticky/image/draw) و دمو روی این‌ها رفتند؛ ترتیبِ درستِ pending→saved یک‌بار
   کدنویسی شد. خط قرمزِ ۷ در `canvas-core/CLAUDE.md`.
3. **⏳ گام ۶٫۱:** harness مرورگریِ `expectSingleUndoReverts(doGesture)` — تنها راهِ
   گرفتنِ property runtimeِ ترتیب (#۳). طبق برنامه به ۶٫۱ موکول شد.

`versionNonce` (#۱) در لایه‌ی mutator است، `captureUpdate` (#۲/#۳) در لایه‌ی نوشتن.

---

## یادداشت‌های i18n (بعد از ممیزیِ مالک)

- **پول:** self-testِ دست‌محاسبه اضافه شد — «ریال ذخیره، تومان فقط ÷۱۰ نمایشی» در
  لایه‌ی i18n بسته شد. **پیشنهادِ باز (شroع M3):** تایپِ برندشده‌ی `Rial` در
  `shared-types` تا کلِ کد نتواند تومان/float را جای ریال بگذارد — نیاز به تاییدِ
  مالک (ADR-021). اعمالِ ذخیره‌سازی کارِ M3/بک‌اند است.
- **کبیسه‌ی جلالی:** ۸ تستِ مرزی با مقادیرِ مرجع اضافه شد (۱۳۹۸–۱۴۰۵، شاملِ کبیسه‌ی
  ۱۳۹۹/۱۴۰۳ و عادیِ ۱۴۰۴). ICU در بازه‌ی محصول با تقویمِ رسمی می‌خوانَد. **ریسکِ
  ثبت‌شده:** واگرایی‌های شناخته‌شده‌ی ICU-با-رسمی برای سال‌های دور (خارج از عمرِ
  محصول)؛ اگر روزی تاریخِ دور لازم شد، باید دوباره سنجیده شود.

---

## تصمیمِ ثبت‌شده — موقعیتِ نوار ابزار (گام ۴٫۲)

**نوار ابزار: پایین، وسط، شناور** (سبکِ فیگما/کانوا) — تاییدِ مالک، ۱۴۰۵/۰۵/۰۲.
دلیل: جهت‌خنثی است، پس دعوای «چپ در برابر راستِ RTL» را کنار می‌زند، فضای عمودیِ
بیشتری می‌دهد، و مدرن‌تر است. مرکزِ افقی با `inset-inline: 0 + margin-inline: auto +
inline-size: fit-content` (نه `left` — گیتِ Stylelint)، عمودیْ `inset-block-end`.

## فاز ۵ در جریان — گام ۵٫۱

### ✅ z-order (لایه‌بندی) — تمام و تاییدِ مرورگر (۱۴۰۵/۰۵/۰۷)

**پروبِ توانمندیِ موتور (اول، طبقِ انضباط):** بیشترِ ۵٫۱ بومیِ موتور است —
`index` یک **ایندکسِ کسریِ بومی** (`a0..`) است و با پاسِ **آرایه‌ی مرتب‌شده** به
`updateScene`، موتور خودش ایندکس را بازتولید می‌کند. پس الگوریتمِ ایندکسِ کسری
اینجا **تکرار نشد** ([ADR-007](ARCHITECTURE_DECISIONS.md#adr-007) + درسِ ADR-024).

- **`reorderElements`** در [`elements/operations.ts`](packages/canvas-core/src/elements/operations.ts) —
  خالص، فقط ترتیبِ آرایه (`front`/`back`/`forward`/`backward`)، `bumpVersion` روی
  جابه‌جاشده‌ها. **۱۱ تستِ واحد** با ترتیبِ دست‌محاسبه.
- **منبعِ واحد** (ADR-024): منوی راست‌کلیک (بردن به جلو/عقب = front/back) و پنل استایل
  (جلو/عقب = یک‌پله) هر دو از همین تابع، از راهِ `applyReorder`ِ دمو. دو دکمه‌ی
  coming-soonِ پنل و دو آیتمِ منو حالا فعال‌اند.
- **تاییدِ مرورگر:** پنلِ «عقب» عنصر را یک لایه پایین برد؛ منوی «بردن به عقب» به ته؛
  و **یک** Ctrl+Z هر کدام را کامل برگرداند (commitGesture IMMEDIATELY + bumpVersion،
  [ADR-026](ARCHITECTURE_DECISIONS.md#adr-026)). نکته: undoِ موتور روی keydownِ
  **canvas** است نه `document` (تله‌ی جدید — در آزمونِ مرورگری به‌کار می‌آید).

### مانده‌ی ۵٫۱ (بعدی)

طبق پروب، اغلب «روشن‌کردن/تایید»اند نه ساختِ نو:
- **انتخابِ کادر + `Shift+Click`** و **گروه‌بندی** (`groupIds` + `Ctrl+G`) — بومی؛ تایید
  در مرورگر + نمایشِ گروه در منوی RTL.
- **snap + راهنمای هم‌ترازی + شبکه** — بومی ولی خاموش؛ روشن‌کردنِ `objectsSnapModeEnabled`
  (و toggleِ `gridModeEnabled`).
- **ابزارِ align/distribute** — API امپراتیو ندارد → تابعِ خالصِ خودمان روی x/y.
  ⚠️ «چپ/راست» عملیاتِ **مختصاتِ بوم** است (آینه نمی‌شود، P6)؛ فقط برچسبِ UI فارسی.

**mini-map:** موکول به بعد از فاز ۴ (در TODO گام ۴٫۳ با `[!]` ثبت شد).

---

<!-- بایگانیِ یافته‌های ۴٫۳ -->
<details>
<summary>یافته‌های گام ۴٫۳ (بایگانی)</summary>

انجام‌شده: **نوار وضعیت** (`7b64d5c`) و **منوی راست‌کلیک** (`82846a8`).

**یافته‌ی موتور (منوی راست‌کلیک):** منوی خودِ Excalidraw یک `onContextMenu`ِ Reactـیِ
delegate‌شده در ریشه است و در `.popover` رندر می‌شود (نه `.excalidraw-contextMenuContainer`).
با گرفتنِ `contextmenu` در فاز capture روی `document` + `preventDefault` +
`stopImmediatePropagation` کامل preempt می‌شود — **هم‌کلاسِ drop/paste، نه محدودیتِ
ورود به ویرایش متن**. در مرورگر هر دو جهت تایید شد. رویدادهای مصنوعی را موتور
نمی‌گیرد (تلهٔ آشنا)، ولی preemptِ ما به isTrusted کاری ندارد.

**تصمیم — «لایه» متعلق به ۵٫۱ است، نه ۴٫۳.** منطقِ لایه‌بندی (fractional index،
ADR-007) صریحاً کارِ ۵٫۱ است. پس در ۴٫۳ «لایه» در **هر دو سطح** (منوی راست‌کلیک و
پنل استایل) coming-soon می‌ماند و در ۵٫۱ با **یک تابعِ مشترک** به هر دو وصل می‌شود.

**تک‌منبعیِ قفل/حذف:** منطق از inline به `elements/operations.ts` منتقل شد
(`toggleLock`, `deleteElements`, `areAllLocked` — خالص، تست‌شده). منوی راست‌کلیک
حالا همین را صدا می‌زند و پنل استایل هم همین را خواهد زد — نه دو پیاده‌سازی (درسِ ADR-024).

**mini-map** موکول شد (تصمیم مالک) — در TODO گام ۴٫۳ با `[!]` ثبت است.

</details>
