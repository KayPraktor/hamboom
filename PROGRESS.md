# PROGRESS — realtime-sync

تاریخ آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۲۲ (2026-08-13)
**★ فازهای ۰ و ۱ کامل ✅** — چهار probe، سه ADR جدید (۰۳۲/۰۳۳/۰۳۴).
**★ فاز ۲ کامل ✅** (گام‌های ۲٫۱ تا ۲٫۴) — `packages/ydoc-schema` تمام است.
**★ فاز ۳ کامل ✅** (۳٫۱ تا ۳٫۷) — دو بومِ **واقعی** در مرورگر همگام می‌شوند،
**همدیگر را می‌بینند** (مکان‌نما، هاله، آواتار، follow) و **تصویر رد و بدل
می‌کنند**؛ `Ctrl+Z` فقط کارِ خودِ کاربر را برمی‌گرداند **و به همتا هم می‌رسد**.
**۲۵/۲۵ E2E** (سه اجرای متوالی) + ۱۶/۱۶ E2Eِ M1.
★★ **گپِ ارث‌رسیده‌ی G-1 نیمه‌ی اولش بسته شد** — و همان‌جا یک باگِ RTL در CSSِ
حضورِ M1 پیدا و **رفع** شد (**B-1**، تاییدِ مالک).
**★ فاز ۴ کامل ✅** (۴٫۱ تا ۴٫۸) — سرورِ `ws` بالا می‌آید، توکن را از پشتِ پورت
می‌سنجد، و اتصالِ نامعتبر را **قبل از رسیدن به اتاق** با کدِ درست رد می‌کند. اتاق
بارگذاری، migrate و تخلیه می‌شود، و **مرزِ اعتماد** سرِ جایش است.
★★ **و حالا کار از دست نمی‌رود:** update **قبل از** ackِ کلاینت در Postgres می‌نشیند
(۳۰–۳۵ms اندازه‌گیری‌شده، پس batch لازم نشد — [ADR-009 به‌روز شد](ARCHITECTURE_DECISIONS.md#adr-009))،
و تستِ `SIGKILL` با دیتابیسِ **واقعی** ثابتش می‌کند: `pnpm rt:durability`.
★★ **و لاگ بی‌کران رشد نمی‌کند:** هر ۵۰۰ update بورد فشرده و updateهای قدیمی حذف
می‌شوند؛ `pnpm rt:compaction` با Postgres و فایل‌سیستمِ **واقعی** ثابت می‌کند که
کلاینتِ تازه **دقیقاً همان سند** را می‌گیرد. همان سنجه یک باگِ همزمانی گرفت که
۴۳ از ۵۰۰ append را می‌بلعید ([ADR-037](ARCHITECTURE_DECISIONS.md#adr-037)).
★★ **و نقش روی هر update سنجیده می‌شود:** `viewer` می‌خوانَد ولی **نمی‌نویسد** —
`pnpm rt:permission` با سوکت و دیتابیسِ واقعی ثابت می‌کند updateِ مهاجم نه در سند
می‌نشیند، نه در دیتابیس، نه نزدِ همتا. و حفره‌ی «توکن نقش را حمل می‌کند» بسته شد.
★★ **و همدیگر را می‌بینند:** حضور از سرور پخش می‌شود، کلاینتِ تازه حاضرانِ قبلی را
می‌گیرد، و قطعِ **ناگهانی** مکان‌نما را فوراً پاک می‌کند — هزار پیامِ ephemeral و
**صفر ردیف** در دیتابیس (`pnpm rt:presence`). همان‌جا هم یک باگِ کهنه‌ی بی‌صدا
پیدا و رفع شد: سرور `meta` می‌نوشت و نسخه‌ی schemaِ بورد گم می‌شد (**F-1**).
★★ **و حالا چندنودی است** ([ADR-006](ARCHITECTURE_DECISIONS.md#adr-006) فاز ۲):
**دو پروسه‌ی واقعی** روی یک Redis همدیگر را می‌بینند، فقط **صاحب** می‌نویسد، و
`board_updates` هیچ ردیفِ تکراری و هیچ شکافی ندارد (`pnpm rt:cluster`).
★★ **و مودبانه می‌رود:** heartbeat اتصالِ نیم‌باز را می‌گیرد، `/readyz` در خاموشی
قرمز می‌شود، کلاینت‌ها کدِ **۱۰۰۱** می‌گیرند، نوشتنِ در جریان تخلیه و snapshot
گرفته می‌شود (`pnpm rt:shutdown`).

**★ گام ۵٫۱ ✅ — و کلاینت هم برمی‌گردد.** ترابریِ WebSocketِ **محصولی** پشتِ همان
`SyncTransport` نشست (هیچ خطی از binder عوض نشد — قولِ ADR-030)، و **کدِ بستن
سیاست را تعیین می‌کند، نه یک backoffِ یکسان**
([ADR-039](ARCHITECTURE_DECISIONS.md#adr-039)): ۱۰۰۱ **فوری**، ۱۰۰۸ بسته به کدِ
`HB_ERROR`، بقیه backoff **+ jitter**. توکن برای **هر تلاش** تازه ساخته می‌شود، و
`HB_AUTH_REFRESH` — که تا این گام سرور **بی‌صدا نادیده‌اش می‌گرفت** — حالا هر دو
نیمه‌اش هست. `pnpm rt:reconnect` فاصله‌ها را **اندازه می‌گیرد**، ادعا نمی‌کند.

**★ گام ۵٫۲ ✅ — و کار از بستنِ تب هم جان به در می‌بَرد.** سند در IndexedDB
می‌نشیند (پشتِ پورتِ `LocalDocStore`)، بازیابی **قبل از** دست‌دادن انجام می‌شود، و
`SaveState` تا وقتی سیم قطع است **هیچ‌وقت** `saved` نمی‌گوید. آشتی با سرور از راهِ
دست‌دادنِ sync است نه بازفرستادنِ updateهای ذخیره‌شده — و همین «تکراری نبودن» را
تضمین می‌کند. معیارِ پذیرش در Chromiumِ واقعی با **تبِ بسته و بازشده** آزموده شد.

★★★ **هفت سنجه‌ی زنده، و هر هفت یک باگ گرفتند که همه‌ی تست‌های واحد سبز از رویش رد
شده بودند** (جدولش در «جمع‌بندیِ فاز ۴»). این دیگر تصادف نیست:
`pnpm rt:durability` · `rt:compaction` · `rt:permission` · `rt:presence` ·
`rt:cluster` · `rt:shutdown` · `rt:reconnect`.

**★ گام ۵٫۳ ✅ — و فاز ۵ کامل شد.** نقش وسطِ کار عوض می‌شود و بوم **بدونِ رفرش**
فقط-خواندنی می‌شود؛ کلاینتِ عقب‌تر پیامِ فارسی می‌گیرد و **نوشتنش قطع می‌شود**
([ADR-040](ARCHITECTURE_DECISIONS.md#adr-040)). ⚠️ و سه پیامِ **مرده** پیدا شد:
`HB_AUTH_REFRESH`، `HB_PERMISSION` و `HB_ERROR` هر سه روی سیم می‌آمدند و هیچ‌کس
مصرفشان نمی‌کرد.

**★★ گام ۶٫۱ ✅ — و گپِ G-1 بعد از دو ماژول بسته شد.** کلِ سناریو روی سرورِ
**واقعی** و روی **دو نودِ جدا** سبز است، و بعد از قطعیِ ۱۰ثانیه‌ای هر دو کلاینت به
**بردارِ وضعیتِ یکسان** می‌رسند. ★ و همین معیار یک باگِ واقعیِ خوشه گرفت که همه‌ی
تست‌های واحد و هر هفت سنجه از رویش رد شده بودند (**F-2** →
[ADR-041](ARCHITECTURE_DECISIONS.md#adr-041)).

**بعدی: گام ۶٫۲ — تست‌های همزمانی و تعارض.**

M1 (`canvas-core`) تحویل و push شد. این ماژول M2 است. تاریخچه‌ی M1 در
[PROGRESS-M1-canvas-core.md](PROGRESS-M1-canvas-core.md) و [TODO-M1-canvas-core.md](TODO-M1-canvas-core.md)
بایگانی است — **قبل از هر تصمیمی درباره‌ی رفتارِ موتور، جدولِ تله‌های
[`canvas-core/CLAUDE.md`](packages/canvas-core/CLAUDE.md) را بخوان.**

---

## انجام شد

### برنامه‌ریزیِ M2 (۱۴۰۵/۰۵/۱۲)

- **[TODO.md](TODO.md) نوشته شد** — ۶ فاز، ۳۰ گام، هر گام با معیارِ پذیرشِ قابل‌سنجش.
  ترتیب: اسکلت → **سه probeِ ریسک** → `ydoc-schema` → binder → سرور → تاب‌آوری → تحویل.
  دلیلِ ترتیب در خودِ فایل: **binder قبل از سرور** آزمودنی است (دو `Y.Doc` مستقیم، بدون شبکه)،
  پس هیچ باگی پشتِ لایه‌ی شبکه پنهان نمی‌شود.
- **کشفِ مهم حین برنامه‌ریزی:** PLAN فرض کرده بود M2 بعد از M3 و M5 می‌آید، ولی **هیچ‌کدام
  ساخته نشده‌اند** — نه `apps/`، نه `infra/`، نه `packages/{config,auth-core,storage}`.
  یعنی `rt-token`، `effectiveBoardRole` (ADR-012) و Object Storage برای snapshot (ADR-009)
  وجود ندارند. این مرزِ M2 را عوض کرد و به تصمیم‌های D-1..D-5 انجامید.
- **دو ناسازگاری در خودِ اسناد پیدا شد** (نه در کد) و هر دو به گام تبدیل شدند:
  - PLAN بخش ۸ binder را در `ydoc-schema` می‌گذارد، ولی PLAN بخش ۲ می‌گوید
    `ydoc-schema → canvas-core ❌`. → [ADR-029](ARCHITECTURE_DECISIONS.md#adr-029)
  - PLAN بخش ۷٫۳ متن را `Y.Text` می‌خواهد، `shared-types` نوشته `text: z.string()`.
    → با نظر حل نمی‌شود؛ **گام ۱٫۳** تجربی حلش می‌کند.

### گام ۰٫۱ — ثبتِ تصمیم‌های مرزی ✅

سه ADR نوشته شد. هیچ کدی در این گام نوشته نشد (عمدی — دروازه‌ی فاز ۰).

| ADR | تصمیم | نکته‌ی کلیدی |
|---|---|---|
| [ADR-029](ARCHITECTURE_DECISIONS.md#adr-029) | `ydoc-schema` خالص + `canvas-sync` برای binder | `import type` رد شد چون هزینه‌اش در **typecheck و ضعیف‌شدنِ گیت** است، نه runtime |
| [ADR-030](ARCHITECTURE_DECISIONS.md#adr-030) | `ws` پشتِ یک seam | دلیل کارایی **نیست**: uWS خارج از npm نصب می‌شود (از گیتِ P1 رد می‌شود) و روی ویندوز P3 را می‌شکند |
| [ADR-031](ARCHITECTURE_DECISIONS.md#adr-031) | پورت‌های `BoardAuthority` و `SnapshotStore` | P4 با **ساخت** حفظ می‌شود نه با قول — M2 اصلاً SDK را نمی‌بیند |

**یک خطر که حین نوشتنِ ADR-031 بیرون آمد و در TODO نبود:** `DevBoardAuthority` یک پیاده‌سازیِ
توسعه است که هرکس `RT_DEV_JWT_SECRET` را بداند می‌تواند برای خودش نقشِ owner صادر کند. اگر
روزی در production زنده بماند، کلِ ADR-012 از پشت دور زده می‌شود. → **گیتِ runtime** به
گام ۴٫۱ اضافه شد: با `APP_ENV=production` سرور باید **بالا نیاید**، نه اینکه هشدار بدهد.

**در حاشیه رفع شد:** ADR-027 و ADR-028 در فهرستِ `ARCHITECTURE_DECISIONS.md` نبودند و
ADR-028 لنگرِ `<a id>` نداشت — یعنی همه‌ی لینک‌های `#adr-028` از PROGRESS/TODO/CLAUDE
شکسته بودند. هر دو درست شد.

### گام ۰٫۲ — اسکلت سه پکیج + گیتِ مرزها ✅

سه واحدِ ADR-029 ساخته شدند: `packages/ydoc-schema` (خالص)، `packages/canvas-sync`
(binder)، `apps/realtime` (سرور) — هر کدام با `CLAUDE.md`، Vitest، و تستِ دودی که
یک ادعای **معماری** می‌آزماید نه یک منطقِ محصولی (مثلاً: سرور می‌تواند `ydoc-schema`
را در Nodeِ خالص مصرف کند؛ `canvas-sync` نگهبانِ echo را از M1 **قرض می‌گیرد**).

**گیت‌های مرزی در منبعِ واحد** (`eslint-config/boundaries.js`) با **خودآزمونِ سه‌لایه**
(۵۰ تست در [`eslint-config/test/boundaries.test.js`](packages/eslint-config/test/boundaries.test.js)):

| لایه | چه چیزی را می‌گیرد | چرا لازم است |
|---|---|---|
| ۱ — الگوها | فهرستِ `forbid` روی خروجیِ **واقعیِ** factory | اگر فهرست عوض شود |
| ۲ — سیم‌کشی | lintِ واقعی روی `eslint.config.js`ِ خودِ پکیج | اگر factory صدا زده **نشود** یا `files` غلط باشد، لایه‌ی ۱ سبز می‌ماند و گیت عملاً وجود ندارد |
| ۳ — manifest | وابستگی‌های اعلام‌شده در `package.json` | گیتِ import فقط `src/` را می‌بیند؛ یک وابستگیِ ممنوعِ اعلام‌شده از چشمش می‌افتد |

**★ لایه‌ی ۲ همان اول یک باگ گرفت:** `apps/realtime/eslint.config.js` مستقیم `globals`
را import می‌کرد، ولی `globals` وابستگیِ `@hamboom/eslint-config` است نه اپ — زیر pnpm
resolve نمی‌شد و **`pnpm lint` آن اپ می‌شکست**. رفع: `nodeGlobals` از `eslint-config/base`
صادر می‌شود. **لایه‌ی ۱ این را نمی‌گرفت** — مصداقِ اینکه چرا هر دو لازم است.

**دو تصحیح نسبت به متنِ اولیه‌ی گام (هر دو در TODO ثبت شد):**

1. **`canvas-sync` فقط `import type` نیست.** گام ۳٫۱ باید `assertEmittable` را
   **به‌صورت مقدار** از M1 صدا بزند، وگرنه مجبور است نگهبانِ echo را از نو بنویسد —
   خلافِ ADR-024. یک تستِ `allowed` این را قفل کرد.
2. **`@hamboom/storage` و `@hamboom/auth-core` برای `apps/realtime` بسته نشدند.**
   ممنوعیت روی `@aws-sdk/*`ِ **خام** است. خودِ P4 مسیرِ `packages/storage` را تجویز
   می‌کند و ADR-012 صریحاً می‌خواهد realtime و API از **یک** `effectiveBoardRole`
   مشترک استفاده کنند؛ بستنشان یعنی بستنِ همان مسیری که ADR تجویز کرده.

**probeِ ثبت‌شده (خلافِ شهود):** در `no-restricted-imports` علامتِ `*` **از `/` عبور
می‌کند** — `@aws-sdk/*` زیرمسیرِ عمیق را هم می‌گیرد و ورودیِ بدونِ گلاب (`@hamboom/sdk`)
هم `@hamboom/sdk/client` را. پس الگوها ساده می‌مانند؛ افزودنِ `/**` زائد است. در
`boundaries.js` مستند و با تست pin شد تا کسی «اصلاحش» نکند.

**وابستگی‌ها:** `yjs` ۱۳٫۶٫۳۲ · `lib0` ۰٫۲٫۱۱۷ · `y-protocols` ۱٫۰٫۷ — هر سه MIT،
`license:check` سبز (۶۸۵ پکیج)، ثبت در `docs/dependencies.md`. **`uWebSockets.js`
افزوده نشد** (ADR-030)؛ `ws` در گام ۴٫۱ می‌آید.

### گام ۰٫۳ — برشِ حداقلیِ زیرساخت ✅

ساخته شد: compose (فقط postgres+redis، با `docker compose config` اعتبارسنجی‌شده)،
`.env.example`، `packages/config` (zod، ترکیب‌پذیر، ۱۱ تست)، migrationِ SQL خام،
اجراکننده‌ی migration با **checksum**، و `scripts/db-smoke.ts`.

**دو تصمیمِ ریز که ارزش ثبت دارند:**

1. **`boards` ساخته نشد و FKها گذاشته نشد** (انحراف از PLAN بخش ۶، با دلیل): اگر M2
   خودش `CREATE TABLE boards` بزند، `0001_init.sql`ِ خودِ M3 بعداً روی جدولِ موجود
   می‌افتد و می‌شکند. M3 دو FK را با `ALTER TABLE` اضافه می‌کند → گام ۶٫۴.
2. **`db:smoke` اسکریپت است نه تستِ vitest**: اگر تست بود، یا `pnpm test` روی ماشینِ
   بدونِ داکر قرمز می‌شد یا خودش را skip می‌کرد — و skip یک **سبزِ دروغین** است.
3. **`.env.example` عمداً ناقص است**: فقط بخش‌هایی که الان مصرف‌کننده دارند. یک
   `.env.example` پر از متغیرِ SMTP و زرین‌پال برای اپ‌هایی که وجود ندارند، اولین
   چیزی است که از واقعیت واگرا می‌شود.

**★ تله‌ی جدید (اولین مصرف‌کننده‌ی Nodeِ خالصِ پروژه):** `canvas-core/CLAUDE.md`
پیش‌بینی کرده بود «اگر روزی یک مصرف‌کننده‌ی Node خالص اضافه شد…» — همان امروز شد.
Node برخلاف Vite پسوندِ importهای نسبی را حدس نمی‌زند و `ERR_MODULE_NOT_FOUND`
می‌دهد. رفع: `allowImportingTsExtensions` در چهار tsconfigِ M2 + ریشه، و پسوندِ `.ts`
صریح. در `packages/config/CLAUDE.md` مستند شد و از دو CLAUDE.mdِ دیگر به آن ارجاع داده شد.

### ✅ تاییدِ زنده‌ی ۰٫۳ — با دیتابیسِ واقعی روی ویندوز (۱۴۰۵/۰۵/۱۲)

- `pnpm db:up` → هر دو کانتینر **healthy**؛ redis → `PONG`.
- `pnpm db:migrate` → اعمال شد. `\d board_updates` روی دیتابیسِ واقعی دقیقاً ستون‌ها و
  `board_updates_seq_uq` را طبق PLAN بخش ۶ نشان می‌دهد.
- `pnpm db:smoke` → **رفت‌وبرگشتِ `bytea` بیت‌به‌بیت سالم** (با بایت‌های ۰x00/0xFF/0x80 —
  اگر جایی به رشته تبدیل شوند، سندِ Yjs خاموش خراب می‌شود)، ایندکسِ یکتا `seq` تکراری را
  رد کرد، `board_snapshots` پذیرفت.
- **idempotency** و ★ **گیتِ checksum** هر دو **در عمل** آزموده شدند: افزودنِ یک کامنت
  به migrationِ اجراشده → رد شد با پیامِ صریح؛ بعد از بازگردانی دوباره سبز.

**★ تعارضِ محیطی که حل شد (برای ماشین‌های بعدی):** یک **PostgreSQL 18 بومی**
(سرویسِ ویندوزِ `postgresql-x64-18`) پورت ۵۴۳۲ را گرفته بود — `pnpm db:migrate` واقعاً
تا آن رسید و `auth_failed` گرفت، که خودش ثابت کرد مسیرِ کد سرتاسر درست است و فقط
دیتابیس اشتباه بود. **راه‌حلِ انتخابیِ مالک:** داکر روی ۵۴۳۳، بدونِ دست‌زدن به سرویسِ
سیستم. فقط `.env`ِ محلی عوض شد؛ `.env.example` و compose روی پیش‌فرضِ PLAN ماندند تا
روی ماشینِ تمیز همان PLAN کار کند (یادداشتش در `.env.example` هست).

### گام ۱٫۱ — ★ probeِ StrictMode ✅ — **دروازه باز شد**

**نتیجه: فرضِ [ADR-028](ARCHITECTURE_DECISIONS.md#adr-028) بازتولید شد و راه‌حلِ نامزدش
تایید شد** → [ADR-032](ARCHITECTURE_DECISIONS.md#adr-032). **دستِ `canvas-core` نخورد.**

| الگو | موتور | state ری‌اکت |
|---|---|---|
| `onReady` (الگوی M1) | ۱ عنصر، ۱ انتخاب ✓ | `onChange` = **۰** ✗ |
| `useEffect([api])` | ۱ عنصر، ۱ انتخاب ✓ | همه دنبال می‌کنند ✓ (شاملِ **تغییرِ** انتخاب) |

- **جای probe:** `packages/canvas-sync/dev/` — طبق ADR-029 در دامنه‌ی M2. همان‌جا در
  گام ۳٫۷ دموی دو-نمونه‌ایِ G-1 هم می‌آید (زیرساختِ Vite/Playwright حالا آماده است).
- ★ **درسِ روشِ آزمایش که باید یادم بماند:** خواندنِ stateِ ری‌اکت به‌تنهایی **مبهم**
  است — صفر ماندنش هم «اشتراک مرده» معنی می‌دهد و هم «تعامل چیزی نکشید». یک
  **شاهدِ کنترلِ مستقل از ری‌اکت** (`window.__hbProbeApi`) اضافه شد که حالتِ موتور را
  مستقیم می‌خواند. **اجرای اول بدونِ آن گمراه شد:** `KeyR` بدونِ focusِ کانتینر به
  ابزار نمی‌رسید، پس درگ یک کادرِ انتخاب می‌کشید نه شکل — و نزدیک بود یک **باگِ تست**
  به‌عنوان یافته‌ی معماری گزارش شود. (همان تله‌ی ثبت‌شده‌ی M1 درباره‌ی صفحه‌کلید.)
- **دو نگهبانِ دائمی** (`e2e/strictmode.spec.ts`، سه اجرای پایدار): الگوی درست
  **زنده است**، و الگوی غلط **هنوز مرده است**. دومی عمدی است — اگر نسخه‌ی بعدیِ
  Excalidraw رفعش کند، آن تست قرمز می‌شود و می‌فهمیم فرضِ ADR-032 دیگر لازم نیست.
- **آنچه اثبات نشد:** *چرا* callbackِ `onReady` به نمونه‌ی دورانداخته می‌چسبد. علتِ
  درونیِ موتور کاوش نشد — نشانه و رفع سنجیده شد، نه مکانیزم.
- ⚠️ **تله‌ی محیطی — رفع شد** (پایین، «پورت‌های dev»).

### ✅ پورت‌های dev — بیرون از بازه‌ی dynamic ویندوز (به درخواستِ مالک)

**مسئله:** Hyper-V/WSL محدوده‌هایی از **بازه‌ی dynamic port** را رزرو می‌کند و `bind`
رویشان `EACCES` می‌دهد. این محدوده‌ها **با هر بوت عوض می‌شوند** — پس E2E یک روز کار
می‌کرد و روزِ بعد نه، بدونِ یک خط تغییر در کد. خطرش این بود که یک شکستِ محیطی با
شکستِ کد اشتباه گرفته شود.

**چرا «پورت را عوض کن» کافی نبود:** اندازه‌گیری نشان داد بازه‌ی dynamic روی این ماشین
**۱۰۲۴–۱۵۰۰۰** است (پیش‌فرضِ ویندوز ۴۹۱۵۲–۶۵۵۳۵). یعنی هر پورتِ زیرِ ۱۵۰۰۰ — از جمله
۵۱۸۰ و ۵۲۸۰ — می‌تواند هر بوت قربانی شود. جابه‌جایی به یک پورتِ آزادِ دیگر فقط شیر
یا خط بود.

**رفع (دو لایه):**

۱. **خروج از کلِ بازه:** `canvas-core` → **۱۵۱۸۰**، `canvas-sync` → **۱۵۲۸۰**. این
   اعداد هم بالای بازه‌ی این ماشین (۱۵۰۰۰) اند و هم زیرِ بازه‌ی پیش‌فرضِ ویندوز
   (۴۹۱۵۲) — یعنی روی **هر دو** پیکربندی امن‌اند، نه فقط اینجا.
   `strictPort: true` هم اضافه شد تا Vite بی‌صدا روی پورتِ بعدی نرود (وگرنه
   Playwright به آدرسِ اشتباه وصل می‌شود و خطا به «سرور بالا نیامد» تغییر شکل می‌دهد).
۲. **شبکه‌ی ایمنی:** [`scripts/check-dev-port.mjs`](scripts/check-dev-port.mjs) در هوکِ
   `predev` هر دو پکیج. اگر روزی باز هم رخ داد، به‌جای `EACCES`ِ گنگ یک پیامِ فارسی
   می‌دهد که **صریحاً می‌گوید مشکلِ محیطی است نه باگِ کد** و دستورهای netsh را نشان
   می‌دهد. با یک پورتِ واقعاً رزروشده (۱۱۰۰) آزموده شد: پیام درست و `exit=1`.

**تاییدِ عملی:** E2E کاملِ M1 روی پورتِ جدید اجرا شد — **۱۶/۱۶ سبز** (و چهار اجرای
متوالیِ سبز). E2E ماژول خودمان هم ۲/۲.

⚠️ **این یک تغییرِ عمدیِ بین‌دامنه‌ای در `canvas-core` بود** (پورت + هوکِ predev +
دو خط سند). به درخواستِ صریحِ مالک؛ **هیچ کدِ محصولیِ M1 عوض نشد.**

### ✅ flake در E2E خودِ M1 — کشف و **رفع شد** (تاییدِ مالک)

اولین اجرای کامل بعد از تغییرِ پورت، تستِ `clipboard.spec.ts` («Ctrl+X عناصر را
می‌بُرد و Ctrl+V هر سه را برمی‌گرداند») افتاد: انتظار ۳، دریافت ۲.

**تشخیص — ربطی به تغییرِ پورت ندارد:**
- در **انزوا** پاس می‌شود (۲/۲).
- **چهار اجرای کاملِ متوالیِ بعدی** همگی ۱۶/۱۶ سبز.
- شکست فقط روی **اولین اجرای سرد** رخ داد (جایی که Vite تازه dep-optimize می‌کند).
  تستِ کلیپ‌بورد با `expect.poll` و مهلتِ ۵ ثانیه‌ای زمان‌حساس است.

**علتِ واقعی (عمیق‌تر از «مهلت کم» بود):** عددِ **۲** خودش سرنخ بود — یعنی paste
مسیرِ «متنِ خارجی» را رفته و **یک استیکی** (ظرف + متن) ساخته، همان باگی که M1 قبلاً
رفع کرده بود. ریشه: `onCut` نشانه‌ی ما را روی رویداد می‌نویسد، ولی **موتور هم روی
همان Ctrl+X کلیپ‌بورد را async با فرمتِ خودش بازنویسی می‌کند**، و کلیپ‌بوردِ سیستم بینِ
تست‌ها مشترک است. در آن پنجره‌ی کوتاه، paste نشانه‌ی **تستِ قبلی** را می‌دید: نه با
tokenِ فعلی برابر، نه فرمتِ Excalidraw → «متنِ خارجی».

**این باگِ محصول نیست** — هیچ انسانی Ctrl+X و Ctrl+V را در چند میلی‌ثانیه پشتِ هم
نمی‌زند. ولی تست داشت زمان‌بندی را می‌آزمود، نه رفتار را.

**رفع (فقط فایلِ تست، هیچ کدِ محصولی):** `waitForInternalClipboard` — صبرِ صریح تا
کلیپ‌بوردِ سیستم محتوای قابل‌شناسایی داشته باشد؛ و `waitForTimeout(200)` در
`threeRects` به `expect.poll` روی یک **شرط** تبدیل شد.

**تاییدِ رفع:** ۵ اجرای کاملِ گرم + ★ **۳ اجرای سردِ اجباری** (با `rm -rf
node_modules/.vite` — یعنی بازتولیدِ دقیقِ شرطی که شکست در آن رخ داد) — هر هشت
اجرا ۱۶/۱۶. هر دو تله در جدولِ E2Eِ [`canvas-core/CLAUDE.md`](packages/canvas-core/CLAUDE.md) ثبت شد.

### گام ۱٫۲ — ★★ probeِ کدکِ Y.Doc ✅ — **ADR-007 تایید شد**

**مهم‌ترین خروجی: بازبینیِ مدلِ سند لازم نیست.** ادعای مرکزیِ ADR-007 در آزمونِ
واقعی برقرار بود. شواهد: [`docs/ydoc-baseline.md`](docs/ydoc-baseline.md) +
دو فایلِ تستِ اجراشدنی (۵ تستِ ادغام در `ydoc-schema`، ۲۱ تست در `canvas-sync`).

| سناریوی دو کلاینتِ همزمان | نتیجه |
|---|---|
| per-property: A رنگ، B جابه‌جایی | ✅ **هر دو ماند** و هر دو سند یکسان شدند |
| ★ **ضدِ ادعا**: نوشتنِ آبجکتِ کامل | ❌ یکی خورده شد |
| همان property · آرایه‌ها · حذفِ نرم + ویرایش | LWW ولی **همگرا** |

**چرا ضدِ ادعا را هم آزمودم:** بدونِ نشان‌دادنِ حالتِ بد، «حالتِ خوب» چیزی اثبات
نمی‌کند — شاید هر دو حالت کار می‌کردند و `Y.Map`ِ تودرتو سربارِ بی‌فایده بود.

**اندازه‌ها (بایتِ واقعی، نه تخمین):** ساختِ استیکی ۱۷۱۵ · **تیکِ درگ ۳۹** ·
تغییرِ رنگ ۳۴ · استروکِ قلم ۹۳۸ · و همان تیکِ درگ با نوشتنِ **کلِ عنصر** ۴۳۸.
یعنی صرفه‌جویی **~۱۱ برابر** — مرتبه‌ای، نه چنددرصدی. روی یک درگِ ۲ثانیه‌ای
(۴۰ تیک): **۱٫۵KB در برابر ۱۷KB**. روی شبکه‌ی ایران این تزئینی نیست.

**وفاداری:** هر ۱۰ نمونه از سازنده‌های **واقعیِ** M1 round-trip شد و
`hbElement.parse` را پاس کرد — نه فقط «معتبر»، بلکه دقیقاً برابرِ ورودی.

### ★ قیدِ چهارم — همان‌جا تصمیم گرفته شد، نه موکول به ۲٫۱ (تاییدِ مالک)

مالک خواست `customData` همین‌جا با یک probeِ کوتاه تعیین تکلیف شود، با استدلالِ
درست: `customData.hb` جایی است که `kind` و پالتِ استیکی و جهتِ متن زندگی می‌کنند —
**قلبِ مدلِ داده‌ی خودمان**، نه یک فیلدِ جانبی.

همان آزمونِ per-property، یک لایه پایین‌تر:

| مدل | A پالت را عوض می‌کند، B همزمان برچسب می‌زند |
|---|---|
| آبجکتِ ساده (وضعِ فعلی) | ❌ **یکی خورده شد** |
| `Y.Map` تودرتو | ✅ هر دو ماند، `kind` هم سالم |

→ **[ADR-033](ARCHITECTURE_DECISIONS.md#adr-033): `customData` تودرتو می‌شود.**
قاعده **بازگشتی و بدونِ استثنا** است (هر آبجکتِ ساده → `Y.Map`)؛ بُرش در عمق
دلبخواهی می‌شد و بعداً کسی باید مرزش را یادش می‌ماند.

**دو چیز که قبل از ثبتِ تصمیم سنجیدم، نه بعدش:**
- **قرارداد سالم می‌مانَد؟** بله — `toJSON()` بازگشتی است و هر ۱۰ نمونه‌ی واقعی
  همچنان `hbElement.parse` را پاس می‌کنند. `shared-types` دست‌نخورده.
- **هزینه‌اش چقدر است؟** ساختِ استیکی ۷۵۸ → ۸۴۹ بایت (**+۱۲٪**)، فقط روی ساخت.
  سربار است نه انفجار.

**یک نکته که شدت را کم کرد و فرض نبود:** `lastEditedBy` فقط هنگامِ **ساخت** نوشته
می‌شود، نه در هر جهش — پس *هر* ویرایشِ همزمان به تعارض نمی‌خورد.

**سه قیدِ دیگر برای گام ۲٫۱:** `undefined` در `Y.Map` نمی‌نشیند (باید **رد** شود نه
`null` — وگرنه parse روی فیلدِ optional می‌افتد) · نوشتنِ بدونِ تغییر = صفر update ·
`line` سازنده ندارد ولی codec باید پوششش دهد.

**یافته‌ی جانبی — تاییدِ دوباره‌ی ADR-029:** `@hamboom/canvas-core` در Nodeِ خالص
بارگذاری **نمی‌شود** (Excalidraw هنگام لودِ ماژول به `window` دست می‌زند). برای
binder بی‌اهمیت است، ولی اگر `ydoc-schema` یا `apps/realtime` اجازه‌ی دیدنش را
داشتند، **سرور به DOM گره می‌خورد**. مرزِ ESLint دقیقاً همین را می‌بندد.

**زیرساختِ تست:** stubِ canvas از **خودِ canvas-core** قرض گرفته شد (`test/setup.ts`)
نه کپی — آن فایل ۱۷۶ خط با دو تله‌ی مستندشده است و نسخه‌ی دومش واگرا می‌شد (ADR-024).

### ⛔ سبزِ دروغینِ گیت — رخ داد، ولی **علتش ایزوله نشد**

**آنچه قطعاً رخ داد:** `pnpm lint` گزارش داد «ESLint: No issues found» با **exit 0**،
در حالی که دو خطای واقعی وجود داشت؛ `npx turbo run lint --force` همان لحظه گرفتشان.

**دو تصحیح روی حرف‌های خودم — هر دو تشخیصم غلط از آب درآمد:**

1. اول گفتم «turbo از کشِ کهنه سبز می‌دهد» (کامیت `cf3b234`). **آزمودم و رد شد:**
   یک فایلِ untracked با خطای لینت را turbo **بدونِ `--force` هم** می‌گیرد.
2. بعد گفتم «پروکسیِ `rtk` خروجی را ماسک می‌کند» (کامیت `f682d6e`). **آن هم رد شد:**
   با یک خطای واقعیِ لینت (`no-debugger`، نه یک warning)، `pnpm lint` در سه تلاشِ
   پیاپی — و حتی با همان ترتیبِ `prettier --write` بلافاصله قبلش — **هر بار درست
   exit 1 داد**.

   ⚠️ تلاشِ دومم برای تکرار هم **خودش معتبر نبود**: با `any` آزمودم که فقط warning
   است و eslint با warning هم exit 0 می‌دهد. یعنی یک لحظه داشتم از یک آزمونِ غلط
   نتیجه‌ی درست می‌گرفتم.

**نتیجه‌ی صادقانه:** یک سبزِ دروغینِ واقعی دیده شد، ولی **علتش معلوم نیست** و
تکرارپذیر نبود. `pnpm test` هم آزموده شد و شکستِ واقعی را **درست** گزارش کرد، پس
مشکل فراگیر نیست.

**چرا با وجودِ نامعلوم بودنِ علت، `pnpm verify` درست است:** به هیچ فرضیه‌ای تکیه
نمی‌کند و **هر دو علتِ نامزد را حذف می‌کند** — اجرا از راهِ `turbo`ِ مستقیم (نه
اسکریپت‌های `pnpm`)، `--force` (پس بدونِ کش)، و ★ تصمیم فقط با **کدِ خروجِ فرایند**
نه متنِ خروجی. خودش با یک خطای عمدی آزموده شد و گرفتش.

**درباره‌ی `rtk`:** hookـش در `~/.claude/settings.json` سراسری است
(`PreToolUse` روی `Bash` → `rtk hook claude`)، پس روی همه‌ی پروژه‌ها اثر دارد. ولی
**شواهدی که مقصر بودنش را ثابت کند پیدا نشد**، پس توصیه‌ای به غیرفعال‌کردنش نمی‌دهم.

**قاعده در [`CLAUDE.md`](CLAUDE.md) ثبت شد** (قاعده‌ی ۳): مبنای تیک‌زدن و کامیت فقط
`pnpm verify` است. روی وضعیتِ فعلی سبزِ واقعی داد (exit 0)، پس چیزی پنهان نمانده.

### گام ۱٫۳ — probeِ متنِ همزمان ✅ → [ADR-034](ARCHITECTURE_DECISIONS.md#adr-034)

**`Y.Text` انتخاب شد** — در سند `Y.Text`، در قرارداد رشته، در binder دیف.
★ **`shared-types` دست‌نخورده ماند و `textDelta` به قرارداد اضافه نشد** — مسیرِ
ممنوعی که مالک گذاشته بود لازم نشد، چون دیف کافی بود.

| مدل | دو کاربر همزمان در دو جای متن |
|---|---|
| رشته‌ی ساده (LWW) | ❌ کاراکترهای یکی پاک شد |
| `Y.Text` + دیف | ✅ هر دو ماند، همگرا |

**هزینه:** ۲۸ بایت برای دیفِ یک کاراکتر در متنِ ۱۰۰۰ کاراکتری.

**★★ تله‌ی ۱ — پایه‌ی زنده، و یک آزمونِ غلطِ خودم:** با پایه‌ی کهنه بازه‌ی `delete`
به ایندکسِ اشتباه می‌افتد و **متن مخدوش می‌شود، نه فقط ناقص** (انتظار «سلام رفیق»،
نتیجه «سلام رفیقا»). ⚠️ **اولین آزمونم برای این تله معتبر نبود** — با ویرایشِ
**افزایشی** آزمودم که اصلاً بازه‌ی حذفی ندارد، پس کاراکترِ همتا سالم ماند و تست
افتاد. خودِ تست فرضِ من را رد کرد؛ با ویرایشِ **جایگزینی** خرابی ظاهر شد.
(دومین باری در این فاز که یک آزمونِ بی‌اثر نزدیک بود نتیجه‌ی غلط بدهد.)

**★★ تله‌ی ۲ — `text` در برابر `originalText`:** قرارداد خودش می‌گوید منبعِ حقیقت
`originalText` است. آزموده شد که `text`ِ emit‌شده‌ی **هر دو** کلاینت بعد از ادغام
غلط است، پس `text` نباید CRDT شود — باید بازمحاسبه شود.

**دو چیز که عمداً اندازه‌گیری نشد چون هنوز ممکن نیست** (هر دو معیارِ پذیرشِ ۳٫۳):
عرضِ پنجره‌ی کهنگی (binderِ واقعی لازم دارد) و پرشِ caret (بین دو گزینه فرقی
نمی‌گذارد، پس تصمیم را عوض نمی‌کرد). **عدد نساختم.**

### گام ۱٫۴ — probeِ انزوای undo ✅ — **فاز ۱ کامل**

`trackedOrigins` انزوا را تضمین می‌کند، حتی وقتی تغییرِ همتا روی **همان عنصر** باشد
(کاربر جابه‌جا می‌کند، همتا رنگ عوض می‌کند → undo فقط جابه‌جایی را برمی‌گرداند).

**★★ یافته‌ی وارونه — مهم‌ترین خروجیِ این گام:** فرض کرده بودم
`new Y.UndoManager(scope)`ِ بدونِ گزینه «همه‌چیز را ردیابی می‌کند». **برعکس است.**
سنجیده شد: `null` ✔ · `undefined` ✔ · `"local-user"` ✘ · `"remote"` ✘.

چون binder موظف است با originِ **نام‌دار** بنویسد، جاافتادنِ `trackedOrigins` یعنی
**undo بی‌صدا هیچ کاری نمی‌کند** — نه خطا، نه هشدار. پین شد در **دو جا**: گام ۳٫۴
در TODO (مقصد) و `canvas-sync/CLAUDE.md` (جایی که سازنده‌ی binder می‌خواند).

⚠️ **یک فرضِ غلطِ دیگر که تست ردش کرد:** undoِ یک **ساخت** کلیدِ عنصر را کامل
برمی‌دارد؛ حذفِ نرم قاعده‌ی ماست برای حذفِ **کاربر**، نه این مسیر. ادعا اصلاح شد به
**همگرایی** (که برقرار است) به‌جای «کلید می‌مانَد».

**دو سدِ مستقل، حالا با شواهد:** `trackedOrigins` جلوی **اعمالِ** تغییرِ remote را
نمی‌گیرد، فقط نمی‌گذارد در undo stack بنشیند. پس جایگزینِ `captureUpdate: "NEVER"`
نیست — آن یکی تاریخچه‌ی **خودِ موتور** را می‌بندد.

> **جمع‌بندیِ فاز ۱:** هر چهار probe سبز. سه فرضِ من در این فاز غلط از آب درآمد و
> هر بار **خودِ تست** ردش کرد — که دقیقاً کارِ probe است.

### گام ۲٫۱ — ساختار سند + codecِ عنصر ✅ (۱۴۰۵/۰۵/۱۳)

اولین **کدِ محصولیِ** M2. سه فایل: [`doc.ts`](packages/ydoc-schema/src/doc.ts)
(پنج ریشه، `createBoardDoc`، `readDocument`)، [`element-codec.ts`](packages/ydoc-schema/src/element-codec.ts)
(`writeElement`/`readElement`)، و `test-fixtures.ts`. **۶۱ تستِ تازه** —
`ydoc-schema` ۲۸ → ۷۵ و `canvas-sync` ۳۲ → ۴۶.

**هر چهار قیدِ ورودیِ فاز ۱ پیاده شد و هرکدام تستِ خودش را دارد:**

| قید | چطور بسته شد |
|---|---|
| `undefined` رد شود نه `null` | `continue` در حلقه + تستِ «`tags` کلید نمی‌گیرد و `parse` سبز می‌مانَد» |
| بدونِ تغییر = صفر update | دیف در برابرِ مقدارِ زنده؛ ادعا با شمارشِ **رویدادِ `update`**، نه آستانه‌ی بایتی |
| `line` پوشش داده شود | نمونه‌ی دستی در `ydoc-schema` — تنها جایی که ممکن است |
| `customData` بازگشتی `Y.Map` | `writeInto` بازگشتی؛ `originalText` تنها `Y.Text`، `text` رشته‌ی مشتق |

**★ دو تصمیمِ طراحی که از درسِ فاز ۱ آمدند، نه از متنِ گام:**

۱. **آرگومانِ `previous` حذف شد.** probe یک اسنپ‌شاتِ ورودی می‌گرفت و با آن دیف
   می‌کرد. codecِ محصولی در برابرِ مقدارِ **زنده‌ی داخلِ سند** دیف می‌گیرد — همان
   درسِ تله‌ی ۱ در گام ۱٫۳، یک لایه بالاتر. نتیجه: پایه‌ی کهنه از راهِ API **قابلِ
   دادن نیست**، و «بدونِ تغییر = صفر update» از یک قرارداد به یک خاصیتِ ساختاری
   تبدیل شد. `applyTextDiff` هم عمداً پارامترِ `base` ندارد.
   ⚠️ این **نیمی** از ریسک است: اگر رشته‌ی ورودی از اسنپ‌شاتِ کهنه‌ی بوم بیاید، دیف
   باز هم می‌تواند کاراکترِ همتا را پاک کند. عرضِ آن پنجره کارِ گام ۳٫۳ است.
۲. **`readDocument` با `index` مرتب می‌کند.** در متنِ گام نبود: ترتیبِ پیمایشِ
   `Y.Map` بعد از ادغامِ چند کلاینت معنایی ندارد، پس بدونِ مرتب‌سازی z-orderِ بورد
   در هر بار بازکردن عوض می‌شد. مقایسه‌ی رشته‌ای روی fractional index — همان
   قراردادی که `canvas-core/src/elements/frame.ts` هم دارد.

**نگهبانِ واگراییِ `BoardDocument` ↔ `CanvasDocument`:** `ydoc-schema` حق ندارد
`canvas-core` را ببیند، پس شکلِ `CanvasDocument` را **تکرار** کرده. یک تکرارِ
بی‌نگهبان دیر یا زود واگرا می‌شود و آن‌وقت `replaceDocument` فقط در زمانِ اجرا
می‌شکند. نگهبان یک **ادعای تایپی** در `canvas-sync` است (تنها پکیجی که هر دو را
می‌بیند) — واگرایی `pnpm verify` را قرمز می‌کند، نه مرورگر را.

**دو چیزی که عمداً اینجا انجام نشد و جایشان ثبت شد** (نه «یادم می‌ماند»):
اعتبارسنجیِ سند هنگام خواندن → **گام ۴٫۲** (مرزِ اعتماد جایی است که سند از
دیتابیس می‌آید؛ `readElement` مسیرِ داغِ هر تغییرِ remote است)، و `transact`
کردنِ خودِ codec → **گام ۳٫۳** (origin مالِ binder است). هر دو در TODO **و** در
`CLAUDE.md`ِ پکیجِ مربوطه پین شدند.

⚠️ **یک فرضِ غلطِ دیگر — این‌بار typecheck ردش کرد، نه تست:**
`Extract<HbElement, { type: "rectangle" }>` برابرِ **`never`** است، چون عضوِ
shapeِ union سه literal دارد (`rectangle|ellipse|diamond`) و به یک literal
assignable نیست. تست‌ها **سبز بودند** و فقط `pnpm verify` گرفتش — مصداقِ اینکه
چرا گیت شاملِ typecheck است. تایپِ صادرشده‌ی `HbShapeElement` درست است.

### گام ۲٫۲ — دارایی‌ها، appState و پینِ کامنت ✅ (۱۴۰۵/۰۵/۱۳)

هر پنج ریشه‌ی سند حالا codec دارند. **۴۲ تستِ تازه** (`ydoc-schema` ۷۵ → ۱۱۷).

**★ یک بازآراییِ لازم قبل از افزودن:** منطقِ نوشتنِ افتراقی به
[`value-codec.ts`](packages/ydoc-schema/src/value-codec.ts) منتقل شد. عنصر،
`appState` و سنجاقِ کامنت **هر سه** یک قاعده دارند (فقط تفاوت‌ها نوشته شود، هر
آبجکتِ ساده بازگشتی `Y.Map`)، و سه کپی یعنی سه رفتاری که واگرا می‌شوند —
همان چیزی که ADR-024 منع کرده. تنها تفاوتشان یک پرچمِ `prune` است که **عمداً
پیش‌فرض ندارد**: شیءِ کامل `true`، patch `false`. انتخابِ غلطش یا داده‌ی پاک‌شده
را برای همیشه نگه می‌دارد، یا یک patch کلِ بقیه را پاک می‌کند — پس باید دیده شود.

**★★ یافته‌ای که تستِ اولم را انداخت — Yjs فقط `Uint8Array` را باینری می‌داند:**

| شکل | مستقیم روی `Y.Map` | تودرتو در یک آبجکتِ ساده |
|---|---|---|
| `Uint8Array` | پذیرفته | پذیرفته |
| `ArrayBuffer` · `Float64Array` · `DataView` · `Blob` | **خودِ Yjs رد می‌کند** | ⚠️ **پذیرفته، و در sync به `{}` تبدیل می‌شود** |

ستونِ دوم **بدتر از باینری در سند** است: روی کلاینتِ نویسنده کاملاً سالم به نظر
می‌رسد (Yjs همان شیءِ درون‌حافظه‌ای را برمی‌گرداند) و فقط برای بقیه گم می‌شود —
بدونِ خطا، بدونِ هشدار. اسکنرِ سند هم آن‌طرف چیزی نمی‌بیند چون دیگر باینری نیست.
یعنی برای این شکل‌ها نگهبانِ **بعد از نوشتن** اصلاً کار نمی‌کند.

→ نگهبان **دو تکه** شد: `findBinaryIn` **قبل** از نوشتن (تنها فرصتِ گرفتنِ
شکل‌های «بی‌صدا»)، و `findBinaryValues`/`assertNoBinary` روی **سند** برای
`Uint8Array` — که مصرف‌کننده‌ی اصلی‌اش مرزِ بارگذاری از دیتابیس در گام ۴٫۲ است.
اسکنِ سند از `doc.share` می‌رود نه از `DOC_ROOTS`: باینری از هر مسیری وارد
می‌شود — `customData` یک عنصر، یا ریشه‌ای که کلاینتِ دیگری ساخته.

**نگهبان در هر دو جهت خودآزمون است.** جهتِ منفی به‌اندازه‌ی مثبت مهم است:
نگهبانی که روی سندِ سالم هم خطا بدهد فقط سریع‌تر دور زده می‌شود. پس یک سندِ
**واقعیِ پر** (هر ۹ نوعِ عنصر + دارایی + appState + سنجاق) باید پاک بماند.

**دو تصمیمِ ریز با دلیل:**

1. **`writeAsset` اعتبارسنجی می‌کند، `writeElement` نه.** دلیلش هزینه است نه
   سلیقه: عنصر ده‌ها بار در ثانیه نوشته می‌شود، دارایی یک بار هنگامِ آپلود.
   بررسیِ صریحِ باینری **با وجودِ** `parse` هم انجام می‌شود، چون zod کلیدِ ناشناخته
   را **بی‌صدا** دور می‌ریزد — و سکوت چیزی است که این پروژه به آن اعتماد نمی‌کند.
2. **`appState` دو نگهبان دارد، نه یکی.** `writeAppState` جلوی کدِ **خودمان** را
   می‌گیرد و `readAppState` جلوی کلاینتِ قدیمی یا دست‌کاری‌شده‌ای که مستقیم روی
   ریشه می‌نویسد. بدونِ دومی، `scrollX`ِ یک نفر همچنان نمای همه را می‌پراند —
   و چون CRDT است، همگرا هم می‌شد: همه به یک نما می‌رسیدند و هیچ‌کس نمی‌فهمید چرا.

**`CommentPin` عمداً در `ydoc-schema` است، نه `shared-types`** — قیدِ فعالِ M2. اگر
M3 لازمش داشت، بردنش تاییدِ مالک می‌خواهد؛ در فهرستِ گام ۶٫۴ ثبت شد.

### گام ۲٫۳ — نسخه‌بندی schema و اسکلت migration ✅ (۱۴۰۵/۰۵/۱۳)

**۱۷ تستِ تازه** (`ydoc-schema` ۱۱۷ → ۱۳۴). رجیستریِ محصولی **خالی** است — هنوز
نسخه‌ی ۱ ایم — ولی **موتورش آزموده است**، که کلِ نکته‌ی این گام بود.

**★ چطور چیزی را آزمودیم که هنوز وجود ندارد:** `migrateDocument` رجیستریِ
**تزریق‌شدنی** می‌گیرد. بدونش تنها راه این بود که تا اولین migrationِ واقعی صبر
کنیم — یعنی اولین migrationِ محصولی روی مسیرِ نیازموده و روی سندهای **واقعیِ
کاربران** اجرا می‌شد. تست‌ها یک زنجیره‌ی ساختگیِ V1→V2→V3 از همان موتور می‌گذرانند.

**★ هر گام تراکنشِ خودش را دارد، نه یک تراکنش برای کلِ زنجیره.** وسوسه‌اش
برعکس است، ولی اگر گامِ دوم خطا بدهد Yjs تغییراتِ گامِ اول را برنمی‌گرداند. اگر مهرِ
نسخه فقط در انتها زده می‌شد، سند **داده‌ی migrate‌شده با برچسبِ نسخه‌ی قدیمی**
می‌مانْد و بارگذاریِ بعدی همان گام را روی داده‌ی already-migrated دوباره اجرا
می‌کرد. تستش هست: بعد از شکستِ گامِ دوم، نسخه روی **۲** می‌ایستد، نه ۱ و نه ۳.

**سه نگهبانِ خودِ رجیستری** (پرشِ نسخه · زنجیره‌ی گسسته · **migration بدونِ
بالابردنِ `SCHEMA_VERSION`**). سومی رایج‌ترین اشتباهِ ممکن است و بدونِ نگهبان
migration **هرگز اجرا نمی‌شد** و هیچ‌کس نمی‌فهمید.

**سندِ جلوتر از این build** خطای صریح می‌دهد و **سند را دست نمی‌زند** (صفر update).
migrateِ رو به عقب وجود ندارد؛ سکوت اینجا یعنی خرابیِ ساختاری که نسخه‌ی جدیدترِ
برنامه درستش کرده بود. کدش همان `CLIENT_TOO_OLD`ی است که به کلاینت می‌رود.

**⚠️ یک تکه از گام ۲٫۴ زودتر آمد:** `src/error-codes.ts`. تنها گزینه‌ی دیگر یک
رشته‌ی جادویی بود که بعداً در `protocol.ts` تکرار می‌شد — و enumی که دو نسخه دارد
enum نیست. در TODO ثبت شد که ۲٫۴ همین را مصرف کند.

### ⚠️ گیت قرمز شد و **تقصیرِ کد نبود** — سقفِ commit ویندوز

وسطِ همین گام `pnpm verify` روی `test` افتاد. **هر ۸ پکیج** با
`FATAL ERROR: ... out of memory` — در حالی که **۷GB رمِ آزاد** بود.

علت رم نبود، **سقفِ commit** بود (رم + page file): ۳۴٫۵ از ۴۰٫۶ گیگ پر، و turbo
پیش‌فرض تا ۱۰ task موازی می‌برد که هرکدام چند workerِ Node است. اجرای سریالیِ
همان لحظه سبز شد (۸/۸ پکیج).

**رفع:** `scripts/verify.mjs` حالا `--concurrency=1` می‌زند. گیت **ضعیف نمی‌شود** —
همان taskها، همان `--force`، تصمیم همچنان فقط با کدِ خروج؛ فقط ~۱۰ ثانیه کندتر
(۹s → ۱۹s).

★ **چرا ۱ و نه ۲:** درسِ پورت‌های رزروشده در گام ۱٫۱ — عددی انتخاب نکن که شیر یا
خط است، از رژیمِ شکست **خارج شو**. `--concurrency=2` روی ماشینِ شلوغ‌تر باز همان
قمار بود.

⚠️ **آنچه ثابت شده و آنچه نشده — با دقت:**

- **ثابت شده:** اجرای **موازی** دو بار پشتِ سر با OOM افتاد و خروجی‌اش ثبت است
  (هر ۸ پکیج، `FATAL ERROR`). سریالی همان لحظه ۸/۸ سبز شد.
- **ثابت نشده:** دو اجرای **سریالی** هم افتادند، ولی خروجی‌شان را **ثبت نکردم** —
  پس نمی‌گویم «آن هم OOM بود». فقط این را می‌دانم که در همان پنجره‌ی فشار رخ داد.
  بعدش **چهار اجرای سریالیِ پیاپی** با exit 0، صفر خطِ `out of memory` و صفر
  assertion سبز شدند.

پس ادعا این نیست که «سریالی تضمینی است»، بلکه این است که «موازی روی این ماشین
قابل‌اتکا نیست». نشانه‌اش در `CLAUDE.md` ثبت شد تا دفعه‌ی بعد **اول خروجی را ببینیم**
و بعد قضاوت کنیم — همان درسِ سبزِ دروغینِ گام ۱٫۲.

### گام ۲٫۴ — کدهای پیامِ پروتکل ✅ (۱۴۰۵/۰۵/۱۳) — **فاز ۲ کامل**

هر هفت پیامِ PLAN بخش ۵٫۳ با encode/decode روی `lib0`. **۲۱ تستِ تازه**
(`ydoc-schema` ۱۳۴ → ۱۵۵).

**★★ دو قاعده‌ای که شکلِ پروتکل را تعیین کردند:**

۱. **نوعِ ناشناخته `null` می‌دهد، و بایتِ اضافه در انتهای پیام خطا نیست.** با هم
   یعنی نسخه‌ی بعدی هم می‌تواند پیامِ تازه اضافه کند و هم به پیامِ **موجود** فیلد
   اضافه کند. در یک اپِ وب که کاربر تبش را هفته‌ها باز نگه می‌دارد، «کلاینتِ
   قدیمی» بخشِ بزرگی از کاربران است؛ بدونِ این، هر دیپلوی که کدِ پیامِ تازه
   بیاورد آن‌ها را می‌شکند.
   ⚠️ ولی پیامِ **خرابِ** نوعِ شناخته‌شده `ProtocolError` می‌دهد. ناشناخته یعنی
   «هنوز بلد نیستم» و خراب یعنی «یک طرف باگ دارد» — قاطی‌کردنشان یعنی باگ‌های
   framing برای همیشه پنهان می‌مانند.
۲. ★ **fail closed.** نقشِ ناشناخته → `viewer`، وضعیتِ ذخیره‌ی ناشناخته →
   `unsaved`. سه گزینه بود و دو تایش بد: **انداختنِ `HB_PERMISSION`** یعنی کاربری
   که همین الان تنزل داده شده با دسترسیِ قبلی ادامه می‌دهد (حفره‌ی مجوز)؛
   **خطا و بستنِ اتصال** یعنی حلقه‌ی وصل‌شدن و افتادن. تنزل به `viewer` **دیده
   می‌شود** و با یک رفرش درست می‌شود. برای وضعیتِ ذخیره هم قراردادِ M1 صریح است
   که باید **حقیقت** را بگوید نه خوش‌بینی.

**دو چیز که سنجیدم، نه فرض کردم:**

- **`lib0.writeVarUint` تا `Number.MAX_SAFE_INTEGER` سالم است** (probe: ۰ تا
  ۲^۵۳−۱). پس `seq` عدد می‌مانَد و `bigint` لازم نیست — وگرنه بی‌دلیل به API
  تحمیل می‌شد.
- **`import "lib0/encoding"` زیر pnpm resolve نمی‌شد** (`ERR_MODULE_NOT_FOUND`).
  «وابستگیِ Yjs است» کافی نیست چون گرافِ node_modules تخت نیست. صریحاً به
  `ydoc-schema` افزوده و در `docs/dependencies.md` ثبت شد؛ `license:check` سبز
  (۶۹۹ پکیج، بدونِ تغییر — از قبل در درخت بود).

**framing:** payloadِ `SYNC`/`AWARENESS`/`HB_EPHEMERAL` **طول‌دار** نوشته می‌شود،
برخلافِ قراردادِ رایجِ `y-websocket` که بقیه‌ی بافر را payload می‌گیرد. هر دو سرِ
این پروتکل خودمانیم، و framingِ یکدست همان چیزی است که قاعده‌ی ۱ را ممکن می‌کند.

**`BoardRole` هم مثلِ `CommentPin` اینجا ماند** نه `shared-types` — در فهرستِ
گام ۶٫۴ ثبت است. ⚠️ با **ایندکس** روی سیم می‌رود، پس فقط به انتهایش اضافه شود؛
جابه‌جا کردنشان یعنی کلاینتِ قدیمی نقشِ اشتباه می‌فهمد.

---

## ★ جمع‌بندیِ فاز ۲ — کامل شد

`packages/ydoc-schema` تمام است: **۱۵۵ تست**، هیچ وابستگی به UI یا سرور، و هر
تصمیمش پشتوانه‌ی سنجیده دارد.

| گام | خروجی | نگهبانِ دائمی |
|---|---|---|
| ۲٫۱ | ساختارِ سند + codecِ per-property | ادغامِ همزمان، صفر updateِ بی‌تغییر، سازگاریِ تایپی با `CanvasDocument` |
| ۲٫۲ | دارایی، appState، سنجاقِ کامنت | نگهبانِ باینری (دو تکه، دوجهته) + نگهبانِ وضعیتِ شخصی (دو لایه) |
| ۲٫۳ | نسخه‌بندی و migration | سه نگهبانِ رجیستری + تستِ شکستِ گامِ میانی |
| ۲٫۴ | پروتکل | سازگاریِ رو به جلو (دو قاعده) + fail closed |

★ **الگویی که در کلِ فاز تکرار شد:** هر جا سکوت ممکن بود، به‌جایش یا **خطا** یا
**بدبینانه‌ترین حالت** انتخاب شد — `undefined` که رد می‌شود نه `null`، کلیدِ
غیرمجازِ appState که خطا می‌دهد نه نادیده، نقشِ ناشناخته که `viewer` می‌شود،
`SCHEMA_VERSION`ِ فراموش‌شده که migration را قرمز می‌کند. تنها استثنای عمدی
**نوعِ پیامِ ناشناخته** است، و دلیلش صریح ثبت شده.

---

## فاز ۳ — binder

### گام ۳٫۱ — اسکلت آداپتور + چرخه‌ی عمر + نگهبانِ echo ✅ (۱۴۰۵/۰۵/۱۴)

`YjsSyncAdapter` زنده است: دو `Y.Doc` بدونِ یک خط کدِ شبکه همگام می‌شوند، عنصر
رفت‌وبرگشت می‌کند، و نگهبانِ echo روی binderِ **واقعی** آزموده شد.
**۲۶ تستِ تازه** (`canvas-sync` ۴۶ → ۷۲).

**★★ دو تله‌ی بی‌صدا که همین‌جا کشف شدند** — هیچ‌کدام حدس نبود:

**۱. فقط updateِ افزایشی کافی نیست.** اول ترابری را روی updateهای خام بستم و
**هفت تست افتاد**. probe علتش را نشان داد: Yjs updateها را در `pendingStructs`
**بایگانی می‌کند و اعمال نمی‌کند**، چون opهای قبلیِ همان کلاینت را ندیده — و
**هیچ خطایی نمی‌دهد**. اینجا شکاف از همان اول هست چون `createBoardDoc` خودش
اولین op را می‌نویسد (`meta.schemaVersion`). رفع: همگام‌سازیِ اولیه با
`y-protocols/sync` — step 1 و step 2 هر دو در `connect`، پس در **یک رفت‌وبرگشت**
همگرا می‌شوند.

اگر این را با «دو Y.Doc را به هم وصل کن» فرض می‌کردم، در فاز ۴ پشتِ لایه‌ی شبکه
ظاهر می‌شد — جایی که تشخیصش چند برابر گران‌تر است.

**۲. `y-protocols` خطاهای داخلِ observer را می‌بلعد.** `readSyncStep2` خودِ
`Y.applyUpdate` را در `try/catch` گذاشته و هر خطا را فقط `console.error` می‌کند
(«This catches errors that are thrown by event handlers» — کامنتِ خودشان). یعنی
اگر `applyRemoteChanges` **داخلِ** observer صدا زده می‌شد، `EchoLoopError` هرگز
به هیچ‌کس نمی‌رسید و **کلِ نگهبانِ M1 به یک خطِ لاگ تنزل پیدا می‌کرد**.

رفع: observer فقط **جمع** می‌کند (`collectRemote`) و تحویل **بیرونِ تراکنش**
انجام می‌شود (`flushRemote`). مزیتِ دومش هم بازگشتی‌بودن است: بوم می‌تواند در
پاسخ بنویسد بدونِ تراکنشِ تودرتو. یک تستِ صریح این ترتیب را قفل کرده.

⚠️ **یک انتظارِ غلطِ خودم که تست اصلاحش کرد:** ادعا کرده بودم خطای بومِ بدرفتارِ B
باید به **A** برسد — چون در M1 هاب درون‌فرایندی بود و `publish` مستقیم
`receiveRemote` را صدا می‌زد. با ترابریِ واقعی این غلط است و **نباید** درست باشد:
خطای بومِ B مالِ ماشینِ B است. ادعای درست: re-emit **در خودِ B** رد می‌شود.

**ترتیبِ داخلِ `connect` عمدی است و با تست قفل شد:** ترابری → همگام‌سازیِ اولیه →
`replaceDocument` → **بعد** observer. اگر observer زودتر بسته شود، کلِ بورد
به‌صورت «تغییرِ remote» روی بومِ خالی می‌ریزد. بینِ دو گامِ آخر هیچ `await`ی
نیست، پس پیامی لای آن‌ها نمی‌افتد.

**StrictMode سمتِ آداپتور:** `connect`ِ دوباره بدونِ `disconnect` **خطا می‌دهد** —
سکوت یعنی دو مجموعه observer روی یک سند و هر تغییرِ remote **دوبار** روی بوم،
باگی که فقط زیر StrictMode ظاهر می‌شود. و بعد از `disconnect` هیچ تغییری به بومِ
قبلی نمی‌رسد (نشتیِ observer، کلاسیک‌ترین باگِ unmount).

**آنچه عمداً هنوز no-op است:** متدهای awareness (گام ۳٫۵). یک تستِ صریح همین
وضعیت را **پین** کرده تا با پیاده‌سازیِ ۳٫۵ قرمز شود — جای یک TODOِ فراموش‌شدنی.
`requestAssetUpload` برعکس **خطا می‌دهد**، چون نتیجه برمی‌گرداند و یک Promiseِ
ساختگی یعنی بوم برای همیشه منتظرِ `fileId` می‌مانَد.

⚠️ **`lib0` به `canvas-sync` هم صریحاً افزوده شد** — همان تله‌ی گام ۲٫۴: زیر pnpm
هر پکیجِ مصرف‌کننده باید خودش اعلامش کند.

### گام ۳٫۲ — `applyRemoteChanges` با `captureUpdate: "NEVER"` ✅ (۱۴۰۵/۰۵/۱۴)

ادعا در **مرورگرِ واقعی** اثبات شد: ب سه بار `Ctrl+Z` می‌زند و استیکیِ الف سرِ
جایش می‌مانَد. **۳۰ تستِ واحدِ تازه** (`eslint-config` ۶۱ → ۷۹، `canvas-sync`
۷۲ → ۸۴) به‌علاوه‌ی **۳ تستِ E2E**.

**★ کشفِ حینِ کار: هیچ‌کس `CanvasInbound` را پیاده نکرده بود.** M1 قرارداد و
چوک‌پوینتِ `commitSystemUpdate` را ساخت، ولی `HamboomCanvas` فقط `onReady(api)`
می‌دهد. وصل‌کردنِ آن دسته به یک آداپتور دقیقاً تعریفِ «binder» است، پس
`src/canvas-binding.ts` هم همین‌جا ساخته شد. فقط دو متدی که روی **صحنه**
می‌نویسند اینجا پیاده‌اند؛ بقیه (حضور، وضعیت، مجوز) حالتِ **رابط** اند و
callback می‌گیرند — وگرنه binder به یک لایه‌ی UI تبدیل می‌شد.

**گیت، دو لایه:** `RuleTester` منطقِ قاعده‌ی تازه (`hamboom/remote-writes-never`)
را می‌آزماید و lintِ واقعی روی `eslint.config.js` **سیم‌کشی** را — همان تفکیکی
که در گام ۰٫۲ یک باگِ واقعی گرفت. قاعده عمداً فقط روی `src/apply-remote.ts`
اعمال می‌شود؛ روی کلِ پکیج، مسیرِ محلی که واقعاً `IMMEDIATELY` می‌خواهد هم خطا
می‌گرفت و قاعده به یک بن‌بست تبدیل می‌شد — و اولین کسی که به مسیرِ محلی دست
می‌زد خاموشش می‌کرد. یک تستِ صریح همین «مجاز بودن در مسیرِ محلی» را قفل کرده.

⚠️ **تله‌ی flat config:** دو configی که به یک فایل اعمال می‌شوند نمی‌توانند
هرکدام `plugins: { hamboom: {...} }`ِ جدا بسازند — ESLint با
`Cannot redefine plugin` می‌افتد. حالا هر دو factory به **یک شیءِ مشترک** ارجاع
می‌دهند.

**★★ دموی زیرِ StrictMode دو باگِ گام ۳٫۱ را بیرون کشید** — هیچ‌کدام در تستِ
واحد دیده نمی‌شد، چون هیچ‌کدام تا آن لحظه واقعاً زیر StrictMode نرفته بود:

۱. **`disconnect` وسطِ `connect`.** افکت اجرا می‌شود، `connect` روی اولین
   `await` معلق می‌مانَد، cleanup فوراً `disconnect` می‌زند، افکت دوباره اجرا
   می‌شود — و ادامه‌ی `connect`ِ **اول** observerهایش را روی سند سوار می‌کرد که
   هیچ `disconnect`ی سراغشان نمی‌آمد. رفع: شمارنده‌ی **نسلِ اتصال** و
   `ConnectionCancelledError`. قاعده‌ای که ماند: هر `await`ِ تازه در `connect`
   باید بعدش همین بررسی را داشته باشد.
۲. **ترابری بعد از `disconnect` دوباره به hub نمی‌پیوست**، پس `connect`ِ بعدی
   روی همان آداپتور مرده می‌مانْد. رفع: `LocalTransport.connect()` — همان کاری
   که ترابریِ واقعیِ فاز ۴ با اتصالِ دوباره‌ی WebSocket می‌کند.

**دو تصحیح روی انتظارِ خودم:**

- اولین نسخه‌ی تستِ E2E انتظار داشت بومِ الف **۲** عنصر داشته باشد. غلط بود:
  استیکیِ **ب** هم از راهِ remote به الف رسیده بود، پس ۴ درست است. ادعای واقعی
  این است که `Ctrl+Z`ِ ب هیچ‌چیز از الف نبرد.
- بارلِ پکیج حالا `apply-remote` را صادر می‌کند و آن ورودیِ اصلیِ `canvas-core`
  را می‌بیند، پس `index.test.ts` دیگر در Nodeِ خالص بالا نمی‌آید. jsdom شد —
  و این محدودیتِ **درستی** است: `canvas-sync` کدِ مرورگر است؛ سرور
  `ydoc-schema` را مصرف می‌کند که همچنان در Node خالص کار می‌کند.

**آنچه عمداً انجام نشد:** «ترتیبِ capture در جریانِ چندمرحله‌ای». مسیرِ remote
الان تک‌مرحله‌ای است، پس موردی وجود ندارد؛ اولین جریانِ چندمرحله‌ای دارایی است
(گام ۳٫۶) و همان‌جا ثبت شد.

### گام ۳٫۳ — مسیرِ محلی: `doc.transact` + جدولِ throttle ✅ (۱۴۰۵/۰۵/۱۴)

**۱۶ تستِ واحدِ تازه** (`canvas-sync` ۸۴ → ۱۰۰) + **۳ تستِ E2E**.

**جدولِ فرکانسِ PLAN ۷٫۴** در [`src/emit-local.ts`](packages/canvas-sync/src/emit-local.ts):
ساخت/حذف/بی‌ژست **فوری** · درگ throttle ۵۰ms · تایپ debounce ۱۵۰ms · مرزِ
`gestureId` فوراً flush («commit نهایی در drop»).

★ **trailingِ خالص، نه leading+trailing** — چون سقفِ PLAN «حداکثر ۴۰» است و
leading یکی اضافه می‌کرد. درگِ ۲ثانیه‌ای با ۲۴۰ رویدادِ ورودی → **≤۴۰ updateِ
Yjs**، و ادغام با **شناسه** انجام می‌شود تا آخرین update دقیقاً مختصاتِ نهایی
باشد. هر دو ادعا هم روی زمان‌بندِ ساختگی و هم روی `Y.Doc`ِ واقعی تست دارند.

★ **`LocalOrigin` از رشته به کلاس تبدیل شد** تا `gestureId` جایی برای نشستن
داشته باشد — و چون `Y.UndoManager` originی را هم که **سازنده‌اش** در
`trackedOrigins` باشد ردیابی می‌کند، گام ۳٫۴ می‌تواند با یک `Set([LocalOrigin])`
هر ژستی را بگیرد.

⚠️ **انحرافِ ثبت‌شده از `sync/README`:** آن سند می‌گوید throttle کارِ **بوم** است،
چون بوم می‌داند ژست کی تمام می‌شود. حرف درستی است و عوض نشده — ولی M1 هیچ‌جا
پیاده‌اش نکرد و ابزارها هنوز به آداپتور وصل نیستند، پس **تا M3 صاحبش اینجاست**
و به‌جای «کی ژست تمام می‌شود» از `gestureId`ِ خودِ قرارداد استفاده می‌کند.

### ★★ دو موردِ پین‌شده‌ی گام ۱٫۳ — **عدد گرفتند**

سنجیده روی دو بومِ **واقعی** در Chromium، چهار اجرا
([`e2e/text-latency.spec.ts`](packages/canvas-sync/e2e/text-latency.spec.ts)؛ ثبت در
[`docs/ydoc-baseline.md`](docs/ydoc-baseline.md) بخش ۶):

| اندازه‌گیری | عدد |
|---|---|
| «update روی `Y.Doc` نشست» → **صحنه** نشانش می‌دهد | **۱۱٫۳ · ۱۳٫۹ · ۱۴٫۵ · ۱۴٫۹ms** |
| همان → **textareaِ باز** نشانش می‌دهد | ★ **هرگز** (>۳۰۰۰ms) |
| پرشِ مکان‌نما وقتی همتا **قبل از** caret درج می‌کند | **۰ کاراکتر** |

★★ **یافته‌ی اصلی، و از عددِ میلی‌ثانیه‌ای مهم‌تر:** ویرایشگرِ متنِ موتور تا وقتی
باز است **کاملاً ایزوله** است. پس «پنجره‌ی کهنگی» یک عددِ میلی‌ثانیه‌ای نیست —
به اندازه‌ی **کلِ مدتی که کاربر در حالِ ویرایش است** باز می‌مانَد. و پرشِ caret
صفر است دقیقاً به همین دلیل، نه چون مشکلی نیست.

پیامدش هم سنجیده شد: وقتی کاربر ویرایشگر را می‌بندد، مقدارِ **خودش** را می‌نویسد
و درجِ همتا **پاک می‌شود**. این باگِ دیف نیست — دیف علیهِ پایه‌ی زنده درست کار
می‌کند — بلکه محدودیتِ ایزوله‌بودنِ ویرایشگرِ موتور است. هر سه رفتار با تست قفل
شده‌اند، پس اگر نسخه‌ی بعدیِ موتور عوضشان کند می‌فهمیم. گزینه‌های رفع ثبت شد؛
**تصمیمش کارِ M3 است**، وقتی `apps/web` واقعی باشد.

**★★ و همین تستِ اندازه‌گیری یک باگِ واقعی بیرون کشید:** اولین اجرا نشان داد
تغییرِ متنِ همتا **اصلاً به صحنه نمی‌رسد**. سند درست ادغام می‌شد ولی صحنه `text`ِ
قدیمی را نگه می‌داشت، چون `text` مشتق است و کسی بازمحاسبه‌اش نمی‌کرد — همان
نیمه‌ی دومِ ADR-034 که در چک‌لیستِ همین گام بود و بدونِ این اندازه‌گیری تا فاز ۶
پنهان می‌مانْد. رفع با `wrapTextGreedy`ِ **خودِ M1**، نه یک الگوریتمِ دوم.


### گام ۳٫۴ — انزوای undo: `Y.UndoManager` + `trackedOrigins` ✅ (۱۴۰۵/۰۵/۱۵)

→ [ADR-035](ARCHITECTURE_DECISIONS.md#adr-035). **۱۱ تستِ واحدِ تازه**
(`canvas-sync` ۱۰۰ → ۱۱۱) + **۳ تستِ E2E** (مجموع ۱۱، دو اجرای پایدار).

**★★ تصمیم با probe گرفته شد، و probe فرضِ من را وارونه کرد.** انتظار داشتم
undoِ موتور روی عنصرِ مشترک کارِ همتا را بخورد و همان دلیلِ کنارگذاشتنش باشد.
**نخورد** — درست کار می‌کرد: `Ctrl+Z`ِ ب فقط رنگِ خودش را برگرداند و جابه‌جاییِ
الف سرِ جایش ماند.

مشکل جای دیگری بود و بدتر: **undoِ موتور به همتا نمی‌رسد.** بومِ ب زرد شد و بومِ
الف **بنفش مانْد** (`#FFF9B1` در برابرِ `#D0C6F5`) — واگرایی، تا وقتی کسی دوباره
آن عنصر را لمس کند. با `Y.UndoManager` این از ریشه نیست: undo یک تراکنشِ عادیِ
Yjs است و از همان مسیرِ همیشگی به همتا می‌رسد.

⚠️ **و یک اشتباهِ خودم در همان probe، که شاهدِ کنترل گرفتش:** اولین اجرا نشان داد
undoِ موتور **هیچ کاری نمی‌کند** و نزدیک بود همان را گزارش کنم. شاهدِ کنترل (همان
سناریو بدونِ تغییرِ remote) هم «هیچ کاری نمی‌کند» داد — یعنی مشکل از **harness**
بود نه موتور: `commitLocal`ِ من `bumpVersion` نمی‌زد، پس موتور عنصر را
«بدونِ تغییر» می‌دید و ورودیِ undo نمی‌ساخت. دقیقاً همان تله‌ی ثبت‌شده‌ی M1، و
دومین باری که شاهدِ کنترل جلوی یک گزارشِ غلط را گرفت (اولی گام ۱٫۱).

**دو پینِ گام ۱٫۴ به نگهبانِ دائمی تبدیل شدند:**

- تستِ **ضدِ ادعا**: یک `UndoManager` بدونِ `trackedOrigins` ساخته می‌شود و ثابت
  می‌شود **هیچ کاری نمی‌کند**. اگر کسی روزی آن گزینه را بردارد، این تست قرمز
  می‌شود؛ بدونش رگرسیون بی‌صدا می‌مانْد.
- undoِ **ساخت** کلید را کامل برمی‌دارد؛ ادعا **همگرایی** است (هر دو سند).

**گروه‌بندی با `gestureId`:** ادغام فقط **درونِ یک ژستِ شناسه‌دار** مجاز است.
سنجیده شد که بدونِ این، ساختِ استیکی و سه ویرایشِ بعدی همگی **یک** `Ctrl+Z` شدند
و کلِ بورد پاک شد.

**⚠️ شکافی از گام ۳٫۱ که همین‌جا بیرون آمد:** تحویلِ تغییرِ غیرِمحلی به بوم فقط به
دریافتِ پیامِ ترابری گره خورده بود. originِ undo نه `LocalOrigin` است و نه از
ترابری می‌آید، پس تغییر در صف می‌مانْد و **بوم undoِ خودش را نمی‌دید**. قاعده‌ی
تازه‌ی ثبت‌شده: هر مسیرِ نوشتنی که originش `LocalOrigin` نیست باید `flushRemote`
را صدا بزند.

**دو ادعای گام ۳٫۲ که اینجا عوض شدند — و آن درست است:** تستِ E2Eِ قبلی می‌گفت
«`Ctrl+Z`ِ ب روی بومِ الف اثری ندارد» و عددِ الف را ۴ می‌خواست. آن ادعاها یک
**باگ** را ثبت می‌کردند (واگرایی)، نه یک خاصیت. حالا undo به همتا می‌رسد و
اعداد ۲ و ۰ اند. کامنتِ هر دو تست همین را می‌گوید تا کسی فکر نکند رگرسیون بوده.

### گام ۳٫۵ — کانالِ حضور: awareness + ephemeral ✅ (۱۴۰۵/۰۵/۱۶)

→ [ADR-036](ARCHITECTURE_DECISIONS.md#adr-036). **۲۳ تستِ واحدِ تازه**
(`canvas-sync` ۱۱۱ → ۱۳۴) + **۳ تستِ E2E** (مجموع ۱۴).

**★★ تصمیم با عدد گرفته شد، نه با خواندنِ قرارداد.** `PeerState.ephemeral` یک
فیلد در همان شیء است، پس طبیعی‌ترین کار این بود که در stateِ awareness هم یک
فیلد باشد. سنجیده شد و بد بود:

| یک به‌روزرسانیِ awareness | بایت |
|---|---|
| state بدونِ استروک | **۱۹۸** |
| همان + استروکِ ۲۰۰نقطه‌ای | **۵۳۶۲** |
| ★ فقط **مکان‌نما** تکان بخورد، استروک دست‌نخورده | باز هم **۵۳۶۲** |

سطرِ سوم کلِ دلیل است: `encodeAwarenessUpdate` دیف نمی‌گیرد و **کلِ state** را
`JSON.stringify` می‌کند. یک ثانیه تکانِ ماوس حینِ کشیدن می‌شد **۲۱۴KB به‌جای
۷٫۹KB**. یک تستِ **ضدِ ادعا** گزینه‌ی ردشده را می‌سازد و همین عددها را نگه می‌دارد.

**★★ دو باگ که فقط چون دو کلاینتِ واقعی وصل شدند بیرون آمدند** — هیچ‌کدام حدس
نبود، و هر دو **بی‌صدا** خراب می‌کردند:

۱. **پاسخِ همتا زودتر از مقداردهیِ دامنه می‌رسید.** ترابریِ لوکال همزمان است:
   معرفیِ ما می‌رفت، همتا در **همان پشته‌ی فراخوانی** جواب می‌داد، و جوابش به
   `handleMessage` می‌رسید که هنوز `this.presence === null` بود — پس بی‌صدا دور
   ریخته می‌شد. نشانه‌اش نامتقارن و گیج‌کننده بود: **الف همتا را می‌دید، ب
   هیچ‌کس را.** رفع: معرفی از سازنده جدا شد (`announce()`) و **بعد از** نشاندنِ
   دامنه صدا زده می‌شود.
   ⚠️ با یک WebSocketِ واقعی این جواب async می‌آمد و باگ **پشتِ شبکه پنهان
   می‌مانْد** — دقیقاً همان دلیلی که فاز ۳ عمداً قبل از فاز ۴ ساخته شد.
۲. ★★ **اتصالِ دوباره برای همیشه نامرئی بود.** awareness هر همتا را با یک
   `clock`ِ صعودی می‌شناسد و هر پیامی با clockِ کوچک‌تر را **بی‌صدا دور می‌ریزد**.
   `clientId` از `doc.clientID` می‌آید و عوض نمی‌شود، ولی یک `Awareness`ِ تازه
   از صفر می‌شمارد — پس بعد از `disconnect`/`connect` همتاها ما را با clockِ ۱
   می‌دیدند در حالی که ۲۰۰ ثبت کرده بودند. **زیر StrictMode این چرخه در هر mount
   رخ می‌دهد**، یعنی حالتِ عادی بود نه استثنا. رفع: شمارنده بین اتصال‌ها حفظ
   می‌شود و به دامنه‌ی تازه تزریق.

**سه قیدِ ورودی رعایت شدند:** ephemeral هرگز داخل سند نرفت (نگهبان در تستِ واحد
**و** مرورگر) · اعدادِ throttle از همان `HB_THROTTLE` آمدند، تکرار نشدند ·
`viewport` ورودی است و از `onScrollChange` می‌آید (مصرف‌کننده‌اش گام ۳٫۷ است).

**سه تصمیمِ ریز که در متنِ گام نبودند:**

- **خروجِ مکان‌نما (`null`) فوری است، نه throttle‌شده** — یک رویدادِ گسسته است
  مثلِ حذف در مسیرِ عنصر. با throttle، مکان‌نمای همتا تا ۴۰ms بعد از خروجش روی
  بوم می‌مانْد.
- **`disconnect` صفِ حضور را flush نمی‌کند**، برخلافِ `dispose`ِ مسیرِ عنصر که
  **باید** بکند. آنجا آخرین تیکِ درگ کارِ کاربر است؛ اینجا آخرین مکان‌نما بعد از
  «رفتم» فقط یک مکان‌نمای زامبی می‌ساخت.
- **`setConnectionState` فقط با تغییرِ عدد.** `applyPeers` قرارداداً پرتکرار است
  (هر ۴۰ms)، ولی وضعیتِ اتصال به یک نشانگرِ ثابتِ رابط وصل است و رندرِ
  ۲۵بار-در-ثانیه‌اش هزینه‌ی بی‌دلیل بود.

**⚠️ یک چیز که آزموده نشد و علتش سنجیده است:** جاروی ۳۰ثانیه‌ایِ خودِ
`y-protocols` (پاک‌کردنِ همتای بی‌خداحافظی). `lib0/time` مقدارِ `Date.now` را در
لحظه‌ی **بارگذاریِ ماژول** می‌گیرد (`export const getUnixTime = Date.now`)، پس
زمان‌بندِ ساختگیِ vitest که کلاسِ `Date` را جایگزین می‌کند به آن ارجاع نمی‌رسد و
جارو هرگز اجرا نمی‌شود. اولین تلاشم هم به همین دلیل شکست خورد.
به‌جایش مسیرِ **واقعیِ فاز ۴** آزموده شد — حذفِ اعلام‌شده با stateِ `null` و
clockِ بزرگ‌تر، همان بایت‌هایی که سرور می‌فرستد — و یک تستِ صریح همان ناتوانی را
**پین** کرد تا کسی دوباره تستِ بی‌اثر ننویسد.

⚠️ **و یک تلاشِ بی‌اثرِ دیگرِ خودم که تست ردش کرد:** برای شبیه‌سازیِ «قطعِ برق»
اول `transport.disconnect()` زدم. کار نکرد و دلیلش در خودِ `LocalTransportHub`
بود: `publish` عضویتِ **فرستنده** را بررسی نمی‌کند، پس keepaliveهای آن آداپتور
همچنان به بقیه می‌رسید و همتا هرگز کهنه نمی‌شد.

### گام ۳٫۶ — دارایی‌ها پشتِ پورت ✅ (۱۴۰۵/۰۵/۱۶)

**۱۸ تستِ واحدِ تازه** (`canvas-sync` ۱۳۴ → ۱۵۱) + **۳ تستِ E2E** (مجموع ۱۷).

**⚠️ ADR تازه‌ای نوشته نشد، و این یک تصمیم بود نه فراموشی:** «پورت به‌جای
وابستگی به M3» همان [ADR-031](ARCHITECTURE_DECISIONS.md#adr-031) است و مسیرِ
Object Storage را P4 تعیین کرده. این گام فقط آن دو را به کار می‌بندد؛ ADRِ سوم
برای همان تصمیم، خودِ تصمیم را رقیق می‌کند.

**★★ چیزی که در متنِ گام نبود و بدونش گام ناقص بود — سمتِ گیرنده.** سند فقط
**متادیتا** دارد و عنصرِ تصویر فقط یک `fileId` است، پس نوشتنِ عنصرِ همتا روی
صحنه به‌تنهایی یک **قابِ خالی** می‌سازد: موتور فایلی به آن نام نمی‌شناسد.
قراردادِ M1 هیچ متدی برای «این فایل را ثبت کن» ندارد — تنها راهش فیلدِ `assets`ِ
خودِ `ElementChangeSet` است. کامنتِ خودِ `apply-remote.ts` این کار را از گام ۳٫۲
به همین‌جا سپرده بود.

**سه تصمیمِ ترتیبی، هر کدام با دلیل:**

| تصمیم | چرا |
|---|---|
| متادیتا **هنگامِ آپلود** در سند نوشته می‌شود | عنصر فقط یک ارجاع است؛ اگر زودتر برسد همتا یک `fileId`ِ آویزان دارد. چون ابزار اول آپلود را await می‌کند، متادیتا **قبلِ** عنصر روی سیم می‌رود |
| انبارِ توسعه **مشترک** است | بدونش ب یک `fileId` می‌گیرد که هیچ‌جا سراغش را ندارد — همان نقشی که در تولید سرور دارد |
| `dispose` روی **انبار** است نه کلاینت | با `disconnect`ِ یک کلاینت، تصویر روی بومِ **همتا** هم سفید می‌شد |

**★ بدهیِ ثبت‌شده‌ی گام ۳٫۲ تسویه شد.** آنجا نوشتم «ترتیبِ capture در جریانِ
چندمرحله‌ای» موردی ندارد چون مسیرِ remote تک‌مرحله‌ای است، و اولین جریانِ
چندمرحله‌ای دارایی است. حالا سنجیده شد: چون صاحبِ undo **Yjs** است (ADR-035) و
ابزارِ M1 فقط **یک بار** در `onInserted` نتیجه را می‌دهد، پشته یک ورودی می‌گیرد و
یک `Ctrl+Z` کلِ تصویر را برمی‌دارد. تستِ مرورگری این را قفل کرد — اگر روزی کسی
مرحله‌ی `pending` را هم emit کند، قرمز می‌شود.

**⚠️ دو یافته که تست بیرون کشید، نه بازبینی:**

۱. **`hbAsset` ابعادِ مثبت می‌خواهد** (`width`/`height` با `.positive()`). اولین
   نسخه‌ی پورت مثلِ دموی M1 صفر می‌فرستاد و `writeAsset` با `ZodError` افتاد. حالا
   پیاده‌سازیِ dev مثلِ سرور از خودِ بایت‌ها decode می‌کند و بیرونِ مرورگر به ۱×۱
   برمی‌گردد.
   ⚠️ **همان صفر امروز در [`canvas-core/dev/App.tsx`](packages/canvas-core/dev/App.tsx)
   هست** — چون آنجا به سندی نمی‌رسد خطا نمی‌دهد، ولی وصل‌شدنش به یک آداپتورِ
   واقعی می‌شکند. **ثبت شد، دست نخورد** (دامنه‌ی M1).
۲. ⚠️ **ادعای E2Eِ خودم غلط بود، نه کد.** برای «باینری در سند نیست» یک PNGِ
   یک‌رنگِ ۱۲۰×۸۰ ساختم که فقط **۴۵۹ بایت** فشرده شد — کوچک‌تر از خودِ متادیتای
   عنصر، پس مقایسه هیچ‌چیز اثبات نمی‌کرد و تست درست افتاد. با نویزِ
   فشرده‌نشدنیِ ۳۰۰×۲۰۰ (۵۰KB+) ادعا معنا پیدا کرد: رشدِ سند **زیر ۳KB** و
   مستقل از حجمِ فایل.

**یک نگهبانِ تازه از جنسِ گام ۲٫۱:** `canvas-core` و `shared-types` هر کدام
فهرستِ MIMEِ مجاز دارند (`HB_IMAGE_MIME_ALLOW` و `HB_ALLOWED_IMAGE_MIME`) و امروز
یکی‌اند. `canvas-sync` تنها پکیجی است که **هر دو** را می‌بیند، پس تستِ
تطابقشان همین‌جاست — دقیقاً جایی که نگهبانِ `BoardDocument ↔ CanvasDocument`
نشست. واگرایی یعنی فایلی که ابزار می‌پذیرد پشتِ پورت رد می‌شود، یا بدتر: برعکس.

**تستِ پین‌شده‌ی دوم هم قرمز شد و به‌روز شد** (`rejects.toThrow(/۳٫۶/)`).
قاعده‌اش عوض نشد — نبودِ پورت همچنان **خطا** می‌دهد، چون یک Promiseِ ساختگی یعنی
بوم برای همیشه منتظرِ `fileId` می‌مانَد. حالا هر دو ادعای پین‌شده‌ی آن بلوک
مصرف شده‌اند و بلوک به «سیم‌کشیِ کاملِ outbound» تبدیل شد.

### گام ۳٫۷ — G-1الف: دو بومِ واقعی با حضورِ رندرشده ✅ (۱۴۰۵/۰۵/۱۶)

**۸ تستِ E2Eِ تازه** (مجموع ۲۵، سه اجرای متوالیِ پایدار). **صفر تستِ واحدِ تازه** —
عمدی: کلِ ادعای این گام «پیکسل روی بومِ واقعی» است و jsdom پیکسل ندارد. **و همین
دلیلِ باگی بود که پیدا شد** (B-1): تستِ واحدِ M1 هم به همین دلیل نمی‌توانست بگیردش.

**★★ روشِ خواسته‌شده کار کرد — و یک باگِ واقعی گرفت.** قیدِ مالک این بود که مقادیر
با **پروجکشنِ دست‌محاسبه** سنجیده شوند، نه با خروجیِ خودِ کد. اگر انتظار را از
`sceneToOverlayPixel` می‌ساختم، هر هفت تست سبز می‌شدند و هیچ‌کس نمی‌فهمید مکان‌نما
روی صفحه جای غلطی است. **این دقیقاً همان سبزِ دروغینی است که این پروژه از آن
می‌ترسد.**

**آنچه کارِ M2 بود، انجام و آزموده شد:**

| زیرمورد | وضعیت |
|---|---|
| sync + نگهبانِ echo با دو موتورِ واقعی | ✅ (با گوش‌دادن به `pageerror`، چون نگهبان **خطا پرتاب می‌کند**) |
| `PeerState` → `transform`ِ هر سه لایه | ✅ دقیقاً برابرِ پروجکشنِ دست‌محاسبه |
| re-project با **panِ خالص** و zoom | ✅ با **چرخِ واقعیِ ماوس** |
| follow با zoomِ متفاوتِ دو پنل | ✅ دقیقاً وسطِ بوم |
| **جای‌گیریِ نهاییِ حضور روی صفحه** | ✅ بعد از رفعِ **B-1** (باگِ RTLِ CSSِ M1 که همین تست پیدایش کرد) |

**سه قیدِ مالک، هر سه رعایت شد:** پروجکشن با تابعِ **مشترکِ** `sceneToOverlayPixel`
(نه نسخه‌ی دوم) · نما از **`onScrollChange`** (نه `getAppState`) · و سنجش با
**پروجکشنِ دست‌محاسبه**.

**⚠️ سه چیز که تست‌ها بیرون کشیدند و هیچ‌کدام حدس نبود:**

۱. **`updateScene`ِ برنامه‌ای `onScrollChange` نمی‌دهد** — پس برای آزمودنِ
   re-project **باید** چرخِ واقعیِ ماوس زد. با `updateScene`، پیاده‌سازیِ **درست**
   هم دوباره پروجکت نمی‌کرد و تست بی‌معنا می‌شد. (و `follow` که خودش برنامه‌ای
   است، به همین دلیل باید `viewport` را دستی هم به‌روز کند.)
۲. **یک چرخِ ماوس یک رویداد نیست، یک رگبار است.** `follow` وسطِ رگبار اجرا می‌شد و
   `onScrollChange`ِ بعدی مقدارش را پس می‌گرفت. رفع: صبر تا **آرام‌گرفتنِ** نما
   (دو خواندنِ پیاپیِ یکسان) — نه مهلتِ ثابت.
۳. **خواندنِ «نما» و «پیکسل» در دو رفت‌وبرگشتِ جدا، یک رندرِ ری‌اکت فاصله دارد.**
   اولین نسخه نما را از **قبلِ** `follow` می‌خواند و `transform` را از **بعدش** —
   تست قرمز شد در حالی که کد درست بود. حالا هر دو در **یک** `evaluate`.
۴. ⚠️ **یک flakeِ ۱ در ۳ که فقط در اجرای کامل دیده می‌شد — و باگِ خودِ تست بود.**
   تستِ هاله شناسه‌ی انتخاب را مستقیم `emitSelection` می‌کرد، ولی خودِ پنل هم روی
   `onChange` انتخاب را پخش می‌کند؛ یک `onChange`ِ دیرهنگامِ بعد از ساخت، مقدارِ
   تزریقی را با آرایه‌ی **خالی** بازمی‌نوشت. رفع: انتخاب با **کلیکِ واقعی** —
   یعنی یک منبع، همان مسیری که کاربر می‌رود. (۶ تکرار + سه اجرای کامل، پایدار.)

**⚠️ و یک برخوردِ تازه با تست‌های قبلی:** ردیفِ آواتارِ همتاها گوشه‌ی بالای بوم
می‌نشیند (در RTL بالا-چپ) و **کلیکِ فوکوسِ** شش تستِ undo/دارایی را می‌بلعید. نقطه‌ی
کلیک به `y=300` رفت. باگ نیست — رابطِ واقعی حالا آنجاست.

**⚠️ یک گپِ سطحِ M1 که همین‌جا دیده شد و دور زده شد:** تبدیلِ **پیکسل → صحنه**
(برای مکان‌نمای محلی) از `@hamboom/canvas-core` صادر نشده و `HamboomCanvas` هم
`onPointerUpdate`ِ موتور را — که مختصاتِ **صحنه** می‌دهد — پاس نمی‌دهد. در دمو
فرمولِ معکوس دستی نوشته شد، با کامنتِ صریح. **M3 قبل از تکرارش باید تکلیفش را
روشن کند.**

---

## فاز ۴ — سرور

### گام ۴٫۱ — دست‌دادن و احراز هویت ✅ (۱۴۰۵/۰۵/۱۷)

**۲۳ تستِ تازه** (`realtime` ۲ → ۲۵). همه با **سرور و سوکتِ واقعی**، نه ماک: کلِ
ادعای این گام «چه چیزی روی سیم رد و بدل می‌شود و در چه ترتیبی» است، و ماک همان
ترتیبی را تایید می‌کند که خودمان نوشته‌ایم.

**★ قیدِ D-2 رعایت شد.** `RtTokenClaims` داخلِ خودِ پورت زندگی می‌کند و
`shared-types` **یک خط هم** عوض نشد. `BoardRole` هم از `ydoc-schema` قرض گرفته
شد نه تکرار — همان نقشی که روی سیم می‌رود باید همان باشد که توکن حمل می‌کند.

**★★ سه تصمیمِ طراحی که در متنِ گام نبودند:**

| تصمیم | چرا |
|---|---|
| **دو نوعِ ردکردن** — مسیرِ اشتباه بدونِ upgrade، توکنِ بد **با** upgrade و `HB_ERROR` | اگر در سطحِ HTTP رد شود، مرورگر فقط یک خطای عمومی می‌بیند و کلاینت نمی‌فهمد باید توکن را **تازه** کند یا کاربر را به ورود بفرستد |
| گیتِ production روی **خودِ پیاده‌سازی** (`developmentOnly`)، نه روی پرچمِ config | با پرچم، اولین مسیرِ فراموش‌شده دورش می‌زد؛ با علامت روی پیاده‌سازی، **هر** تزریقی از گیت رد می‌شود |
| `maskSubject` تنها راهِ لاگِ شناسه + `redactSecrets` روی **کلِ خط** | P7 به‌عنوان **ساختار**، نه قاعده: چیزی که از قلم می‌افتد همیشه همان فیلدی است که کسی فکرش را نکرده |

**دو کدِ تازه‌ی خطا** (`TOKEN_INVALID`، `TOKEN_MISSING`) در `ydoc-schema` — نه
`shared-types`. سه وضعیتِ احراز هویت سه **کارِ متفاوت** از کلاینت می‌خواهند
(ورود / تازه‌کردن / رویدادِ امنیتی)، پس یک کدِ مشترک یعنی پیامِ غلط به کاربر.
⚠️ ولی توکنِ معتبرِ **بوردِ دیگر** عمداً `FORBIDDEN` می‌گیرد تا این خطا به ابزارِ
شمارشِ بورد تبدیل نشود. سازگاریِ رو به جلوی گام ۲٫۴ همین را ممکن کرد.

**⚠️ JWT دستی سنجیده می‌شود، بدونِ کتابخانه — با هزینه‌ی صریح.** این کد قرار است
با آمدنِ `auth-core`ِ M3 **حذف** شود؛ وابستگیِ runtime برایش بدهیِ بی‌دلیل بود (و
P1 هم هر پکیجِ تازه را از گیتِ لایسنس رد می‌کند). هزینه‌اش این است که
اعتبارسنجیِ JWTِ دست‌نویس جای اشتباهِ کلاسیک دارد، پس هر سه‌ی مهم **تستِ حمله**
دارند: سردرگمیِ `alg` (`none` رد می‌شود) · مقایسه‌ی **زمان‌ثابت** · و `exp`ِ
**اجباری** (توکنِ بدونِ انقضا رد می‌شود، نه «برای همیشه معتبر»).

**⚠️ یک ناسازگاری که حین کار رفع شد:** گیتِ production ابتدا **همزمان** throw
می‌کرد در حالی که تابع `Promise` برمی‌گرداند — یعنی صداکننده‌ای که `.catch()`
می‌نوشت آن را از دست می‌داد. برای یک گیتِ **امنیتی**، «بسته به اینکه چطور صدایش
بزنی فرق می‌کند» پذیرفتنی نیست. همان اصلاح روی `verify` هم اعمال شد.

**⚠️ و یک flakeِ قدیمی که همین گیت بیرونش کشید:** تستِ «ساعتِ lib0» از گام ۳٫۵
یک مبدأ برای **دو ساعتِ متفاوت** گرفته بود (`expected 59999 to be 60000`) —
ساعتِ ساختگی در لحظه‌ی `useFakeTimers` یخ می‌زند ولی مبدأ از ساعتِ **واقعی**
خوانده شده بود. طنزش این است که همان تست قرار بود درباره‌ی قاطی‌کردنِ همین دو
ساعت هشدار بدهد. حالا دو مبدأ دارد؛ سه اجرا پایدار.

### گام ۴٫۲ — چرخه‌ی عمرِ اتاق و مرزِ اعتماد ✅ (۱۴۰۵/۰۵/۱۷)

**۱۵ تستِ تازه** (`realtime` ۲۵ → ۴۰).

**★★ رفتارِ انتخاب‌شده برای عنصرِ نامعتبر: قرنطینه.** پینِ گام ۲٫۱ می‌گفت
«**سکوت جواب نیست** — شمرده و لاگ شود، نه بی‌صدا رد؛ و یک عنصرِ خراب نباید کلِ
بورد را خالی کند». سه گزینه بود:

| گزینه | چرا |
|---|---|
| **قرنطینه** (انتخاب‌شده) | از سندِ درون‌حافظه‌ای برداشته، شمرده و لاگ می‌شود. عبارتِ «نه بی‌صدا **رد** شود» خودش می‌گوید رد کردن درست است، فقط باید صدادار باشد |
| نگه‌داشتن و سرو کردن | مرزِ اعتماد را بی‌معنا می‌کند: هر کلاینت باید خودش دفاع کند، در حالی که `readElement`ِ کلاینت هم اعتبارسنجی نمی‌کند |
| ردِ کلِ بورد | صریحاً همان چیزی که پین منعش کرده |

⚠️ **هزینه‌اش صریح است:** قرنطینه یعنی حذف، و حذف یعنی از دست رفتنِ آن عنصر. برای
گام ۴٫۴ ثبت شد که snapshot از سندِ قرنطینه‌شده این پاک‌سازی را **دائمی** می‌کند —
آنجا باید تصمیم گرفته شود آیا مطلوب است یا باید نسخه‌ی خام آرشیو شود.

**چهار نوع ناسالمی گرفته می‌شود** و هر چهار تست دارند: مقداری که `Y.Map` نیست ·
`readElement` که می‌ترکد (پس در `try` است، وگرنه یک عنصرِ خراب کلِ بارگذاری را
می‌انداخت) · شکلی که با `hbElement` نمی‌خواند · و ★ **کلیدی که با `element.id`
نمی‌خواند** — که در schema نیست ولی یعنی سند از دو مسیر نوشته شده و هر ارجاعی
(`frameId`، `boundElements`) می‌تواند به جای اشتباه برود.

★ **و نگهبانِ جهتِ منفی:** سندِ سالم هیچ‌چیز را قرنطینه نمی‌کند. بدونش یک
اعتبارسنجیِ بیش‌ازحد سخت‌گیر که بورد را خالی می‌کرد هم «سبز» به نظر می‌رسید.

**★★ نشتِ دومی که در متنِ گام نبود و تست پیدایش کرد — شکافِ علّی.** اولین نسخه‌ی
تستِ بارگذاری، snapshot و update را از **دو سندِ جدا** ساخت و عنصرِ دوم اصلاً
نیامد. علتش همان تله‌ی گام ۳٫۱ بود، این‌بار سمتِ **پایداری**: Yjs updateِ
بی‌پیشینه را در `pendingStructs` بایگانی می‌کند و **هیچ خطایی نمی‌دهد**.

یعنی اگر لاگِ updateِ گام ۴٫۳ با snapshot پشتِ سرِ هم نباشد، بورد **ناقص** بالا
می‌آید و کسی نمی‌فهمد. حالا مرزِ اعتماد این را هم می‌بیند: `pendingStructs` در
گزارشِ بارگذاری هست و با `logger.error` گزارش می‌شود.
⚠️ `store.pendingStructs` **API عمومی نیست** — دسترسی محافظت‌شده است تا اگر شکلش
عوض شد بارگذاری نترکد، و تستِ صریحش همان موقع قرمز می‌شود.
★ **قیدِ گام ۴٫۳:** لاگِ update باید از `doc.on("update")`ِ **خودِ اتاق** بیاید و
با snapshot پیوسته باشد.

**دو تصمیمِ ریزِ دیگر:** سقفِ نود **قبل از** بارگذاری بررسی می‌شود (وگرنه سند را
از دیتابیس می‌کشیم و بعد دور می‌اندازیم) · و `SERVER_BUSY` کدِ **تازه**‌ای است
چون کارِ کلاینت با `DOC_TOO_LARGE` فرق می‌کند: یکی موقتی است و باید دوباره تلاش
کند، آن یکی دائمی.

**★ پایه‌ی مشترکِ `RtProtocolError`:** گام ۴٫۱ یک منبعِ رد داشت، ۴٫۲ منبعِ دوم را
آورد و همان‌جا معلوم شد سرور نباید بداند خطا از کدام لایه است — فقط کدش. یک
مسیرِ رد در `server.ts`، نه یکی به‌ازای هر لایه. تستِ یکپارچه‌اش سقفِ نود را از
راهِ سوکتِ **واقعی** می‌سنجد.

---

### گام ۴٫۳ — پایداری: لاگِ update در Postgres ✅ (۱۴۰۵/۰۵/۲۰)

**۱۰ تستِ تازه** (`realtime` ۴۰ → ۵۰) + یک سنجه‌ی زنده:
[`scripts/rt-durability.ts`](scripts/rt-durability.ts) (`pnpm rt:durability`).

**عدد گرفته شد و batch لازم نشد.** ADR-009 تصمیمِ «همزمان یا batchِ ۱ثانیه‌ای» را
صریحاً به اندازه‌گیریِ M2 موکول کرده بود. فاصله‌ی ژست تا `saved`ِ واقعی
**۳۰–۳۵ms** درآمد (شش اجرا: ۳۳/۳۰/۳۵/۳۱/۳۴/۳۲)، یعنی مسیرِ همزمان **زیرِ آستانه‌ی
ادراک** است و batch فقط یک پنجره‌ی از‌دست‌رفتنی اضافه می‌کرد — دقیقاً همان چیزی که
ADR می‌خواست نبیند. ADR-009 با همین عدد و **قیدِ محیطش** به‌روز شد: این مشاهده‌ی
Postgresِ همان ماشین است، نه ادعای معماری؛ با دیتابیسِ شبکه‌ای باید دوباره سنجید.

**`seq` درونِ همان `INSERT` گرفته می‌شود.** وسوسه‌اش `SELECT max(seq)` بعد `INSERT`
بود، ولی آن دو دستور یک پنجره‌ی مسابقه بینشان دارند. ایندکسِ یکتای
`(board_id, seq)` سدِ **دوم** می‌مانَد (تلاشِ دوباره روی ۲۳۵۰۵)، نه جایگزینِ اولی.
و `saved` ساعتِ **دیتابیس** را حمل می‌کند (`RETURNING created_at`) نه ساعتِ اپ —
وگرنه دو تب روی دو نود دو عدد نشان می‌دهند.

**★ و `saved`ِ خوش‌بینانه‌ی کلاینت حذف شد.** `canvas-sync` هنگام اتصال خودش
`status: "saved"` می‌گذاشت. حالا `unsaved` می‌گذارد و **تنها منبعِ `saved`**
پیامِ `HB_ROOM_INFO`ِ سرور است. یک نشانگرِ ذخیره که خودش تصمیم می‌گیرد، همان
خوش‌بینی‌ای است که ADR-009 منعش کرده.

#### ★★ تستِ SIGKILL شکست خورد — و سه باگِ واقعی داشت

قیدِ مالک این بود: «اگر کلاینت *ذخیره شد* دیده و بعد از kill داده نیست، گام قبول
نیست». اولین اجرا **دقیقاً همان** را نشان داد: `saved` در ۵۹ms، و **صفر ردیف** در
دیتابیس. ⚠️ و در همان لحظه **هر ۵۰ تستِ واحد سبز بودند.**

| باگ | چه بود | چرا واحد نگرفتش |
|---|---|---|
| **۱. `22P02` روی هر append** | `origin_user_id` ستونِ `uuid` است، ولی `sub`ِ توکن لزوماً uuid نیست → **هیچ** updateی نمی‌نشست | لاگِ حافظه‌ای uuid نمی‌فهمد |
| **۲. `saved`ِ لحظه‌ی اتصال** | باگِ ۱ را می‌پوشاند: سنجه `saved` را می‌دید و خوشحال می‌شد | سنجه هنوز وجود نداشت |
| **۳. پیامِ گم‌شده روی `open`** | `ws` پیامِ رسیده پیش از وصل‌شدنِ شنونده‌ی اتاق را **بی‌صدا** می‌انداخت | تست‌ها بعد از استقرار پیام می‌فرستادند |

**رفعِ ۱ — دوام نباید به یک فیلدِ ممیزی گره بخورد.** `auditableUserId` فقط uuidِ
واقعی را عبور می‌دهد و بقیه را `NULL` می‌کند، **با یک هشدارِ یک‌باره** (نه هر
update، وگرنه لاگ پر می‌شود). ممیزی از دست می‌رود ولی دیده می‌شود؛ دوام قربانی
نمی‌شود.

**رفعِ ۲ — سنجه به `seq` گره خورد، نه به دیدنِ صرفِ `saved`.** حالا
`waitForSavedAfter(seqAtJoin)` منتظرِ **جلو رفتنِ** شماره می‌مانَد. درسش کلی‌تر از
این باگ است: یک وضعیتِ درست که **قبلاً** هم درست بوده، هیچ چیزی را اثبات نمی‌کند.

**رفعِ ۳ — پنجره‌ی دست‌دادن `pause` می‌شود.** سوکت پیش از هر `await` متوقف و بعد
از تحویل به اتاق `resume` می‌شود.
⚠️ **و روی مسیرِ ردکردن هم باید `resume` شود** — بارِ اول نشد و دستِ‌دادنِ بستن
معلق ماند: چهار تستِ ردِ گام ۴٫۱ قرمز شدند و همان‌جا گرفته شد.

#### ★★ سبزِ دروغینِ تستِ دودِ ۰٫۲ — واقعی شد (تاییدِ مالک)

ادعای آن تست معماری بود: «سرور می‌تواند `ydoc-schema` را در Nodeِ خالص مصرف کند».
ولی زیرِ **vitest** اجرا می‌شد، که همان Node نیست — resolverِ Vite پسوند را حدس
می‌زند و TypeScript را transform می‌کند. Node هیچ‌کدام را نمی‌کند.

حالا یک **فرایندِ Nodeِ جدا** (`node -e` با importِ پویا) ماژول را بار می‌کند، و
همان لحظه دو چیزی که پشتِ سبزِ دروغین پنهان بودند بیرون آمدند:
importهای نسبیِ **بدونِ پسوند** (`ERR_MODULE_NOT_FOUND`) و **parameter property**
(`ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` — Node فقط تایپ strip می‌کند، transform نه).
شش جا اصلاح شد. نگهبانش خودآزمون شد: با برگرداندنِ عمدیِ یک parameter property
**قرمز شد**.
⚠️ روی ویندوز `import("G:\\…")` با `ERR_UNSUPPORTED_ESM_URL_SCHEME` می‌افتد
(`g:` را پروتکل می‌بیند) — باید `file://` URL باشد.

**`allowImportingTsExtensions` به `packages/tsconfig/base.json` رفت،** نه به تک‌تکِ
پکیج‌ها: هر پکیج **سورسِ** وابستگی‌هایش را هم typecheck می‌کند، پس گذاشتنش فقط روی
`shared-types` پنج خطای `TS5097` در `canvas-core` می‌داد. با `noEmit` سازگار است.
★ **تاییدِ مالک گرفته شد** چون به `shared-types` دست می‌خورد — ولی صفر تغییر در
تایپ، API یا رفتار؛ فقط پسوندِ import.

### گام ۴٫۴ — snapshot و فشرده‌سازی ✅ (۱۴۰۵/۰۵/۲۰)

**۱۵ تستِ تازه** (`realtime` ۵۰ → ۸۳ با تست‌های دیگرِ همین گام) + سنجه‌ی زنده‌ی
[`scripts/rt-compaction.ts`](scripts/rt-compaction.ts) (`pnpm rt:compaction`).

#### ★★ ارثیه‌ی ۱ — «قرنطینه + snapshot» تصمیم گرفته شد

تصمیمِ معلقِ گام ۴٫۲ این بود: اگر از سندِ **قرنطینه‌شده** snapshot بگیریم، آن حذف
**دائمی** می‌شود. جواب: **snapshot از لاگ بازسازی می‌شود، نه از سندِ اتاق.** پس
قرنطینه همان چیزی می‌مانَد که ۴٫۳ تعریفش کرد — یک نمای زمانِ بارگذاری — و داده‌ی
خام در snapshot هم زنده می‌مانَد.

★ **مهارش ساختاری است، نه قاعده‌ای:** امضای `compact(boardId, targetSeq)` اصلاً
`Y.Doc` نمی‌گیرد، پس «اشتباهاً از سندِ زنده snapshot گرفتن» چیزی نیست که یادت
برود؛ چیزی است که **نمی‌شود نوشت**. سودِ دومش: snapshot از **همان مسیری** ساخته
می‌شود که بارگذاری می‌سازد.

#### ★★ ارثیه‌ی ۲ — حذف، تله‌ی شکافِ علّی را مسلح می‌کند

تا وقتی همه‌ی updateها در لاگ‌اند، بدترین حالت کندی است. بعد از `prune`، یک مرزِ
جابه‌جا یعنی بوردِ **بی‌خطا ناقص** و بی‌راهِ برگشت. پس ترتیب:

| مرحله | اگر اینجا بیفتد |
|---|---|
| ۱. بازسازی از لاگ | هیچ اتفاقی نیفتاده |
| ۲. ★ ردِ شکافِ علّی در **خودِ بازسازی** | snapshot گرفته **نمی‌شود** — وگرنه نقص دائمی می‌شد |
| ۳. نوشتنِ بایت‌ها | فایلِ یتیم، لاگ سالم |
| ۴. ★★ **بازخوانی از انبار** و مقایسه‌ی state vector | همان‌جا لو می‌رود، قبل از هر حذفی |
| ۵. ثبتِ رکورد ← **نقطه‌ی commit** | فایلِ یتیم، لاگ سالم |
| ۶. `prune` | بی‌خطر: updateهای پاک‌نشده دوباره اعمال می‌شوند و Yjs idempotent است |

⚠️ ترتیبِ برعکس (`prune` قبل از ثبت) **تنها** ترتیبی است که داده را می‌برد.
چهار تستِ «حذفِ زودهنگام» این را قفل کرده‌اند — از جمله انباری که **دروغ می‌گوید**:
`put` موفق برمی‌گردد ولی بایت‌های ناقص می‌نشیند. بدونِ مرحله‌ی ۴، همان‌جا داده‌ی
واقعی حذف می‌شد.

#### ★★★ و سنجه‌ی ۵۰۰تایی یک باگِ واقعی گرفت — ۴۳ از ۵۰۰ append گم می‌شد

**۷۹ تستِ واحد سبز بودند.** علتش `READ COMMITTED` است: `seq` از
`GREATEST(MAX(seq), …) + 1` می‌آید و دو تراکنشِ همزمان ردیفِ commitنشده‌ی هم را
**نمی‌بینند**، پس هر دو یک عدد می‌گیرند و یکی به ایندکسِ یکتا می‌خورد. پنج تلاش
تمام می‌شود و update **از بین می‌رود** — بی‌صدا، چون کاربر فقط `unsaved` می‌بیند.

⚠️ **تستِ گام ۴٫۳ هیچ شانسی نداشت: آنجا یک update بود.** همزمانی فقط زیرِ بار
دیده می‌شود — و این دقیقاً همان الگوی درسِ ۴٫۳ است، یک پله بالاتر.

رفع: **تخصیصِ `seq` به‌ازای هر بورد سریالی است**
([ADR-037](ARCHITECTURE_DECISIONS.md#adr-037)) — اندازه‌گیری‌شده **~۱۵۷۰ append در
ثانیه** برای یک بورد، و بوردهای مختلف کاملاً موازی. ایندکسِ یکتا و تلاشِ دوباره
می‌مانند، ولی از این به بعد نقششان «سدِ آخر برای چند نود» است تا گام ۴٫۷.
★ یک هدیه‌ی جانبی: `HB_ROOM_INFO.seq` دیگر **عقب نمی‌رود** (پیش‌تر یک appendِ
کندترِ با seqِ کوچک‌تر عددِ بزرگ‌ترِ قبلی را بازمی‌نوشت).

#### ★★ دو تله‌ی `createBoardDoc` که همین سنجه بیرون کشید

`createBoardDoc()` هر بار `meta.schemaVersion` را با یک **`clientID`ِ تازه**
می‌نویسد. دو جا این را می‌ساخت:

| کجا | چه می‌شد |
|---|---|
| **فشرده‌سازی** | هر snapshot یک مدخلِ کلاینتِ دیگر را **برای همیشه** در state vector می‌نشاند — رشدِ بی‌کران در همان ساختاری که قرار بود کوچک شود. نگهبانش تستی است که بعد از پنج دور `expected 6 to be 1` می‌داد |
| **بارگذاریِ اتاق** | هر بار که اتاق بالا می‌آمد یک opِ زائد؛ و کلاینتی که بعد از ری‌استارت دوباره وصل شود همان op را در step2 به سرور **برمی‌گرداند** — این‌بار با originِ کلاینت، یعنی **پایدار** |

هر دو رفع شد: فشرده‌سازی با `new Y.Doc()` بازسازی می‌کند و اتاق فقط برای بوردِ
**واقعاً نو** `createBoardDoc` می‌زند.

#### ★ و یک درس درباره‌ی خودِ سنجه

اولین نسخه ۵۰۰ پیام را در **یک تیک** فرستاد و `seq = 1` گرفت. باگ نبود: Yjs
`applyUpdate`هایی را که در پاکسازیِ یک تراکنش به هم می‌رسند **ادغام می‌کند**، پس
۵۰۰ پیام یک ردیفِ لاگ شد (و داده هم گم نشد). ولی آن‌وقت سنجه چیزی را که ادعا
می‌کرد نمی‌سنجید — آستانه روی **ردیف‌های لاگ** است. حالا هر ژست منتظرِ ackِ خودش
می‌مانَد؛ هم قطعی است، هم همان کاری که کاربرِ واقعی می‌کند.

★ **و مرجعِ مقایسه هم عوض شد:** برابریِ خامِ state vector با کلاینتِ اول اشتباه
بود، چون هر نمونه‌ی اتاق یک opِ `meta`ِ زودگذر می‌زند که هرگز پایدار نمی‌شود.
مرجعِ درست **خودِ دیتابیس** است: سندی که از `snapshot + updateهای باقی‌مانده`
ساخته می‌شود، بدونِ رد شدن از هیچ کدِ سروری — یک شاهدِ **مستقل** که اگر بارگذاریِ
سرور و محتوای دیتابیس واگرا شوند، لو می‌دهد.

### گام ۴٫۵ — اعمالِ مجوز روی هر update ✅ (۱۴۰۵/۰۵/۲۰)

**۱۸ تستِ تازه** (`realtime` ۸۳ → ۱۰۶) + سنجه‌ی زنده‌ی
[`scripts/rt-permission.ts`](scripts/rt-permission.ts) (`pnpm rt:permission`).

**جای بررسی، مسیرِ پیام است — و قبل از `readSyncMessage`.** ADR-012 می‌گوید «در
هر update، نه فقط هنگام اتصال». و ترتیب اجباری است: Yjs **عقب‌گرد ندارد**، پس
updateی که اعمال شود در سند مانده — حتی اگر بلافاصله خطا بدهیم، پخش نکنیم و
پایدار نکنیم. «اعمال کن، بعد پشیمان شو» در CRDT وجود ندارد.

★ **و `step1` عمداً استثناست.** پیامِ syncِ Yjs سه زیرنوع دارد و فقط دوتاشان
نوشتن‌اند؛ اگر «چه چیزی کم دارم؟» را هم می‌بستیم، `viewer` وصل می‌شد و بوردِ
**خالی** می‌دید — یعنی نقشِ تماشاگر بی‌معنا می‌شد. تستِ جهتِ منفی همین را قفل
می‌کند.

#### ★★ حفره‌ای که متنِ گام نگفته بود: توکن حرفِ آخر را می‌زد

نقش داخلِ `rtToken` است و **توکن تغییر نمی‌کند**. پس اجرای مجوز روی هر update،
به‌تنهایی، فقط **تا اولین اتصالِ مجدد** دوام داشت: کاربرِ تنزل‌داده‌شده کافی بود
تبش را ببندد و باز کند تا با همان توکن دوباره `editor` شود.

★ رفعش همان چیزی است که ADR-012 از اول می‌گفت — نقش یک مقدارِ **محاسبه‌شده**
است، نه یک claimِ ذخیره‌شده: پورت متدِ اختیاریِ `currentRole` گرفت و دست‌دادن از
آن می‌پرسد.
⚠️ **و `undefined` با `null` یکی نیست:** اولی یعنی «نظری ندارم، نقشِ توکن بمانَد»
و دومی یعنی «دسترسی برداشته شده» → اتصال رد می‌شود. `??` روی این دو رفتارِ یکسان
می‌داد و یک کاربرِ اخراج‌شده تا انقضای توکن وصل می‌مانْد.

**تغییرِ نقش وسطِ کار push است، نه polling** (`applyRoleChange`): هیچ پنجره‌ی «تا
انقضای کش» وجود ندارد که کاربرِ تنزل‌داده‌شده در آن هنوز بنویسد. نقش روی **خودِ
نشست** عوض می‌شود، نه در یک نقشه‌ی کنارش — چون `handleMessage` روی
`session.role` قضاوت می‌کند و هر نگه‌داریِ موازی یک منبعِ دومِ حقیقت بود.

#### ★★ ADR-038 — `HB_ERROR` همیشه یعنی «ببند» نیست

`protocol.ts` نوشته بود «`{code, message}` سپس بستنِ اتصال»، ولی خودِ TODOِ ۴٫۵
برای updateِ `viewer` «رد + `HB_ERROR{FORBIDDEN}`» خواسته. بستن در این حالت هم
**غلط** است (تماشاگر حقِ دیدن دارد) و هم **بی‌اثر** (با همان توکن برمی‌گردد —
یک حلقه‌ی بستن و اتصالِ مجدد که هیچ‌چیز را اعمال نمی‌کند). پس: ردِ **دست‌دادن**
می‌بندد، ردِ **پیام** نمی‌بندد و `HB_PERMISSION` را هم همراه می‌برد.

#### ★ و باز هم سنجه‌ی زنده چیزی گرفت که تست‌های واحد نگرفتند

هر اتصالِ **کاملاً سالمِ** `viewer` یک `FORBIDDEN` می‌گرفت: پروتکلِ sync ایجاب
می‌کند کلاینت به step1ِ سرور با step2 جواب بدهد، و آن step2 برای تماشاگرِ تازه
**صفر op** دارد. فنی درست بود، در عمل یک هشدارِ امنیتیِ **کاذب** به‌ازای هر
اتصالِ سالم — و هشداری که همیشه می‌آید همان هشداری است که کسی نمی‌خوانَد.
★ حالا updateِ بدونِ op «نوشتن» شمرده نمی‌شود، و هزینه‌ی این بررسی **فقط** روی
مسیرِ ردشده است.

⚠️ **گپِ G-2 (ثبت‌شده، بلوکه‌کننده نیست):** `commenter` فعلاً کنارِ `viewer` است.
تشخیصِ اینکه یک updateِ باینری کدام ریشه را دست می‌زند — **قبل از اعمالش** —
کارِ کوچکی نیست. انتخابِ آگاهانه‌ی fail-closed: مسیرِ کامنت هنوز در کلاینت وجود
ندارد (کارِ M3)، پس امروز چیزی نمی‌شکند؛ برعکسش یعنی `commenter` می‌توانست کلِ
بورد را جابه‌جا کند.

### گام ۴٫۶ — awareness و ephemeral سمتِ سرور ✅ (۱۴۰۵/۰۵/۲۰)

**۱۳ تستِ تازه** (`realtime` ۱۰۶ → ۱۱۹) + سنجه‌ی زنده‌ی
[`scripts/rt-presence.ts`](scripts/rt-presence.ts) (`pnpm rt:presence`).

**سرور یک `Awareness`ِ واقعی نگه می‌دارد، نه بازپخشِ خالی.** بازپخش ساده‌تر بود و
دو چیز را می‌شکست: کلاینتی که وسطِ کار وصل شود تا **اولین تکانِ** هر همتا او را
نمی‌بیند (و همتای ساکن ممکن است هرگز تکان نخورد)، و پاک‌کردنِ مکان‌نمای رفته یعنی
فرستادنِ به‌روزرسانی با clockِ **بعدیِ** همان `clientID` — که بدونِ نگه‌داشتنِ
clockها ساختنی نیست. `setLocalState(null)` سرور را یک **دفتر** نگه می‌دارد، نه یک
شرکت‌کننده.

★ **تضمینِ «ephemeral پایدار نمی‌شود» ساختاری است:** هیچ‌کدام از این دو مسیر به
سند دست نمی‌زنند، و پایداری به `doc.on("update")` گره خورده. یعنی لازم نیست کسی
یادش بماند ذخیره نکند.

★★ **و پینِ گام ۳٫۵ اینجا جواب گرفت:** آنجا نوشتیم جاروی ۳۰ثانیه‌ای با زمان‌بندِ
ساختگی آزمودنی نیست چون `lib0/time` مقدارِ `Date.now` را در بارگذاریِ ماژول
می‌گیرد، و «مسیرِ واقعی این است که در فاز ۴ سرور قطعِ سوکت را می‌بیند». همان مسیر
حالا هست — و **بهتر از جاروست**: پاک‌سازی فوری است، نه تا ۳۰ ثانیه بعد. سنجه با
`terminate()` (نه بستنِ مودبانه) می‌سنجدش.

#### ★★★ و همین گام یک باگِ کهنه و بی‌صدا را بیرون کشید — F-1 بسته شد

بعد از تغییراتِ ۴٫۶، سنجه‌ی **فشرده‌سازی** (گام ۴٫۴) در ۳ اجرا از ۴ افتاد:
state vectorِ کلاینتِ تازه یک مدخلِ **تک‌opیِ** اضافه داشت که در دیتابیس نبود.
زنجیره‌اش این بود:

۱. سرور برای بوردِ نو `meta.schemaVersion` را با `clientID`ِ **خودش** می‌نوشت.
   آن op هرگز پایدار نمی‌شد (originش `ClientOrigin` نیست).
۲. کلاینت هم `meta.schemaVersion`ِ خودش را داشت. دو نوشتن روی **یک کلید** یعنی
   یکی برنده و دیگری **حذف**.
۳. وقتی opِ سرور برنده می‌شد، کلاینت opِ خودش را حذف‌شده علامت می‌زد — و آن
   **delete** در updateِ بعدیِ کلاینت **پایدار می‌شد**.
۴. پس لاگ opِ meta را داشت با یک سنگِ قبر رویش: `getSchemaVersion` می‌شد
   `undefined`، هر بارگذاری دوباره **مهر** می‌زد، و هر مهر یک opِ سرورِ دیگر بود.

⚠️ **کاملاً بی‌صدا:** عناصر همه سالم بودند و sync کار می‌کرد؛ فقط نسخه‌ی schemaِ
بورد گم می‌شد. و **تصادفی** بود — بستگی داشت به اینکه کدام `clientID` برنده شود،
برای همین تا امروز خودش را نشان نداده بود.

★ **رفع، هم‌راستا با مرزی که ۴٫۲ کشید (فقط کلاینت‌ها می‌نویسند):** سندِ اتاق
همیشه خالی ساخته می‌شود و migration روی سندِ **تهی** اجرا نمی‌شود — آنجا چیزی
برای مهاجرت نیست و تنها اثرش همان مهر بود. اولین کلاینت `meta` را با خودش
می‌آورد و **آن** پایدار می‌شود.

✅ **این F-1 را در عمل می‌بندد** (به‌جای موکول کردنش): سوالِ F-1 این بود که «آیا
opهای سرور باید پایدار شوند؟» و جواب این شد که **سرور اصلاً نباید opی بنویسد**.
سه اجرای پیاپیِ سنجه‌ی فشرده‌سازی سبز، و هر سه با `migrated: null` — یعنی نسخه‌ی
schema در snapshot زنده است.
⚠️ **باقی‌مانده‌ی نظری:** بوردی که لاگش **غیرِ تهی** باشد ولی `meta` نداشته باشد
(نویسنده‌ای بیرون از کلاینت‌های ما) باز هم مهر می‌خورد و آن مهر پایدار نمی‌شود.
امروز چنین نویسنده‌ای وجود ندارد.

### گام ۴٫۷ — Redis pub/sub و قفلِ صاحب ✅ (۱۴۰۵/۰۵/۲۰)

**۱۵ تستِ تازه** (`realtime` ۱۱۹ → ۱۳۴) + سنجه‌ی زنده‌ی
[`scripts/rt-cluster.ts`](scripts/rt-cluster.ts) (`pnpm rt:cluster`) با **دو
پروسه‌ی واقعی**. وابستگیِ تازه: `ioredis` (MIT — گیتِ P1 با ۷۲۵ پکیج سبز).

**همه‌چیز پشتِ پورت مانْد** ([ADR-030](ARCHITECTURE_DECISIONS.md#adr-030)):
`BoardBus` و `OwnerLock` دو پورت‌اند با پیاده‌سازیِ Redis و پیاده‌سازیِ حافظه‌ای.
سودش فوری بود — کلِ منطقِ چندنودی با دو `RoomManager` روی یک گذرگاهِ حافظه‌ای
تست می‌شود، **بدونِ Redis**، و سنجه‌ی زنده فقط چیزهایی را می‌سنجد که واقعاً به
Redis نیاز دارند.

#### ★★ سه چیزی که ترتیب و اتمیک‌بودن در آن‌ها حیاتی بود

**۱. اشتراک _قبل از_ خواندن از دیتابیس، با بافر.** Redis pub/sub تحویل را تضمین
نمی‌کند: نودی که در لحظه‌ی انتشار مشترک نباشد پیام را **هرگز** نمی‌گیرد. ترتیبِ
برعکس یک شکافِ **دائمی** می‌ساخت — updateای که بینِ `load` و `subscribe` منتشر
شود نه در آن خواندن هست و نه بعداً می‌رسد.

**۲. تمدید و رهاکردنِ قفل با Lua.** «اگر مالِ من است، تمدیدش کن» با دو دستور یک
پنجره دارد که اجاره می‌تواند **بینشان** منقضی شود؛ آن‌وقت `EXPIRE`ِ ما اجاره‌ی
نودِ دیگر را تمدید می‌کند و **دو نود همزمان صاحب** می‌شوند — دقیقاً چیزی که این
قفل برای جلوگیری‌اش هست. ⚠️ همان شکلِ باگِ `SELECT max(seq)` بعد `INSERT` در گام
۴٫۳، این‌بار در Redis.

**۳. ADR-009 در خوشه.** نودی که نمی‌نویسد حق ندارد بگوید «ذخیره شد». صاحب بعد از
نوشتنِ واقعی `SAVED{seq}` را پخش می‌کند و هر نود از روی **آن** به کلاینت‌هایش
`saved` می‌گوید؛ تا آن لحظه `saving` می‌بینند. سنجه‌ی زنده هر دو کلاینت را
می‌سنجد، نه فقط آن که روی صاحب نشسته.

#### ★★★ ضدِ حلقه: تستی که خودش تست نبود

نسخه‌ی اولِ تست **انتشارها را می‌شمرد**. خودآزمونِ گیت نشان داد با برداشتنِ
`if (envelope.node === nodeId)` هر ۱۳۲ تست **سبز می‌مانند**.

★ علتش ظریف است: وقتی نود updateِ **خودش** را پس می‌گیرد، Yjs می‌بیند چیزی عوض
نشده، پس رویدادِ `update` نمی‌دهد و انتشارِ دومی رخ نمی‌دهد — یعنی حلقه‌ی
*تشدیدشونده* نمی‌سازد و شمارنده ساکت می‌مانَد. ولی بی‌اثر هم نیست: `EPHEMERAL`
بی‌قید و شرط به همه‌ی نشست‌های محلی پخش می‌شود، پس بدونِ گیت **فرستنده استروکِ
خودش را پس می‌گیرد**.

⚠️ درسش عمومی است: **ادعا را همان‌جا بسنج که اثر دارد**، نه جایی که شمردنش راحت
است. تستِ تازه مسیرِ دریافت را مستقیم با یک envelopeِ خودبرچسب می‌راند و بدونِ
گیت قرمز می‌شود — به‌همراه یک ضدِ ادعا (همان دو پیام از نودِ **دیگر** باید
پردازش شوند)، وگرنه یک `return`ِ بی‌قید و شرط هم پاس می‌شد.

#### ★ دو باگ که تست‌های خودم پیش از Redis گرفتند

| باگ | اثر |
|---|---|
| **ephemeral منتشر نمی‌شد** | فقط محلی پخش می‌شد؛ استروکِ زنده تنها برای کسانی دیده می‌شد که تصادفاً روی همان نود بودند |
| **نوشتنِ جبرانی در گرفتنِ _اولِ_ صاحبی هم اجرا می‌شد** | یک ردیفِ حالتِ کامل به‌ازای **هر باز شدنِ اتاق** — که کارِ فشرده‌سازی را بی‌صدا خنثی می‌کرد |

★ نوشتنِ جبرانی خودش لازم است، ولی فقط در **انتقالِ** صاحبی: نودی که تازه صاحب
می‌شود ممکن است updateهایی در حافظه داشته باشد که صاحبِ مرده هرگز ننوشته.
سنجه‌ی زنده همین را می‌سنجد (`SIGKILL` روی نودِ ۱ → نودِ ۲ صاحب شد و نوشتن ادامه
یافت).

⚠️ **و یک مدلِ غلط در خودِ تست:** اول به هر نود یک `MemoryOwnerLock`ِ جدا داده
بودم، پس **هر دو** صاحب می‌شدند و ردیف‌های تکراری ساختند. قفل در واقعیت **یک**
داور دارد؛ حالا دفترِ قفل بینِ نودها مشترک است.

### گام ۴٫۸ — heartbeat، خاموشیِ مودبانه، سقف‌ها ✅ (۱۴۰۵/۰۵/۲۰) — **فاز ۴ کامل**

**۱۰ تستِ تازه** (`realtime` ۱۳۴ → ۱۴۴) + سنجه‌ی زنده‌ی
[`scripts/rt-shutdown.ts`](scripts/rt-shutdown.ts) (`pnpm rt:shutdown`).

**heartbeat دو کار می‌کند و هر دو لازم‌اند:** اتصال را از دیدِ لودبالانسر زنده
نگه می‌دارد (فاصله کوتاه‌تر از idle timeoutِ Ingress — الزامِ ADR-006)، و ★
**اتصالِ نیم‌باز** را پیدا می‌کند. روی شبکه‌ای که وسط راه بسته می‌شود، TCP تا
دقایق «باز» می‌مانَد در حالی که هیچ بایتی رد نمی‌شود؛ آن سوکت **نه `close`
می‌دهد نه خطا**، یعنی مکان‌نمای کاربرِ رفته یخ می‌زند و اتاق هم تخلیه نمی‌شود.
⚠️ سوکتِ بی‌پاسخ `terminate` می‌شود نه `close` — همتای مرده دستِ‌دادنِ بستن را
هم تمام نمی‌کند.

**`/healthz` و `/readyz` عمداً یکی نیستند:** در خاموشی `readyz` رد می‌کند تا
لودبالانسر ما را کنار بگذارد، ولی `healthz` سبز می‌مانَد — اگر آن هم رد می‌شد،
K8s پاد را **وسطِ تخلیه** می‌کشت.

#### ★★ ترتیبِ خاموشی، و چرا از `main.ts` بیرون آمد

ترتیب **کلِ ادعای این گام** است: بدرقه با `1001` → تخلیه → snapshot → پس دادنِ
قفل → بستنِ منابع. اگر تخلیه قبل از بدرقه می‌آمد، کلاینت‌ها هنوز می‌نوشتند و صف
هیچ‌وقت خالی نمی‌شد.

★ پس در [`shutdown.ts`](apps/realtime/src/shutdown.ts) نشست، نه در `main.ts` که
عمداً تست به آن نمی‌رسد. حالا دو تست ترتیب را قفل کرده‌اند و با جابه‌جایی دو
خط قرمز می‌شوند.

★ **و نوشتن‌های در جریان شمرده می‌شوند:** سیگنالی که وسطِ یک `append` برسد دیگر
آن update را نمی‌برد. کلاینت هنوز «ذخیره شد» ندیده بود، پس ADR-009 فنی نقض
نمی‌شد — ولی کارِ کاربر **بی‌صدا** می‌رفت، و معیارِ پذیرش صریح است.

#### ★★ دو چیزی که سنجه گرفت و من اشتباه نوشته بودم

| اشتباه | چه شد |
|---|---|
| `shutdown()` بلافاصله برمی‌گشت | `close()`ِ بعدی سوکت‌ها را `terminate` می‌کرد و قابِ بستن **هرگز نمی‌رسید** — کلاینت **۱۰۰۶** می‌دید نه ۱۰۰۱. «مودبانه» یعنی صبر تا رسیدنِ خبر (با سقف، تا کلاینتِ مرده گروگان نگیرد) |
| `markAlive` روی رویدادِ `connection` | با `noServer: true` آن رویداد **اصلاً emit نمی‌شود** — `handleUpgrade` فقط callback را صدا می‌زند. هیچ سوکتی «زنده» علامت نمی‌خورد و **اولین تیکِ heartbeat همه‌ی کلاینت‌ها را می‌کشت** |

⚠️ دومی را تستِ «اتصالِ سالم چند دوره زنده می‌مانَد» گرفت — تستی که فقط به این
دلیل نوشته شد که ادعای مثبت هم نگهبان داشته باشد. بدونش heartbeat در تست سبز
بود و در تولید هر ۲۵ ثانیه همه را قطع می‌کرد.

#### ⚠️ محدودیتِ محیطی — ثبت‌شده، نه پنهان

**روی ویندوز نمی‌شود `SIGTERM` به فرزند فرستاد.** `child.kill("SIGTERM")` فرایند
را بی‌درنگ می‌کشد و هیچ handlerی اجرا نمی‌شود — آزموده شد: فرزندی با
`process.once("SIGTERM", …)` بدونِ اجرای handler با `exit code=1, signal=null`
تمام شد.

★ پس سنجه **همان تابعی** را صدا می‌زند که `main.ts` به سیگنال وصل کرده، و بقیه‌ی
مسیر کاملاً واقعی است (Postgres، Redis، سوکت، فایل‌سیستم). تحویلِ خودِ سیگنال کارِ
سیستم‌عامل است و روی لینوکس — جایی که مستقر می‌شویم — رفتارِ استانداردِ Node.
**این را ادعا نمی‌کنیم که آزموده‌ایم.**

#### ★ یک درسِ کوچک از خودِ سنجه

اولین نسخه «هیچ updateای گم نشد» را با `MAX(seq)`ِ `board_updates` می‌سنجید و
**اشتباه قرمز شد**: خودِ خاموشی snapshot می‌گیرد و ردیف‌ها را **حذف** می‌کند، پس
آن عدد صفر بود در حالی که هیچ‌چیز گم نشده بود. حقیقتِ پایدار «لاگ + snapshot»
است — همان نشانه‌ی بلندترین `seq` که [ADR-037](ARCHITECTURE_DECISIONS.md#adr-037)
تعریف کرده.

---

## ★★ جمع‌بندیِ فاز ۴ — کامل شد (۴٫۱ تا ۴٫۸)

`apps/realtime` یک سرورِ واقعی است: احراز هویت می‌کند، سند را نگه می‌دارد و
پایدار می‌کند، مجوز را روی **هر** update می‌سنجد، حضور را پخش می‌کند، روی چند نود
کار می‌کند، و مودبانه می‌رود. **۱۴۴ تستِ واحد + شش سنجه‌ی زنده.**

| گام | خروجی | نگهبانِ دائمی |
|---|---|---|
| ۴٫۱ | دست‌دادن + احراز هویت پشتِ پورت | سه کدِ ردِ متمایز · گیتِ production روی **خودِ پیاده‌سازی** · دو تستِ P7 (یکی نگهبان را می‌آزماید) |
| ۴٫۲ | چرخه‌ی عمرِ اتاق + **مرزِ اعتماد** | چهار نوع ناسالمی + **نگهبانِ جهتِ منفی** (سندِ سالم چیزی قرنطینه نمی‌کند) |
| ۴٫۳ | دوام **قبل از** ack ([ADR-009](ARCHITECTURE_DECISIONS.md#adr-009) با عددِ واقعی) | `pnpm rt:durability` — ژست → «ذخیره شد» → `SIGKILL` → داده هست |
| ۴٫۴ | snapshot + فشرده‌سازی با **ترتیبِ امنِ حذف** | `pnpm rt:compaction` + چهار تستِ «حذفِ زودهنگام» (یکی با انباری که **دروغ می‌گوید**) |
| ۴٫۵ | مجوز روی **هر** update ([ADR-012](ARCHITECTURE_DECISIONS.md#adr-012)) | `pnpm rt:permission` — مهاجمِ واقعی · نقشِ ناشناخته **fail-closed** |
| ۴٫۶ | حضور و ephemeral از سرور | `pnpm rt:presence` — هزار ephemeral، **صفر ردیف** · قطعِ ناگهانی → مکان‌نما پاک |
| ۴٫۷ | خوشه: pub/sub + قفلِ صاحب ([ADR-006](ARCHITECTURE_DECISIONS.md#adr-006) فاز ۲) | `pnpm rt:cluster` — دو پروسه، بدونِ ردیفِ تکراری · ضدِ حلقه **روی مسیرِ دریافت** |
| ۴٫۸ | heartbeat + خاموشیِ مودبانه + کاوشِ سلامت | `pnpm rt:shutdown` — کدِ ۱۰۰۱، snapshot، ژستِ لحظه‌ی آخر · ترتیبِ خاموشی با تست قفل شده |

### ★★★ الگویی که در کلِ فاز تکرار شد — و باید با خودمان به فاز ۵ ببریم

**هر شش سنجه‌ی زنده باگی گرفتند که همه‌ی تست‌های واحد سبز از رویش رد شده بودند.**
این دیگر تصادف نیست، یک الگوست:

| گام | چه چیزی سبز بود ولی خراب | چرا واحد نمی‌دید |
|---|---|---|
| ۴٫۳ | **هر** append با `22P02` می‌افتاد | لاگِ حافظه‌ای `uuid` نمی‌فهمد |
| ۴٫۴ | **۴۳ از ۵۰۰** append گم می‌شد | همزمانی فقط **زیرِ بار** دیده می‌شود |
| ۴٫۵ | `FORBIDDEN`ِ کاذب روی **هر** اتصالِ سالمِ `viewer` | رفتارِ پروتکل فقط با کلاینتِ واقعی |
| ۴٫۶ | **نسخه‌ی schemaِ بورد بی‌صدا گم می‌شد** | تصادفی بود — بستگی به اینکه کدام `clientID` برنده شود |
| ۴٫۷ | ephemeral بینِ نودها پخش نمی‌شد | یک نود، این مسیر را اصلاً ندارد |
| ۴٫۸ | کدِ **۱۰۰۶** به‌جای ۱۰۰۱ | دستِ‌دادنِ بستن روی سیمِ واقعی معنا دارد |

★ **و دو درسِ روش‌شناختی که گران‌تر از خودِ باگ‌ها بودند:**

۱. **تستِ خودآزمون‌نشده گیت نیست.** ضدِ حلقه‌ی ۴٫۷ (شمردنِ انتشار) با برداشتنِ
   گیت **سبز مانْد** — چون نودی که updateِ خودش را پس می‌گیرد اصلاً رویدادی
   نمی‌سازد. ادعا را باید همان‌جا سنجید که **اثر** دارد، نه جایی که شمردنش راحت
   است.
۲. ★★ **ادعای مثبت هم نگهبان می‌خواهد.** باگِ `markAlive` در ۴٫۸ در همه‌ی تست‌ها
   سبز بود و در تولید **هر ۲۵ ثانیه همه‌ی کلاینت‌ها را قطع می‌کرد**. تنها چیزی که
   گرفتش، تستی بود که می‌گفت «اتصالِ **سالم** باید زنده بمانَد». تستِ «چیزِ بد رخ
   نمی‌دهد» بدونِ جفتش («چیزِ خوب رخ می‌دهد») نصفِ گیت است.

---

## ★ جمع‌بندیِ فاز ۳ تا اینجا (۳٫۱ تا ۳٫۶)

`packages/canvas-sync` binderِ کارآمدی است: دو بومِ **واقعی** در مرورگر همگام
می‌شوند، همدیگر را می‌بینند، تصویر رد و بدل می‌کنند، و هیچ‌کدام یک خط کدِ شبکه
ندارند. **۱۵۱ تستِ واحد + ۱۷ تستِ E2E.**

| گام | خروجی | نگهبانِ دائمی |
|---|---|---|
| ۳٫۱ | چرخه‌ی عمر + نگهبانِ echo + همگام‌سازیِ اولیه | «بومِ بدرفتار» روی binderِ واقعی · نشتیِ observer زیر StrictMode |
| ۳٫۲ | `captureUpdate: "NEVER"` روی مسیرِ remote | قاعده‌ی ESLintِ **دولایه‌آزمون** + سه E2E |
| ۳٫۳ | جدولِ فرکانسِ PLAN ۷٫۴ | ≤۴۰ update در درگِ ۲ثانیه‌ای · یک استروک = یک update |
| ۳٫۴ | [ADR-035](ARCHITECTURE_DECISIONS.md#adr-035) — Yjs صاحبِ undo | تستِ **ضدِ ادعا**ی `trackedOrigins` + سه E2Eِ دو-کلاینته |
| ۳٫۵ | [ADR-036](ARCHITECTURE_DECISIONS.md#adr-036) — دو کانالِ حضور | تستِ **ضدِ ادعا**ی اندازه + «سند یک بایت هم بزرگ نمی‌شود» (واحد **و** مرورگر) |
| ۳٫۶ | پورتِ `AssetTransport` + ثبتِ بایت‌ها سمتِ گیرنده | «فایلِ ۵۰KB، رشدِ سند زیر ۳KB» + یک `Ctrl+Z` برای کلِ تصویر |

★ **الگویی که در کلِ فاز تکرار شد — هر تله فقط با دو کلاینتِ واقعی دیده شد، نه
در تستِ تک‌نفره.** هفت موردِ ثبت‌شده که هیچ‌کدام حدس نبودند:

| تله | چطور پیدا شد |
|---|---|
| شکافِ علّیِ Yjs (updateِ افزایشی بایگانی می‌شود) | هفت تست افتاد، بعد probe |
| `y-protocols` خطای observer را می‌بلعد | خواندنِ سورس بعد از یک تستِ نامفهوم |
| `disconnect` وسطِ `connect` نشت می‌کرد | اولین اجرای دمو زیر StrictMode |
| `text` مشتق است و بازمحاسبه نمی‌شد | تستِ اندازه‌گیریِ متن |
| تحویلِ غیرمحلی به `handleMessage` گره خورده بود | اولین `Ctrl+Z` روی `UndoManager` |
| پاسخِ **همزمانِ** همتا زودتر از مقداردهیِ دامنه می‌رسید | فهرستِ همتاهای ب خالی مانْد |
| شمارنده‌ی awareness با هر اتصال از صفر شروع می‌شد | تستِ چرخه‌ی connect/disconnect/connect |
| عنصرِ تصویر بدونِ ثبتِ بایت‌ها = قابِ خالی | تستِ مرورگریِ گام ۳٫۶ |

⚠️ **و دو بار شاهدِ کنترل جلوی یک گزارشِ غلط را گرفت** (گام‌های ۱٫۱ و ۳٫۴). هر دو
بار نشانه واقعی بود ولی علت از **harness** می‌آمد، نه از کدِ محصولی. به‌علاوه
**چهار آزمونِ بی‌اثرِ خودم** که خودِ تست ردشان کرد (ویرایشِ افزایشی در ۱٫۳ ·
شبیه‌سازیِ قطعِ برق با `transport.disconnect()` · جاروی ۳۰ثانیه‌ای با زمان‌بندِ
ساختگی · و PNGِ یک‌رنگی که کوچک‌تر از متادیتای خودش بود).

---

### گام ۵٫۱ — اتصالِ مجدد با backoff + jitter ✅ (۱۴۰۵/۰۵/۲۲)

**فاز ۵ شروع شد، و اولین گامش کلاینت را در برابرِ شبکه‌ی ایران بیمه می‌کند** —
ریسکی که PLAN بخش ۱۰ **بالا** ارزیابی کرده.

**چه ساخته شد:**

| فایل | چیست |
|---|---|
| [`backoff.ts`](packages/canvas-sync/src/backoff.ts) | تابعِ **خالصِ** زمان‌بندی — نمایی، با سقف، با jitterِ نیمه |
| [`websocket-transport.ts`](packages/canvas-sync/src/websocket-transport.ts) | ترابریِ محصولی + ماشینِ حالت + سیاستِ کدِ بستن |
| `adapter.ts` → `handleStatus` | نگاشتِ `TransportStatus` به **پنج حالتِ** `ConnectionState` |
| `awareness.ts` → `reannounce()` | معرفیِ دوباره **بدونِ پاک‌کردنِ حالت**، با clockِ جلورفته |
| `server.ts` → `wireAuthRefresh` | نیمه‌ی سرورِ `HB_AUTH_REFRESH` |
| [`scripts/rt-reconnect.ts`](scripts/rt-reconnect.ts) | سنجه‌ی زنده — `pnpm rt:reconnect` |

**★★ تصمیمِ مرکزی — [ADR-039](ARCHITECTURE_DECISIONS.md#adr-039):** یک backoffِ
یکسان برای هر قطعی، هر دو سرِ طیف را خراب می‌کند. جدولِ واکنش **اختراع نشد** —
کامنت‌های خودِ `error-codes.ts` از گام ۲٫۳ نوشته بودند که `SERVER_BUSY` «موقتی»
است و `DOC_TOO_LARGE` «دائمی» و «کارِ کلاینت در هرکدام فرق می‌کند»؛ اینجا فقط به
کد ترجمه شد.

**★ سنجه‌ی زنده، با اعدادِ اندازه‌گیری‌شده:**

| ادعا | اندازه‌گیری |
|---|---|
| کدِ ۱۰۰۱ صبر نمی‌کند | `nextRetryMs = 0` روی یک خاموشیِ **واقعی** |
| فاصله‌ها نمایی رشد می‌کنند | `57 · 965 · 1717 · 2155` ms (ساعتِ دیوار) |
| jitter رگبار را می‌شکند | سه کلاینت، **یک تلاشِ یکسان**: `472/349/305` ms |
| بازگشت بدونِ رفرش | ژستِ آفلاین پایدار شد، دو سند همگرا |
| ۱۰۰۸ تلاش نمی‌کند | صفر تلاشِ تازه در ۲٫۵ ثانیه‌ی بعدش |

**★★ و خودِ سنجه خودآزمون شد** (درسِ گام ۴٫۷): با `jitter: 0` بندِ jitter افتاد ·
با «۱۰۰۱ = backoff» بندِ خاموشیِ مودبانه افتاد · با توکنِ **کَش‌شده** بندِ «بدونِ
رفرش برمی‌گردد» افتاد. سنجه‌ای که با برداشتنِ چیزی که می‌سنجد سبز بماند، سنجه نیست.

**سه چیزی که این گام کشف کرد و در متنِ گام نبود:**

۱. ★★ **معرفیِ دوباره‌ی حضور، نه فرستادنِ دوباره.** فرضِ اولیه این بود که بعد از
   اتصالِ مجدد کافی است همان بایت‌های awareness دوباره بروند. **غلط:** سرور با
   بسته‌شدنِ سوکت حذفِ ما را پخش کرده و همتاها آن را با **همان clock** ثبت
   کرده‌اند؛ `applyAwarenessUpdate` پیامی با clockِ مساوی را **بی‌صدا** دور
   می‌ریزد. یعنی بدونِ بالا بردنِ شمارنده، برگشتنِ ما برای همه **نامرئی** بود —
   همان تله‌ی گام ۳٫۵، این‌بار از سمتِ شبکه. تستش یک **کنترلِ منفی** دارد که
   دقیقاً ثابت می‌کند تکرارِ همان پیام کار نمی‌کند.
۲. ⚠️ **سقفِ «فوری»، و باگی در خودِ سقف.** بدونِ سقف، تامین‌کننده‌ی توکنی که
   مقدارِ منقضی را کَش کند یک حلقه‌ی تنگ با تمامِ توانِ CPU می‌سازد. نسخه‌ی اولِ
   سقف شمارنده را در شاخه‌ی backoff **صفر می‌کرد** و نتیجه‌اش `۰، ۰، backoff، ۰،
   ۰` بود — یعنی دو تلاش از هر سه هنوز بی‌فاصله. تست گرفتش؛ حالا فقط با یک
   اتصالِ **موفق** صفر می‌شود.
۳. ⚠️ **اولین ادعای jitterِ سنجه بی‌معنا بود.** همه‌ی فاصله‌ها را در یک مجموعه
   می‌ریخت و «متمایز بودن» را می‌سنجید — که بدونِ jitter هم برقرار است، چون خودِ
   backoff رشد می‌کند. ادعای درست **هم‌تلاش‌شماره** است: سه کلاینت در تلاشِ N، سه
   فاصله‌ی متفاوت. تا وقتی این اصلاح نشده بود، `jitter: 0` هم از سنجه رد می‌شد.

**★ یک شکافِ بسته‌شده:** `0x10 HB_AUTH_REFRESH` از گام ۲٫۴ در پروتکل بود ولی سرور
**بی‌صدا نادیده‌اش می‌گرفت** — بدترین حالت، چون شبیهِ کارکردن است. حالا در
`server.ts` (نه `room.ts`: اتاق `BoardAuthority` ندارد و نباید داشته باشد) با سه
قید: هویت عوض نمی‌شود · نقش از `effectiveRole` می‌آید نه از claim · و ردش اتصال
را **نمی‌بندد** ([ADR-038](ARCHITECTURE_DECISIONS.md#adr-038)).

⚠️ **دو چیزی که این گام عمداً ادعا نمی‌کند:** حالتِ `offline` فقط از
`navigator.onLine` می‌آید که در Node **وجود ندارد**، پس سنجه‌ی زنده هرگز به آن
نمی‌رسد و نگهبانش تستِ واحد با محیطِ ساختگی است. و خودِ `YjsSyncAdapter` در
سنجه نیست: `@hamboom/canvas-core` را می‌کشد که importهای نسبی‌اش پسوندِ `.ts`
ندارند و در Nodeِ خالص بارگذاری نمی‌شود (دامنه‌ی M1) — پس نگاشتِ وضعیت و معرفیِ
دوباره‌ی حضور با **تستِ واحد** پوشش داده شده‌اند، نه روی سیم.

**تست:** `canvas-sync` ۱۵۳ → **۲۰۰**، `realtime` ۱۴۴ → **۱۴۹**. هر گیتِ تازه با
برداشتنِ خودش قرمز شد (jitter · قاعده‌ی ۱۰۰۱ · `reannounce` · نگهبانِ `linkUp` ·
ادعانکردنِ زودهنگامِ `connected` · سیم‌کشیِ تازه‌سازی · بررسیِ `sub`).

---

### گام ۵٫۲ — صفِ آفلاین با `y-indexeddb` ✅ (۱۴۰۵/۰۵/۲۲)

**گام ۵٫۱ کاری کرد که کلاینت از قطعی برگردد؛ این یکی کاری می‌کند که از بستنِ تب
هم برگردد.**

**چه ساخته شد:**

| فایل | چیست |
|---|---|
| [`local-store.ts`](packages/canvas-sync/src/local-store.ts) | پورتِ `LocalDocStore` + پیاده‌سازیِ `y-indexeddb` |
| `adapter.ts` → `connect` | بازیابیِ محلی **قبل از** دست‌دادن، با نگهبانِ نسل |
| `adapter.ts` → `collectOffline`/`reportUnsaved` | شمارشِ عنصرمحور + بدبینیِ `SaveState` |
| [`dev/OfflineDemo.tsx`](packages/canvas-sync/dev/OfflineDemo.tsx) | یک بوم + سوکتِ واقعی + IndexedDB (`#offline`) |
| [`scripts/rt-dev-server.ts`](scripts/rt-dev-server.ts) | سرورِ realtimeِ **حافظه‌ای** — همان کدِ محصولی، بدونِ داکر |
| [`e2e/offline.spec.ts`](packages/canvas-sync/e2e/offline.spec.ts) | معیارِ پذیرش، با **تبِ بسته و بازشده** |

**★★ تصمیمِ مرکزی: این یک صفِ پیام نیست.** وسوسه‌ی طبیعی این است که updateهای
نرفته را در IndexedDB صف کنیم و با برگشتِ اتصال دوباره بفرستیم. **غلط است** — و
دقیقاً به تله‌ی گام ۳٫۱ می‌خورَد: updateِ افزایشیِ بی‌پیشینه در `pendingStructs`
**بی‌صدا** بایگانی می‌شود و بورد ناقص بالا می‌آید بدونِ هیچ خطایی. اینجا فقط
**خودِ سند** ذخیره می‌شود و آشتی کارِ دست‌دادنِ `y-protocols/sync` است — همان
دست‌دادنی که ۵٫۱ روی **هر** بار باز شدن تکرارش می‌کند. «هیچ‌کدام تکراری نیست»
خاصیتِ همین انتخاب است، نه یک منطقِ حذفِ تکراری.

**سه چیزی که این گام کشف کرد:**

۱. ★★ **واحدِ `pendingChanges` غلط بود.** در ۵٫۱ تعدادِ updateهای نرفته شمرده
   می‌شد؛ عددش درست بود ولی رشته‌ی فارسیِ M1
   (`"آفلاین — {pending} تغییرِ معلق"`) آن را **به کاربر نشان می‌دهد** و یک درگِ
   ده‌ثانیه‌ای روی **یک** استیکی «۲۰۰ تغییرِ معلق» می‌ساخت. حالا واحدش **عنصر**
   است. تستش همان درگِ ۲۰۰تیکی است و انتظارش `۱`.
۲. ⚠️ **`SaveState` باید بدونِ شمارنده هم صادق باشد.** حالتِ واقعی: سیم قطع
   می‌شود و کاربر **هیچ کاری نمی‌کند**. با شمارنده‌ی صفر، وضعیت روی `saved`
   می‌مانْد — یعنی «همه‌چیز امن است» در حالی که هیچ‌کس نمی‌تواند تاییدش کند. حالا
   قطعِ سیم **خودش** `unsaved` می‌کند، و باز شدنِ سوکت `saving` است نه `saved`.
۳. ⚠️ **و صادقانه: E2E ترتیبِ «بازیابی قبل از دست‌دادن» را نمی‌آزماید.** با
   برداشتنِ `await` تستِ مرورگر **سبز مانْد** — چون ترابریِ WebSocket روی هر باز
   شدن دست‌دادن را تکرار می‌کند و همان تاخیر را می‌پوشانَد. آن ادعا را تستِ
   واحد با یک انبارِ عمداً کند نگه می‌دارد. پس این ترتیب یک تضمینِ **ترتیبِ
   رابط** است (بومِ خالی چشمک نزند)، نه سدِ از دست رفتنِ داده — و همین‌طور هم
   نوشته شد.

**★ سرورِ E2E بدونِ داکر است، و این به نفعِ تست شد.**
[`rt-dev-server.ts`](scripts/rt-dev-server.ts) همان سرور و اتاق و مجوزِ محصولی
است با انبارِ حافظه‌ای. ★ چون با هر ری‌استارت **خالی** بالا می‌آید، دیدنِ
استیکی‌ها روی کلاینتِ دوم تنها یک توضیح دارد: کلاینتِ اول بعد از بازگشت
فرستاده‌شان. نقطه‌ی `GET /dev-token` هم عمداً شکلی است که M3 باید بسازد، و باعث
می‌شود مرورگر **واقعاً برای هر تلاش توکنِ تازه** بگیرد.

**خودآزمونِ تست‌ها:** با برداشتنِ انبارِ محلی بندِ «تب بسته و باز» افتاد · با ادعای
دروغینِ `saved` بندِ صداقت افتاد (بعد از اینکه ادعا را **قبل از** ویرایش‌ها
بردم — در جای اول، شمارنده جوابش را می‌داد و چیزی ثابت نمی‌کرد) · و در تستِ
واحد، بدونِ `await` بندِ ترتیب افتاد.

**تست:** `canvas-sync` ۲۰۰ → **۲۰۹**، E2E ۲۵ → **۲۶**. `y-indexeddb` ۹٫۰٫۱۲ (MIT)
افزوده شد و گیتِ لایسنس روی **۷۳۲ پکیج** سبز ماند.

⚠️ **یک یادداشت برای M3:** هر بار بازکردنِ تب `createBoardDoc()` صدا زده می‌شود و
یک opِ `meta.schemaVersion` با `clientID`ِ تازه می‌نویسد. برای یک نشست ناچیز است،
ولی روی عمرِ یک بورد بی‌کران رشد می‌کند. **رفتارِ امروزِ همه‌ی کلاینت‌هاست و در این
گام عوض نشد** — تصمیمش با شکلِ نهاییِ بارگذاریِ بورد در M3 گره خورده.

---

### گام ۵٫۳ — تغییرِ نقش وسطِ session و نسخه‌ی ناسازگار ✅ (۱۴۰۵/۰۵/۲۲) — **فاز ۵ کامل**

**چه ساخته شد:**

| فایل | چیست |
|---|---|
| [`permissions.ts`](packages/canvas-sync/src/permissions.ts) | نقش → مجوزهای **رابط** (advisory) |
| `adapter.ts` → `applyRole` / `handleProtocolError` | شاخه‌های `HB_PERMISSION` و `HB_ERROR` |
| `adapter.ts` → `refuseIfTooOld` / `refuse` | سنجشِ نسخه و کنار کشیدن |
| `scripts/rt-dev-server.ts` → `/dev-role` | تغییرِ نقش از بیرون — شکلی که M3 خواهد داشت |
| [`e2e/role-change.spec.ts`](packages/canvas-sync/e2e/role-change.spec.ts) | معیارِ پذیرش، هر دو نیمه |

**★★ یافته‌ی اصلی، و برای سومین بار همان الگو:** `HB_PERMISSION` از گام ۴٫۵ روی
سیم می‌آمد — روی هر ردِ نوشتن، روی `applyRoleChange`، و از ۵٫۱ روی تازه‌سازیِ
توکن — و `handleMessage`ِ آداپتور **هیچ شاخه‌ای برایش نداشت**. همین‌طور
`HB_ERROR`. یعنی سرور درست کار می‌کرد، پیام درست می‌رسید، و کلاینت **بی‌صدا دورش
می‌ریخت**.

★ اولین بارش `HB_AUTH_REFRESH` در ۵٫۱ بود. **قاعده‌ای که از این سه بار درآمد:**
اگر کدی در `protocol.ts` تعریف شده و هیچ‌کس مصرفش نمی‌کند، یا مرده است یا یک
باگِ باز — و تفاوتشان را باید **صریح** نوشت، نه به نبودِ خطا اعتماد کرد.

**★★ تصمیمِ معماری — [ADR-040](ARCHITECTURE_DECISIONS.md#adr-040):** سنجشِ نسخه
را **کلاینت** انجام می‌دهد، نه سرور. طرحِ اولیه (در کامنتِ خودِ
`checkClientVersion`) سرور را داور گرفته بود، ولی سرور **نسخه‌ی کلاینت را
نمی‌داند** — نه در URLِ دست‌دادن جایی برایش هست و نه در پروتکل.
★ و دلیلِ قوی‌تری که از گام ۵٫۲ آمد: حالا سندِ **جلوتر** می‌تواند از IndexedDB
بیاید (تبِ دیگری با بیلدِ جدیدتر) و **اصلاً از سرور رد نشود**؛ داوریِ سرور آن
حالت را اصلاً نمی‌بیند.

**سه چیزِ دیگر که این گام تثبیت کرد:**

۱. **رد کردن یعنی قطعِ نوشتن، نه هشدار.** دلیلش در خودِ `checkClientVersion`
   نوشته شده و ملایم نیست: کلاینتِ عقب‌تر ساختاری را که نمی‌فهمد بازنویسی می‌کند
   و **برای بقیه هم خرابش می‌کند**. پس ترتیب هم عمدی است — اول قطع، بعد خبر.
۲. **پیام باید فارسی باشد، و درون‌خطی.** `StatusBar`ِ M1 متنِ حالتِ `error` را
   همان‌طور که هست چاپ می‌کند (کامنتش: «از سرور می‌آید — از قبل فارسی»)، و
   `packages/i18n` دامنه‌ی M2 نیست. همان کاری که `RtProtocolError`ِ سرور می‌کند.
۳. ⚠️ **`permissions.ts` گیت نیست.** گیتِ واقعی سرور است و روی هر update
   می‌سنجد (ADR-012). واگرایی‌اش حفره‌ی امنیتی نمی‌سازد، تجربه‌ی بد می‌سازد —
   کاربر ابزاری می‌بیند که هر بار بی‌صدا رد می‌شود. یک تست فهرستِ نویسنده‌ها را
   صریح پین کرده تا هر تغییری دیده شود. **صاحبِ واحدش M3 است** (`auth-core`).

**★ و ادعا از خودِ موتور خوانده می‌شود:** تستِ E2E `getAppState().viewModeEnabled`
را می‌سنجد، نه stateِ ری‌اکتِ دمو را. وگرنه فقط ادعای خودمان را با ادعای خودمان
سنجیده بودیم.

⚠️ **یک تصحیحِ روشِ تست:** ترابریِ ساختگیِ اول در `disconnect` فقط `onMessage` را
پاک می‌کرد و `onStatus` را نگه می‌داشت — چیزی که ترابریِ واقعی نمی‌کند. نتیجه‌اش
یک `connected`ِ **غیرممکن** بعد از کنار کشیدن بود. علاوه بر رفعِ fake، خودِ
آداپتور هم نگهبان گرفت: بعد از `refuse` هیچ رویدادِ وضعیتی پذیرفته نمی‌شود.

**خودآزمون:** با دور ریختنِ `HB_PERMISSION` بندِ فقط-خواندنی (هم واحد هم E2E)
افتاد · با برداشتنِ سنجشِ نسخه پنج تستِ واحد و بندِ کلاینتِ عقب‌تر افتادند.

**تست:** `canvas-sync` ۲۰۹ → **۲۲۵**، E2E ۲۶ → **۲۸**.

---

## ★★ جمع‌بندیِ فاز ۵ — کامل شد (۵٫۱ تا ۵٫۳)

**فاز ۵ جوابِ ریسکِ «پایداریِ WebSocket روی شبکه‌ی ایران» است** که PLAN بخش ۱۰
**بالا** ارزیابی‌اش کرده. حالا کلاینت از قطعی برمی‌گردد، از بستنِ تب هم
برمی‌گردد، و وقتی سرور نقش یا نسخه را عوض می‌کند **بی‌صدا نمی‌مانَد**.

| گام | خروجی | نگهبانِ دائمی |
|---|---|---|
| ۵٫۱ | ترابریِ محصولی + سیاستِ کدِ بستن ([ADR-039](ARCHITECTURE_DECISIONS.md#adr-039)) | `pnpm rt:reconnect` — فاصله‌های **اندازه‌گیری‌شده** + jitter روی سیم |
| ۵٫۲ | پایداریِ محلی پشتِ `LocalDocStore` + `SaveState`ِ صادق | `e2e/offline.spec.ts` — **تبِ بسته و بازشده** |
| ۵٫۳ | نقش و نسخه ([ADR-040](ARCHITECTURE_DECISIONS.md#adr-040)) | `e2e/role-change.spec.ts` — `viewModeEnabled`ِ **خودِ موتور** |

**★★ درسِ تکرارشونده‌ی این فاز — سه پیامِ مرده:** `HB_AUTH_REFRESH` (۵٫۱)،
`HB_PERMISSION` و `HB_ERROR` (۵٫۳) هر سه از فازهای قبل در پروتکل بودند و هیچ‌کس
مصرفشان نمی‌کرد. هیچ‌کدام خطا نمی‌داد؛ همه **شبیهِ کارکردن** بودند. این بدترین
حالتِ ممکن است و تنها راهِ پیدا کردنشان این بود که یک گام صریحاً بپرسد «این پیام
کجا مصرف می‌شود؟».

★ **و الگوی سنجش هم تثبیت شد:** هر ادعایی که مرورگر لازم دارد (IndexedDB،
`viewModeEnabled`، «crash نمی‌کند») در E2E است و بقیه در تستِ واحد — و **هر دو
طرف خودآزمون شدند**. جایی هم که E2E ادعایی را **نمی‌سنجید** (ترتیبِ بازیابی در
۵٫۲) صریح نوشته شد، نه پنهان.

---

### گام ۶٫۱ — G-1ب روی سرورِ واقعی ⚠️ **نیمه‌تمام** (۱۴۰۵/۰۵/۲۲)

**چه ساخته شد:**

| فایل | چیست |
|---|---|
| `dev/SyncPairDemo.tsx` → `paneTransport` | همان دمو، این‌بار با WebSocketِ واقعی از راهِ پارامترهای هش |
| [`e2e/pair-helpers.ts`](packages/canvas-sync/e2e/pair-helpers.ts) | helperهای مشترکِ ۳٫۷ و ۶٫۱ — **یک** اوراکلِ پروجکشن |
| `scripts/rt-dev-server.ts --pg` | همان ترکیبِ `main.ts`: Postgres + Redis + فایل‌سیستم |
| [`e2e/g1-server.spec.ts`](packages/canvas-sync/e2e/g1-server.spec.ts) | معیارِ پذیرش، پنج مرحله |
| `playwright.server.config.ts` | اجرای **جدا** — این یکی داکر لازم دارد |

**★ آنچه ثابت شد (هر اجرا سبز):** کلِ سناریوی G-1 روی مسیرِ **واقعی** —
sync از راهِ دو نودِ جدا، رندرِ حضور روی پیکسلِ سنجیده با پروجکشنِ
**دست‌محاسبه**، re-projectِ panِ خالص (باگِ Q1)، و follow. ★ و مهم‌تر از خودِ
نتیجه: **هیچ harnessِ دومی نوشته نشد** — همان صفحه، همان کامپوننت‌ها، همان
اوراکل، فقط ترابریِ متفاوت. اگر برای این گام یک صفحه‌ی تازه می‌ساختیم، سبز
شدنش چیزی درباره‌ی مسیرِ واقعی ثابت نمی‌کرد.

**★★ و همگرایی بعد از قطعیِ ۱۰ثانیه‌ای** — که همان‌جا یک باگِ واقعیِ خوشه بیرون
کشید (**F-2**، بالا): بینِ مرگِ صاحب و انقضای اجاره هیچ نودی صاحب نیست، پس
updateِ کلاینت **هیچ‌جا پایدار نمی‌شد**. رفع با تاییدِ مالک →
[ADR-041](ARCHITECTURE_DECISIONS.md#adr-041). سه اجرای متوالیِ سبز، و با برداشتنِ
رفع دوباره قرمز.

★ **و گپِ G-1 — ارثِ M1 — بالاخره بسته شد** در
[`sync/README.md`](packages/canvas-core/src/sync/README.md)، تنها تغییرِ مجاز در
دامنه‌ی M1.

**دو چیزِ کوچک که سرِ راه پیدا شد:**

۱. ⚠️ **شناسه‌ی بورد باید UUIDِ خام باشد.** ستونِ `board_id` از نوعِ `uuid` است و
   `brd-<uuid>` همان‌جا در `onJoin` می‌ترکد — که کلاینت آن را یک `FORBIDDEN`ِ
   عادی می‌بیند. **fail-closed درست کار کرد** (جزئیات فقط در لاگِ سرور، ADR-031)،
   ولی برای کسی که تست می‌نویسد گیج‌کننده است. برای M3 یک نکته: خطای **شکلِ**
   شناسه بهتر است کدِ خودش را داشته باشد.
۲. ⚠️ **مقایسه‌ی دو مقدارِ متغیر باید در یک `evaluate` باشد.** نسخه‌ی اولِ ادعای
   همگرایی یک‌بار بردارِ ب را می‌گرفت و بعد الف را poll می‌کرد — یعنی هدفِ متحرک
   را با عکسِ لحظه‌ی صفر می‌سنجید. همان تله‌ی «دو `evaluate` یک رندر فاصله
   دارند» از گام ۳٫۷، این‌بار روی محورِ زمان.

**تست:** E2E ۲۸ → **۲۹** (۲۸ تای بی‌نیاز از داکر + ۱ تای این گام، جدا) و
`realtime` ۱۴۹ → **۱۵۱** (دو تستِ واحدِ F-2). هر هفت سنجه‌ی زنده بعد از تغییرِ
خوشه دوباره اجرا شدند و **همه سبز**.

---

## تصمیم‌های گرفته‌شده

**پنج تصمیمِ مرزی — تاییدِ مالک ۱۴۰۵/۰۵/۱۲** (جدولِ کامل در صدرِ [TODO.md](TODO.md)):

| # | تصمیم |
|---|---|
| D-1 | M2 یک **برشِ حداقلی** از زیرساخت می‌سازد: compose فقط با postgres+redis، `.env.example`، `packages/config`. M5 بعداً کاملش می‌کند. |
| D-2 | پورتِ `BoardAuthority` + `DevBoardAuthority`. ★ **claimهای `rtToken` داخلِ پورت، نه `shared-types`.** |
| D-3 | پورتِ `SnapshotStore` + `FsSnapshotStore`. ★ گیتِ ESLintِ `@aws-sdk/*` و `@hamboom/sdk` باید **با `RuleTester` خودآزمون** باشد. |
| D-4 | `ws` به‌جای `uWebSockets.js`. |
| D-5 | پکیجِ سومِ `canvas-sync` برای binder. |

### ★ دو قیدِ فعال که تا پایانِ M2 باید رعایت شوند

۱. **M2 بدونِ هیچ تغییری در `shared-types` تمام می‌شود.** اگر جایی لازم شد، یعنی از تعریفِ
   تصمیم‌های تاییدشده خارج شده‌ایم → **توقف و پرسش از مالک**، نه ادامه دادن.
   - مسیرِ ممنوعِ مشخص در گام ۱٫۳: اگر دیفِ متن کافی نبود، **قرارداد را پهن نکن**
     (مثلاً `textDelta` در `ElementChangeSet`) — آن تغییرِ `shared-types` است.
   - ✅ **یک استثنای صریح، تاییدِ مالک ۱۴۰۵/۰۵/۲۰ (گام ۴٫۳):** افزودنِ پسوندِ `.ts` به
     ده importِ **نسبیِ درونِ** `shared-types`. حکمِ مالک: «این از تعریفِ D-2 بیرون است —
     قید برای جلوگیری از تغییرِ قرارداد و شکلِ تایپ‌ها بود، نه پسوندِ importِ نسبی.»
     **صفر تغییر در تایپ، API یا رفتار**، و بدونش سرور هرگز بیرون از vitest بالا نمی‌آمد.
     ★ قید همچنان پابرجاست: هر تغییرِ **شکلِ** قرارداد باز هم توقف و پرسش می‌خواهد.
۲. **گام ۱٫۱ (probeِ StrictMode): اگر شکست خورد، هیچ تغییری در `canvas-core` نده.** با
   **گزینه‌ها و هزینه‌ی هرکدام** برگرد، نه با یک راه‌حلِ آماده (قیدِ صریحِ مالک). سه گزینه‌ی
   نامزد از قبل در گام ۱٫۱ فهرست شده‌اند.

---

## بلوکه (نیاز به تصمیم مالک)

### ✅ F-2 — بینِ دو صاحب، updateِ کلاینت **بی‌صدا** گم می‌شد (کشفِ ۶٫۱، **رفع‌شده**)

> **★ رفع شد با گزینه‌ی «الف» — تاییدِ مالک ۱۴۰۵/۰۵/۲۲ →
> [ADR-041](ARCHITECTURE_DECISIONS.md#adr-041).** اتاق هنگام باز شدن روی گذرگاه
> می‌پرسد «این بورد را دارید؟» و هر نودی که دارد حالتِ کاملش را می‌فرستد.
> **خودآزمون:** با برداشتنِ رفع، تستِ G-1ب دوباره قرمز شد (۲ از ۲) و دو تستِ
> واحدِ تازه در `room.test.ts` هم افتادند. با رفع: **سه اجرای متوالیِ سبز**.

**چطور پیدا شد:** معیارِ پذیرشِ G-1ب. مرحله‌های ۱ تا ۴ در هر اجرا سبزند؛ مرحله‌ی
۵ (همگرایی بعد از قطعیِ ۱۰ثانیه‌ای) در **۳ اجرا از ۴** قرمز است.

**شواهدِ خام** (`e2e/g1-server.spec.ts`، دو نودِ واقعی روی Postgres + Redis):

| لحظه | بردارِ وضعیتِ الف | بردارِ وضعیتِ ب |
|---|---|---|
| پیش از قطعی | `الف@۱، ب@۱` | `الف@۱، ب@۱` |
| در قطعی (هر دو کار کردند) | `الف@۹۶، ب@۱` | `الف@۱، ب@۹۴` |
| ۳۰ ثانیه بعد از بازگشت | `الف@۹۶، ب@۹۴` ✅ | `الف@**۱**، ب@۹۴` ❌ |

یعنی کارِ **ب** به الف رسید، ولی کارِ **الف** به ب **هرگز** نرسید — و هیچ خطایی
هم در کار نبود. لاگِ سرور علتِ ساختاری را نشان می‌دهد:

```
اتاق بارگذاری شد … owner:false     ← نودِ ۱
اتاق بارگذاری شد … owner:false     ← نودِ ۲     (هر دو، بعد از ری‌استارت)
…۲۰ ثانیه بعد…
صاحبِ بورد شدیم                     ← تازه حالا
```

**مکانیزم (قابلِ استدلال، نه فقط مشاهده):** قفلِ صاحب یک اجاره‌ی ۳۰ثانیه‌ای در
Redis است. وقتی نودِ صاحب می‌میرد، اجاره‌اش **تا انقضا** دستِ یک نودِ مرده
می‌مانَد. در آن پنجره:

1. هیچ نودی صاحب نیست، پس `persistAndFanout` وارد شاخه‌ی `!room.owner` می‌شود و
   **هیچ‌چیز در Postgres نوشته نمی‌شود** (درست طبقِ ADR-006 فاز ۲).
2. تنها نسخه‌ی آن update بیرون از کلاینت، **حافظه‌ی همان نودی** است که گرفتتش.
3. نودِ دیگر آن را فقط از گذرگاه می‌تواند بگیرد. اگر نگیرد — چون اتاقش هنوز باز
   نشده بود، یا چون pub/sub تحویل را تضمین نمی‌کند — **هرگز** به آن نمی‌رسد:
   گذرگاه گذشته را نگه نمی‌دارد و Postgres چیزی ندارد.
4. صاحبِ تازه که می‌آید، **حالتِ خودش** را می‌نویسد؛ اگر آن update را ندیده
   باشد، برای همیشه رفته است.

⚠️ **ADR-009 فنی نقض نمی‌شود** — به کاربر «ذخیره شد» گفته نشده (نودِ غیرِ صاحب
`saving` می‌فرستد) و کلاینت داده را دارد. ولی **دو کلاینت برای همیشه واگرا
می‌مانند** و هیچ‌کس خبردار نمی‌شود. این دقیقاً همان کلاسِ باگی است که کلِ فاز ۴
دنبالش بود.

**سه گزینه، با هزینه:**

| # | راه‌حل | هزینه | ریسک |
|---|---|---|---|
| **الف** | ★ **تبادلِ حالت هنگام بازکردنِ اتاق**: نود موقعِ load روی گذرگاه «این بورد را دارید؟» می‌پرسد و هر نودی که اتاق را باز دارد `encodeStateAsUpdate` را برمی‌گرداند | یک kindِ تازه‌ی گذرگاه + ~۱۵ خط. `handleBus` کدِ ناشناخته را از قبل بی‌صدا رد می‌کند، پس استقرارِ چرخشی امن است | کم — idempotent، و حالتِ عادی را عوض نمی‌کند |
| **ب** | نودِ **غیرِ صاحب** updateهای پایدارنشده را بافر کند و به اولین صاحب تحویل بدهد | حافظه‌ی بی‌کران در قطعیِ طولانی؛ باید سقف و سیاستِ دورریز داشته باشد | متوسط — یک مسیرِ حالت‌دارِ تازه در `room.ts` |
| **ج** | وقتی صاحبی نیست، نوشتن **پذیرفته نشود** (`HB_ERROR` یا `unsaved`ِ صریح) | کاربر تا ۳۰ ثانیه نمی‌تواند بنویسد — درست ولی خشن | کم برای داده، بد برای تجربه |

★ **پیشنهادِ من گزینه‌ی الف است**: کوچک‌ترین تغییر، و مسئله‌ی کلی‌تری را هم حل
می‌کند («نودی که اتاق را دیرتر باز می‌کند حالتِ تازه‌تر را از دست می‌دهد»)، نه
فقط پنجره‌ی بی‌صاحبی.

⚠️ **چرا خودم اعمالش نکردم:** این رفتارِ **خوشه** را عوض می‌کند و ADR-006 فاز ۲
یک تصمیمِ تاییدشده است. طبق قاعده‌ی ۷، راهش یک ADRِ **تازه** است — و آن تصمیمِ
شماست، نه یک اثرِ جانبیِ گامِ ۶٫۱.

⚠️ **و تست عمداً طوری نوشته نشد که پنجره‌ی بی‌صاحبی را دور بزند** (مثلاً صبر تا
آمدنِ صاحب). آن یعنی پنهان‌کردنِ همان چیزی که این گام برای پیدا کردنش وجود دارد.

★★ **و یک درسِ محیطی، ثبت‌شده:** وسطِ آزمودنِ رفع، سه اجرای پیاپی قرمز شد و
نشانه‌اش **شبیهِ رگرسیون** بود. علتش رفع نبود — کانتینرهای داکر خاموش شده بودند
و `join` روی `store.load` معلق می‌مانْد. نشانه‌ی تشخیصی صریح است: در لاگِ سرور
«اتصال پذیرفته شد» هست ولی **«اتاق بارگذاری شد» نیست**، و کلاینت `connected`
نشان می‌دهد چون سوکت باز است ولی `socket.pause()` هنوز برنداشته شده. همان
قاعده‌ی CLAUDE.md: **اول محیط را ببین، بعد قضاوت کن.**

- هیچ موردِ بازی نیست. (**F-2** بالا و **B-1** پایین، هر دو با تاییدِ مالک رفع شدند.)

### ✅ F-1 — `meta.schemaVersion` پایدار نمی‌شد (کشفِ ۴٫۴، **رفع‌شده در ۴٫۶**)

> **رفع شد و تصمیمِ مالک لازم نشد.** علتِ واقعی این نبود که «opهای سرور پایدار
> نمی‌شوند»، بلکه این بود که **سرور اصلاً op می‌نوشت**: نوشتنِ `meta` توسطِ سرور
> با نوشتنِ کلاینت رقابت می‌کرد و حذفِ حاصل، پایدار می‌شد. حالا سرور هیچ opی
> نمی‌نویسد (گام ۴٫۶ در بالا) و نسخه‌ی schemaِ کلاینت زنده می‌مانَد.
> متنِ اصلی برای تاریخچه پایین می‌مانَد.

هنگام ردیابیِ مقایسه‌ی state vector معلوم شد لاگِ یک بورد می‌تواند **هیچ**
`meta.schemaVersion`ی نداشته باشد. علتش ساده است: تنها opهایی پایدار می‌شوند که
originشان `ClientOrigin` باشد، و `meta` را یا `createBoardDoc`ِ **سرور** می‌نویسد
یا `migrateDocument` — هیچ‌کدام از راهِ کلاینت نمی‌آیند. پس هر بار که اتاق بالا
می‌آید دوباره مهر می‌خورد و آن مهر هم دوباره پایدار نمی‌شود.

**امروز بی‌ضرر است** چون `SCHEMA_VERSION` و `EARLIEST_SCHEMA_VERSION` هر دو ۱اند،
پس «نسخه‌ی نانوشته» و «نسخه‌ی ۱» یک معنا دارند. ⚠️ **ولی با آمدنِ نسخه‌ی ۲
معنادار می‌شود:** بوردی که مهرش پایدار نشده، برای همیشه «نسخه‌ی ۱» خوانده می‌شود
حتی بعد از migrate — یعنی migration هر بار از نو اجرا می‌شود.

**تصمیمی که لازم است:** آیا opهای خودِ سرور (`DOC_INIT_ORIGIN` و
`DOC_MIGRATION_ORIGIN`) باید پایدار شوند؟ این یعنی گسترشِ «چه چیزی پایدار
می‌شود» فراتر از `ClientOrigin` — مرزی که گام ۴٫۲ عمداً کشید. **دست نزدم**؛
جایش گام ۲٫۳/۴٫۲ است یا یک ADR تازه، نه وسطِ ۴٫۴.

### ⚠️ G-2 — `commenter` فعلاً مثلِ `viewer` رفتار می‌شود (کشفِ گام ۴٫۵، بلوکه‌کننده نیست)

نقشِ `commenter` قرار است **فقط سنجاقِ کامنت** بنویسد، نه عنصر. ولی تشخیصِ اینکه
یک updateِ باینریِ Yjs کدام ریشه را دست می‌زند — **قبل از اعمالش** — کارِ کوچکی
نیست، و بعد از اعمال هم Yjs عقب‌گرد ندارد. دو راهِ ممکن هر دو گران‌اند: بازرسیِ
ساختارِ update پیش از اعمال، یا نگه‌داشتنِ یک سندِ سایه.

**انتخابِ امروز صریحاً fail-closed است:** `commenter` نمی‌نویسد. مسیرِ کامنت هنوز
در کلاینت وجود ندارد (کارِ M3)، پس چیزی نمی‌شکند — و برعکسش یعنی یک `commenter`
می‌توانست کلِ بورد را جابه‌جا کند.

**تصمیمی که لازم است، وقتی کامنت واقعی شد:** تفکیکِ مجوز **به‌ازای ریشه‌ی سند**.
جایش همان گامی است که سنجاقِ کامنت را زنده می‌کند، نه اینجا.

---

## ✅ B-1 — باگِ RTL در CSSِ حضورِ M1 — **رفع شد** (تاییدِ مالک ۱۴۰۵/۰۵/۱۶)

**تصمیم:** گزینه‌ی «الف» — رفع در M1. دلیلِ مالک: «یک باگِ محصولی است که P6 را
نقض می‌کند و کامنتِ خودِ `PeerCursors` هم می‌گوید موقعیت باید فیزیکی باشد؛ قیدِ
دست‌نزدن به `canvas-core` برای تغییرِ **معماری** بود، نه قفل‌کردنِ یک باگِ دوخطی.»

**⚠️ ولی رفعِ لفظی ممکن نبود، و علتش را حین کار پیدا کردم:** یک گیتِ **Stylelint**
برای [ADR-016](ARCHITECTURE_DECISIONS.md#adr-016) وجود دارد
([`stylelint.config.js`](stylelint.config.js)) که `left`/`right` را در CSS **ممنوع**
کرده — دقیقاً همان چیزی که نویسنده را به `inset-inline-start` رسانده بود. پس
`left: 0` گیت را می‌شکست.

**★ راهِ درست از خودِ همان config آمد:** کامنتش صریح می‌گوید «استثنای بوم:
`direction` (نه left/right) مجاز است — بوم داخل خودش LTR است». پس لایه‌های حضور
— که فضای **مختصاتِ بوم** اند — `direction: ltr` گرفتند، و نامِ کاربر با
`direction: rtl` روی خودِ label فارسی و RTL مانْد (P6).

| | قبل | بعد |
|---|---|---|
| `transform.x` | ۲۲۰ | ۲۲۰ (همیشه درست بود) |
| `left`ِ واقعیِ مکان‌نما | **۷۷۹٫۴** | **۲۲۰** ✅ |
| `left`ِ واقعیِ هاله | **۴۷۱** | **۶۰** ✅ |

**تایید:** تستِ پین‌شده همان‌طور که قرار بود **قرمز شد**، و حالا ادعای درست را
نگه می‌دارد (پیکسلِ واقعی = پروجکشنِ دست‌محاسبه). به‌علاوه `stylelint` سبز،
**۴۷۴/۴۷۴** تستِ واحدِ M1 و **۱۶/۱۶** E2Eِ M1 سبز.

### ★★ دو تقویتِ گیت که از همین‌جا آمدند (تاییدِ مالک ۱۴۰۵/۰۵/۱۶)

**۱. `lint (CSS)` وارد `pnpm verify` شد.** گیتِ Stylelintِ ADR-016 وجود داشت ولی
فقط دستی اجرا می‌شد — «گیتی که فقط دستی اجرا شود گیت نیست». اهمیتش همین‌جا عملی
ثابت شد: جلوی راه‌حلِ اشتباهِ B-1 را گرفت. گلاب عمداً پهن‌تر از `lint:css`ِ ریشه
است (`**/*.css`) تا CSSِ `apps/*`ِ فاز ۴ هم پوشش داشته باشد. با خطای عمدی آزموده
شد: exit 2 → بعد از رفع exit 0.

**۲. تست‌ها تک-forkـی شدند.** `--concurrency=1` فقط taskهای turbo را سریالی
می‌کرد؛ هر vitest **زیرِ** آن باز به تعدادِ هسته worker می‌ساخت. این **پنجمین
باری** بود که سقفِ commit کار را متوقف می‌کرد — شش اجرای پیاپیِ گیت افتاد
(۱۱→۸→۱→۷→۳→۲ خطِ OOM، همیشه فقط `canvas-core`) در حالی که همان پکیج با یک fork
هر ۴۷۴ تست را در ~۵ ثانیه پاس می‌کند. بعد از تغییر: **سه اجرای پیاپیِ سبز، صفر
خطِ OOM**، زیرِ همان فشارِ حافظه‌ای که قبلش شش بار انداخته بودش.
★ گیت ضعیف نشد و دوباره با خطای عمدی آزموده شد (exit 1).

<details>
<summary>شرحِ اصلیِ B-1 (بایگانی)</summary>

### ★★ B-1 — باگِ RTL در CSSِ حضورِ M1 (کشف‌شده در گام ۳٫۷)

**نشانه:** مکان‌نما و هاله‌ی همتا روی محورِ افقی به‌اندازه‌ی **کلِ عرضِ بوم** به
راست پرتاب می‌شوند. محورِ عمودی سالم است.

**علت — سنجیده، نه حدس:** `transform`ِ هر دو کامپوننت **درست** است (تستِ
پروجکشنِ دست‌محاسبه اثباتش می‌کند)، ولی CSSشان با `inset-inline-start: 0`
جای‌گیری می‌کند. سندِ ما `dir="rtl"` است، پس آن به `right: 0` ترجمه می‌شود و مبدأ
به لبه‌ی **راست** می‌رود:

```
leftِ واقعی = عرضِ لایه − عرضِ عنصر + transform.x
```

| اندازه‌گیری | مکان‌نما | هاله |
|---|---|---|
| عرضِ لایه | ۶۳۵px | ۶۳۵px |
| عرضِ عنصر | ۷۵٫۶px | ۲۲۴px |
| `transform.x` (درست) | ۲۲۰ | ۶۰ |
| `left`ِ واقعی | **۷۷۹٫۴** | **۴۷۱** |

**فایل‌ها (هر دو در M1):** `packages/canvas-core/src/ui/peer-cursors.css`
(`.hb-peer-cursor`) و `peer-selections.css` (`.hb-peer-halo`).

★ **این خلافِ قاعده‌ی خودِ پروژه است:** [P6](CLAUDE.md) صریحاً استثنا کرده که
«مختصاتِ بوم هرگز آینه نمی‌شود». مکان‌نما مختصاتِ بوم است، پس باید با
`left`/`top`ِ **فیزیکی** بنشیند. کامنتِ خودِ `PeerCursors` هم همین را می‌گوید
(«موقعیت با transformِ فیزیکی است، نه logical») — فقط CSS دنبالش نرفته.

**چرا M1 نگرفت:** تستِ واحد در jsdom است و jsdom **layout ندارد** — فقط رشته‌ی
`transform` را می‌سنجد، که درست است. و حضورِ دموی M1 از یک `BroadcastChannel`
می‌آمد؛ همان گپِ G-1 که این گام قرار بود ببندد.

**گزینه‌ها و هزینه‌ها:**

| گزینه | کار | هزینه / ریسک |
|---|---|---|
| **الف — رفع در M1** (پیشنهاد) | دو خط CSS: `inset-inline-start` → `left` در `.hb-peer-cursor` و `.hb-peer-halo` | دست‌زدن به ماژولِ تحویل‌شده، **نیاز به تاییدِ شما**. ریسکِ فنی ~صفر: این لایه‌ها همان استثنای P6 اند و مصرف‌کننده‌ی دیگری ندارند. تستِ پین‌شده‌ی موجود بلافاصله تاییدش می‌کند |
| **ب — override در CSSِ دموی M2** | چند خط در `dev/styles.css` | **سبزِ دروغین می‌سازد**: تست، override من را اثبات می‌کند نه کامپوننتِ محصولی؛ و M3 همچنان می‌شکند. **توصیه نمی‌کنم** |
| **ج — دست‌نزدن** | فقط همین ثبت + تستِ پین‌شده | هزینه به M3 منتقل می‌شود: اولین بارِ `apps/web` حضور را غلط نشان می‌دهد. نیمه‌ی رندرِ G-1 باز می‌مانَد |

**وضعیتِ فعلی:** گزینه‌ی **ج** اجرا شده — یک تستِ **پین‌شده** در
[`e2e/presence-render.spec.ts`](packages/canvas-sync/e2e/presence-render.spec.ts)
رابطه‌ی دقیقِ نقص را ثبت می‌کند و روزی که رفع شود **قرمز** می‌شود. هیچ فایلی از
`canvas-core` دست نخورد.

</details>

---

## ★ جمع‌بندیِ فاز ۱ — کامل شد

چهار probe اجرا شد. هدفِ فاز **شواهد** بود نه کدِ محصولی، و هر چهار خروجی به یک
**تصمیمِ ثبت‌شده** یا یک **قیدِ اجرایی** رسید:

| probe | خروجی | فایل |
|---|---|---|
| ۱٫۱ StrictMode | [ADR-032](ARCHITECTURE_DECISIONS.md#adr-032) — الگوی `useEffect([api])` تایید شد؛ دروازه‌ی فاز ۳ باز | `canvas-sync/e2e/strictmode.spec.ts` |
| ۱٫۲ کدکِ Y.Doc | **ADR-007 تایید شد** (بازبینی لازم نیست) + [ADR-033](ARCHITECTURE_DECISIONS.md#adr-033) | `ydoc-schema/src/merge-probe.test.ts` · `canvas-sync/src/codec-probe.test.ts` |
| ۱٫۳ متنِ همزمان | [ADR-034](ARCHITECTURE_DECISIONS.md#adr-034) — `Y.Text` + دیف | `ydoc-schema/src/text-probe.test.ts` |
| ۱٫۴ انزوای undo | `trackedOrigins` تایید شد + یافته‌ی وارونه‌ی پیش‌فرض | `ydoc-schema/src/undo-probe.test.ts` |

**سه ADR جدید در این فاز:** ۰۳۲ (StrictMode) · ۰۳۳ (`customData` تودرتو) ·
۰۳۴ (متن). به‌علاوه‌ی ۰۲۹/۰۳۰/۰۳۱ از فاز ۰ → مجموعاً **۳۵ ADR**.

★ **سه فرضِ من در این فاز غلط از آب درآمد و هر بار خودِ تست ردش کرد**، نه بازبینی:
پایه‌ی کهنه با ویرایشِ افزایشی خرابی نمی‌سازد (فقط با جایگزینی) · undoِ ساخت کلید
را برمی‌دارد (حذفِ نرم این مسیر نیست) · پیش‌فرضِ `UndoManager` فقط `null` را ردیابی
می‌کند نه همه‌چیز. **این دقیقاً کارِ probe است** — اگر همان فرض‌ها مستقیم به فاز ۲
می‌رفتند، سه باگِ خاموش می‌ساختند.

---

## بایگانی: ورودی‌های گام ۵٫۲ — **انجام شد**

هر سه ورودیِ ثبت‌شده محقق شد؛ شرحش بالا در «گام ۵٫۲».

---

## قدم بعدی — فاز ۶، گام ۶٫۱: ★★ G-1ب — دو نمونه روی سرورِ **واقعی**

**فاز ۵ تمام است** (جمع‌بندی‌اش بالا). فاز ۶ تحویل است، و اولین گامش نیمه‌ی
دومِ گپِ ارث‌رسیده‌ی **G-1** از M1 را می‌بندد.

### ★ آنچه آماده است — از نو نساز

- **harnessِ گام ۳٫۷** (`dev/SyncPairDemo.tsx` + `e2e/presence-render.spec.ts`)
  کلِ سناریوی G-1 را روی `LocalTransportHub` می‌راند: sync، رندرِ حضور،
  re-project با هر تغییرِ viewport، و follow. ۶٫۱ باید همان را روی **سرورِ
  واقعی** ببرد، نه اینکه از نو بنویسدش.
- ★ **ترابری از قبل جایگزین‌پذیر است:** `createWebSocketTransport` همان
  `SyncTransport` است. عوض کردنِ `new LocalTransport(hub)` با آن **هیچ خطی از
  binder را عوض نمی‌کند** — قولی که ADR-030 داد و ۵٫۱ ادایش کرد.
- **دو راهِ بالا آوردنِ سرور، و هر دو آزموده:** `scripts/rt-dev-server.ts`
  (حافظه‌ای، بدونِ داکر — الگویش در E2Eهای ۵٫۲ و ۵٫۳) و نودِ کاملِ
  Postgres+Redis (الگویش در `scripts/rt-*.ts`).
  ⚠️ **معیارِ پذیرشِ ۶٫۱ صریحاً «Postgres + Redis بالا» می‌خواهد**، پس اینجا
  نسخه‌ی حافظه‌ای کافی نیست.

### ⚠️ سه چیزی که ۶٫۱ باید مواظبشان باشد

۱. ★★ **«دو نمونه روی دو نودِ سرور»** هم در معیار هست (گام ۴٫۷). یعنی دو
   `createRtServer` روی یک Redis و یک Postgres — دقیقاً همان چیزی که
   `rt:cluster` می‌سازد.
۲. **مقایسه‌ی حالت با `state vector`، نه چشمی.** معیار صریح است: بعد از قطعیِ
   ۱۰ثانیه‌ای هر دو کلاینت باید به **یک حالتِ یکسان** برسند.
۳. ⚠️ **چهار تله‌ی تستِ گام ۳٫۷ هنوز برقرارند** (جدولشان در
   `canvas-sync/CLAUDE.md`): ردیفِ آواتار کلیک را می‌بلعد · `updateScene`ِ
   برنامه‌ای `onScrollChange` نمی‌دهد · یک چرخِ ماوس یک رگبار است · «نما» و
   «پیکسل» باید در **یک** `page.evaluate` خوانده شوند.

★ **و بعدش G-1 در [`sync/README.md`](packages/canvas-core/src/sync/README.md)
بسته می‌شود** — تنها تغییرِ مجاز در دامنه‌ی M1 (گام ۶٫۴).

<details>
<summary>بایگانی: ورودی‌های ۵٫۳ (انجام شد)</summary>

هر سه ورودی محقق شد. آنچه ثبت شده بود: `HB_PERMISSION` از سرور می‌آید و آداپتور
دورش می‌ریخت · `CanvasPermissions`/`setPermissions`/`viewModeEnabled` از M1
آماده بودند · `checkClientVersion` و کدِ مرگبارِ `CLIENT_TOO_OLD` در ترابری از
قبل بودند. ⚠️ و سه قید: تنزلِ نقش اتصال را نبندد · کد را ببین نه رسیدنِ خطا را ·
`fail closed` را نشکن.

</details>

### ★ آنچه آماده است — از نو نساز

- **`HB_PERMISSION` از سرور می‌آید و امروز هم می‌آید.** گام ۴٫۵ آن را روی هر ردِ
  نوشتن می‌فرستد، `applyRoleChange` روی تغییرِ واقعیِ نقش، و گام ۵٫۱ روی
  تازه‌سازیِ توکن. آداپتور امروز این پیام را **می‌گیرد و دور می‌ریزد**
  (`handleMessage` هیچ شاخه‌ای برایش ندارد) — همان الگوی «بی‌صدا» که ۵٫۱ برای
  `HB_AUTH_REFRESH` بست.
- **`CanvasPermissions` و `setPermissions` از قرارداد M1 موجودند**، و
  `createCanvasBinding` از قبل به `ui.setPermissions` وصلشان می‌کند. M1
  `viewModeEnabled` را دارد.
- **`checkClientVersion` و `CLIENT_TOO_OLD` در `ydoc-schema` از گام ۲٫۳ آماده‌اند**،
  و ترابری از قبل `CLIENT_TOO_OLD` را **مرگبار** می‌شمارد (ADR-039) — پس حلقه‌ی
  اتصالِ مجدد ساخته نمی‌شود. آنچه مانده پیامِ فارسی و درخواستِ رفرش است.

### ⚠️ سه چیزی که ۵٫۳ باید مواظبشان باشد

۱. ★★ **تنزلِ نقش نباید اتصال را ببندد** ([ADR-038](ARCHITECTURE_DECISIONS.md#adr-038)).
   کاربری که `viewer` شده حقِ **دیدن** دارد؛ و بستن هم بی‌اثر است چون با همان
   توکن برمی‌گردد.
۲. ⚠️ **`HB_ERROR{FORBIDDEN}` همراهِ `HB_PERMISSION` می‌آید.** کلاینت باید
   **کد** را ببیند نه صرفِ رسیدنِ خطا را — وگرنه یک تنزلِ عادی به یک بنرِ خطای
   ترسناک تبدیل می‌شود. ترابری این را از قبل درست می‌کند (فقط بستن با ۱۰۰۸
   مرگبار است)، ولی مسیرِ **پیام** هنوز کلاینتی ندارد.
۳. ★ **`fail closed` را نشکن:** `readRole` نقشِ ناشناخته را به `viewer` نگاشت
   می‌کند (گام ۲٫۴). اگر کلاینت این را «نمی‌دانم پس تغییرش نده» تفسیر کند، یک
   کاربرِ تنزل‌داده‌شده با دسترسیِ قبلی ادامه می‌دهد.

⚠️ **قیدِ `shared-types` تا پایانِ M2 برقرار است** — تا اینجا صفر تغییر.

<details>
<summary>بایگانی: ورودی‌های ۵٫۲ (انجام شد)</summary>

- ترابری از قبل قطعی را می‌فهمد؛ `handleStatus` نگاشتش می‌کند.
- ★ تله‌ی «هیچ‌کدام تکراری نباشد» همان شکافِ علّی است: آشتی باید با
  `y-protocols/sync` باشد نه با فرستادنِ updateهای ذخیره‌شده.
- ⚠️ `createBoardDoc()` روی سندِ بازیابی‌شده — یادداشتش بالا در «گام ۵٫۲» ماند.

</details>

### ★★ بایگانی: سه کدِ بستن (ورودیِ ۵٫۱ — **انجام شد**)

پیاده‌سازی و جدولِ نهایی در [ADR-039](ARCHITECTURE_DECISIONS.md#adr-039). این
جدولِ اولیه برای تاریخ می‌مانَد چون تحلیلش درست از آب درآمد:

| کد | کِی می‌آید | کارِ **درستِ** کلاینت | اگر اشتباه رفتار کند |
|---|---|---|---|
| **۱۰۰۱** `Going Away` | خاموشیِ مودبانه‌ی نود (گام ۴٫۸) | **فوراً** دوباره وصل شو — نودِ دیگری آماده است و `/readyz`ِ این یکی از قبل قرمز شده | با backoff، کاربر چند ثانیه بی‌دلیل قطع می‌مانَد در حالی که خوشه سالم است |
| **۱۰۰۸** `Policy` + `HB_ERROR{code}` | ردِ احراز هویت یا مجوز (گام ۴٫۱) | **تلاشِ دوباره نکن.** بسته به کد: `TOKEN_EXPIRED` → توکن را تازه کن؛ `TOKEN_MISSING` → کاربر را به ورود بفرست؛ `FORBIDDEN` → پیامِ «دسترسی نداری» | حلقه‌ی بی‌پایانِ اتصالِ مجدد با توکنی که هرگز قبول نمی‌شود |
| **قطعِ ناگهانی** (۱۰۰۶ یا خطای شبکه) | شبکه، heartbeatِ ناموفق، مرگِ نود | backoff **نمایی + jitter** با سقف | بدونِ jitter، همه‌ی کلاینت‌ها **هم‌زمان** برمی‌گردند و همان نودی را می‌کشند که تازه بالا آمده |

⚠️ **`HB_ERROR` همیشه یعنی «ببند» نیست** ([ADR-038](ARCHITECTURE_DECISIONS.md#adr-038)):
ردِ **پیام** (مثلاً updateِ یک `viewer`) اتصال را **باز** می‌گذارد و همراهش
`HB_PERMISSION` می‌آید. کلاینت باید از **کد** تشخیص بدهد، نه از خودِ رسیدنِ
`HB_ERROR`.

هر سه ورودی محقق شد، با یک **افزوده**: کدِ `HB_ERROR` هم می‌تواند ۱۰۰۸ را به
تلاشِ دوباره برگرداند (`TOKEN_EXPIRED` فوری، `SERVER_BUSY`/`ROOM_CLOSED` با
backoff) — چیزی که در جدولِ اولیه «تلاشِ دوباره نکن» فرض شده بود. و «کدِ
ناشناخته» **مرگبار** شد، نه گذرا.

★ **و تصمیمِ معلقِ `HB_AUTH_REFRESH` گرفته شد: هر دو نیمه ساخته شد.** نیمه‌ی
کلاینتِ تنها یک no-opِ بی‌صدا بود، و بی‌صدا بودن بدتر از نبودن است.

⚠️ **قیدِ `shared-types` رعایت شد** — صفر تغییر، حتی در پسوندِ import.

<details>
<summary>بایگانی: ورودی‌های فاز ۴ (گام‌های ۴٫۱–۴٫۳ انجام شدند)</summary>

**فاز ۳ تمام است.** آنچه برای فاز ۴ آماده گذاشته:

- **پروتکلِ کامل** (گام ۲٫۴): هر ۷ کدِ پیامِ PLAN ۵٫۳ با encode/decode و
  سازگاریِ رو به جلو. سرور همان `encodeMessage`/`decodeMessage` را مصرف می‌کند.
- **seamِ ترابری** ([`SyncTransport`](packages/canvas-sync/src/transport.ts)):
  آداپتور فقط این interface را می‌شناسد. فاز ۴ باید یک پیاده‌سازیِ WebSocket
  بدهد؛ **هیچ خطی از binder عوض نمی‌شود** — همان قولی که ADR-030 داد.
- **بایت‌هایی که از قبل آزموده‌اند:** هر چیزی که سرور قرار است پخش کند (sync،
  awareness، ephemeral) امروز بین دو کلاینتِ واقعی در مرورگر رد و بدل می‌شود.

★ **گام ۴٫۱ سه ورودیِ ثبت‌شده دارد:** `ws` (D-4) · claimهای `rtToken` **داخلِ
پورتِ `BoardAuthority`** و نه `shared-types` (اگر لازم شد بیرون برود →
**متوقف شو و بپرس**) · و ★ گیتِ runtime علیهِ `DevBoardAuthority` در production
(سرور باید **بالا نیاید**، نه اینکه هشدار بدهد — ADR-031).

⚠️ **نیمه‌ی دومِ G-1** (همین سناریوی دو-نمونه‌ای با **سرورِ واقعی**) گام ۶٫۱ است،
نه فاز ۴.

</details>

<details>
<summary>بایگانی: ورودی‌های گام ۳٫۷ (انجام شد)</summary>

**این همان گپِ ارث‌رسیده‌ی M1 است** که آنجا «اصلاً قابلِ نوشتن نبود» چون binder
وجود نداشت. حالا وجود دارد و **نیمی از کارِ صفحه هم انجام شده**.

### ★ آنچه از قبل آماده است — از نو نساز

دموی `#pair` ([`dev/SyncPairDemo.tsx`](packages/canvas-sync/dev/SyncPairDemo.tsx))
دو بومِ واقعی زیر StrictMode روی یک hub دارد، و `window.__hbPair[name]` این‌ها را
می‌دهد: `api` · `outbound` · `doc` · `commitLocal` · `peers()` · `docBytes()` ·
`ingestImage()` · `assets()` · `engineFiles()`. شمارنده‌ی `data-role="peers"` هم
روی هدرِ هر پنل است.

★ **پس ۳٫۷ فقط دو چیز کم دارد: رندر، و پروجکشن.**

### ★★ چهار زیرموردِ G-1 — دو تایش عملاً بسته است

| زیرمورد | وضعیت |
|---|---|
| **sync** (ساخت در A → ظهور در B، و B دوباره emit نکند) | ✅ در ۳٫۱ و ۳٫۲ با binderِ واقعی آزموده شد |
| **رندرِ حضور** از `applyPeers` (مکان‌نما، هاله، آواتار) | ❌ کانالش در ۳٫۵ ساخته شد، **پیکسلش نه** |
| ★ **re-projectِ حضور** با هر تغییرِ viewport | ❌ اصلاً شروع نشده |
| **follow** (پرش روی مکان‌نمای طرف با viewportِ مستقل) | ❌ |

### ★ سه قیدِ ثبت‌شده که ۳٫۷ باید رعایت کند

1. ★★ **پروجکشن با تابعِ مشترکِ
   [`sceneToOverlayPixel`](packages/canvas-core/src/ui/presence-projection.ts)** —
   نه یک نسخه‌ی دوم (ADR-024). آن تابع `viewport` را ورودیِ **صریح** می‌گیرد.
2. ★★ **مقدارِ `viewport` از `onScrollChange` بیاید، نه `getAppState()`** — که
   درست بعد از جابه‌جاییِ نما **یک فریمْ کهنه** است. همین باگ در M1 مکان‌نمای همتا
   را روی **panِ خالص** جا می‌گذاشت (خط قرمزِ ۴ در `canvas-sync/CLAUDE.md`).
   ⚠️ گام ۳٫۵ فقط کانال را ساخت: `emitViewport` هر مقداری را که بدهی پخش می‌کند و
   قضاوتی درباره‌ی منبعش ندارد. **رعایتِ این قید کارِ همین گام است.**
3. ★ **مقادیرِ re-project با پروجکشنِ دست‌محاسبه مقایسه شوند، نه با خروجیِ خودِ
   کد** — همان روشی که در M1 باگِ panِ خالص را بیرون کشید.

**معیارِ پذیرش:** هر چهار زیرمورد سبز و پایدار (سه اجرای متوالی)، سپس بندِ G-1
در [`sync/README.md`](packages/canvas-core/src/sync/README.md) به «نیمه‌ی اول ✅»
به‌روز شود.

</details>

---

## وضعیت گیت‌ها

`pnpm verify` **سبزِ واقعی، exit 0** — **هفت گیت**: typecheckِ ریشه · typecheckِ
پکیج‌ها · lint · ★ **lint (CSS)** · test · هر دو گیتِ لایسنس (**۷۳۲ پکیج** بعد از
افزودنِ `y-indexeddb`، self-test ۱۷/۱۷). همه با `--force`، تست‌ها **تک-forkـی**، و
تصمیم فقط با کدِ خروجِ فرایند.

**۱۱۸۱ تستِ واحد در ۸ پکیج** (اندازه‌گیری‌شده، نه تخمین):

| پکیج | تست | چیست |
|---|---|---|
| canvas-core | ۴۷۴ | از M1، دست‌نخورده |
| ydoc-schema | ۱۵۵ | codecِ هر پنج ریشه + نگهبانِ باینری + migration + پروتکل + probeهای فاز ۱ |
| **canvas-sync** | **۲۲۵** | آداپتور (۳٫۱) · مسیرِ remote (۳٫۲) · throttle (۳٫۳) · undo (۳٫۴) · حضور (۳٫۵) · دارایی (۳٫۶) · codec · ★ **اتصالِ مجدد (۵٫۱)** · ★ **آفلاین و صداقتِ `SaveState` (۵٫۲)** |
| **realtime** | **۱۵۱** | کلِ فاز ۴ — دست‌دادن · اتاق و مرزِ اعتماد · دوام · فشرده‌سازی · مجوز · حضور · خوشه · خاموشی · ★ تازه‌سازیِ توکن (۵٫۱) |
| eslint-config | ۷۹ | گیت‌های مرزی (سه‌لایه) + `process.env` + دو قاعده‌ی `captureUpdate` |
| shared-types · i18n | ۴۴ + ۴۲ | از M1 |
| config | ۱۱ | خواندنِ env |

★ **و هفت سنجه‌ی زنده که عمداً بیرونِ `pnpm verify` اند** (دیتابیس و Redisِ زنده
لازم دارند): `rt:durability` · `rt:compaction` · `rt:permission` · `rt:presence` ·
`rt:cluster` · `rt:shutdown` · **`rt:reconnect`**. ⚠️ **`skip` نمی‌شوند و نباید
بشوند** — هر هفت باگی گرفتند که تست‌های واحد سبز از رویش رد شده بودند.
✅ هر هفت بعد از تغییرهای فاز ۵ دوباره اجرا شدند و **همه سبز**.

**E2E:** ۱۶ تستِ M1 (کاملاً سبز روی پورتِ جدید، ۸ اجرا شامل ۳ اجرای سرد) +
**۲۸ تست در canvas-sync** (+ **۱ تای جدا** برای گام ۶٫۱ که داکر لازم دارد): ۲ probeِ StrictMode · ۳ انزوای undo (۳٫۲) ·
۳ اندازه‌گیریِ متن (۳٫۳) · ۳ دو-کلاینته‌ی undo (۳٫۴) · ۳ حضور (۳٫۵) ·
۳ دارایی (۳٫۶) · ★ **۸ رندرِ حضور و G-1الف (۳٫۷)** — شاملِ **دو نگهبانِ B-1** که
پیکسلِ **واقعی** را با پروجکشنِ دست‌محاسبه می‌سنجند (نه فقط `transform`) · ★★ **۱ صفِ
آفلاین (۵٫۲)** — با سرورِ realtimeِ واقعی و **تبِ بسته و بازشده** · ★★ **۲ نقش و
نسخه (۵٫۳)** — `viewModeEnabled`ِ خودِ موتور، و «crash نمی‌کند» با شنودِ
`pageerror`.
در `pnpm verify` نیستند (مرورگر لازم دارند) ولی ادعاهایی را می‌آزمایند که **فقط**
آنجا ممکن است — undo در jsdom اصلاً وجود ندارد. سه اجرای پایدار.

**یک قرمزِ ثبت‌شده و بی‌ربط:** `pnpm format:check` روی `.claude/settings.local.json`
هشدار می‌دهد. فایل untracked و محلی است؛ ربطی به M2 ندارد و دست نزدم.
