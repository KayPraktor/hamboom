# PROGRESS — realtime-sync

تاریخ آخرین به‌روزرسانی: ۱۴۰۵/۰۵/۱۳ (2026-08-05)
**★ فازهای ۰ و ۱ کامل ✅** — چهار probe، سه ADR جدید (۰۳۲/۰۳۳/۰۳۴).
**★ فاز ۲ کامل ✅** (گام‌های ۲٫۱ تا ۲٫۴) — هر پنج ریشه‌ی سند codec دارد، مسیرِ
migration **آزموده** است، و پروتکل هر ۷ پیام را دارد. `pnpm verify` سبز (exit 0).
**بعدی: فاز ۳ — گام ۳٫۱ (اسکلت آداپتور + چرخه‌ی عمر + نگهبانِ echo). ★ قلبِ M2.**

M1 (`canvas-core`) تحویل و push شد. این ماژول M2 است. تاریخچه‌ی M1 در
[PROGRESS-M1-canvas-core.md](PROGRESS-M1-canvas-core.md) و [TODO-M1-canvas-core.md](TODO-M1-canvas-core.md)
بایگانی است — **قبل از هر تصمیمی درباره‌ی رفتارِ موتور، جدولِ تله‌های
[`canvas-core/CLAUDE.md`](packages/canvas-core/CLAUDE.md) را بخوان.**

---

## انجام شد

### برنامه‌ریزیِ M2 (۱۴۰۵/۰۵/۱۲)

- **[TODO.md](TODO.md) نوشته شد** — ۶ فاز، ۳۰ گام، هر گام با معیارِ پذیرشِ قابل‌سنجش.
  ترتیب: اسکلت → **سه probeِ ریسک** → `ydoc-schema` → binder → سرور → تاب‌آوری → تحویل.
  دلیلِ ترتیب در خودِ فایل: **binder قبل از سرور** آزمودنی است (دو `Y.Doc` مستقیم، بدون شبکه)،
  پس هیچ باگی پشتِ لایه‌ی شبکه پنهان نمی‌شود.
- **کشفِ مهم حین برنامه‌ریزی:** PLAN فرض کرده بود M2 بعد از M3 و M5 می‌آید، ولی **هیچ‌کدام
  ساخته نشده‌اند** — نه `apps/`، نه `infra/`، نه `packages/{config,auth-core,storage}`.
  یعنی `rt-token`، `effectiveBoardRole` (ADR-012) و Object Storage برای snapshot (ADR-009)
  وجود ندارند. این مرزِ M2 را عوض کرد و به تصمیم‌های D-1..D-5 انجامید.
- **دو ناسازگاری در خودِ اسناد پیدا شد** (نه در کد) و هر دو به گام تبدیل شدند:
  - PLAN بخش ۸ binder را در `ydoc-schema` می‌گذارد، ولی PLAN بخش ۲ می‌گوید
    `ydoc-schema → canvas-core ❌`. → [ADR-029](ARCHITECTURE_DECISIONS.md#adr-029)
  - PLAN بخش ۷٫۳ متن را `Y.Text` می‌خواهد، `shared-types` نوشته `text: z.string()`.
    → با نظر حل نمی‌شود؛ **گام ۱٫۳** تجربی حلش می‌کند.

### گام ۰٫۱ — ثبتِ تصمیم‌های مرزی ✅

سه ADR نوشته شد. هیچ کدی در این گام نوشته نشد (عمدی — دروازه‌ی فاز ۰).

| ADR | تصمیم | نکته‌ی کلیدی |
|---|---|---|
| [ADR-029](ARCHITECTURE_DECISIONS.md#adr-029) | `ydoc-schema` خالص + `canvas-sync` برای binder | `import type` رد شد چون هزینه‌اش در **typecheck و ضعیف‌شدنِ گیت** است، نه runtime |
| [ADR-030](ARCHITECTURE_DECISIONS.md#adr-030) | `ws` پشتِ یک seam | دلیل کارایی **نیست**: uWS خارج از npm نصب می‌شود (از گیتِ P1 رد می‌شود) و روی ویندوز P3 را می‌شکند |
| [ADR-031](ARCHITECTURE_DECISIONS.md#adr-031) | پورت‌های `BoardAuthority` و `SnapshotStore` | P4 با **ساخت** حفظ می‌شود نه با قول — M2 اصلاً SDK را نمی‌بیند |

**یک خطر که حین نوشتنِ ADR-031 بیرون آمد و در TODO نبود:** `DevBoardAuthority` یک پیاده‌سازیِ
توسعه است که هرکس `RT_DEV_JWT_SECRET` را بداند می‌تواند برای خودش نقشِ owner صادر کند. اگر
روزی در production زنده بماند، کلِ ADR-012 از پشت دور زده می‌شود. → **گیتِ runtime** به
گام ۴٫۱ اضافه شد: با `APP_ENV=production` سرور باید **بالا نیاید**، نه اینکه هشدار بدهد.

**در حاشیه رفع شد:** ADR-027 و ADR-028 در فهرستِ `ARCHITECTURE_DECISIONS.md` نبودند و
ADR-028 لنگرِ `<a id>` نداشت — یعنی همه‌ی لینک‌های `#adr-028` از PROGRESS/TODO/CLAUDE
شکسته بودند. هر دو درست شد.

### گام ۰٫۲ — اسکلت سه پکیج + گیتِ مرزها ✅

سه واحدِ ADR-029 ساخته شدند: `packages/ydoc-schema` (خالص)، `packages/canvas-sync`
(binder)، `apps/realtime` (سرور) — هر کدام با `CLAUDE.md`، Vitest، و تستِ دودی که
یک ادعای **معماری** می‌آزماید نه یک منطقِ محصولی (مثلاً: سرور می‌تواند `ydoc-schema`
را در Nodeِ خالص مصرف کند؛ `canvas-sync` نگهبانِ echo را از M1 **قرض می‌گیرد**).

**گیت‌های مرزی در منبعِ واحد** (`eslint-config/boundaries.js`) با **خودآزمونِ سه‌لایه**
(۵۰ تست در [`eslint-config/test/boundaries.test.js`](packages/eslint-config/test/boundaries.test.js)):

| لایه | چه چیزی را می‌گیرد | چرا لازم است |
|---|---|---|
| ۱ — الگوها | فهرستِ `forbid` روی خروجیِ **واقعیِ** factory | اگر فهرست عوض شود |
| ۲ — سیم‌کشی | lintِ واقعی روی `eslint.config.js`ِ خودِ پکیج | اگر factory صدا زده **نشود** یا `files` غلط باشد، لایه‌ی ۱ سبز می‌ماند و گیت عملاً وجود ندارد |
| ۳ — manifest | وابستگی‌های اعلام‌شده در `package.json` | گیتِ import فقط `src/` را می‌بیند؛ یک وابستگیِ ممنوعِ اعلام‌شده از چشمش می‌افتد |

**★ لایه‌ی ۲ همان اول یک باگ گرفت:** `apps/realtime/eslint.config.js` مستقیم `globals`
را import می‌کرد، ولی `globals` وابستگیِ `@hamboom/eslint-config` است نه اپ — زیر pnpm
resolve نمی‌شد و **`pnpm lint` آن اپ می‌شکست**. رفع: `nodeGlobals` از `eslint-config/base`
صادر می‌شود. **لایه‌ی ۱ این را نمی‌گرفت** — مصداقِ اینکه چرا هر دو لازم است.

**دو تصحیح نسبت به متنِ اولیه‌ی گام (هر دو در TODO ثبت شد):**

1. **`canvas-sync` فقط `import type` نیست.** گام ۳٫۱ باید `assertEmittable` را
   **به‌صورت مقدار** از M1 صدا بزند، وگرنه مجبور است نگهبانِ echo را از نو بنویسد —
   خلافِ ADR-024. یک تستِ `allowed` این را قفل کرد.
2. **`@hamboom/storage` و `@hamboom/auth-core` برای `apps/realtime` بسته نشدند.**
   ممنوعیت روی `@aws-sdk/*`ِ **خام** است. خودِ P4 مسیرِ `packages/storage` را تجویز
   می‌کند و ADR-012 صریحاً می‌خواهد realtime و API از **یک** `effectiveBoardRole`
   مشترک استفاده کنند؛ بستنشان یعنی بستنِ همان مسیری که ADR تجویز کرده.

**probeِ ثبت‌شده (خلافِ شهود):** در `no-restricted-imports` علامتِ `*` **از `/` عبور
می‌کند** — `@aws-sdk/*` زیرمسیرِ عمیق را هم می‌گیرد و ورودیِ بدونِ گلاب (`@hamboom/sdk`)
هم `@hamboom/sdk/client` را. پس الگوها ساده می‌مانند؛ افزودنِ `/**` زائد است. در
`boundaries.js` مستند و با تست pin شد تا کسی «اصلاحش» نکند.

**وابستگی‌ها:** `yjs` ۱۳٫۶٫۳۲ · `lib0` ۰٫۲٫۱۱۷ · `y-protocols` ۱٫۰٫۷ — هر سه MIT،
`license:check` سبز (۶۸۵ پکیج)، ثبت در `docs/dependencies.md`. **`uWebSockets.js`
افزوده نشد** (ADR-030)؛ `ws` در گام ۴٫۱ می‌آید.

### گام ۰٫۳ — برشِ حداقلیِ زیرساخت ✅

ساخته شد: compose (فقط postgres+redis، با `docker compose config` اعتبارسنجی‌شده)،
`.env.example`، `packages/config` (zod، ترکیب‌پذیر، ۱۱ تست)، migrationِ SQL خام،
اجراکننده‌ی migration با **checksum**، و `scripts/db-smoke.ts`.

**دو تصمیمِ ریز که ارزش ثبت دارند:**

1. **`boards` ساخته نشد و FKها گذاشته نشد** (انحراف از PLAN بخش ۶، با دلیل): اگر M2
   خودش `CREATE TABLE boards` بزند، `0001_init.sql`ِ خودِ M3 بعداً روی جدولِ موجود
   می‌افتد و می‌شکند. M3 دو FK را با `ALTER TABLE` اضافه می‌کند → گام ۶٫۴.
2. **`db:smoke` اسکریپت است نه تستِ vitest**: اگر تست بود، یا `pnpm test` روی ماشینِ
   بدونِ داکر قرمز می‌شد یا خودش را skip می‌کرد — و skip یک **سبزِ دروغین** است.
3. **`.env.example` عمداً ناقص است**: فقط بخش‌هایی که الان مصرف‌کننده دارند. یک
   `.env.example` پر از متغیرِ SMTP و زرین‌پال برای اپ‌هایی که وجود ندارند، اولین
   چیزی است که از واقعیت واگرا می‌شود.

**★ تله‌ی جدید (اولین مصرف‌کننده‌ی Nodeِ خالصِ پروژه):** `canvas-core/CLAUDE.md`
پیش‌بینی کرده بود «اگر روزی یک مصرف‌کننده‌ی Node خالص اضافه شد…» — همان امروز شد.
Node برخلاف Vite پسوندِ importهای نسبی را حدس نمی‌زند و `ERR_MODULE_NOT_FOUND`
می‌دهد. رفع: `allowImportingTsExtensions` در چهار tsconfigِ M2 + ریشه، و پسوندِ `.ts`
صریح. در `packages/config/CLAUDE.md` مستند شد و از دو CLAUDE.mdِ دیگر به آن ارجاع داده شد.

### ✅ تاییدِ زنده‌ی ۰٫۳ — با دیتابیسِ واقعی روی ویندوز (۱۴۰۵/۰۵/۱۲)

- `pnpm db:up` → هر دو کانتینر **healthy**؛ redis → `PONG`.
- `pnpm db:migrate` → اعمال شد. `\d board_updates` روی دیتابیسِ واقعی دقیقاً ستون‌ها و
  `board_updates_seq_uq` را طبق PLAN بخش ۶ نشان می‌دهد.
- `pnpm db:smoke` → **رفت‌وبرگشتِ `bytea` بیت‌به‌بیت سالم** (با بایت‌های ۰x00/0xFF/0x80 —
  اگر جایی به رشته تبدیل شوند، سندِ Yjs خاموش خراب می‌شود)، ایندکسِ یکتا `seq` تکراری را
  رد کرد، `board_snapshots` پذیرفت.
- **idempotency** و ★ **گیتِ checksum** هر دو **در عمل** آزموده شدند: افزودنِ یک کامنت
  به migrationِ اجراشده → رد شد با پیامِ صریح؛ بعد از بازگردانی دوباره سبز.

**★ تعارضِ محیطی که حل شد (برای ماشین‌های بعدی):** یک **PostgreSQL 18 بومی**
(سرویسِ ویندوزِ `postgresql-x64-18`) پورت ۵۴۳۲ را گرفته بود — `pnpm db:migrate` واقعاً
تا آن رسید و `auth_failed` گرفت، که خودش ثابت کرد مسیرِ کد سرتاسر درست است و فقط
دیتابیس اشتباه بود. **راه‌حلِ انتخابیِ مالک:** داکر روی ۵۴۳۳، بدونِ دست‌زدن به سرویسِ
سیستم. فقط `.env`ِ محلی عوض شد؛ `.env.example` و compose روی پیش‌فرضِ PLAN ماندند تا
روی ماشینِ تمیز همان PLAN کار کند (یادداشتش در `.env.example` هست).

### گام ۱٫۱ — ★ probeِ StrictMode ✅ — **دروازه باز شد**

**نتیجه: فرضِ [ADR-028](ARCHITECTURE_DECISIONS.md#adr-028) بازتولید شد و راه‌حلِ نامزدش
تایید شد** → [ADR-032](ARCHITECTURE_DECISIONS.md#adr-032). **دستِ `canvas-core` نخورد.**

| الگو | موتور | state ری‌اکت |
|---|---|---|
| `onReady` (الگوی M1) | ۱ عنصر، ۱ انتخاب ✓ | `onChange` = **۰** ✗ |
| `useEffect([api])` | ۱ عنصر، ۱ انتخاب ✓ | همه دنبال می‌کنند ✓ (شاملِ **تغییرِ** انتخاب) |

- **جای probe:** `packages/canvas-sync/dev/` — طبق ADR-029 در دامنه‌ی M2. همان‌جا در
  گام ۳٫۷ دموی دو-نمونه‌ایِ G-1 هم می‌آید (زیرساختِ Vite/Playwright حالا آماده است).
- ★ **درسِ روشِ آزمایش که باید یادم بماند:** خواندنِ stateِ ری‌اکت به‌تنهایی **مبهم**
  است — صفر ماندنش هم «اشتراک مرده» معنی می‌دهد و هم «تعامل چیزی نکشید». یک
  **شاهدِ کنترلِ مستقل از ری‌اکت** (`window.__hbProbeApi`) اضافه شد که حالتِ موتور را
  مستقیم می‌خواند. **اجرای اول بدونِ آن گمراه شد:** `KeyR` بدونِ focusِ کانتینر به
  ابزار نمی‌رسید، پس درگ یک کادرِ انتخاب می‌کشید نه شکل — و نزدیک بود یک **باگِ تست**
  به‌عنوان یافته‌ی معماری گزارش شود. (همان تله‌ی ثبت‌شده‌ی M1 درباره‌ی صفحه‌کلید.)
- **دو نگهبانِ دائمی** (`e2e/strictmode.spec.ts`، سه اجرای پایدار): الگوی درست
  **زنده است**، و الگوی غلط **هنوز مرده است**. دومی عمدی است — اگر نسخه‌ی بعدیِ
  Excalidraw رفعش کند، آن تست قرمز می‌شود و می‌فهمیم فرضِ ADR-032 دیگر لازم نیست.
- **آنچه اثبات نشد:** *چرا* callbackِ `onReady` به نمونه‌ی دورانداخته می‌چسبد. علتِ
  درونیِ موتور کاوش نشد — نشانه و رفع سنجیده شد، نه مکانیزم.
- ⚠️ **تله‌ی محیطی — رفع شد** (پایین، «پورت‌های dev»).

### ✅ پورت‌های dev — بیرون از بازه‌ی dynamic ویندوز (به درخواستِ مالک)

**مسئله:** Hyper-V/WSL محدوده‌هایی از **بازه‌ی dynamic port** را رزرو می‌کند و `bind`
رویشان `EACCES` می‌دهد. این محدوده‌ها **با هر بوت عوض می‌شوند** — پس E2E یک روز کار
می‌کرد و روزِ بعد نه، بدونِ یک خط تغییر در کد. خطرش این بود که یک شکستِ محیطی با
شکستِ کد اشتباه گرفته شود.

**چرا «پورت را عوض کن» کافی نبود:** اندازه‌گیری نشان داد بازه‌ی dynamic روی این ماشین
**۱۰۲۴–۱۵۰۰۰** است (پیش‌فرضِ ویندوز ۴۹۱۵۲–۶۵۵۳۵). یعنی هر پورتِ زیرِ ۱۵۰۰۰ — از جمله
۵۱۸۰ و ۵۲۸۰ — می‌تواند هر بوت قربانی شود. جابه‌جایی به یک پورتِ آزادِ دیگر فقط شیر
یا خط بود.

**رفع (دو لایه):**

۱. **خروج از کلِ بازه:** `canvas-core` → **۱۵۱۸۰**، `canvas-sync` → **۱۵۲۸۰**. این
   اعداد هم بالای بازه‌ی این ماشین (۱۵۰۰۰) اند و هم زیرِ بازه‌ی پیش‌فرضِ ویندوز
   (۴۹۱۵۲) — یعنی روی **هر دو** پیکربندی امن‌اند، نه فقط اینجا.
   `strictPort: true` هم اضافه شد تا Vite بی‌صدا روی پورتِ بعدی نرود (وگرنه
   Playwright به آدرسِ اشتباه وصل می‌شود و خطا به «سرور بالا نیامد» تغییر شکل می‌دهد).
۲. **شبکه‌ی ایمنی:** [`scripts/check-dev-port.mjs`](scripts/check-dev-port.mjs) در هوکِ
   `predev` هر دو پکیج. اگر روزی باز هم رخ داد، به‌جای `EACCES`ِ گنگ یک پیامِ فارسی
   می‌دهد که **صریحاً می‌گوید مشکلِ محیطی است نه باگِ کد** و دستورهای netsh را نشان
   می‌دهد. با یک پورتِ واقعاً رزروشده (۱۱۰۰) آزموده شد: پیام درست و `exit=1`.

**تاییدِ عملی:** E2E کاملِ M1 روی پورتِ جدید اجرا شد — **۱۶/۱۶ سبز** (و چهار اجرای
متوالیِ سبز). E2E ماژول خودمان هم ۲/۲.

⚠️ **این یک تغییرِ عمدیِ بین‌دامنه‌ای در `canvas-core` بود** (پورت + هوکِ predev +
دو خط سند). به درخواستِ صریحِ مالک؛ **هیچ کدِ محصولیِ M1 عوض نشد.**

### ✅ flake در E2E خودِ M1 — کشف و **رفع شد** (تاییدِ مالک)

اولین اجرای کامل بعد از تغییرِ پورت، تستِ `clipboard.spec.ts` («Ctrl+X عناصر را
می‌بُرد و Ctrl+V هر سه را برمی‌گرداند») افتاد: انتظار ۳، دریافت ۲.

**تشخیص — ربطی به تغییرِ پورت ندارد:**
- در **انزوا** پاس می‌شود (۲/۲).
- **چهار اجرای کاملِ متوالیِ بعدی** همگی ۱۶/۱۶ سبز.
- شکست فقط روی **اولین اجرای سرد** رخ داد (جایی که Vite تازه dep-optimize می‌کند).
  تستِ کلیپ‌بورد با `expect.poll` و مهلتِ ۵ ثانیه‌ای زمان‌حساس است.

**علتِ واقعی (عمیق‌تر از «مهلت کم» بود):** عددِ **۲** خودش سرنخ بود — یعنی paste
مسیرِ «متنِ خارجی» را رفته و **یک استیکی** (ظرف + متن) ساخته، همان باگی که M1 قبلاً
رفع کرده بود. ریشه: `onCut` نشانه‌ی ما را روی رویداد می‌نویسد، ولی **موتور هم روی
همان Ctrl+X کلیپ‌بورد را async با فرمتِ خودش بازنویسی می‌کند**، و کلیپ‌بوردِ سیستم بینِ
تست‌ها مشترک است. در آن پنجره‌ی کوتاه، paste نشانه‌ی **تستِ قبلی** را می‌دید: نه با
tokenِ فعلی برابر، نه فرمتِ Excalidraw → «متنِ خارجی».

**این باگِ محصول نیست** — هیچ انسانی Ctrl+X و Ctrl+V را در چند میلی‌ثانیه پشتِ هم
نمی‌زند. ولی تست داشت زمان‌بندی را می‌آزمود، نه رفتار را.

**رفع (فقط فایلِ تست، هیچ کدِ محصولی):** `waitForInternalClipboard` — صبرِ صریح تا
کلیپ‌بوردِ سیستم محتوای قابل‌شناسایی داشته باشد؛ و `waitForTimeout(200)` در
`threeRects` به `expect.poll` روی یک **شرط** تبدیل شد.

**تاییدِ رفع:** ۵ اجرای کاملِ گرم + ★ **۳ اجرای سردِ اجباری** (با `rm -rf
node_modules/.vite` — یعنی بازتولیدِ دقیقِ شرطی که شکست در آن رخ داد) — هر هشت
اجرا ۱۶/۱۶. هر دو تله در جدولِ E2Eِ [`canvas-core/CLAUDE.md`](packages/canvas-core/CLAUDE.md) ثبت شد.

### گام ۱٫۲ — ★★ probeِ کدکِ Y.Doc ✅ — **ADR-007 تایید شد**

**مهم‌ترین خروجی: بازبینیِ مدلِ سند لازم نیست.** ادعای مرکزیِ ADR-007 در آزمونِ
واقعی برقرار بود. شواهد: [`docs/ydoc-baseline.md`](docs/ydoc-baseline.md) +
دو فایلِ تستِ اجراشدنی (۵ تستِ ادغام در `ydoc-schema`، ۲۱ تست در `canvas-sync`).

| سناریوی دو کلاینتِ همزمان | نتیجه |
|---|---|
| per-property: A رنگ، B جابه‌جایی | ✅ **هر دو ماند** و هر دو سند یکسان شدند |
| ★ **ضدِ ادعا**: نوشتنِ آبجکتِ کامل | ❌ یکی خورده شد |
| همان property · آرایه‌ها · حذفِ نرم + ویرایش | LWW ولی **همگرا** |

**چرا ضدِ ادعا را هم آزمودم:** بدونِ نشان‌دادنِ حالتِ بد، «حالتِ خوب» چیزی اثبات
نمی‌کند — شاید هر دو حالت کار می‌کردند و `Y.Map`ِ تودرتو سربارِ بی‌فایده بود.

**اندازه‌ها (بایتِ واقعی، نه تخمین):** ساختِ استیکی ۱۷۱۵ · **تیکِ درگ ۳۹** ·
تغییرِ رنگ ۳۴ · استروکِ قلم ۹۳۸ · و همان تیکِ درگ با نوشتنِ **کلِ عنصر** ۴۳۸.
یعنی صرفه‌جویی **~۱۱ برابر** — مرتبه‌ای، نه چنددرصدی. روی یک درگِ ۲ثانیه‌ای
(۴۰ تیک): **۱٫۵KB در برابر ۱۷KB**. روی شبکه‌ی ایران این تزئینی نیست.

**وفاداری:** هر ۱۰ نمونه از سازنده‌های **واقعیِ** M1 round-trip شد و
`hbElement.parse` را پاس کرد — نه فقط «معتبر»، بلکه دقیقاً برابرِ ورودی.

### ★ قیدِ چهارم — همان‌جا تصمیم گرفته شد، نه موکول به ۲٫۱ (تاییدِ مالک)

مالک خواست `customData` همین‌جا با یک probeِ کوتاه تعیین تکلیف شود، با استدلالِ
درست: `customData.hb` جایی است که `kind` و پالتِ استیکی و جهتِ متن زندگی می‌کنند —
**قلبِ مدلِ داده‌ی خودمان**، نه یک فیلدِ جانبی.

همان آزمونِ per-property، یک لایه پایین‌تر:

| مدل | A پالت را عوض می‌کند، B همزمان برچسب می‌زند |
|---|---|
| آبجکتِ ساده (وضعِ فعلی) | ❌ **یکی خورده شد** |
| `Y.Map` تودرتو | ✅ هر دو ماند، `kind` هم سالم |

→ **[ADR-033](ARCHITECTURE_DECISIONS.md#adr-033): `customData` تودرتو می‌شود.**
قاعده **بازگشتی و بدونِ استثنا** است (هر آبجکتِ ساده → `Y.Map`)؛ بُرش در عمق
دلبخواهی می‌شد و بعداً کسی باید مرزش را یادش می‌ماند.

**دو چیز که قبل از ثبتِ تصمیم سنجیدم، نه بعدش:**
- **قرارداد سالم می‌مانَد؟** بله — `toJSON()` بازگشتی است و هر ۱۰ نمونه‌ی واقعی
  همچنان `hbElement.parse` را پاس می‌کنند. `shared-types` دست‌نخورده.
- **هزینه‌اش چقدر است؟** ساختِ استیکی ۷۵۸ → ۸۴۹ بایت (**+۱۲٪**)، فقط روی ساخت.
  سربار است نه انفجار.

**یک نکته که شدت را کم کرد و فرض نبود:** `lastEditedBy` فقط هنگامِ **ساخت** نوشته
می‌شود، نه در هر جهش — پس *هر* ویرایشِ همزمان به تعارض نمی‌خورد.

**سه قیدِ دیگر برای گام ۲٫۱:** `undefined` در `Y.Map` نمی‌نشیند (باید **رد** شود نه
`null` — وگرنه parse روی فیلدِ optional می‌افتد) · نوشتنِ بدونِ تغییر = صفر update ·
`line` سازنده ندارد ولی codec باید پوششش دهد.

**یافته‌ی جانبی — تاییدِ دوباره‌ی ADR-029:** `@hamboom/canvas-core` در Nodeِ خالص
بارگذاری **نمی‌شود** (Excalidraw هنگام لودِ ماژول به `window` دست می‌زند). برای
binder بی‌اهمیت است، ولی اگر `ydoc-schema` یا `apps/realtime` اجازه‌ی دیدنش را
داشتند، **سرور به DOM گره می‌خورد**. مرزِ ESLint دقیقاً همین را می‌بندد.

**زیرساختِ تست:** stubِ canvas از **خودِ canvas-core** قرض گرفته شد (`test/setup.ts`)
نه کپی — آن فایل ۱۷۶ خط با دو تله‌ی مستندشده است و نسخه‌ی دومش واگرا می‌شد (ADR-024).

### ⛔ سبزِ دروغینِ گیت — رخ داد، ولی **علتش ایزوله نشد**

**آنچه قطعاً رخ داد:** `pnpm lint` گزارش داد «ESLint: No issues found» با **exit 0**،
در حالی که دو خطای واقعی وجود داشت؛ `npx turbo run lint --force` همان لحظه گرفتشان.

**دو تصحیح روی حرف‌های خودم — هر دو تشخیصم غلط از آب درآمد:**

1. اول گفتم «turbo از کشِ کهنه سبز می‌دهد» (کامیت `cf3b234`). **آزمودم و رد شد:**
   یک فایلِ untracked با خطای لینت را turbo **بدونِ `--force` هم** می‌گیرد.
2. بعد گفتم «پروکسیِ `rtk` خروجی را ماسک می‌کند» (کامیت `f682d6e`). **آن هم رد شد:**
   با یک خطای واقعیِ لینت (`no-debugger`، نه یک warning)، `pnpm lint` در سه تلاشِ
   پیاپی — و حتی با همان ترتیبِ `prettier --write` بلافاصله قبلش — **هر بار درست
   exit 1 داد**.

   ⚠️ تلاشِ دومم برای تکرار هم **خودش معتبر نبود**: با `any` آزمودم که فقط warning
   است و eslint با warning هم exit 0 می‌دهد. یعنی یک لحظه داشتم از یک آزمونِ غلط
   نتیجه‌ی درست می‌گرفتم.

**نتیجه‌ی صادقانه:** یک سبزِ دروغینِ واقعی دیده شد، ولی **علتش معلوم نیست** و
تکرارپذیر نبود. `pnpm test` هم آزموده شد و شکستِ واقعی را **درست** گزارش کرد، پس
مشکل فراگیر نیست.

**چرا با وجودِ نامعلوم بودنِ علت، `pnpm verify` درست است:** به هیچ فرضیه‌ای تکیه
نمی‌کند و **هر دو علتِ نامزد را حذف می‌کند** — اجرا از راهِ `turbo`ِ مستقیم (نه
اسکریپت‌های `pnpm`)، `--force` (پس بدونِ کش)، و ★ تصمیم فقط با **کدِ خروجِ فرایند**
نه متنِ خروجی. خودش با یک خطای عمدی آزموده شد و گرفتش.

**درباره‌ی `rtk`:** hookـش در `~/.claude/settings.json` سراسری است
(`PreToolUse` روی `Bash` → `rtk hook claude`)، پس روی همه‌ی پروژه‌ها اثر دارد. ولی
**شواهدی که مقصر بودنش را ثابت کند پیدا نشد**، پس توصیه‌ای به غیرفعال‌کردنش نمی‌دهم.

**قاعده در [`CLAUDE.md`](CLAUDE.md) ثبت شد** (قاعده‌ی ۳): مبنای تیک‌زدن و کامیت فقط
`pnpm verify` است. روی وضعیتِ فعلی سبزِ واقعی داد (exit 0)، پس چیزی پنهان نمانده.

### گام ۱٫۳ — probeِ متنِ همزمان ✅ → [ADR-034](ARCHITECTURE_DECISIONS.md#adr-034)

**`Y.Text` انتخاب شد** — در سند `Y.Text`، در قرارداد رشته، در binder دیف.
★ **`shared-types` دست‌نخورده ماند و `textDelta` به قرارداد اضافه نشد** — مسیرِ
ممنوعی که مالک گذاشته بود لازم نشد، چون دیف کافی بود.

| مدل | دو کاربر همزمان در دو جای متن |
|---|---|
| رشته‌ی ساده (LWW) | ❌ کاراکترهای یکی پاک شد |
| `Y.Text` + دیف | ✅ هر دو ماند، همگرا |

**هزینه:** ۲۸ بایت برای دیفِ یک کاراکتر در متنِ ۱۰۰۰ کاراکتری.

**★★ تله‌ی ۱ — پایه‌ی زنده، و یک آزمونِ غلطِ خودم:** با پایه‌ی کهنه بازه‌ی `delete`
به ایندکسِ اشتباه می‌افتد و **متن مخدوش می‌شود، نه فقط ناقص** (انتظار «سلام رفیق»،
نتیجه «سلام رفیقا»). ⚠️ **اولین آزمونم برای این تله معتبر نبود** — با ویرایشِ
**افزایشی** آزمودم که اصلاً بازه‌ی حذفی ندارد، پس کاراکترِ همتا سالم ماند و تست
افتاد. خودِ تست فرضِ من را رد کرد؛ با ویرایشِ **جایگزینی** خرابی ظاهر شد.
(دومین باری در این فاز که یک آزمونِ بی‌اثر نزدیک بود نتیجه‌ی غلط بدهد.)

**★★ تله‌ی ۲ — `text` در برابر `originalText`:** قرارداد خودش می‌گوید منبعِ حقیقت
`originalText` است. آزموده شد که `text`ِ emit‌شده‌ی **هر دو** کلاینت بعد از ادغام
غلط است، پس `text` نباید CRDT شود — باید بازمحاسبه شود.

**دو چیز که عمداً اندازه‌گیری نشد چون هنوز ممکن نیست** (هر دو معیارِ پذیرشِ ۳٫۳):
عرضِ پنجره‌ی کهنگی (binderِ واقعی لازم دارد) و پرشِ caret (بین دو گزینه فرقی
نمی‌گذارد، پس تصمیم را عوض نمی‌کرد). **عدد نساختم.**

### گام ۱٫۴ — probeِ انزوای undo ✅ — **فاز ۱ کامل**

`trackedOrigins` انزوا را تضمین می‌کند، حتی وقتی تغییرِ همتا روی **همان عنصر** باشد
(کاربر جابه‌جا می‌کند، همتا رنگ عوض می‌کند → undo فقط جابه‌جایی را برمی‌گرداند).

**★★ یافته‌ی وارونه — مهم‌ترین خروجیِ این گام:** فرض کرده بودم
`new Y.UndoManager(scope)`ِ بدونِ گزینه «همه‌چیز را ردیابی می‌کند». **برعکس است.**
سنجیده شد: `null` ✔ · `undefined` ✔ · `"local-user"` ✘ · `"remote"` ✘.

چون binder موظف است با originِ **نام‌دار** بنویسد، جاافتادنِ `trackedOrigins` یعنی
**undo بی‌صدا هیچ کاری نمی‌کند** — نه خطا، نه هشدار. پین شد در **دو جا**: گام ۳٫۴
در TODO (مقصد) و `canvas-sync/CLAUDE.md` (جایی که سازنده‌ی binder می‌خواند).

⚠️ **یک فرضِ غلطِ دیگر که تست ردش کرد:** undoِ یک **ساخت** کلیدِ عنصر را کامل
برمی‌دارد؛ حذفِ نرم قاعده‌ی ماست برای حذفِ **کاربر**، نه این مسیر. ادعا اصلاح شد به
**همگرایی** (که برقرار است) به‌جای «کلید می‌مانَد».

**دو سدِ مستقل، حالا با شواهد:** `trackedOrigins` جلوی **اعمالِ** تغییرِ remote را
نمی‌گیرد، فقط نمی‌گذارد در undo stack بنشیند. پس جایگزینِ `captureUpdate: "NEVER"`
نیست — آن یکی تاریخچه‌ی **خودِ موتور** را می‌بندد.

> **جمع‌بندیِ فاز ۱:** هر چهار probe سبز. سه فرضِ من در این فاز غلط از آب درآمد و
> هر بار **خودِ تست** ردش کرد — که دقیقاً کارِ probe است.

### گام ۲٫۱ — ساختار سند + codecِ عنصر ✅ (۱۴۰۵/۰۵/۱۳)

اولین **کدِ محصولیِ** M2. سه فایل: [`doc.ts`](packages/ydoc-schema/src/doc.ts)
(پنج ریشه، `createBoardDoc`، `readDocument`)، [`element-codec.ts`](packages/ydoc-schema/src/element-codec.ts)
(`writeElement`/`readElement`)، و `test-fixtures.ts`. **۶۱ تستِ تازه** —
`ydoc-schema` ۲۸ → ۷۵ و `canvas-sync` ۳۲ → ۴۶.

**هر چهار قیدِ ورودیِ فاز ۱ پیاده شد و هرکدام تستِ خودش را دارد:**

| قید | چطور بسته شد |
|---|---|
| `undefined` رد شود نه `null` | `continue` در حلقه + تستِ «`tags` کلید نمی‌گیرد و `parse` سبز می‌مانَد» |
| بدونِ تغییر = صفر update | دیف در برابرِ مقدارِ زنده؛ ادعا با شمارشِ **رویدادِ `update`**، نه آستانه‌ی بایتی |
| `line` پوشش داده شود | نمونه‌ی دستی در `ydoc-schema` — تنها جایی که ممکن است |
| `customData` بازگشتی `Y.Map` | `writeInto` بازگشتی؛ `originalText` تنها `Y.Text`، `text` رشته‌ی مشتق |

**★ دو تصمیمِ طراحی که از درسِ فاز ۱ آمدند، نه از متنِ گام:**

۱. **آرگومانِ `previous` حذف شد.** probe یک اسنپ‌شاتِ ورودی می‌گرفت و با آن دیف
   می‌کرد. codecِ محصولی در برابرِ مقدارِ **زنده‌ی داخلِ سند** دیف می‌گیرد — همان
   درسِ تله‌ی ۱ در گام ۱٫۳، یک لایه بالاتر. نتیجه: پایه‌ی کهنه از راهِ API **قابلِ
   دادن نیست**، و «بدونِ تغییر = صفر update» از یک قرارداد به یک خاصیتِ ساختاری
   تبدیل شد. `applyTextDiff` هم عمداً پارامترِ `base` ندارد.
   ⚠️ این **نیمی** از ریسک است: اگر رشته‌ی ورودی از اسنپ‌شاتِ کهنه‌ی بوم بیاید، دیف
   باز هم می‌تواند کاراکترِ همتا را پاک کند. عرضِ آن پنجره کارِ گام ۳٫۳ است.
۲. **`readDocument` با `index` مرتب می‌کند.** در متنِ گام نبود: ترتیبِ پیمایشِ
   `Y.Map` بعد از ادغامِ چند کلاینت معنایی ندارد، پس بدونِ مرتب‌سازی z-orderِ بورد
   در هر بار بازکردن عوض می‌شد. مقایسه‌ی رشته‌ای روی fractional index — همان
   قراردادی که `canvas-core/src/elements/frame.ts` هم دارد.

**نگهبانِ واگراییِ `BoardDocument` ↔ `CanvasDocument`:** `ydoc-schema` حق ندارد
`canvas-core` را ببیند، پس شکلِ `CanvasDocument` را **تکرار** کرده. یک تکرارِ
بی‌نگهبان دیر یا زود واگرا می‌شود و آن‌وقت `replaceDocument` فقط در زمانِ اجرا
می‌شکند. نگهبان یک **ادعای تایپی** در `canvas-sync` است (تنها پکیجی که هر دو را
می‌بیند) — واگرایی `pnpm verify` را قرمز می‌کند، نه مرورگر را.

**دو چیزی که عمداً اینجا انجام نشد و جایشان ثبت شد** (نه «یادم می‌ماند»):
اعتبارسنجیِ سند هنگام خواندن → **گام ۴٫۲** (مرزِ اعتماد جایی است که سند از
دیتابیس می‌آید؛ `readElement` مسیرِ داغِ هر تغییرِ remote است)، و `transact`
کردنِ خودِ codec → **گام ۳٫۳** (origin مالِ binder است). هر دو در TODO **و** در
`CLAUDE.md`ِ پکیجِ مربوطه پین شدند.

⚠️ **یک فرضِ غلطِ دیگر — این‌بار typecheck ردش کرد، نه تست:**
`Extract<HbElement, { type: "rectangle" }>` برابرِ **`never`** است، چون عضوِ
shapeِ union سه literal دارد (`rectangle|ellipse|diamond`) و به یک literal
assignable نیست. تست‌ها **سبز بودند** و فقط `pnpm verify` گرفتش — مصداقِ اینکه
چرا گیت شاملِ typecheck است. تایپِ صادرشده‌ی `HbShapeElement` درست است.

### گام ۲٫۲ — دارایی‌ها، appState و پینِ کامنت ✅ (۱۴۰۵/۰۵/۱۳)

هر پنج ریشه‌ی سند حالا codec دارند. **۴۲ تستِ تازه** (`ydoc-schema` ۷۵ → ۱۱۷).

**★ یک بازآراییِ لازم قبل از افزودن:** منطقِ نوشتنِ افتراقی به
[`value-codec.ts`](packages/ydoc-schema/src/value-codec.ts) منتقل شد. عنصر،
`appState` و سنجاقِ کامنت **هر سه** یک قاعده دارند (فقط تفاوت‌ها نوشته شود، هر
آبجکتِ ساده بازگشتی `Y.Map`)، و سه کپی یعنی سه رفتاری که واگرا می‌شوند —
همان چیزی که ADR-024 منع کرده. تنها تفاوتشان یک پرچمِ `prune` است که **عمداً
پیش‌فرض ندارد**: شیءِ کامل `true`، patch `false`. انتخابِ غلطش یا داده‌ی پاک‌شده
را برای همیشه نگه می‌دارد، یا یک patch کلِ بقیه را پاک می‌کند — پس باید دیده شود.

**★★ یافته‌ای که تستِ اولم را انداخت — Yjs فقط `Uint8Array` را باینری می‌داند:**

| شکل | مستقیم روی `Y.Map` | تودرتو در یک آبجکتِ ساده |
|---|---|---|
| `Uint8Array` | پذیرفته | پذیرفته |
| `ArrayBuffer` · `Float64Array` · `DataView` · `Blob` | **خودِ Yjs رد می‌کند** | ⚠️ **پذیرفته، و در sync به `{}` تبدیل می‌شود** |

ستونِ دوم **بدتر از باینری در سند** است: روی کلاینتِ نویسنده کاملاً سالم به نظر
می‌رسد (Yjs همان شیءِ درون‌حافظه‌ای را برمی‌گرداند) و فقط برای بقیه گم می‌شود —
بدونِ خطا، بدونِ هشدار. اسکنرِ سند هم آن‌طرف چیزی نمی‌بیند چون دیگر باینری نیست.
یعنی برای این شکل‌ها نگهبانِ **بعد از نوشتن** اصلاً کار نمی‌کند.

→ نگهبان **دو تکه** شد: `findBinaryIn` **قبل** از نوشتن (تنها فرصتِ گرفتنِ
شکل‌های «بی‌صدا»)، و `findBinaryValues`/`assertNoBinary` روی **سند** برای
`Uint8Array` — که مصرف‌کننده‌ی اصلی‌اش مرزِ بارگذاری از دیتابیس در گام ۴٫۲ است.
اسکنِ سند از `doc.share` می‌رود نه از `DOC_ROOTS`: باینری از هر مسیری وارد
می‌شود — `customData` یک عنصر، یا ریشه‌ای که کلاینتِ دیگری ساخته.

**نگهبان در هر دو جهت خودآزمون است.** جهتِ منفی به‌اندازه‌ی مثبت مهم است:
نگهبانی که روی سندِ سالم هم خطا بدهد فقط سریع‌تر دور زده می‌شود. پس یک سندِ
**واقعیِ پر** (هر ۹ نوعِ عنصر + دارایی + appState + سنجاق) باید پاک بماند.

**دو تصمیمِ ریز با دلیل:**

1. **`writeAsset` اعتبارسنجی می‌کند، `writeElement` نه.** دلیلش هزینه است نه
   سلیقه: عنصر ده‌ها بار در ثانیه نوشته می‌شود، دارایی یک بار هنگامِ آپلود.
   بررسیِ صریحِ باینری **با وجودِ** `parse` هم انجام می‌شود، چون zod کلیدِ ناشناخته
   را **بی‌صدا** دور می‌ریزد — و سکوت چیزی است که این پروژه به آن اعتماد نمی‌کند.
2. **`appState` دو نگهبان دارد، نه یکی.** `writeAppState` جلوی کدِ **خودمان** را
   می‌گیرد و `readAppState` جلوی کلاینتِ قدیمی یا دست‌کاری‌شده‌ای که مستقیم روی
   ریشه می‌نویسد. بدونِ دومی، `scrollX`ِ یک نفر همچنان نمای همه را می‌پراند —
   و چون CRDT است، همگرا هم می‌شد: همه به یک نما می‌رسیدند و هیچ‌کس نمی‌فهمید چرا.

**`CommentPin` عمداً در `ydoc-schema` است، نه `shared-types`** — قیدِ فعالِ M2. اگر
M3 لازمش داشت، بردنش تاییدِ مالک می‌خواهد؛ در فهرستِ گام ۶٫۴ ثبت شد.

### گام ۲٫۳ — نسخه‌بندی schema و اسکلت migration ✅ (۱۴۰۵/۰۵/۱۳)

**۱۷ تستِ تازه** (`ydoc-schema` ۱۱۷ → ۱۳۴). رجیستریِ محصولی **خالی** است — هنوز
نسخه‌ی ۱ ایم — ولی **موتورش آزموده است**، که کلِ نکته‌ی این گام بود.

**★ چطور چیزی را آزمودیم که هنوز وجود ندارد:** `migrateDocument` رجیستریِ
**تزریق‌شدنی** می‌گیرد. بدونش تنها راه این بود که تا اولین migrationِ واقعی صبر
کنیم — یعنی اولین migrationِ محصولی روی مسیرِ نیازموده و روی سندهای **واقعیِ
کاربران** اجرا می‌شد. تست‌ها یک زنجیره‌ی ساختگیِ V1→V2→V3 از همان موتور می‌گذرانند.

**★ هر گام تراکنشِ خودش را دارد، نه یک تراکنش برای کلِ زنجیره.** وسوسه‌اش
برعکس است، ولی اگر گامِ دوم خطا بدهد Yjs تغییراتِ گامِ اول را برنمی‌گرداند. اگر مهرِ
نسخه فقط در انتها زده می‌شد، سند **داده‌ی migrate‌شده با برچسبِ نسخه‌ی قدیمی**
می‌مانْد و بارگذاریِ بعدی همان گام را روی داده‌ی already-migrated دوباره اجرا
می‌کرد. تستش هست: بعد از شکستِ گامِ دوم، نسخه روی **۲** می‌ایستد، نه ۱ و نه ۳.

**سه نگهبانِ خودِ رجیستری** (پرشِ نسخه · زنجیره‌ی گسسته · **migration بدونِ
بالابردنِ `SCHEMA_VERSION`**). سومی رایج‌ترین اشتباهِ ممکن است و بدونِ نگهبان
migration **هرگز اجرا نمی‌شد** و هیچ‌کس نمی‌فهمید.

**سندِ جلوتر از این build** خطای صریح می‌دهد و **سند را دست نمی‌زند** (صفر update).
migrateِ رو به عقب وجود ندارد؛ سکوت اینجا یعنی خرابیِ ساختاری که نسخه‌ی جدیدترِ
برنامه درستش کرده بود. کدش همان `CLIENT_TOO_OLD`ی است که به کلاینت می‌رود.

**⚠️ یک تکه از گام ۲٫۴ زودتر آمد:** `src/error-codes.ts`. تنها گزینه‌ی دیگر یک
رشته‌ی جادویی بود که بعداً در `protocol.ts` تکرار می‌شد — و enumی که دو نسخه دارد
enum نیست. در TODO ثبت شد که ۲٫۴ همین را مصرف کند.

### ⚠️ گیت قرمز شد و **تقصیرِ کد نبود** — سقفِ commit ویندوز

وسطِ همین گام `pnpm verify` روی `test` افتاد. **هر ۸ پکیج** با
`FATAL ERROR: ... out of memory` — در حالی که **۷GB رمِ آزاد** بود.

علت رم نبود، **سقفِ commit** بود (رم + page file): ۳۴٫۵ از ۴۰٫۶ گیگ پر، و turbo
پیش‌فرض تا ۱۰ task موازی می‌برد که هرکدام چند workerِ Node است. اجرای سریالیِ
همان لحظه سبز شد (۸/۸ پکیج).

**رفع:** `scripts/verify.mjs` حالا `--concurrency=1` می‌زند. گیت **ضعیف نمی‌شود** —
همان taskها، همان `--force`، تصمیم همچنان فقط با کدِ خروج؛ فقط ~۱۰ ثانیه کندتر
(۹s → ۱۹s).

★ **چرا ۱ و نه ۲:** درسِ پورت‌های رزروشده در گام ۱٫۱ — عددی انتخاب نکن که شیر یا
خط است، از رژیمِ شکست **خارج شو**. `--concurrency=2` روی ماشینِ شلوغ‌تر باز همان
قمار بود.

⚠️ **آنچه ثابت شده و آنچه نشده — با دقت:**

- **ثابت شده:** اجرای **موازی** دو بار پشتِ سر با OOM افتاد و خروجی‌اش ثبت است
  (هر ۸ پکیج، `FATAL ERROR`). سریالی همان لحظه ۸/۸ سبز شد.
- **ثابت نشده:** دو اجرای **سریالی** هم افتادند، ولی خروجی‌شان را **ثبت نکردم** —
  پس نمی‌گویم «آن هم OOM بود». فقط این را می‌دانم که در همان پنجره‌ی فشار رخ داد.
  بعدش **چهار اجرای سریالیِ پیاپی** با exit 0، صفر خطِ `out of memory` و صفر
  assertion سبز شدند.

پس ادعا این نیست که «سریالی تضمینی است»، بلکه این است که «موازی روی این ماشین
قابل‌اتکا نیست». نشانه‌اش در `CLAUDE.md` ثبت شد تا دفعه‌ی بعد **اول خروجی را ببینیم**
و بعد قضاوت کنیم — همان درسِ سبزِ دروغینِ گام ۱٫۲.

### گام ۲٫۴ — کدهای پیامِ پروتکل ✅ (۱۴۰۵/۰۵/۱۳) — **فاز ۲ کامل**

هر هفت پیامِ PLAN بخش ۵٫۳ با encode/decode روی `lib0`. **۲۱ تستِ تازه**
(`ydoc-schema` ۱۳۴ → ۱۵۵).

**★★ دو قاعده‌ای که شکلِ پروتکل را تعیین کردند:**

۱. **نوعِ ناشناخته `null` می‌دهد، و بایتِ اضافه در انتهای پیام خطا نیست.** با هم
   یعنی نسخه‌ی بعدی هم می‌تواند پیامِ تازه اضافه کند و هم به پیامِ **موجود** فیلد
   اضافه کند. در یک اپِ وب که کاربر تبش را هفته‌ها باز نگه می‌دارد، «کلاینتِ
   قدیمی» بخشِ بزرگی از کاربران است؛ بدونِ این، هر دیپلوی که کدِ پیامِ تازه
   بیاورد آن‌ها را می‌شکند.
   ⚠️ ولی پیامِ **خرابِ** نوعِ شناخته‌شده `ProtocolError` می‌دهد. ناشناخته یعنی
   «هنوز بلد نیستم» و خراب یعنی «یک طرف باگ دارد» — قاطی‌کردنشان یعنی باگ‌های
   framing برای همیشه پنهان می‌مانند.
۲. ★ **fail closed.** نقشِ ناشناخته → `viewer`، وضعیتِ ذخیره‌ی ناشناخته →
   `unsaved`. سه گزینه بود و دو تایش بد: **انداختنِ `HB_PERMISSION`** یعنی کاربری
   که همین الان تنزل داده شده با دسترسیِ قبلی ادامه می‌دهد (حفره‌ی مجوز)؛
   **خطا و بستنِ اتصال** یعنی حلقه‌ی وصل‌شدن و افتادن. تنزل به `viewer` **دیده
   می‌شود** و با یک رفرش درست می‌شود. برای وضعیتِ ذخیره هم قراردادِ M1 صریح است
   که باید **حقیقت** را بگوید نه خوش‌بینی.

**دو چیز که سنجیدم، نه فرض کردم:**

- **`lib0.writeVarUint` تا `Number.MAX_SAFE_INTEGER` سالم است** (probe: ۰ تا
  ۲^۵۳−۱). پس `seq` عدد می‌مانَد و `bigint` لازم نیست — وگرنه بی‌دلیل به API
  تحمیل می‌شد.
- **`import "lib0/encoding"` زیر pnpm resolve نمی‌شد** (`ERR_MODULE_NOT_FOUND`).
  «وابستگیِ Yjs است» کافی نیست چون گرافِ node_modules تخت نیست. صریحاً به
  `ydoc-schema` افزوده و در `docs/dependencies.md` ثبت شد؛ `license:check` سبز
  (۶۹۹ پکیج، بدونِ تغییر — از قبل در درخت بود).

**framing:** payloadِ `SYNC`/`AWARENESS`/`HB_EPHEMERAL` **طول‌دار** نوشته می‌شود،
برخلافِ قراردادِ رایجِ `y-websocket` که بقیه‌ی بافر را payload می‌گیرد. هر دو سرِ
این پروتکل خودمانیم، و framingِ یکدست همان چیزی است که قاعده‌ی ۱ را ممکن می‌کند.

**`BoardRole` هم مثلِ `CommentPin` اینجا ماند** نه `shared-types` — در فهرستِ
گام ۶٫۴ ثبت است. ⚠️ با **ایندکس** روی سیم می‌رود، پس فقط به انتهایش اضافه شود؛
جابه‌جا کردنشان یعنی کلاینتِ قدیمی نقشِ اشتباه می‌فهمد.

---

## ★ جمع‌بندیِ فاز ۲ — کامل شد

`packages/ydoc-schema` تمام است: **۱۵۵ تست**، هیچ وابستگی به UI یا سرور، و هر
تصمیمش پشتوانه‌ی سنجیده دارد.

| گام | خروجی | نگهبانِ دائمی |
|---|---|---|
| ۲٫۱ | ساختارِ سند + codecِ per-property | ادغامِ همزمان، صفر updateِ بی‌تغییر، سازگاریِ تایپی با `CanvasDocument` |
| ۲٫۲ | دارایی، appState، سنجاقِ کامنت | نگهبانِ باینری (دو تکه، دوجهته) + نگهبانِ وضعیتِ شخصی (دو لایه) |
| ۲٫۳ | نسخه‌بندی و migration | سه نگهبانِ رجیستری + تستِ شکستِ گامِ میانی |
| ۲٫۴ | پروتکل | سازگاریِ رو به جلو (دو قاعده) + fail closed |

★ **الگویی که در کلِ فاز تکرار شد:** هر جا سکوت ممکن بود، به‌جایش یا **خطا** یا
**بدبینانه‌ترین حالت** انتخاب شد — `undefined` که رد می‌شود نه `null`، کلیدِ
غیرمجازِ appState که خطا می‌دهد نه نادیده، نقشِ ناشناخته که `viewer` می‌شود،
`SCHEMA_VERSION`ِ فراموش‌شده که migration را قرمز می‌کند. تنها استثنای عمدی
**نوعِ پیامِ ناشناخته** است، و دلیلش صریح ثبت شده.

---

## تصمیم‌های گرفته‌شده

**پنج تصمیمِ مرزی — تاییدِ مالک ۱۴۰۵/۰۵/۱۲** (جدولِ کامل در صدرِ [TODO.md](TODO.md)):

| # | تصمیم |
|---|---|
| D-1 | M2 یک **برشِ حداقلی** از زیرساخت می‌سازد: compose فقط با postgres+redis، `.env.example`، `packages/config`. M5 بعداً کاملش می‌کند. |
| D-2 | پورتِ `BoardAuthority` + `DevBoardAuthority`. ★ **claimهای `rtToken` داخلِ پورت، نه `shared-types`.** |
| D-3 | پورتِ `SnapshotStore` + `FsSnapshotStore`. ★ گیتِ ESLintِ `@aws-sdk/*` و `@hamboom/sdk` باید **با `RuleTester` خودآزمون** باشد. |
| D-4 | `ws` به‌جای `uWebSockets.js`. |
| D-5 | پکیجِ سومِ `canvas-sync` برای binder. |

### ★ دو قیدِ فعال که تا پایانِ M2 باید رعایت شوند

۱. **M2 بدونِ هیچ تغییری در `shared-types` تمام می‌شود.** اگر جایی لازم شد، یعنی از تعریفِ
   تصمیم‌های تاییدشده خارج شده‌ایم → **توقف و پرسش از مالک**، نه ادامه دادن.
   - مسیرِ ممنوعِ مشخص در گام ۱٫۳: اگر دیفِ متن کافی نبود، **قرارداد را پهن نکن**
     (مثلاً `textDelta` در `ElementChangeSet`) — آن تغییرِ `shared-types` است.
۲. **گام ۱٫۱ (probeِ StrictMode): اگر شکست خورد، هیچ تغییری در `canvas-core` نده.** با
   **گزینه‌ها و هزینه‌ی هرکدام** برگرد، نه با یک راه‌حلِ آماده (قیدِ صریحِ مالک). سه گزینه‌ی
   نامزد از قبل در گام ۱٫۱ فهرست شده‌اند.

---

## بلوکه (نیاز به تصمیم مالک)

- هیچ موردِ بازی نیست.

---

## ★ جمع‌بندیِ فاز ۱ — کامل شد

چهار probe اجرا شد. هدفِ فاز **شواهد** بود نه کدِ محصولی، و هر چهار خروجی به یک
**تصمیمِ ثبت‌شده** یا یک **قیدِ اجرایی** رسید:

| probe | خروجی | فایل |
|---|---|---|
| ۱٫۱ StrictMode | [ADR-032](ARCHITECTURE_DECISIONS.md#adr-032) — الگوی `useEffect([api])` تایید شد؛ دروازه‌ی فاز ۳ باز | `canvas-sync/e2e/strictmode.spec.ts` |
| ۱٫۲ کدکِ Y.Doc | **ADR-007 تایید شد** (بازبینی لازم نیست) + [ADR-033](ARCHITECTURE_DECISIONS.md#adr-033) | `ydoc-schema/src/merge-probe.test.ts` · `canvas-sync/src/codec-probe.test.ts` |
| ۱٫۳ متنِ همزمان | [ADR-034](ARCHITECTURE_DECISIONS.md#adr-034) — `Y.Text` + دیف | `ydoc-schema/src/text-probe.test.ts` |
| ۱٫۴ انزوای undo | `trackedOrigins` تایید شد + یافته‌ی وارونه‌ی پیش‌فرض | `ydoc-schema/src/undo-probe.test.ts` |

**سه ADR جدید در این فاز:** ۰۳۲ (StrictMode) · ۰۳۳ (`customData` تودرتو) ·
۰۳۴ (متن). به‌علاوه‌ی ۰۲۹/۰۳۰/۰۳۱ از فاز ۰ → مجموعاً **۳۵ ADR**.

★ **سه فرضِ من در این فاز غلط از آب درآمد و هر بار خودِ تست ردش کرد**، نه بازبینی:
پایه‌ی کهنه با ویرایشِ افزایشی خرابی نمی‌سازد (فقط با جایگزینی) · undoِ ساخت کلید
را برمی‌دارد (حذفِ نرم این مسیر نیست) · پیش‌فرضِ `UndoManager` فقط `null` را ردیابی
می‌کند نه همه‌چیز. **این دقیقاً کارِ probe است** — اگر همان فرض‌ها مستقیم به فاز ۲
می‌رفتند، سه باگِ خاموش می‌ساختند.

---

## قدم بعدی — فاز ۳، گام ۳٫۱ (اسکلت آداپتور + چرخه‌ی عمر + نگهبانِ echo)

★ **قلبِ M2.** پیاده‌سازیِ `CanvasSyncAdapter` — همان interfaceی که M1 تعریف کرد.
مرجعِ رفتاری: [`canvas-core/src/sync/local-adapter.ts`](packages/canvas-core/src/sync/local-adapter.ts)
و چرخه‌ی عمرِ ۹مرحله‌ایِ [`sync/README.md`](packages/canvas-core/src/sync/README.md).
**تا پایان فاز ۳ هیچ کدِ شبکه‌ای نوشته نمی‌شود** — دو `Y.Doc` مستقیم به هم وصل می‌شوند.

**چهار چیزی که فاز ۲ آماده تحویل داده و نباید دوباره ساخته شوند:**

1. `writeElement`/`readElement`/`readDocument` — codecِ کاملِ per-property.
2. `writeAppState`/`writeAsset`/`writeCommentPin` با نگهبان‌هایشان.
3. `encodeMessage`/`decodeMessage` — پروتکلِ آماده برای فاز ۴.
4. `migrateDocument`/`checkClientVersion`.

**★ سه قیدی که binder از فاز ۱ و ۲ به ارث می‌برد** (همه در دو جا ثبت‌اند —
TODO و [`canvas-sync/CLAUDE.md`](packages/canvas-sync/CLAUDE.md)):

- **الگوی StrictMode-safe** ([ADR-032](ARCHITECTURE_DECISIONS.md#adr-032)):
  اشتراک در `useEffect([api])`، **نه** در callbackِ `onReady` — زیر StrictMode
  مرده می‌مانَد و binder بی‌صدا هیچ‌چیز emit نمی‌کند.
- **`assertEmittable` در اولین خطِ `emitElementChanges`** — از `canvas-core` قرض
  گرفته می‌شود، از نو نوشته نمی‌شود (ADR-024).
- **هر changeset در یک `doc.transact(fn, origin)` با originِ نام‌دار** — codec
  عمداً خودش `transact` نمی‌کند، و نبودش هم ترافیک را چند برابر می‌کند و هم
  تغییرِ remote را به undo stackِ محلی نشت می‌دهد.

**چهار موردِ پین‌شده که مقصدشان فازهای بعدی است** (گم نشوند): عرضِ پنجره‌ی کهنگی و
پرشِ caret → گام ۳٫۳ · پیش‌فرضِ `UndoManager` و undoِ ساخت → گام ۳٫۴ ·
★ `transact` با originِ نام‌دار → گام ۳٫۳ · اعتبارسنجیِ سند هنگام بارگذاری (به‌همراه
`assertNoBinary`) → گام ۴٫۲. همه در **دو جا** ثبت‌اند: TODO (مقصد) و `CLAUDE.md`ِ
پکیجِ مربوطه.

**محیط برای session بعدی:** دیتابیس روی **۵۴۳۳** است (نه ۵۴۳۲) — `pnpm db:up`
کافی است؛ کانتینرها `hamboom-postgres` و `hamboom-redis`. پورت‌های dev: canvas-core
روی **۱۵۱۸۰**، canvas-sync روی **۱۵۲۸۰** (بیرون از بازه‌ی dynamic ویندوز).
★ **گیتِ معتبر فقط `pnpm verify` است** — دلیلش در `CLAUDE.md`.

---

## وضعیت گیت‌ها

`pnpm verify` **سبزِ واقعی، exit 0** — typecheckِ ریشه · typecheckِ پکیج‌ها · lint ·
test · هر دو گیتِ لایسنس (۶۹۹ پکیج، self-test ۱۷/۱۷). همه با `--force` و تصمیم فقط
با کدِ خروجِ فرایند.

**۸۳۵ تستِ واحد در ۸ پکیج** (+۶۱ در ۲٫۱، +۴۲ در ۲٫۲، +۱۷ در ۲٫۳، +۲۱ در ۲٫۴):

| پکیج | تست | چیست |
|---|---|---|
| canvas-core | ۴۷۴ | از M1، دست‌نخورده |
| ydoc-schema | ۱۵۵ | codecِ هر پنج ریشه + نگهبانِ باینری + migration + پروتکل + probeهای فاز ۱ |
| eslint-config | ۶۱ | گیت‌های مرزی (سه‌لایه) + انضباطِ `process.env` |
| canvas-sync | ۴۶ | وفاداریِ codec با عناصرِ **واقعی** + probeِ کدک + دود |
| shared-types · i18n | ۴۴ + ۴۲ | از M1 |
| config | ۱۱ | خواندنِ env |
| realtime | ۲ | دود |

**E2E:** ۱۶ تستِ M1 (کاملاً سبز روی پورتِ جدید، ۸ اجرا شامل ۳ اجرای سرد) +
۲ تستِ probeِ StrictMode در canvas-sync. در `pnpm verify` نیستند (مرورگر لازم دارند)؛
دستی اجرا می‌شوند.

**یک قرمزِ ثبت‌شده و بی‌ربط:** `pnpm format:check` روی `.claude/settings.local.json`
هشدار می‌دهد. فایل untracked و محلی است؛ ربطی به M2 ندارد و دست نزدم.
