# PROGRESS — canvas-core

تاریخ آخرین به‌روزرسانی: ۱۴۰۵/۰۴/۳۱ (2026-07-22)
گام فعلی: ۱٫۳ تمام شد → **۱٫۴ بلوکه، منتظر تایید مالک**

## انجام شد

| گام | وضعیت | commit |
|---|---|---|
| ۰٫۱ اسکلت مونوریپو | ✅ | `d697903` |
| — تنظیم `.gitattributes` (eol=lf) | ✅ | `767b15c` |
| ۰٫۲ پکیج canvas-core + دمو + تست | ✅ | `b83f821` |
| ۱٫۱ نصب موتور + wrapper | ✅ | این commit |
| ۱٫۲ فونت Vazirmatn (جزئی، پیش‌نیاز spike) | ⏳ | این commit |
| ۱٫۳ spike متن فارسی | ✅ | این commit |

**راستی‌آزمایی (همه سبز):** `typecheck` · `lint` · `test` ۹/۹ · `format:check` ·
`license:check` (self-test ۱۷/۱۷ + ۵۸۰ پکیج) · دموی مرورگر

## وضعیت تاییدها (۱۴۰۵/۰۴/۳۱)

- [x] عبور به پله‌ی B در ADR-003 — **تایید شد**، گام ۱٫۴ آزاد است
- [x] استثنای لایسنس `khroma` — **تایید شد**
- [x] `MutationObserver` به‌عنوان راه‌حل فعلی تصرف جهت سند — **تایید شد**
- [x] پذیرفتن «پاره‌شدن اتصال در کلمه‌ی بلندتر از ظرف» — **تایید شد**
- [ ] تایید بصری bidi روی `#spike` — تنها موردی که عددی اثبات نشد

## گام ۱٫۳ب — تکمیل spike (پس از سوال مالک)

مالک پرسید آیا اعداد، رفتار مکان‌نما در ویرایش inline، و کپی/پیست پوشش داده
شده‌اند. جواب: اعداد بله (جزئی)، دو مورد دیگر **نه**. حق با او بود — مسیر کد
ویرایشگر کاملاً از مسیر رندر جداست. اضافه شد:

| موضوع | نتیجه |
|---|---|
| اعداد فارسی و لاتین | ✅ رقم‌ها «قوی» نیستند، جهت را خراب نمی‌کنند |
| جهت ویرایشگر | ✅ موتور خودش `dir="auto"` + `unicode-bidi: plaintext` می‌گذارد |
| چپ‌چینی ویرایشگر | ⚠️ از `element.textAlign` می‌آید — فقط پیش‌فرض عوض شود |
| مکان‌نما | ✅ ۹ از ۱۱ رشته دست‌نخورده؛ فقط tab خرابش می‌کند — **روی لاتین هم همان** |
| کپی/پیست | ✅ ۴۱ از ۴۱ کاراکتر سالم |
| **`dir="auto"` روی «board برای تیم ماست»** | ❌ `ltr` می‌دهد → [ADR-024](ARCHITECTURE_DECISIONS.md#adr-024) |

**این آخری طراحی گام ۱٫۴ را عوض کرد:** `detectBaseDirection` که قرار بود
الگوریتم استاندارد «اولین کاراکتر قوی» را پیاده کند، باید بر اساس **اکثریت**
کاراکترهای قوی تصمیم بگیرد.

جزئیات کامل با شواهد عددی: [docs/spike-persian-text.md](docs/spike-persian-text.md)

## نتیجه‌ی spike در یک نگاه

| موضوع | پیش‌بینی PLAN | نتیجه‌ی واقعی |
|---|---|---|
| شکل‌دهی حروف | ریسک بالا | ✅ درست کار می‌کند |
| اندازه‌گیری عرض | ریسک بالا | ✅ درست کار می‌کند |
| شکست خط | ریسک بالا | ✅ روی مرز کلمه، نیم‌فاصله سالم |
| فونت فارسی | در نظر گرفته نشده بود | ✅ با ترفند CSS، بدون patch |
| ترتیب bidi | در نظر گرفته نشده بود | ❌ **تنها موردی که patch لازم دارد** |
| تصرف جهت سند | در نظر گرفته نشده بود | ❌ خنثی شد با نگهبان |

ریسک ADR-003 از «ممکن است مجبور به فورک شویم» به «یک patch کوچک نگه‌داری می‌کنیم»
کاهش پیدا کرد.

## تصمیم‌های گرفته‌شده (کاندید ADR)

**از گام ۰٫۱–۰٫۲:** (بدون تغییر — ۱۰ مورد، در commit های قبلی مستند شده)
`license-check` سه‌سطحی با self-test؛ prod خطا/dev هشدار؛ ESLint بدون type-check
فعلاً؛ اسناد فارسی از prettier مستثنا؛ canvas-core پکیج JIT؛ دو فایل پیکربندی
Vite جدا؛ `packages/tsconfig` خودبسنده؛ vitest بدون globals؛ `ENGINE_STAGE`
به‌عنوان کد + تست.

**از گام ۱٫۱–۱٫۳:**

11. **نگهبان P2 به‌جای اعتماد.** `assertAssetPathConfigured()` نبودِ تنظیم مسیر
    دارایی‌ها را به خطای صریح تبدیل می‌کند. دلیل: نقض P2 اینجا **بی‌صدا** است —
    چیزی خطا نمی‌دهد، فقط از اینترنت دانلود می‌کند. چنین نقصی تا production می‌رسد.
12. **بازصادرات کنترل‌شده از موتور.** هرچه از `@excalidraw/excalidraw` لازم است،
    از `src/index.ts` عبور می‌کند. اگر روزی به فورک رفتیم، سطح تماس یک فایل است.
13. **stub canvas در jsdom با مرز صریح.** `test/canvas-stub.ts` فقط برای اینکه
    import ماژول نترکد. `measureText` عدد ساختگی می‌دهد و این در فایل با تاکید
    مستند است — هر تست jsdom که درباره‌ی عرض متن ادعا کند، دروغ می‌گوید.
14. **`server.deps.inline` برای Excalidraw در vitest.** پکیج یک JSON را بدون
    `with { type: "json" }` وارد می‌کند؛ Node رد می‌کند ولی Vite هندل می‌کند.
15. **تزریق فونت با `unicode-range` به‌جای ثبت فونت سفارشی.** موتور API عمومی
    برای فونت سفارشی ندارد، ولی CSS اجازه می‌دهد چند face با یک نام خانواده و
    بازه‌های متفاوت تعریف شود. این کل مسئله‌ی فونت را بدون patch حل کرد.
16. **اثبات shaping با اندازه‌گیری، نه با چشم.** اگر حروف جدا رندر می‌شدند، عرض
    رشته با جمع عرض کاراکترها برابر می‌شد. ۳۸٪ اختلاف در فارسی و ۰٪ در لاتین،
    اثبات قطعی shaping است. این روش برای تست رگرسیون هم قابل استفاده است.

## گام ۱٫۴ — بخش انجام‌شده

| فایل | چیست | تست |
|---|---|---|
| `src/text/bidi.ts` | تشخیص جهت با اکثریت (ADR-024) | ۱۷ |
| `src/text/normalize.ts` | `normalizePersian` + `persianSearchKey` | ۱۸ |
| `src/engine/canvas-direction.ts` | wrapper روی `fillText` (ADR-025) | ۱۰ |
| `patches/README.md` | ثبت باگ U-1 و patch پشتیبان P-1 | — |

**۵۴ تست سبز.** `typecheck` · `lint` · `format:check` هم سبز.

### دو باگ واقعی که تست‌ها گرفتند

1. **`isRTLChar` ارقام فارسی را حرف قوی می‌شمرد.** بازه‌ی دستی `U+0600–U+06FF`
   شامل `U+06F0–U+06F9` هم می‌شود. با `\p{L}` + `\p{Script=…}` حل شد.
2. **stub canvas سیم‌کشی را غیرقابل‌تست کرده بود.** آبجکت ساده برمی‌گرداند، پس
   wrap کردن prototype هرگز به آن نمی‌رسید — و بدتر: jsdom اصلاً
   `CanvasRenderingContext2D` را تعریف نمی‌کند. stub حالا خودش آن را می‌سازد.
   (باگ سوم داخل همین اصلاح: `ensureContextConstructor` هر بار `fillText` را
   روی noop برمی‌گرداند و wrapper تست را پاک می‌کرد.)

## ✅ گام ۱٫۴ تمام شد

| کار | نتیجه |
|---|---|
| `text/bidi.ts` — تشخیص جهت با اکثریت | ۱۷ تست |
| `text/normalize.ts` — `normalizePersian` + `persianSearchKey` | ۱۸ تست |
| `engine/canvas-direction.ts` — wrapper روی `fillText` | ۱۰ تست |
| `engine/editor-direction.ts` — جهت ویرایشگر inline | ۱۰ تست |
| `theme/fonts.css` — از `dev/` به پکیج منتقل شد | — |
| پیش‌فرض `textAlign` از `defaultTextAlignFor` | — |
| **patch** | **لازم نشد** |

**۶۴ تست سبز.** `typecheck` · `lint` · `format:check` · `license:check` سبز.

**تایید در مرورگر:**

- شمارنده‌ی فراخوانی hook: **۳۸** در یک بارگذاری → ADR-025 اثبات شد
- فونت‌ها بعد از انتقال سالم: `Excalifont` هشت face دارد و
  `fonts.check('20px Excalifont','سلام')` مثبت است
- نگهبان ویرایشگر روی «board برای تیم ماست» جواب `rtl` می‌دهد در حالی که
  `dir="auto"` مرورگر `ltr` می‌گفت — دقیقاً موردی که ADR-024 برایش وجود دارد

**یک کار موکول شد:** تست رگرسیون هش پیکسلی به گام ۶٫۱ رفت. jsdom پیکسل تولید
نمی‌کند و تست واقعی به Playwright نیاز دارد؛ اضافه‌کردن یک وابستگی سنگین فقط
برای یک تست الان ارزشش را ندارد. جایگزین: شمارنده‌ی زنده که در چک‌لیست ارتقای
نسخه‌ی `patches/README.md` ثبت شده — اگر صفر شد یعنی ADR-025 شکسته.

## بایگانی — مسیری که به ADR-025 رسید

به‌جای patch تاییدشده، یک راه بدون patch پیدا شد: wrap کردن
`CanvasRenderingContext2D.prototype.fillText` که `ctx.direction` را از روی
خود متن ست می‌کند. اگر جواب بدهد، پروژه روی **پله‌ی A** می‌ماند.

**ولی هنوز اثبات نشده که موتور از همین مسیر متن می‌کشد.** در این session
اسکرین‌شات در دسترس نبود، رویدادهای مصنوعی اشاره‌گر/چرخ را موتور نمی‌پذیرفت و
میانبر کیبورد هم به بوم نمی‌رسید — پس هیچ رندر مجددی تولید نشد. شمارنده صفر
ماند، **ولی هش پیکسل بوم هم قبل و بعد یکسان ماند، یعنی رندری اتفاق نیفتاده
بود.** صفر ماندن در آن شرایط شاهد نیست.

**نتیجه:** شمارنده بالا رفت. ADR-025 پذیرفته شد، پله‌ی A حفظ شد، patch لازم نشد.

## قدم بعدی — فاز ۲

فاز ۱ (دروازه‌ی ریسک متن فارسی) کامل بسته شد. قدم بعد گام **۲٫۱** است:
انواع عنصر در `packages/shared-types` با zod.

⚠️ این گام `packages/shared-types` را برای اولین‌بار می‌سازد. طبق
[ADR-021](ARCHITECTURE_DECISIONS.md#adr-021) ساختن اولیه‌اش مجاز است، ولی
**هر تغییر بعدی نیاز به تایید مالک دارد**.

بعد از آن گام **۲٫۲** — قرارداد `CanvasSyncAdapter` — که مهم‌ترین خروجی این
ماژول برای M2 است.

**یک تصمیم معلق که قبل از M3 باید گرفته شود:** آیا `normalizePersian` به
`shared-types` منتقل شود تا backend هم از همان استفاده کند؟ اگر نه، متنی که از
API یا seed می‌آید نرمال‌نشده می‌ماند و در جستجو مچ نمی‌شود.

**قاعده‌ی انسجام (از ADR-024):** جهت در سه جا مصرف می‌شود — `ctx.direction`،
صفت `dir` ویرایشگر، و `textAlign` عنصر. هر سه باید از یک تابع بیایند، وگرنه
متن هنگام ورود و خروج از حالت ویرایش می‌پرد.

## پله‌ی فعلی ADR-003

**A — بسته‌ی npm.** هنوز هیچ patch ای اعمال نشده. عبور به B **پیشنهاد شده و
منتظر تایید** است.
