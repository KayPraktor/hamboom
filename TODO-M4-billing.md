# TODO-M4-billing.md — ماژول M4: `billing` (پرداخت و اشتراک)

> **وضعیت (۱۴۰۵/۰۶/۱۴): فاز ۰–۴ ✅ (جز گام ۲٫۴ که به ۵٫۵ موکول شد).**
> قدمِ بعد: **فاز ۵ (مسیرهای billingِ `apps/api`)**. نُه تصمیمِ مرزی تایید و به
> ADR-049…ADR-054 تبدیل شدند؛ دروازه‌ی probeها با یک پرداختِ واقعیِ سندباکس باز شد.
>
> **نقطه‌ی ورود:** [`docs/m4-handoff.md`](docs/m4-handoff.md) — سندِ تحویلِ M3 به M4.
>
> **قبل از شروع بخوان:** [PLAN.md](PLAN.md) بخش‌های ۴ (env)، ۵٫۱ (DTOها)، ۵٫۲
> (endpointها، ردیف‌های «پرداخت»)، ۶ (schema، بلوکِ «اشتراک و پرداخت») و ۸ (ماژول M4) ·
> [ARCHITECTURE_DECISIONS.md](ARCHITECTURE_DECISIONS.md) — به‌ویژه **ADR-001، ADR-005،
> ADR-012، ADR-014، ADR-015، ADR-018، ADR-020، ADR-021، ADR-045، ADR-046** ·
> [`apps/api/README.md`](apps/api/README.md) · [`packages/auth-core/README.md`](packages/auth-core/README.md)
> (الگوی پورت) · [`packages/sdk/README.md`](packages/sdk/README.md).
>
> **ماژول‌های تمام‌شده:** [TODO-M1-canvas-core.md](TODO-M1-canvas-core.md) · [TODO.md](TODO.md) (M2) ·
> [TODO-M3-backend-api.md](TODO-M3-backend-api.md). **این فایل فقط M4 است.**

---

## دامنه‌ی M4

طبق [PLAN بخش ۸](PLAN.md): **`apps/api/src/routes/billing.ts` + `apps/web/src/billing/`**،
به‌علاوه‌ی چیزی که PLAN آن‌جا ننوشته ولی [ADR-014](ARCHITECTURE_DECISIONS.md#adr-014)
اجباری‌اش می‌کند: **یک پورتِ `PaymentGateway` با پیاده‌سازیِ mock و زرین‌پال**، و
**آشتی‌دهیِ دوره‌ای**.

M4 مسئولِ پنج چیز است:

۱. **قرارداد** — DTOهای `Plan`/`Subscription`/`Invoice` و فیلدهای مالیِ `Team` که M3
   عمداً جا گذاشت ([`shared-types/src/api/team.ts:16-22`](packages/shared-types/src/api/team.ts)).
۲. **جریانِ پرداخت** — `checkout` → درگاه → `callback` → **verifyِ سرور-به-سرور** →
   فعال‌سازیِ اشتراک، همه در یک تراکنش.
۳. **پول درست** — ریالِ صحیح، VAT، کوپن، فاکتور با شماره‌ی جلالی، **یک قاعده‌ی گِردکردن**.
۴. **اعمالِ ظرفیت** — `usage_counters` که امروز مرده است زنده شود و واقعاً جلوی
   عبور از سقفِ پلن را بگیرد.
۵. **آشتی‌دهی** — پرداختِ گم‌شده (کاربر پول داد و مرورگر را بست) پیدا و تسویه شود.

**چه چیزی بیرونِ M4 است:** استردادِ کامل (`refund`، چون GraphQL+OAuth است — پایین) ·
پنلِ ادمینِ پلتفرم = **M6** · Dockerfileهای production و K8s و CI = **M5** ·
`apps/worker` = هنوز به تعویق (M4-D4) · فاز ۱۰ی M3 (قالب/کامنت/نسخه/خروجی) = دست‌نخورده.

★ **انتظار می‌رود M4 صفر لمسِ M1/M2 داشته باشد.** `canvas-core`، `canvas-sync`،
`ydoc-schema` و `apps/realtime` هیچ ربطی به پول ندارند. اگر جایی ناچار شدی، **متوقف شو
و بپرس** (درسِ ۲ی M3).

---

## قوانین اجرای loop (همان انضباطِ M1/M2/M3)

۱. **ترتیب را رعایت کن.** فاز ۰ و ۱ **دروازه‌ی کلِ ماژول‌اند**.
۲. **مبنای تیک‌زدن و کامیت فقط `pnpm verify` است** ([CLAUDE.md](CLAUDE.md) §«چرا
   `pnpm verify`»). اگر verify روی `test` افتاد، **اول محیط را ببین**.
۳. **تیک زدن فقط بعد از تحققِ «معیار پذیرش».** محقق نشد، ننویس انجام شد.
۴. **هیچ dependency ای بدونِ لایسنسِ MIT/Apache-2.0/BSD/ISC/0BSD**؛ بعد از هر افزودن
   `pnpm license:check` و ثبت در [`docs/dependencies.md`](docs/dependencies.md) (P1).
   ⚠️ گیتِ لایسنس **بدونِ `--strict`** اجرا می‌شود، پس تخلفِ dev-scope فقط **هشدار**
   است و exit 0 می‌دهد ([`scripts/license-check.ts:324`](scripts/license-check.ts)) —
   یعنی یک devDependencyِ copyleft از گیت رد می‌شود. برای پکیجِ پول این را **دستی** ببین.
۵. **دامنه‌ی خودت را رعایت کن.** M4 صاحبِ `packages/billing-core`، مسیرهای billingِ
   `apps/api`، و `apps/web/src/billing/` است.
۶. ★ **تغییرِ `shared-types` تاییدِ مالک می‌خواهد** (ADR-021). M4 **باید** قراردادِ
   billing را اضافه کند، پس این یک تاییدِ **دسته‌ای** است (M4-D2) — ولی دو موردِ
   واقعاً باز (M4-D2a/M4-D2b) **جداگانه** گیت می‌شوند.
۷. **پایان هر session:** `PROGRESS-M4-billing.md` با «چه شد / چه تصمیمی گرفتم / قدم بعد».
۸. **گامِ بلوکه‌شده:** با `[!]` علامت بزن، دلیل بنویس، برو گام مستقلِ بعدی.
۹. ★ **اول probe، بعد کد.** هرجا رفتارِ زرین‌پال/Postgres/idempotency فرض شده، قبل از
   منطق با یک آزمایشِ **واقعی** ثابتش کن (فاز ۱).
۱۰. ★★ **یک گیتِ آزموده‌نشده گیت نیست.** هر تست و هر threshold باید با یک **شکستنِ
    عمدی** قرمز شده باشد. در M2 و M3 بارها یک تستِ تازه با برداشتنِ همان چیزی که
    می‌سنجید سبز مانْد.
۱۱. ★★★ **مخصوصِ M4 — «در Node سبز» یعنی «در Node سبز».** گران‌ترین باگِ M3 (گام ۱۱٫۲)
    این بود که `redirect:"manual"` در Node کار می‌کرد و در مرورگر نه. جریانِ پرداخت
    **یک ریدایرکتِ کاملِ مرورگر به بیرون و برگشت** است — پس فاز ۹ **باید در مرورگر**
    اثبات شود، نه با تستِ Node.

---

## قیدهای سختِ M4

| اصل | معنای عملی در M4 |
|---|---|
| **P1** | هیچ SDKِ زرین‌پالی نصب نمی‌شود — `fetch` بومیِ Node ۲۴ کافی است. صفر dependency جدید هدف است. |
| **P2/P3** | ★ **`PAYMENT_PROVIDER=mock` پیش‌فرضِ توسعه است** (M4-D5). sandboxِ زرین‌پال یک **سرویسِ خارجی روی اینترنت** است؛ اگر پیش‌فرض شود، ماشینِ آفلاین و CIِ بدونِ egress جریانِ پرداختِ مرده دارند. |
| **P4** | billing به Object Storage کاری ندارد. (فاکتورِ PDF → نیازمندِ Chromium → `apps/worker` → **بیرونِ M4**.) |
| **P5** | ★★ پول همیشه `BIGINT` **ریال**. روی سیم عددِ **صحیح** (`PLAN.md:453`). هیچ float، هیچ تومان، هیچ `z.bigint()` در DTO. |
| **P6** | صفحه‌ی قیمت و فاکتور RTLِ واقعی با logical properties. ⚠️ `pnpm lint:css`ِ ریشه فقط `packages/*` را می‌بیند؛ `verify` گلابِ `**/*.css` دارد — پس CSSِ `apps/web` **فقط در verify** گیت می‌شود. |
| **P7** | ★ هیچ PII/شناسه‌ی پرداخت در لاگ: `authority`، `ref_id`، `card_pan`، `merchant_id`، شماره‌ی موبایلِ `metadata`. |

### ★★ سه چیزی که ADR-014 غیرقابل‌مذاکره کرده

۱. **هرگز به پارامترهای callback اعتماد نکن.** `Status=OK` یک ادعای مرورگر است.
۲. **verify باید idempotent باشد** — با `SELECT … FOR UPDATE` روی ردیفِ `payments`.
۳. **آشتی‌دهی اجباری است** — بدونش پول گرفته می‌شود و سرویس داده نمی‌شود.

---

## ★★ آنچه probeهای فاز صفرِ برنامه‌ریزی **همین حالا** ثابت کردند

این‌ها با تماسِ **زنده** با زرین‌پال و خواندنِ کدِ واقعی به دست آمدند. **فرض نیستند** —
ولی فاز ۱ باید دوباره در محیطِ خودمان اثباتشان کند.

### زرین‌پال — قراردادِ واقعی (منبع: مستنداتِ رسمی + probeِ زنده)

| موضوع | یافته |
|---|---|
| میزبان | **`payment.zarinpal.com`** — نه `api.zarinpal.com` (که امروز ۴۰۳ داد). سندباکس: `sandbox.zarinpal.com` |
| متدها | `pg/v4/payment/{request,verify,inquiry,unVerified,reverse,feeCalculation}.json` — همه POST + JSON |
| ریدایرکت | `https://payment.zarinpal.com/pg/StartPay/{authority}` |
| callback | فقط دو پارامتر: `?Authority=…&Status=OK|NOK` |
| ★★ **۱۰۰ در برابر ۱۰۱** | verifyِ اول ⇒ `100`. **هر verifyِ بعدیِ همان تراکنش ⇒ `101`** («تراکنش وریفای شده است»). یعنی **{۱۰۰،۱۰۱} = پرداخت‌شده**؛ فقط ۱۰۰ حق دارد اشتراک را فعال کند |
| ★ واحد | پیش‌فرض **IRR (ریال)** — اثباتِ زنده: `amount:10000` بدونِ currency ⇒ صفحه‌ی درگاه «۱۰٬۰۰۰ ریال». ولی `IRT` هم مجاز است ⇒ **همیشه صریح `"currency":"IRR"` بفرست** |
| سقف/کف | کف **۱۰۰۰ ریال** (اثباتِ زنده: `amount:100` ⇒ خطای `-9`)؛ سقف ۱۰۰ میلیون تومان (خطای `-41`) |
| سندباکس | ✅ **ادعای ADR-014 تایید شد**: فقط تعویضِ میزبان + یک UUIDِ دلخواه به‌عنوان merchant. اثباتِ زنده با `11111111-…-111111111111` ⇒ `authority: "S000…zy578z"` |
| اندازه‌ها | `authority` دقیقاً **۳۶ کاراکتر** ⇒ `varchar(80)`ِ فعلی کافی است. `card_pan` ماسک‌شده (۱۶) جا می‌شود. ⚠️ `card_hash` (۶۴ هگز) **ستون ندارد** |
| ★ شکلِ خطا | موفق ⇒ HTTP 200 و `"errors": []` (**آرایه**). ناموفق ⇒ **non-2xx** و `"errors": {…}` (**شیء**). اثباتِ زنده: `-9`⇒۴۲۲، `-10`⇒۴۲۲، `-51`⇒**۴۰۱** |
| آشتی‌دهی | `inquiry.json` وضعیت می‌دهد ولی مستندات صریحاً می‌گوید **برای verify استفاده نکن**. `unVerified.json` سقفِ **۱۰۰ تراکنشِ آخر** دارد ⇒ منبعِ حقیقت **جدولِ خودمان** است |
| استرداد | ⛔ `refund` اصلاً در REST نیست — **GraphQL + OAuth2** روی `next.zarinpal.com`. ولی `reverse.json` هست: فقط تا **۳۰ دقیقه**، و **IPِ سرور باید در پنل whitelist شود** (خطای `-62`) |

⚠️ **چهار چیز هنوز نامعلوم است و فاز ۱ باید ببندد:** (الف) آیا سندباکس واقعاً گذارِ
۱۰۰→۱۰۱ را بازتولید می‌کند (probe نتوانست پرداختِ سندباکس را غیرتعاملی کامل کند —
صفحه‌اش کپچا و OTP دارد) · (ب) بازه‌ی مجازِ verify قبل از بازگشتِ خودکارِ پول **هیچ‌جا
عدد ندارد** و بازه‌ی sweep به آن وابسته است · (ج) `metadata.auto_verify` (اگر روشن
باشد، پول بدونِ ما capture می‌شود و ردیفِ ما `pending` می‌مانَد) · (د) بزرگیِ بیشینه‌ی
`ref_id` (اگر از ۲^۵۳ بگذرد `JSON.parse` بی‌صدا خرابش می‌کند).

### کدِ خودمان — پنج بمبِ ساعتی

| # | یافته | چرا بی‌صداست |
|---|---|---|
| **B-1** | ★★ [`idempotency.ts:44,48`](apps/api/src/idempotency.ts) روی هر درخواستِ **غیر-POST** یا **بدونِ `Authorization`** بی‌صدا رد می‌شود. callbackِ زرین‌پال یک **GETِ مرورگر بدونِ هدرِ auth** است ⇒ میان‌افزار **اصلاً اجرا نمی‌شود** | یک session می‌بیند «idempotency داریم» و callback را وصل می‌کند؛ هر رفرشِ مرورگر اشتراک را دوباره فعال می‌کند |
| **B-2** | ★ `SUM()` روی `bigint` نوعِ `numeric` (OID 1700) برمی‌گرداند، ولی [`db.ts:36`](apps/api/src/plugins/db.ts) فقط OID **۲۰** را ثبت کرده ⇒ **رشته** | `sum + vat` می‌شود الحاقِ رشته و `sum > limit` مقایسه‌ی الفبایی (`"9" > "10000"`). گاردِ `RangeError` هرگز شلیک نمی‌کند چون چیزی پارس نشده |
| **B-3** | کوئرسِ `int8` **فقط داخلِ `createDbPool`** ثبت می‌شود. `apps/realtime` و هر هفت `scripts/rt-*.ts` مستقیم `new pg.Pool` می‌زنند | اسکریپتِ آشتی‌دهی که از رویشان کپی شود `amount_rial` را **رشته** می‌خواند و هیچ خطایی نمی‌دهد |
| **B-4** | **هیچ `CHECK` روی هیچ ستونِ وضعیتِ billing نیست** (شش `CHECK` جای دیگر هست). `status='Active'` تمیز درج می‌شود و **بیرونِ** `subscriptions_active_uq` می‌افتد | تیم دو اشتراکِ «فعال» می‌گیرد در حالی که هر `WHERE status='active'` می‌گوید پلنی ندارد |
| **B-5** | `usage_counters.members_count` **همیشه صفر** است — درج می‌شود ([`teams.ts:53`](apps/api/src/routes/teams.ts)) و هیچ‌جا به‌روز نمی‌شود | سقفِ عضو روی پلنِ رایگان **اصلاً وجود ندارد**، و هر «سقف اعمال شد» امروز اثبات‌ناپذیر است |

---

## تصمیم‌های مرزی M4 — ★ نیاز به تاییدِ مالک (فاز ۰، بدون کد)

| # | موضوع | پیشنهادِ من | تاییدِ لازم |
|---|---|---|---|
| **M4-D1** | **عمقِ دامنه** | فازهای ۰–۱۰ همه در M4. **استردادِ کامل (`refund`) بیرون** — GraphQL+OAuth است، در dev اصلاً اجرا نمی‌شود (P3). `reverse.json` (پنجره‌ی ۳۰ دقیقه) هم **بیرون**، چون whitelistِ IP می‌خواهد که فقط با حسابِ واقعی ممکن است. جای هر دو: M6 (پنلِ ادمین). | ✅ **بله** (۱۴۰۵/۰۶/۱۴) |
| **M4-D2** | **جمعیت‌بخشیِ `shared-types`** | تاییدِ **دسته‌ای** مثلِ M3-D2: primitiveِ `rial`، سه enum، `Plan`/`Subscription`/`Invoice`/`InvoiceLineItem`، چهار فیلدِ مالیِ `Team`، کدهای خطا، و بدنه‌های درخواست. شکلشان در PLAN §۵٫۱ از قبل تثبیت است. | ✅ **تاییدِ دسته‌ای** (۱۴۰۵/۰۶/۱۴) |
| **M4-D2a** ★گیت | **تناقضِ خودِ PLAN** — `Team.subscriptionStatus` شش مقدار دارد (با `"none"`)، `Subscription.status` پنج ([`PLAN.md:479`](PLAN.md) در برابرِ [`PLAN.md:556`](PLAN.md)). | **دو enumِ جدا**: `subscriptionStatus` (۵) برای خودِ اشتراک، و `teamSubscriptionStatus` (۶، با `none`) برای تیمِ بی‌اشتراک. یکی‌کردنشان یعنی یک اشتراکِ واقعی بتواند `none` باشد. | ✅ **دو enumِ جدا** (۱۴۰۵/۰۶/۱۴) |
| **M4-D2b** ★گیت | **`Coupon` در PLAN §۵٫۱ اصلاً وجود ندارد** — فقط جدولش هست. پس هر شکلِ عمومی برایش یک قراردادِ **کاملاً نو** است. | **کوپن وارد قرارداد نشود.** فقط `couponCode: string` در بدنه‌ی checkout می‌رود و اثرش به‌صورت `discountRial` روی `Invoice` دیده می‌شود. کمترین سطح، بدونِ افشای موجودی/سقفِ کوپن به کلاینت. | ✅ **کوپن بیرونِ قرارداد** (۱۴۰۵/۰۶/۱۴) |
| **M4-D3** | **جای پورتِ `PaymentGateway`** | **`packages/billing-core`** (قرینه‌ی `auth-core`). دلیل: ریاضیِ پول (VAT، کوپن، دوره، شماره‌ی فاکتور) خالص و پرریسک است و **گیتِ پوشش** می‌خواهد؛ `apps/api` امروز اصلاً `test:coverage` ندارد، پس هر چیزی که آن‌جا برود **بیرونِ گیتِ ۶۰٪** است. | ✅ **`packages/billing-core`** (۱۴۰۵/۰۶/۱۴) |
| **M4-D4** ★گیت | **میزبانِ آشتی‌دهی** (ADR-014 قاعده ۳) — هیچ scheduler ای در ریپو نیست و `apps/worker` به تعویق افتاده. | **دو تکه:** (الف) منطقِ sweep یک تابعِ خالص در `billing-core` + یک probeِ اجرایی `pnpm billing:reconcile` (قرینه‌ی هفت `rt:*`) که **همین حالا** قابلِ اجراست؛ (ب) یک پلاگینِ بازه‌ایِ ساده در `apps/api` **پشتِ فلگ**، با ثبتِ صریح که چندنودی‌شدنش کارِ M5 است. **`apps/worker` باز هم ساخته نمی‌شود.** | ✅ **سه‌تکه، بدونِ worker** (۱۴۰۵/۰۶/۱۴) |
| **M4-D5** ★گیت | **پیش‌فرضِ درگاه در توسعه** — [`PLAN.md:398`](PLAN.md) می‌گوید `PAYMENT_PROVIDER=zarinpal` + `ZARINPAL_MODE=sandbox`. | **انحراف از PLAN: پیش‌فرضِ `.env.example` بشود `mock`.** سندباکس یک سرویسِ خارجی روی اینترنت است و P2/P3 را می‌شکند. sandbox حالتِ سومِ **دستی** برای spike بماند. | ✅ **پیش‌فرض `mock`** (۱۴۰۵/۰۶/۱۴) |
| **M4-D6** | **VAT** | `VAT_PERCENT` پیکربندی بماند و **پیش‌فرضِ `.env.example` صفر** باشد؛ نرخِ هر فاکتور **در خودِ فاکتور** ذخیره شود، نه از یک عددِ سراسری بازمحاسبه. نرخِ production تصمیمِ **مالک + حسابدار** است. (probe نرخِ عمومیِ **۱۰٪** را یافت — از ۹٪ با قانونِ بودجه‌ی ۱۴۰۳ — ولی «آیا هم‌بوم اصلاً در نظامِ مالیاتی ثبت شده و حق دارد VAT بگیرد» یک پرسشِ حقوقی است، نه فنی.) | ✅ **پیش‌فرض صفر، منجمد در فاکتور** (۱۴۰۵/۰۶/۱۴) |
| **M4-D7** | **گیتِ پوششِ کدِ پول** | `packages/billing-core` با threshold **۹۰٪** وارد `pnpm verify` شود (نه ۶۰٪). دلیل: خطِ بی‌پوشش در ریاضیِ پول با خطِ بی‌پوشش در UI یکی نیست. | ✅ **۹۰٪ در verify** (۱۴۰۵/۰۶/۱۴) |
| **M4-D8** | **سیاستِ downgrade وقتی تیم از سقفِ پلنِ جدید گذشته** | **تنزلِ فقط‌خواندنی**: هیچ داده‌ای حذف نمی‌شود؛ فقط **ساختِ جدید** بسته می‌شود تا زیرِ سقف برگردد. پیام فارسیِ صریح. | ✅ **تنزلِ فقط‌خواندنی** (۱۴۰۵/۰۶/۱۴) |
| **M4-D9** | **`usage_counters`: کش یا مشتق؟** | جدول **کشِ نوشتنی** بماند (طبقِ نامش) ولی **هر گیت با یک `SELECT count(*)`ِ واقعی در همان تراکنش** تصمیم بگیرد. کش فقط برای **نمایش** است. دلیل: کشِ اشتباه یعنی پول یا سرویسِ رایگان؛ `count` روی این ابعاد ارزان است. | ✅ **کش فقط برای نمایش** (۱۴۰۵/۰۶/۱۴) |

> ✅ **هر نُه تصمیم تایید شد (۱۴۰۵/۰۶/۱۴)** و به شش ADR تبدیل شدند. از این‌جا به بعد
> تغییرِ هرکدام یک ADRِ **جدید** می‌خواهد، نه ویرایشِ این جدول.

**ADRهای نوشته‌شده — نگاشتِ تصمیم به ADR:**

| ADR | عنوان | تصمیم‌های پوشش‌داده |
|---|---|---|
| [ADR-049](ARCHITECTURE_DECISIONS.md#adr-049) | `packages/billing-core` — پورتِ درگاه و ریاضیِ پول بیرون از `apps/api` | M4-D1، M4-D3، M4-D5، M4-D7 |
| [ADR-050](ARCHITECTURE_DECISIONS.md#adr-050) | idempotencyِ پرداخت روی **قفلِ ردیفِ `payments`** | — (پیامدِ ADR-014 + یافته‌ی B-1) |
| [ADR-051](ARCHITECTURE_DECISIONS.md#adr-051) | آشتی‌دهی: تابعِ خالص + probe + پلاگینِ پشتِ فلگ | M4-D4 |
| [ADR-052](ARCHITECTURE_DECISIONS.md#adr-052) | یک قاعده‌ی گِردکردن؛ نرخِ VAT منجمد در فاکتور | M4-D6 |
| [ADR-053](ARCHITECTURE_DECISIONS.md#adr-053) | ظرفیت با `count(*)`ِ واقعی؛ `usage_counters` کشِ نمایش | M4-D8، M4-D9 |
| [ADR-054](ARCHITECTURE_DECISIONS.md#adr-054) | ریالِ عددِ صحیح، دو enumِ وضعیت، کوپن بیرونِ قرارداد | M4-D2، M4-D2a، M4-D2b |

---

## فازها

### ✅ فاز ۰ — تصمیم‌های مرزی (بدون کد) — **تمام شد (۱۴۰۵/۰۶/۱۴)**

| # | گام | معیار پذیرش |
|---|---|---|
| ✅ ۰٫۱ | ارائه‌ی M4-D1…M4-D9 به مالک با هزینه/جایگزین | هر نُه تصمیم ✅ خورد (جدولِ بالا) |
| ✅ ۰٫۲ | نوشتنِ ADR-049…ADR-054 بر اساسِ تصمیم‌ها | هر شش ADR در `ARCHITECTURE_DECISIONS.md` + شش ردیف در فهرست؛ **هیچ ADR قدیمی ویرایش نشد** |
| ✅ ۰٫۳ | ساختِ [`PROGRESS-M4-billing.md`](PROGRESS-M4-billing.md) | فایل با ساختارِ «چه شد / چه تصمیمی / قدم بعد» موجود |

---

### فاز ۱ — probeها (بدون منطقِ محصول) ★ دروازه

> **درسِ گران‌قیمت:** در M3 سه باگِ نمایش تا **فاز ۱۱** زنده ماندند چون هیچ‌وقت واقعاً
> اجرا نشده بودند. این فاز می‌گوید «قبل از منطق، رفتار را ببین».

| # | گام | معیار پذیرش | خودآزمون |
|---|---|---|---|
| ✅ ۱٫۱ | [`scripts/billing-probe-gateway.ts`](scripts/billing-probe-gateway.ts) — تماسِ **واقعی** با سندباکس (`pnpm billing:probe-gateway -- --request` سپس `-- --verify`) | ✅ **هفت چک سبز، با یک پرداختِ واقعیِ سندباکس توسطِ مالک.** ★★ verifyِ اول `code=100` (`ref_id=476569601`)، verifyِ دوم **`code=101`** — پس {۱۰۰،۱۰۱} = پرداخت‌شده. و: authorityِ ۳۶کاراکتری · خطا = **HTTP 422 + `errors` شیء** · verifyِ پرداخت‌نشده = **HTTP 401 + `-51`** | ✅ مبلغ +۱ ریال ⇒ **HTTP 401 + `-50`** («مبلغ پرداخت‌شده با مبلغِ ارسالی متفاوت است») — خطرِ «اختلافِ یک ریال» اثبات شد |
| ✅ ۱٫۲ | [`scripts/billing-probe-db.ts`](scripts/billing-probe-db.ts) — سه حالتِ parser کنارِ هم | ✅ **B-2 و B-3 هر دو تایید شدند** و از فرضیه دقیق‌ترند — جدولِ پایین | ✅ با ثبتِ OIDِ ۱۷۰۰ همان کوئری `number` داد |
| ✅ ۱٫۳ | [`apps/api/probe/idempotency-callback.ts`](apps/api/probe/idempotency-callback.ts) — چهار ترکیبِ متد×auth | ✅ **B-1 تایید شد:** شکلِ callback (GET بدونِ auth) ⇒ handler **۲ بار** اجرا شد، هیچ replayی. و هر گارد **به‌تنهایی** کافی است (GET+auth هم ۲، POST بدونِ auth هم ۲) | ✅ POST+auth ⇒ **۱** اجرا + `idempotent-replay` ⇒ probe واقعاً میان‌افزار را صدا می‌زند |
| ✅ ۱٫۴ | probeِ قفل: دو تراکنشِ **واقعاً هم‌زمان** روی یک ردیفِ `payments` | ✅ دومی **۳۱۳ms** منتظر ماند و بعد وضعیتِ به‌روز («paid») را دید | ✅ بدونِ `FOR UPDATE` دومی بی‌درنگ «pending» دید ⇒ دو فعال‌سازی |
| ✅ ۱٫۵ | [`scripts/billing-probe-math.ts`](scripts/billing-probe-math.ts) — ۶ موردِ واقعی | ✅ `subtotal − discount + vat === total` روی هر ۶ مورد **دقیق**، و هیچ خروجیِ اعشاری | ✅ مسیرِ دوم **۲ از ۶** را واگرا کرد — و بدتر از انتظار (پایین) |

★★ **دروازه باز شد — هر پنج گام سبز (۱۴۰۵/۰۶/۱۴).** فاز ۲ می‌تواند شروع شود.

#### ★★ آنچه probeها **عوض کردند** (نه فقط تایید)

**۱٫۱ — سه چیزِ تازه که در برنامه نبود:**

۱. ★ **پاسخِ `101` شکلِ «موفق» دارد، نه شکلِ «خطا».** HTTP **200** و
   `errors: []` (**آرایه**). یعنی قاعده‌ی «آرایه ⇒ موفق، شیء ⇒ خطا» درست است و
   ۱۰۱ **در سمتِ موفق** می‌نشیند — آداپتور باید `data.code` را بخواند، نه دنبالِ
   `errors` بگردد. اگر کسی ۱۰۱ را «خطا» فرض کند، هم شکلِ پاسخ و هم HTTP گمراهش می‌کنند.
۲. ⚠️ **authorityِ پرداخت‌نشده منقضی می‌شود.** اولین تلاش با پیامِ «شناسه پرداخت
   ارسال شده منقضی گردیده است» برگشت (~۲۰ دقیقه بعد از ساخت). عددِ دقیقش را نداریم،
   ولی یعنی ردیفِ `pending` **پنجره‌ی محدودی** برای تسویه دارد → ورودیِ مستقیمِ
   بازه‌ی sweep در **فاز ۷**، و دلیلِ اینکه صفحه‌ی checkout نباید لینکِ درگاه را
   کَش کند یا دوباره نشان دهد.
۳. **قراردادِ callback زنده تایید شد:** مرورگر واقعاً به
   `…/billing/callback?Authority=…&Status=OK` هدایت شد — دقیقاً همان دو پارامتر،
   بدونِ چیزِ دیگر. (صفحه‌اش «reachable نیست» داد چون dev serverِ وب بالا نبود؛
   ربطی به درگاه ندارد.)
   ⊕ `ref_id` در JS **`number`** آمد با ۹ رقم (`476569601`) — یک نقطه‌داده در برابرِ
   ابهامِ «اگر از ۲^۵۳ بگذرد چه؟». ابهام **باز می‌مانَد** (یک نمونه اثبات نیست).

**۱٫۲ — B-2 و B-3 یک چیز نیستند، و حالتِ میانی خطرناک‌ترین است:**

| کجا | parserِ ثبت‌شده | `sum()` | `count(*)` | ستونِ خام |
|---|---|---|---|---|
| اسکریپت/worker با `new pg.Pool` | **هیچ** | رشته | رشته | **رشته** |
| ★ `apps/api` (از راهِ `createDbPool`) | فقط OIDِ ۲۰ | **رشته** | عدد | عدد |
| با OIDِ ۱۷۰۰ هم | ۲۰ + ۱۷۰۰ | عدد | عدد | عدد |

ردیفِ وسط دقیقاً همان‌جایی است که کدِ billing نوشته می‌شود: `sum+۱` می‌شود `«40000001»`
(الحاق) و `sum > ۹٬۰۰۰٬۰۰۰` می‌شود `false` (مقایسه‌ی الفبایی) — در حالی که **ستون و
`count` درست‌اند**، پس کدِ اطراف سالم به‌نظر می‌رسد. گاردِ `RangeError` هم شلیک نمی‌کند
چون اصلاً چیزی پارس نشده.

**۱٫۵ — مسیرِ دوم فقط «یک ریال» نبود.** علاوه بر اختلافِ ۱ ریالِ پیش‌بینی‌شده، در موردِ
«کوپنِ بزرگ‌تر از مبلغ» مبلغِ **منفی** (`−۴۴۰٬۰۰۰`) تولید کرد، چون clampِ
`Math.min(discount, subtotal)` را نداشت. یعنی یک کوپنِ بزرگ می‌توانست مبلغِ منفی به درگاه
بفرستد. → قیدِ اجباریِ گام ۳٫۵.

**⊕ ورودیِ گام ۳٫۶ (رایگان به‌دست آمد):** مرزِ سالِ جلالی با روزِ UTC هم‌راستا **نیست** —
`۲۰۲۷-۰۳-۲۰T۲۰:۰۰Z` سالِ جلالیِ ۱۴۰۵ می‌دهد و یک ساعت بعد ۱۴۰۶، در حالی که سالِ UTC هر دو
۲۰۲۷ است. پس شماره‌ی فاکتور و `issued_at` باید از **یک منبعِ زمانِ واحد** بیایند.

**⊕ دو چیزِ کوچک که حین probe معلوم شدند:** `teams.owner_user_id` و `payments.initiated_by`
هیچ‌کدام `ON DELETE` ندارند (حذفِ کاربر با ۲۳۵۰۳ می‌افتد) → گام ۴٫۴ · و `no-console` برای
`scripts/**` خاموش است ولی برای `probe/`ِ داخلِ یک پکیج نه ⇒ مثلِ `probe/`ِ storage به
`ignores` اضافه شد.

---

### ✅ فاز ۲ — قرارداد (`packages/shared-types`) ★ ADR-021 — **تمام شد (۱۴۰۵/۰۶/۱۴)**

| # | گام | معیار پذیرش |
|---|---|---|
| ✅ ۲٫۱ | primitiveِ `rial` در [`primitives.ts`](packages/shared-types/src/api/primitives.ts): `z.number().int().nonnegative()` | ⚠️ **هیچ فیلدِ پولی `z.number()`ِ خام یا `z.bigint()` نباشد.** `z.bigint()` هنگامِ `JSON.stringify` می‌ترکد — ۵۰۰ی بی‌کد |
| ✅ ۲٫۲ | سه enum: `subscriptionStatus` (۵)، `teamSubscriptionStatus` (۶، با `none`)، `billingPeriod`، `invoiceStatus` — طبقِ M4-D2a | هر enum یک tupleِ `as const` + `z.enum` + `z.infer`، مثلِ `boardRoles` |
| ✅ ۲٫۳ | DTOها: `plan`، `subscription`، `invoice`، `invoiceLineItem` (**نام‌دار**، نه inline) | `pnpm typecheck` سبز؛ در OpenAPI هر کدام یک componentِ واقعی باشند نه شیءِ بی‌نام |
| **[!] ۲٫۴** | چهار فیلدِ مالیِ `Team` — **به فاز ۵ موکول شد** | ⚠️ **بلوکه:** افزودنشان به‌صورتِ الزامی، `toTeam`ِ [`apps/api/src/dto.ts`](apps/api/src/dto.ts) را در **typecheck** می‌شکند، و هیچ‌چیز نمی‌تواند پُرشان کند تا seedِ پلن‌ها (فاز ۴) و کوئریِ ظرفیت (فاز ۶) بیایند. پُرکردنشان با مقدارِ ساختگی یعنی دروغ در قرارداد. جایش **گام ۵٫۵** است، کنارِ `GET /teams/:id/billing/subscription` |
| ✅ ۲٫۵ | کدهای خطا **به انتهای** `apiErrorCodes` اضافه شوند (قاعده‌ی خودِ فایل) | حداقل: `PLAN_NOT_FOUND`، `PLAN_INACTIVE`، `SUBSCRIPTION_NOT_FOUND`، `COUPON_INVALID`، `COUPON_EXHAUSTED`، `PAYMENT_NOT_FOUND`، `PAYMENT_FAILED`، `GATEWAY_UNAVAILABLE`، `QUOTA_EXCEEDED` |
| ✅ ۲٫۶ | بدنه‌ها در `requests.ts`: `checkoutBody` | ★ **`checkoutBody` هیچ فیلدِ ریالی ندارد** (ADR-014: «هیچ مبلغی از کلاینت پذیرفته نمی‌شود») |
| ✅ ۲٫۷ | schemaِ **query** برای callback (`Authority`/`Status` با حرفِ بزرگ) | اولین schemaی query بعد از `pageQuery` — جایش (requests یا primitives) در PROGRESS ثبت شود |
| ✅ ۲٫۸ | تست‌ها در `api.test.ts` + **به‌روزرسانیِ کامنتِ سرِ [`index.ts`](packages/shared-types/src/api/index.ts)** که می‌گوید این DTOها عمداً غایب‌اند | ★★ **سه خودآزمون:** `rial` عددِ اعشاری را رد کند · `rial` مقدارِ `bigint` را رد کند · هیچ فیلدِ پولی `.optional()` نباشد (فقط `.nullable()`) |

> ⚠️ **چرا `.optional()` ممنوع است:** SDK پاسخ را **اصلاً اعتبارسنجی نمی‌کند**
> ([`client.ts:178`](packages/sdk/src/client.ts) یک castِ خالی است). پس فیلدِ جاافتاده
> بی‌صدا `undefined` می‌شود و `formatRial(undefined)` در مرورگر «NaN ریال» می‌نویسد،
> بدونِ هیچ خطایی در کلِ پشته.

---

### ✅ فاز ۳ — `packages/billing-core` (پورت + ریاضیِ پول) — **تمام شد (۱۴۰۵/۰۶/۱۴)**

| # | گام | معیار پذیرش | خودآزمون |
|---|---|---|---|
| ✅ ۳٫۱ | ساختِ پکیج با **کلِ چک‌لیستِ ۹تایی**: `package.json`، `tsconfig`، `eslint.config.js` (با `processEnvDiscipline()`)، factoryِ `billingCoreBoundaries()` در [`packages/eslint-config/boundaries.js`](packages/eslint-config/boundaries.js)، هر سه لایه‌ی تستش در `boundaries.test.js`، `vitest.config.ts` با **threshold ۹۰٪**، `test:coverage`، ثبت در `docs/dependencies.md` | ⚠️ **پکیجی که `test:coverage` نداشته باشد بی‌صدا از گیتِ پوشش معاف است** — turbo taskِ تعریف‌نشده را رد می‌کند و verify همچنان `✔` می‌زند. دقیقاً همان اشتباهی که گیت برای رفعش ساخته شد | یک importِ ممنوع (`pg`) بگذار ⇒ lint باید قرمز شود |
| ✅ ۳٫۲ | پورتِ `PaymentGateway` با سه متد ADR-014 (`createPayment`، `verifyPayment`، `refund?` — اختیاری بماند) | interface خالص، بدونِ `fetch`، بدونِ `pg` | — |
| ✅ ۳٫۳ | `MockGateway` — پیش‌فرضِ dev (M4-D5) | ★ فلگِ `developmentOnly` **روی خودِ پیاده‌سازی**، و ادعایش **داخلِ `buildApp()`** نه در فایلِ ورودی (قرینه‌ی [`board-authority.ts:43-49`](apps/realtime/src/auth/board-authority.ts)) | `APP_ENV=production` + `PAYMENT_PROVIDER=mock` ⇒ باید **بالا نیاید** |
| ✅ ۳٫۴ | `ZarinpalGateway` روی `fetch` بومی | ★★ سه قاعده از probe: (۱) **هرگز روی `res.ok` شاخه نزن** — بدنه را همیشه بخوان و روی `code` تصمیم بگیر · (۲) schemaی پاسخ باید **هر دو شکلِ `errors`** (آرایه و شیء) را بپذیرد · (۳) `{100,101}` = پرداخت‌شده | پاسخِ `-51` (HTTP 401) را شبیه‌سازی کن ⇒ باید «پرداخت نشده» بدهد، نه exception |
| ✅ ۳٫۵ | ریاضیِ پول: `computeInvoice(plan, period, seats, coupon, vatPercent)` — همه صحیحِ ریال، **یک** قاعده‌ی گِردکردنِ مستند | ★ `subtotal - discount + vat === total` **دقیقاً**؛ همان تابع هم مبلغِ درگاه را می‌دهد هم فاکتور را ⇒ اختلافِ ۱ ریال ساختاراً ناممکن | دو مسیرِ محاسبه بساز ⇒ تست باید قرمز شود |
| ✅ ۳٫۶ | شماره‌ی فاکتور (`HB-1405-000123`) و محاسبه‌ی دوره | ⚠️ سالِ جلالی از `Asia/Tehran` ولی `issued_at` از `now()`ِ UTC — بینِ ۲۰:۳۰ UTC تا نیمه‌شبِ آخرِ اسفند این دو **سالِ متفاوت** می‌دهند. یک منبعِ زمانِ واحد، تست‌شده روی همان مرز | زمان را روی ۱۴۰۵/۱۲/۲۹ ساعت ۲۱:۰۰ UTC بگذار ⇒ باید همان سالی را بدهد که DB می‌دهد |
| ✅ ۳٫۷ | `zarinpalEnvSchema` در [`config/src/sections.ts`](packages/config/src/sections.ts) + افزودن به `.env.example` | ⚠️ **`envInt` را برای `VAT_PERCENT` استفاده نکن** — `positive()` مقدارِ `0` را رد می‌کند و M4-D6 صفر را پیش‌فرض کرده. ⚠️ فیلدهای زرین‌پال در سطحِ schema **اختیاری** بمانند وگرنه `pnpm dev` merchant واقعی می‌خواهد (P3) | `VAT_PERCENT=0` بگذار ⇒ باید بالا بیاید |

**★ سه گیتِ تازه، هر سه با شکستنِ عمدی قرمز شدند:**

| شکستن | نتیجه |
|---|---|
| حذفِ `money.test.ts` | `✖ Coverage for lines (85%) does not meet global threshold (90%)` |
| `import pg` در `src/money.ts` | `✖ no-restricted-imports` با همان پیامِ دلیل |
| `ZARINPAL_CURRENCY=IRT` | رد شد (`z.literal("IRR")` — جلوی ضریبِ ۱۰) |

★ و `VAT_PERCENT=0` **بالا می‌آید** — چون `envIntFromZero` جای `envInt` (که `positive()`
است و صفر را رد می‌کرد) نوشته شد.

⚠️ **یک ناسازگاریِ واقعی حین تست پیدا و رفع شد:** `MockGateway.createPayment` نوعِ
`Promise` اعلام کرده بود ولی `assertGatewayAmount` را **همزمان** صدا می‌زد — یعنی پرتابش
با `.catch()` گرفته نمی‌شد و فراخوان را غافلگیر می‌کرد. متدها `async` شدند.

**پوشش: ۹۹٫۲۸٪ خط · ۹۴٫۲۳٪ شاخه · ۱۰۰٪ تابع** (۵۴ تست).

---

### ✅ فاز ۴ — migration `0004` (بستنِ حفره‌های schema) — **تمام شد (۱۴۰۵/۰۶/۱۴)**

> ⚠️ **`0001_init.sql` منجمد است.** ویرایشش روی ماشینی که migrate کرده خطای checksum
> می‌دهد و روی ماشینِ تازه schemaی متفاوت می‌سازد. هر اصلاحی در `0004` می‌آید.
> ⚠️ کلِ migration در **یک تراکنش** اجرا می‌شود ⇒ `CREATE INDEX CONCURRENTLY` ممنوع.

| # | گام | معیار پذیرش |
|---|---|---|
| ✅ ۴٫۱ | `CHECK` روی هر ستونِ وضعیتِ billing (رفعِ **B-4**) | `INSERT … status='Active'` باید **رد** شود |
| ✅ ۴٫۲ | جدولِ `coupon_redemptions` با یکتاییِ `(coupon_code, team_id)` | دو redeemِ هم‌زمانِ یک کوپن ⇒ دومی ۲۳۵۰۵، نه دو تخفیف |
| ✅ ۴٫۳ | پیوندِ `subscriptions.activated_by_payment_id` + قیمتِ منجمدِ `subscriptions.unit_price_rial` | «پرداخت شد ولی سرویس داده نشد» با یک join قابلِ گزارش شود؛ تغییرِ قیمتِ پلن وسطِ پرداخت verify را خراب نکند |
| ✅ ۴٫۴ | اصلاحِ FK/ON DELETE: `payments.initiated_by` و `invoices.team_id` | ⚠️ **به `invoices` هرگز `ON DELETE CASCADE` نده** — سوابقِ مالی را نابود می‌کند. رفتارِ درست `RESTRICT`ِ صریح است |
| ✅ ۴٫۵ | ستونِ `payments.card_hash` + نرخِ VATِ منجمد روی `invoices` | `card_hash` دیگر بی‌صدا در `verify_payload` گم نشود؛ `vat_percent` هر فاکتور در خودش باشد |
| ✅ ۴٫۶ | افزودنِ سناریوهای billing به [`scripts/db-fk-test.ts`](scripts/db-fk-test.ts) | حذفِ تیم با فاکتورِ پرداخت‌شده باید **مسدود** شود؛ `pnpm db:fk-test` سبز |

**[`0004_billing_integrity.sql`](apps/api/migrations/0004_billing_integrity.sql)** اعمال شد و
**۷ سناریوی تازه** به [`db-fk-test`](scripts/db-fk-test.ts) اضافه شد (۱۰ چک، همه سبز).

★★ **دو چیز فراتر از برنامه اضافه شد، چون probeها لازمشان کردند:**

۱. **رابطه‌ی ADR-052 حالا یک `CHECK` دیتابیسی هم هست** — `subtotal − discount + vat = total`،
   به‌علاوه‌ی `discount <= subtotal`. یعنی حتی اگر روزی کسی «مسیرِ دومِ محاسبه» بسازد (همان
   چیزی که probeِ ۱٫۵ مبلغِ **منفی**ش را دید)، درج **رد** می‌شود. کد و دیتابیس هر دو نگهبانند.
۲. **`invoice_sequences`** — شماره‌ی فاکتور با `INSERT … ON CONFLICT DO UPDATE … RETURNING`
   اتمیک گرفته می‌شود، نه `max(number)+1` که دو درخواستِ هم‌زمان را **بعد از جابه‌جاییِ پول**
   می‌شکست.

★ **خودآزمون:** با حذفِ `invoices_total_ck` و `subscriptions_status_ck` دو چک **قرمز** شدند،
و با بازگرداندنشان دوباره سبز.

⚠️ **seedِ پلن‌ها عمداً ناقص است:** فقط `free` فعال است (قیمتش صفر، پس عددی اختراع نشده).
`pro` و `team` با `is_active = false` نشسته‌اند چون **قیمت‌گذاری تصمیمِ مالک است، نه این
migration** — تا کسی عددِ واقعی نگذارد، در فهرستِ عمومی نمی‌آیند و قابلِ خرید نیستند.

---

### فاز ۵ — `apps/api`: مسیرهای billing

الگو دقیقاً همان [`routes/boards.ts`](apps/api/src/routes/boards.ts) است:
`registerBillingRoutes(app, deps)` + فراخوانی در [`app.ts`](apps/api/src/app.ts).

| # | گام | معیار پذیرش | خودآزمون |
|---|---|---|---|
| ۵٫۱ | `GET /billing/plans` (عمومی) | بدونِ auth کار کند؛ خروجی **دقیقاً** `plan[]`ِ shared-types (ADR-045) | — |
| ۵٫۲ | `POST /teams/:id/billing/checkout` (نقشِ `owner`) | ★ مبلغ **کاملاً سمتِ سرور** از `plans` محاسبه شود؛ ردیفِ `payments` با `idempotency_key` قبل از تماس با درگاه ساخته شود | مبلغ را در بدنه بفرست ⇒ باید نادیده گرفته شود |
| ۵٫۳ | ★★ `GET /billing/zarinpal/callback` | **قلبِ M4.** `SELECT … FOR UPDATE` روی `payments` → اگر از قبل `paid` بود همان نتیجه (بدونِ فعال‌سازیِ دوباره) → وگرنه verifyِ سرور-به-سرور → فعال‌سازیِ اشتراک + فاکتور **در همان تراکنش** | ⚠️ **B-1**: میان‌افزارِ `idempotency` اینجا **اجرا نمی‌شود**؛ تنها حفاظ همین قفلِ ردیف است. آزمون: دو callbackِ هم‌زمان ⇒ **یک** اشتراک، **یک** فاکتور |
| ۵٫۴ | `POST /billing/payments/:id/verify` (بازیابیِ دستی، `owner`) | همان مسیرِ ۵٫۳ را با همان قفل بزند — نه یک کپیِ دوم | callbackِ واقعی و verifyِ دستی را هم‌زمان بزن ⇒ باز هم یک اشتراک |
| ۵٫۵ | `GET /teams/:id/billing/{subscription,invoices}` (`admin`) + `POST …/cancel` (`owner`) | گیتِ نقش با `requireTeamRole`ِ موجود؛ **عدمِ عضویت ⇒ ۴۰۴، کم‌بودنِ نقش ⇒ ۴۰۳** (تقارنِ عمدیِ M3 حفظ شود) | با نقشِ `member` بزن ⇒ ۴۰۳ |
| ۵٫۶ | ثبت در `ROUTES`ِ [`openapi.ts`](apps/api/src/openapi.ts) + تگِ `billing` + `COMPONENT_SCHEMAS` | ★ گاردِ دریفتِ [`openapi.test.ts`](apps/api/src/openapi.test.ts) خودش این را اجباری می‌کند — مسیرِ ثبت‌شده‌ی مستندنشده verify را می‌شکند | یک مسیر را از `ROUTES` بردار ⇒ verify باید قرمز شود |
| ۵٫۷ | P7: افزودنِ `authority`، `refId`، `cardPan`، `merchantId` به `LOG_REDACT_PATHS` + قفل با تست | ⚠️ **redact فقط روی propertyِ شیء کار می‌کند، نه داخلِ template string.** دو جای موجود ([`app.ts:118`](apps/api/src/app.ts) کدِ OTP و [`teams.ts:178`](apps/api/src/routes/teams.ts) توکنِ دعوت) دقیقاً همین دور زدن را دارند و mockِ درگاه از رویشان کپی خواهد شد | یک `authority` در لاگ بگذار ⇒ تست باید قرمز شود |
| ۵٫۸ | محدودیتِ نرخِ اختصاصی روی callback | مسیرِ عمومیِ callback نباید فقط سطلِ سراسریِ IP را داشته باشد (IPهای درگاه کم‌اند ⇒ throttleِ callbackِ واقعی) — الگوی [`routes/auth.ts:65`](apps/api/src/routes/auth.ts) | — |

---

### فاز ۶ — اعمالِ ظرفیت (زنده‌کردنِ `usage_counters`)

> امروز هیچ سقفی اعمال نمی‌شود. تا این فاز، «پلنِ پولی» فقط یک ردیف در دیتابیس است.

| # | گام | معیار پذیرش | خودآزمون |
|---|---|---|---|
| ۶٫۱ | مسیرِ **نوشتنِ** `usage_counters` (رفعِ **B-5**) در ساخت/حذفِ بورد، افزودن/حذفِ عضو، commitِ دارایی | شمارنده‌ها بعد از هر عمل با `count(*)`ِ واقعی برابر باشند | یک مسیرِ به‌روزرسانی را بردار ⇒ تست باید دریفت را ببیند |
| ۶٫۲ | گیتِ سقف در همان تراکنشِ ساخت، با `count(*)`ِ واقعی (M4-D9) | ⚠️ **`max_boards = -1` یعنی نامحدود** — چکِ ساده‌ی `count >= max` روی پلنِ نامحدود **همه‌چیز را می‌بندد** (`count >= -1` همیشه true) | پلنِ نامحدود بگذار ⇒ ساختِ بورد باید کار کند |
| ۶٫۳ | تفکیکِ «عضو» از «مهمان» در شمارش | امروز `guest` هم عضو شمرده می‌شود؛ تصمیم صریح و یکسان بینِ DTO و گیت باشد | — |
| ۶٫۴ | ⚠️ کاربرِ `is_staff` نباید از گیت رد شود | [`roles.ts:65`](packages/auth-core/src/roles.ts) به staff روی **هر** بورد نقشِ `owner` می‌دهد. گیتی که «نقش را حل کن، owner رد شود» بنویسد، به هر staff **همه‌ی امکاناتِ پولی** را رایگان می‌دهد | کاربرِ staff بساز ⇒ باید همچنان به سقف بخورد |
| ۶٫۵ | سیاستِ downgradeِ M4-D8 | تیمِ بالای سقف: خواندن آزاد، **ساختِ جدید بسته**، پیام فارسیِ صریح؛ **هیچ حذفی** | تیمِ ۵۰ بوردی را به پلنِ ۳ بوردی ببر ⇒ ۵۰ بورد سالم بمانند و بوردِ ۵۱ ساخته نشود |

---

### فاز ۷ — آشتی‌دهی (ADR-014 قاعده ۳)

| # | گام | معیار پذیرش | خودآزمون |
|---|---|---|---|
| ۷٫۱ | تابعِ خالصِ sweep در `billing-core` | ورودی: ردیف‌های `pending`ِ کهنه‌تر از N دقیقه. خروجی: تصمیم به ازای هرکدام. بدونِ I/O | — |
| ۷٫۲ | `scripts/billing-reconcile.ts` + `pnpm billing:reconcile` (قرینه‌ی هفت `rt:*`) | ⚠️ **B-3**: این اسکریپت **باید** از `createDbPool` استفاده کند، نه `new pg.Pool` — وگرنه `amount_rial` رشته می‌شود و بی‌صدا | استخر را به `new pg.Pool` عوض کن ⇒ تست باید نوعِ رشته را بگیرد |
| ۷٫۳ | سنجه‌ی زنده: پرداختِ نیمه‌تمام (کاربر مرورگر را بست) → sweep → اشتراک فعال | ★ همان شکلِ `rt:permission`: یک شکستِ واقعی، نه یک mock | sweep را خاموش کن ⇒ اشتراک باید `pending` بماند |
| ۷٫۴ | پلاگینِ بازه‌ای در `apps/api` پشتِ فلگ (M4-D4 بخشِ ب) | خاموش‌بودنش پیش‌فرض باشد؛ ثبتِ صریح که چندنودی‌شدن کارِ M5 است | — |

---

### فاز ۸ — `packages/sdk`

| # | گام | معیار پذیرش |
|---|---|---|
| ۸٫۱ | فضای‌نامِ `billing` در [`client.ts`](packages/sdk/src/client.ts) | `plans()` با `noAuth: true` (تنها precedentش `auth.*` است) |
| ۸٫۲ | ★ پشتیبانیِ `Idempotency-Key` — **امروز اصلاً وجود ندارد** | `Opts` باید هدر بپذیرد؛ بدونش checkout قابلِ idempotent کردن نیست |
| ۸٫۳ | گسترشِ [`scripts/sdk-contract.ts`](scripts/sdk-contract.ts) با `MockGateway` | ⚠️ **SDK پاسخ را اعتبارسنجی نمی‌کند و این اسکریپت بیرونِ `pnpm verify` است.** بدونِ این چک‌ها verify کاملاً سبز می‌مانَد در حالی که `Team.limits` در مرورگر `undefined` است — **دقیقاً همان یافته‌ی فاز ۶ی M3** |

---

### فاز ۹ — `apps/web` ★ اثبات در مرورگر

| # | گام | معیار پذیرش |
|---|---|---|
| ۹٫۱ | صفحه‌ی `/pricing` (عمومی) | ⚠️ نامِ مسیرِ SPA نباید با پیشوندِ API یکی باشد وگرنه پروکسیِ dev صفحه را می‌بلعد ⇒ SPA: `/pricing`، API: `/billing` |
| ۹٫۲ | افزودنِ پیشوندِ API به `API_PREFIXES`ِ [`vite.config.ts`](apps/web/vite.config.ts) | ⚠️ فراموشی‌اش یعنی dev با HTTP 200 صفحه‌ی `index.html` برمی‌گرداند و خطا شبیهِ «قطعیِ شبکه» دیده می‌شود |
| ۹٫۳ | صفحه‌ی billingِ تیم + سنجه‌ی مصرف + فهرستِ فاکتور | اولین مصرف‌کننده‌ی `formatRial`/`formatToman` و `formatJalaliDate` (تا امروز **صفر call site** داشتند) |
| ۹٫۴ | ★★ صفحه‌ی بازگشت از درگاه | ⚠️ **دو تله‌ی کشنده:** (۱) توکنِ دسترسی فقط در closureی SDK زندگی می‌کند و بازگشت از درگاه یک **بارگذاریِ سردِ صفحه** است ⇒ صفحه‌ی callback نباید `RequireAuth` داشته باشد وگرنه به `/login` می‌پرد؛ (۲) کوکیِ refresh روی `path=/auth` است ⇒ مسیرِ `/billing` **هیچ کوکی‌ای نمی‌گیرد**. `done` refِ StrictMode هم لازم است ([`InviteAcceptPage.tsx:12`](apps/web/src/team/InviteAcceptPage.tsx)) |
| ۹٫۵ | ★★★ **اثباتِ کاملِ جریان در مرورگر** با `MockGateway` | خرید → ریدایرکت → بازگشت → اشتراک فعال، **دیده‌شده در مرورگر**. ⚠️ در M3 ثابت شد screenshot در این محیط time-out می‌کند ⇒ اثبات با اندازه‌گیریِ DOM + کلیکِ واقعی + بازرسیِ شبکه |
| ۹٫۶ | **هیچ دکمه‌ی «به‌زودی» نساز** | درسِ ۴ی M3: تا درگاه واقعاً کار نکند، دکمه‌ی ارتقا وجود ندارد |

---

### فاز ۱۰ — تحویل

| # | گام | معیار پذیرش |
|---|---|---|
| ۱۰٫۱ | `README` برای `billing-core` + بخشِ billing در READMEی `apps/api` و `sdk` | مثلِ پنج READMEی فاز ۱۱٫۲ی M3 |
| ۱۰٫۲ | بازتولیدِ `docs/api.md` + `docs/openapi.json` با `node scripts/gen-openapi.ts` | ⚠️ این اسکریپت **در verify نیست** ⇒ فراموشی‌اش سندِ کهنه می‌سازد بی‌آنکه چیزی قرمز شود |
| ۱۰٫۳ | `docs/m5-handoff.md` | همان ساختارِ `m4-handoff.md`: پورت‌ها، ارثیه‌ها، سقف‌ها، درس‌ها |
| ۱۰٫۴ | به‌روزرسانیِ `CLAUDE.md` (وضعیت + دستورهای `billing:*`) و `PROGRESS-M4` | — |
| ۱۰٫۵ | ★ **گیتِ نهایی**: `pnpm verify` سبز + هر ۷ سنجه‌ی realtime سبز + سنجه‌های billing سبز | ⚠️ هفت سنجه‌ی realtime **نباید** بشکنند — M4 به آن‌ها دست نمی‌زند، ولی migrationِ ۴ به schemaی مشترک دست می‌زند |

---

## آنچه M4 عمداً نمی‌سازد

| مورد | چرا | کجا می‌رود |
|---|---|---|
| استردادِ کامل (`refund`) | GraphQL + OAuth2 روی `next.zarinpal.com` — در dev اصلاً اجرا نمی‌شود (P3) | M6 |
| `reverse.json` (لغوِ ۳۰ دقیقه‌ای) | whitelistِ IPِ سرور در پنل لازم دارد ⇒ فقط با حسابِ واقعی | M6 |
| فاکتورِ PDF (`GET /invoices/:id/pdf`) | Chromium لازم دارد (ADR-019) ⇒ `apps/worker` | بعد از `apps/worker` |
| `apps/worker` | باز هم به تعویق (M4-D4) | M5 |
| `IdpayGateway` | پورت جایش را باز می‌گذارد؛ پیاده‌سازی مصرف‌کننده ندارد | وقتی لازم شد |
| فاز ۱۰ی M3 (قالب/کامنت/نسخه/خروجی) | ارثیه‌ی دست‌نخورده | دورِ بعد |
| room affinity | ADR-048، تریگرِ عددیِ حافظه | M5 |

---

## ⚠️ تله‌های محیطی (میراثِ M3، هنوز زنده)

پورت‌ها روی این ماشین: **DB ۵۴۳۳ · Redis ۷۳۷۹ · MinIO ۹۶۰۰**. رنجِ excludedِ ویندوز
**بعد از هر ری‌استارت جابه‌جا می‌شود** (در M3 سه بار دیده شد). اگر سرویسی bind نشد:
`netsh interface ipv4 show excludedportrange protocol=tcp` و پورتِ بیرونِ رنج بگذار.
