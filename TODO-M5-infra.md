# TODO-M5-infra.md — ماژول M5: `infra` (زیرساخت و استقرار)

> **وضعیت (۱۴۰۵/۰۶/۱۸): فاز ۰ تا ۷ ✅.**
> ⛔ **و از حالا `APP_ENV=production` عمداً بالا نمی‌آید** تا سرویسِ پیامکِ واقعی سیم‌کشی
> شود — دقیقاً مثلِ گیتِ زرین‌پال. `staging` کاملاً کار می‌کند.
> قدمِ بعد: **فاز ۸ (نگهداشت و پاک‌سازی)** ⏳ — منتظرِ عددهای M5-D9.
>
> **نقطه‌ی ورود:** [`docs/m5-handoff.md`](docs/m5-handoff.md) — سندِ تحویلِ M4 به M5.
>
> **قبل از شروع بخوان:** [PLAN.md](PLAN.md) §M5 و §۴ (env) و §۱۰ (ریسک‌ها) ·
> [ARCHITECTURE_DECISIONS.md](ARCHITECTURE_DECISIONS.md) — به‌ویژه **ADR-031** (پیاده‌سازیِ dev
> در production بالا نمی‌آید)، **ADR-048** (تریگرِ عددیِ affinity)، **ADR-050/055/056**
> (آشتی‌دهی)، **ADR-057** (سقفِ اتصال)، و **ADR-058…ADR-062** (همین ماژول) ·
> [`docs/realtime-baseline.md`](docs/realtime-baseline.md) — سقف‌های اندازه‌گیری‌شده.
>
> **ماژول‌های تمام‌شده:** [TODO-M1](TODO-M1-canvas-core.md) · [TODO.md](TODO.md) (M2) ·
> [TODO-M3](TODO-M3-backend-api.md) · [TODO-M4](TODO-M4-billing.md). **این فایل فقط M5 است.**

---

## دامنه‌ی M5

**داخل:** ایمیجِ production (`api`/`realtime`/`web`) · استقرار روی آروان · CI · سخت‌سازیِ
پیکربندی و راز · رصدپذیریِ حداقلی · انتخابِ رهبر برای آشتی‌دهی · پشتیبان و **مشقِ بازیابی** ·
کارهای نگهداشت و پاک‌سازی · سخت‌سازیِ شبکه‌ی ایران.

⛔ **بیرون:**

| مورد | چرا | کجا |
|---|---|---|
| room affinity | تریگرِ عددیِ [ADR-048](ARCHITECTURE_DECISIONS.md#adr-048) **نرسیده** — روی یک نودیم و اصلاً نمی‌توانیم اندازه‌اش بگیریم (فاز ۵ همین را درست می‌کند) | وقتی تریگر رسید |
| K8s / Helm / HPA / PDB | [ADR-059](ARCHITECTURE_DECISIONS.md#adr-059) | وقتی تریگرِ ADR-048 یا الزامِ دسترس‌پذیری بیاید |
| `apps/worker` | [ADR-062](ARCHITECTURE_DECISIONS.md#adr-062) — کارِ دوره‌ای worker نمی‌خواهد | اولین کارِ **مقیم** (احتمالاً PDF) |
| OTel / Grafana / trace | [ADR-061](ARCHITECTURE_DECISIONS.md#adr-061) | نودِ دوم |
| `refund` / `reverse` | GraphQL+OAuth و whitelistِ IP — در dev اجراناپذیر (P3) | **M6** |
| پنلِ ادمین | داشبوردِ تیم مالِ M3/M4 بود؛ این پنلِ داخلی است | **M6** |
| فاز ۱۰ی M3 (قالب/کامنت/نسخه/خروجی) | ارثیه‌ی دست‌نخورده | دورِ بعد |

---

## ✅ نُه تصمیمِ مرزی — تایید شد (۱۴۰۵/۰۶/۱۷)

| # | تصمیم | نتیجه |
|---|---|---|
| **M5-D1** | شکلِ استقرار | **Compose روی VM** → [ADR-059](ARCHITECTURE_DECISIONS.md#adr-059) (انحرافِ ثبت‌شده از PLAN §M5) |
| **M5-D2** | build یا بی‌build | **سورس می‌رود، stripping می‌مانَد** → [ADR-058](ARCHITECTURE_DECISIONS.md#adr-058) |
| **M5-D3** | انتخابِ رهبر | **advisory lockِ Postgres** → [ADR-060](ARCHITECTURE_DECISIONS.md#adr-060) |
| **M5-D4** | عمقِ رصدپذیری | **حداقلی، ولی حتماً گیجِ حافظه‌ی اتاق** → [ADR-061](ARCHITECTURE_DECISIONS.md#adr-061) |
| **M5-D5** | `apps/worker` | **ساخته نمی‌شود** → [ADR-062](ARCHITECTURE_DECISIONS.md#adr-062) |
| **M5-D6** | راز و staging | env-fileِ محدودشده روی VM (بدونِ سرویسِ خارجی، P2) · staging = همان compose با env دیگر |
| **M5-D7** | مقصدِ پشتیبان | **باکتِ جدا** از دارایی‌ها — یک دامنه‌ی خرابی برای داده و پشتیبانش یعنی هیچ پشتیبانی |
| **M5-D8** | بلاک‌کننده‌های launch | **فاز ۱–۴ و ۷** بلاک‌کننده‌اند · فاز ۵–۶ بلاک‌کننده‌ی **نودِ دوم**اند · فاز ۸–۹ بعد از launch هم می‌آیند |
| **M5-D9** ⏳ | نگهداشتِ داده (Q6ی PLAN) | ⚠️ **عددها هنوز از مالک نیامده** — فقط فاز ۸ را بلاک می‌کند، نه زودتر |
| **M5-D10** ✅ | مقصدِ رجیستریِ ایمیج | **بسته شد** ([ADR-063](ARCHITECTURE_DECISIONS.md#adr-063)، ۱۴۰۵/۰۶/۱۸): انتقال با `docker save` روی ssh؛ رجیستری **بهینه‌سازیِ اختیاری** است. ★ استدلالِ قدیمی روی عددِ غلطِ ۱٫۲GB بنا بود — واقعی **۱۸۵MB** برای هر سه |

### ✅⏳ وضعیتِ حساب‌ها (پاسخِ مالک، ۱۴۰۵/۰۶/۱۷)

**حسابِ آروان ✅ · دامنه ✅ · زرین‌پال ⏳ (هنوز تایید نکرده‌اند).**

★★ **زرین‌پال دقیقاً یک چیز را بلاک می‌کند: بالاآمدنِ `APP_ENV=production`** — و این رفتارِ
عمدیِ کدِ خودمان است: `assertGatewayAllowed` نمی‌گذارد `MockGateway` در production بالا بیاید
(وگرنه هر اشتراکی رایگان فعال می‌شود) و `createZarinpal` بدونِ `ZARINPAL_MERCHANT_ID`ِ
۳۶کاراکتری در **بوت** می‌شکند.

★ **ولی `APP_ENV=staging` با mock کاملاً بالا می‌آید.** پس **کلِ فاز ۲ تا ۹ روی staging جلو
می‌رود** و هیچ‌کدام منتظرِ زرین‌پال نمی‌مانند. فقط سوئیچِ نهاییِ production منتظر است.

⚠️ و یک تصمیمِ بازِ PLAN (Q4) این‌جا لازم می‌شود: **حقیقی یا حقوقی؟** — نوعِ حسابِ زرین‌پال
و مدارکش به آن بسته است.

### زیرساختی که باید از آروان تهیه شود (فاز ۲ به بعد)

| # | چه چیزی | اندازه‌ی پیشنهادی | چرا این عدد |
|---|---|---|---|
| ۱ | یک **ماشین مجازی** | **۸GB رم / ۴ vCPU / ~۱۰۰GB SSD** | حافظه قیدِ بایند است نه CPU: `realtime` با heapِ ۴گیگ ≈ ~۴۰ بوردِ ۵۰۰۰عنصری (ریاضیِ [ADR-048](ARCHITECTURE_DECISIONS.md#adr-048)) یا ~۴۰۰ بوردِ ۵۰۰عنصری؛ + Postgres ~۲G + Redis + api + پروکسی |
| ۲ | **فضای ذخیره‌سازیِ S3 — سه باکت** | مصرفی | `assets` و `snapshots` (از قبل در config) + ⚠️ **باکتِ سومِ جدا برای پشتیبان** (M5-D7) |
| ۳ | **DNS** روی دامنه‌ی موجود | — | رکورد به IPِ همان VM |
| ۴ | IPِ عمومی | — | معمولاً همراهِ VM |
| ۵ | اسنپ‌شاتِ دیسکِ VM (اگر هست) | — | مکمل است، **جایگزینِ `pg_dump` نیست** |

**لازم نیست:** کوبرنتیزِ ابری ([ADR-059](ARCHITECTURE_DECISIONS.md#adr-059)) · VMِ دوم
(staging فعلاً روی همان ماشین، M5-D6) · دیتابیسِ managed (فاز ۷ پشتیبان را به‌هرحال می‌سازد).

⚠️ **دو چیز که باید probe شوند، نه فرض:** **CDN جلوی WebSocket** (منبعِ کلاسیکِ
timeout/بافرینگ — پیشنهاد: ترافیکِ WS مستقیم روی VM، CDN فقط جلوی فایلِ ایستا) و **گواهیِ
TLS** (از آروان یا ACME روی VM — کدام از این شبکه قابل‌اتکاست در فاز ۹ اندازه گرفته می‌شود).

### ✅ فازِ ۴٫۵ (سیم‌کشیِ پیامکِ واقعی) — **انجام شد** (۱۴۰۵/۰۶/۱۸)

**سرویس: sms.ir.** قالب تایید شد (`#CODE#`) و شناسه‌اش `265541`.

| # | گام | نتیجه |
|---|---|---|
| ✅ ۴٫۵٫۱ | پورت → پیاده‌سازیِ واقعی | [`sms-ir.ts`](packages/auth-core/src/sms-ir.ts) — جفتِ `ZarinpalGateway` |
| ✅ ۴٫۵٫۲ | پیکربندی | `smsEnvSchema`؛ شکلِ قالب در config مانْد، نه در دامنه |
| ✅ ۴٫۵٫۳ | انتخاب در بوت | [`apps/api/src/sms.ts`](apps/api/src/sms.ts) — جفتِ `plugins/payment.ts` |
| ✅ ۴٫۵٫۴ | گاردها | پیکربندیِ ناقص ⇒ شکست در **بوت** · کلیدِ جای‌نگه‌دار ⇒ گاردِ production |
| ✅ ۴٫۵٫۵ | probeِ زنده | [`sms:probe`](scripts/sms-probe.ts) — بدنه‌ی خام را چاپ می‌کند |
| ✅ ۴٫۵٫۶ | ★★ **تاییدِ روی گوشیِ واقعی** | **انجام شد** (۱۴۰۵/۰۶/۱۸، مالک): پیامک رسید و **عددِ داخلِ پیامک با کدِ probe یکی بود** ⇒ نامِ پارامتر درست است |

★★ **گیتِ launch باز شد:** با `SMS_PROVIDER=smsir`، `APP_ENV=production` دیگر روی پیامک
نمی‌شکند. اثبات‌شده با چهار حالتِ بوت — آخری‌اش حالا `GatewayNotAllowedError` می‌دهد،
یعنی **تنها بلاک‌کننده‌ی باقی‌مانده زرین‌پال است**.

✅ **و هر دو چیزی که هیچ تستی نمی‌گرفتشان، با گوشیِ واقعی اثبات شد:** پیامک **رسید**
(نه فقط «صف شد») و **جای کد پر بود و با کدِ probe یکی** — یعنی `SMS_IR_PARAM_NAME=CODE`
با متنِ قالب می‌خواند. ⇒ کلِ زنجیره‌ی ورودِ کاربرِ واقعی از این‌جا به بعد کار می‌کند.

⚠️ **متنِ قالبِ فعلی با «این یک پیام تست از هم بوم است» شروع می‌شود** — کاربرِ واقعی
همین را می‌بیند. نسخه‌ی تولیدی باید جدا تایید شود (و شناسه‌اش عوض می‌شود).

⚠️⚠️ **کلیدِ API دو بار در گفت‌وگو آمده و باید باطل شود.** در هیچ فایلی نوشته نشد.

### وقتی زرین‌پال تایید کرد، سه چیز از پشتیبانی بپرس

هر سه ورودیِ **مستقیمِ** بازه‌ی sweepِ آشتی‌دهی‌اند و هیچ‌کدام از کد درنمی‌آیند:

۱. **بازه‌ی مجازِ verify پیش از بازگشتِ خودکارِ پول** — امروز `BILLING_PENDING_EXPIRE_HOURS=72`
   یک **حدس** است.
۲. رفتارِ `metadata.auto_verify` (تنظیمِ پنل).
۳. بیشینه‌ی `ref_id` — یک نمونه‌ی ۹رقمی دیده شد؛ ستون `varchar` است که اگر از ۲^۵۳ گذشت نشکند.

⊕ و بعدش `listUnverified` (تنها متدِ اثبات‌نشده‌ی آداپتور) با تماسِ زنده قابلِ اثبات می‌شود.

---

## قوانین اجرای loop (همان انضباطِ M1…M4)

۱. **مبنای تیک‌زدن و کامیت فقط `pnpm verify` است** ([CLAUDE.md](CLAUDE.md) §«چرا `pnpm verify`»).
۲. **تیک زدن فقط بعد از تحققِ «معیار پذیرش»**. محقق نشد، ننویس انجام شد.
۳. ★ **اول probe، بعد کد** — هرجا رفتارِ داکر/شبکه/Node فرض شده، با یک آزمایشِ **واقعی**
   ثابتش کن. فاز ۱ کلاً همین است.
۴. ★★ **یک گیتِ آزموده‌نشده گیت نیست.** هر ادعا باید با یک **شکستنِ عمدی** قرمز شده باشد.
۵. ★★ **و مخصوصِ M5: یک پشتیبانِ بازیابی‌نشده پشتیبان نیست.** همان قاعده، این‌بار روی داده.
۶. **هیچ dependency ای بدونِ لایسنسِ مجاز** — بعد از هر افزودن `pnpm license:check` (P1).
   ⚠️ گیت بدونِ `--strict` اجرا می‌شود، پس تخلفِ dev-scope فقط **هشدار** است.
۷. ★ **P3 نشکند:** هر چیزی که به production اضافه می‌شود باید در `docker compose up`ِ لوکال هم
   بالا بیاید، وگرنه یا لازم نیست یا باید پشتِ فلگ باشد.
۸. **پایان هر session:** [`PROGRESS-M5-infra.md`](PROGRESS-M5-infra.md) با «چه شد / چه
   تصمیمی گرفتم / قدم بعد».

---

### ✅ فاز ۰ — تصمیم‌های مرزی (بدون کد) — **تمام شد (۱۴۰۵/۰۶/۱۷)**

| # | گام | معیار پذیرش |
|---|---|---|
| ✅ ۰٫۱ | ارائه‌ی M5-D1…M5-D9 با هزینه/جایگزین | نقشه تایید شد؛ M5-D9 و دو واقعیتِ محیطی باز مانْد و **جای بلاک‌شدنشان مشخص شد** |
| ✅ ۰٫۲ | نوشتنِ [ADR-058…ADR-062](ARCHITECTURE_DECISIONS.md#adr-058) | پنج ADR — شمارِ کل ۵۷ → **۶۲** |
| ✅ ۰٫۳ | ساختِ این فایل + [`PROGRESS-M5-infra.md`](PROGRESS-M5-infra.md) | — |

---

### ✅ فاز ۱ — probe — **تمام شد (۱۴۰۵/۰۶/۱۷)**

| # | گام | نتیجه‌ی **اندازه‌گیری‌شده** |
|---|---|---|
| ✅ ۱٫۱ | ایمیجِ دستیِ `apps/api` | بوت شد، به Postgres وصل شد، `/readyz` = **۲۰۰ در ۱۷ms**. ✅ خودآزمون: `enum` ⇒ `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` در **بارگذاریِ ماژول** (خطِ بعدش هرگز اجرا نشد) |
| ✅ ۱٫۲ | حجم و بوتِ سرد | **۴۹۶MB** (api) · **۴۹۱MB** (realtime) · بوتِ سرد **۴۸۳ms** ⇒ [ADR-058](ARCHITECTURE_DECISIONS.md#adr-058) **پابرجاست**؛ type-stripping هزینه‌ی محسوسی ندارد |
| ✅ ۱٫۳ | رجیستری و ایمیجِ پایه | `node:24-bookworm-slim` در **۴۳ ثانیه**. ⚠️ **از ماشینِ توسعه** — همین probe باید روی VMِ آروان تکرار شود |
| ✅ ۱٫۴ | `apps/realtime` در کانتینر | بالا آمد؛ WS از بیرون در دسترس و **fail-closed سالم**: `1008 TOKEN_INVALID`. ⚠️ و یک نقصِ واقعی پیدا شد (پایین) |
| ✅ ۱٫۵ | [`infra:probe-lock`](scripts/infra-probe-lock.ts) — **۴ چک** | انحصار · ★★ **مرگِ نشست ⇒ آزادسازیِ خودکار** · ⚠️ **آزادسازی روی اتصالِ دیگرِ استخر کار نمی‌کند** · استقلالِ کلیدها |

★★ **نقصِ واقعی که فقط نصبِ production پیدایش کرد:** `apps/realtime` از `@hamboom/config`
استفاده می‌کند ولی آن را **در `package.json` اعلام نکرده بود** — نه در `dependencies` و نه
در `devDependencies`. در dev و در `pnpm verify` هرگز نمی‌شکند (ورک‌اسپیس حلش می‌کند)؛ فقط
یک نصبِ `--prod` آن را نشان می‌دهد. اعلام شد و ایمیج بالا آمد.

⚠️ **و یک ممیزیِ سرتاسری هم انجام شد:** همان کلاسِ نقص در هیچ پکیجِ دیگری نبود — ولی
هیچ گیتی هم جلویش را نمی‌گیرد (گامِ ۳٫۶).

⚠️ **دو یافته‌ی جانبی که به فازهای بعد می‌روند:** `apps/realtime` **هیچ health endpointی
ندارد** (فاز ۵) · و `/readyz`ِ api وقتی دیتابیس نیست **۸ ثانیه** طول می‌کشد و **هیچ لاگی
از علت نمی‌دهد** (فاز ۵).

---

### ✅ فاز ۲ — ایمیج‌های production — **تمام شد (۱۴۰۵/۰۶/۱۸)**

| # | گام | نتیجه‌ی **اثبات‌شده** |
|---|---|---|
| ✅ ۲٫۱ | Dockerfileِ `api` و `realtime` | هر دو با `APP_ENV=production` بالا آمدند و **healthy** شدند. ★★ گیت‌های ADR-031 زنده‌اند: production+`mock` ⇒ **exit 1**، مسیرِ غلطِ callback ⇒ **exit 1** |
| ✅ ۲٫۲ | Dockerfileِ `apps/web` | buildِ Vite → nginx، **۱۴۲MB**. ★★ و یک نقص پیدا شد که فقط ایمیج نشانش می‌داد: خروجیِ `/assets/`ِ Vite با **پیشوندِ api** تصادم داشت ⇒ `assetsDir: "static"` |
| ✅ ۲٫۳ | reverse proxy با همان پیشوندها | `api-locations.conf` از `apps/web/src/api-prefixes.ts` **تولید** می‌شود؛ گیتِ [`infra:check-proxy`](scripts/infra-check-proxy.ts) داخلِ `pnpm verify` (گیت‌ها ۸ → **۱۰**). ★ همان اجرای اول **دو دریفتِ واقعی** گرفت: `/public` نبود، `/links` مرده بود |
| ✅ ۲٫۴ | `docker-compose.prod.yml` + سند | کلِ استک روی شبکه‌ی تمیز؛ `migrate` خودکار پیش از api. سند: [`infra/README.md`](infra/README.md) |
| ✅ ۲٫۵ | آشتی‌دهی از **همان ایمیجِ api** | exit 0 (و با `--dry-run`). ⚠️ و یک نقص: اسکریپت‌های اپراتور در ایمیج resolve نمی‌شدند |

★★ **دو نقصِ resolve/config که فقط اجرای واقعی نشانشان داد** (شرح در
[PROGRESS](PROGRESS-M5-infra.md)): پروژه‌ی **ریشه** وابستگیِ runtime داشت و اعلام نکرده بود
(همان کلاسِ نقصِ فاز ۱، یک طبقه بالاتر) · و کانتینرِ آشتی‌دهی **کلِ** configِ api را می‌خواهد.

⚠️⚠️ **یافته‌ای که تصمیمِ مالک می‌خواهد:** `createMockSmsProvider` در `APP_ENV=production` هم
بالا می‌آید — نقضِ [ADR-031](ARCHITECTURE_DECISIONS.md#adr-031) و **بلاک‌کننده‌ی launch**
(هیچ کاربرِ واقعی نمی‌تواند وارد شود، و کدِ OTP در لاگ می‌نشیند — در همین مشق از لاگ خوانده شد).
گاردِ بوتش گامِ ۴٫۱ است؛ **انتخابِ سرویسِ پیامکِ ایرانی تصمیمِ مالک است، مثلِ زرین‌پال.**

### ✅ فاز ۳ — CI — **تمام شد (۱۴۰۵/۰۶/۱۸)، با اجرای واقعی روی GitHub**

★★ **معیارِ پذیرش «سبز روی PR» بود و حالا اندازه‌گیری شده است، نه ادعا:** هر سه job روی
runnerِ واقعی سبزند (`93d3195`).

| # | گام | نتیجه‌ی **اثبات‌شده** |
|---|---|---|
| ✅ ۳٫۱ | job اصلی: `pnpm verify` روی هر PR | سبز روی runner در **۲m۱۵s**. **هیچ گیتی در YAML کپی نشده** — فقط `pnpm verify` |
| ✅ ۳٫۲ | jobِ دومِ با سرویس | **هر ۱۸ مرحله سبز** در **۲m۰۰s** (هفت سنجه‌ی realtime ۸۳s). ★ از `docker-compose.yml`ِ **خودِ ریپو** (P3) و `.env` از `.env.example` ⇒ آن فایل هم آزموده می‌شود |
| ✅ ۳٫۳ | ★★ گیتِ کهنگیِ سند | [`gen-openapi --check`](scripts/gen-openapi.ts) — **داخلِ `pnpm verify`**، با شکستنِ عمدی قرمز شد |
| ✅ ۳٫۴ | ثبتِ صریحِ گیت‌های اجراناپذیر در CI | جدولِ چهارتایی در صدرِ [`ci.yml`](.github/workflows/ci.yml) با جبرانِ هرکدام |
| ✅ ۳٫۵ | buildِ ایمیج (فقط main) | سبز در **۱m۰۴s** (web ۳۱s · api ۲۱s · realtime ۸s). **بدونِ push** (**M5-D10**)، ولی با یک ادعای واقعی: **گاردِ بوت داخلِ ایمیجِ ساخته‌شده** پاس شد |
| ✅ ۳٫۶ | ★ گیتِ وابستگیِ اعلام‌نشده | [`deps:check`](scripts/check-workspace-deps.ts) — **داخلِ `pnpm verify`**، با **بازساختنِ هر دو نقصِ تاریخی** قرمز شد |

### ★★ سه نقص که هیچ‌کدام را مشقِ محلی نمی‌گرفت

**۱ (مشقِ محلی):** `pnpm db:smoke` از **M3** شکسته بود — `0002_board_fks.sql` روی
`board_updates.board_id` یک FK گذاشت و اسکریپت هنوز UUIDِ تصادفی درج می‌کرد (`23503`).
ماه‌ها قرمز بود و کسی ندید، **چون هیچ‌چیز خودکار اجرایش نمی‌کرد**.

**۲ و ۳ (فقط اجرای واقعی):** مرحله‌ی انتظار `node scripts/…` بود بدونِ `--env-file` ⇒
`.env` ساخته می‌شد و **هیچ‌کس نمی‌خواندش** (محلی همیشه با `--env-file-if-exists` اجرا شده
بود) · و `docker images` **حداکثر یک آرگومان** می‌گیرد، پس جدولِ حجم می‌افتاد **بعد از**
اینکه هر سه ایمیج build شده و گاردِ بوت پاس شده بود.

⚠️ **درسِ روشیِ این فاز:** «محلی سبز شد» برای CI شرطِ لازم است، نه کافی — همان رابطه‌ای
که «همه‌ی گیت‌ها سبزند» با کدِ پول دارد (درسِ M4).

⚠️ **و یک محدودیتِ ابزار که ثبت می‌شود:** خواندنِ لاگِ Actions از API نیاز به دسترسیِ
**admin** دارد (`403 Must have admin rights`). هر دو نقص از روی **نامِ مرحله‌ی افتاده** +
بازتولیدِ محلی تشخیص داده شدند. اگر روزی شکستی از نامِ مرحله معلوم نشد، متنِ لاگ لازم است.

### ✅ فاز ۴ — سخت‌سازیِ پیکربندی و راز — **تمام شد (۱۴۰۵/۰۶/۱۸)**

| # | گام | نتیجه‌ی **اثبات‌شده** |
|---|---|---|
| ✅ ۴٫۱ | ★★ گاردهای بوتِ جاافتاده | چهار گارد، همه با اجرای واقعیِ **ایمیج** اثبات شدند (exit 1): پیامکِ ساختگی · رازِ ضعیف · دیتابیسِ دورِ بدونِ SSL · متغیرِ dev-only. ★ همه‌ی تخلف‌ها **با هم** گزارش می‌شوند |
| ✅ ۴٫۲ | مرورِ env + راز/خطرناک | [`.env.production.example`](.env.production.example) با علامتِ ‼️/⚠️/★ روی تک‌تک. ★★ و configِ آشتی‌دهی **باریک** شد: `env \| grep -c "JWT_SECRET\|S3_"` داخلِ آن کانتینر حالا **صفر** است |
| ✅ ۴٫۳ | staging با همان compose | کلِ استک با `APP_ENV=staging` بالا آمد، هر ۵ سرویس healthy، `/` و `/healthz` و `/billing/plans` همه ۲۰۰ |
| ✅ ۴٫۴ | ★ بازبینیِ ADR-031 روی `apps/realtime` | دو یافته (پایین) — هر دو بسته شد |

### ★★ گاردی که یک تصمیمِ مالک را اجباری می‌کند

`assertSmsProviderAllowed` دقیقاً مثلِ `assertGatewayAllowed` است: با `APP_ENV=production`،
`MockSmsProvider` **بالا نمی‌آید**. ⛔ **یعنی production تا سیم‌کشیِ یک سرویسِ پیامکِ ایرانی
اصلاً استارت نمی‌خورد** — که رفتارِ درستی است، چون با mock هیچ کاربرِ واقعی نمی‌تواند وارد
شود و کدِ ورود در **لاگ** می‌نشیند (در فاز ۲ با اجرای واقعی دیده شد).

⏳ **انتخابِ سرویسِ پیامک تصمیمِ مالک است** (مثلِ زرین‌پال) — دومین بلاک‌کننده‌ی launch.

### ⚠️ گاردِ SSL عمداً «همیشه true» **نیست**

[ADR-059](ARCHITECTURE_DECISIONS.md#adr-059) یعنی Postgres در همان شبکه‌ی compose است و
پورتِ هاست ندارد؛ یک قاعده‌ی «همیشه SSL» استقرارِ خودمان را **روزِ اول** می‌شکست. قاعده این
است: **میزبانِ نقطه‌دار یا IP ⇒ اتصال از این ماشین بیرون می‌رود ⇒ SSL اجباری.** پس حالتِ
خطرناکِ واقعی (بردنِ `DATABASE_URL` به دیتابیسِ Managed و یادرفتنِ SSL) می‌شکند، ولی چیدمانِ
امروز کار می‌کند. ⚠️ **این یک اثبات نیست، یک گارد است** — و در کد همین‌طور نوشته شده.

### ★ دو یافته‌ی ممیزیِ ۴٫۴

**۱. `devAuthEnvSchema` مرده بود.** `RT_DEV_JWT_SECRET` برای `DevBoardAuthority` بود و آن
پیاده‌سازی در **M3 فاز ۷** حذف شد — از آن روز این بخش هیچ مصرف‌کننده‌ای نداشت، برخلافِ
قاعده‌ی خودِ `packages/config`. حذف شد؛ و حالا **وجودش** در production یک تخلف است.

**۲. ★★ `BoardAuthority.developmentOnly` اختیاری بود** در حالی که همان پرچم در
`PaymentGateway` و `SmsProvider` **اجباری** است ⇒ یک پیاده‌سازی که فراموشش کند بی‌صدا
`undefined` می‌شود و **از گیت رد می‌شود**. ⚠️ و این فرضی نبود: بدلِ تستِ خودِ همان گیت
دقیقاً همین کار را می‌کرد. اجباری شد.

### ✅ فاز ۵ — رصدپذیریِ حداقلی — **تمام شد (۱۴۰۵/۰۶/۱۸)**

| # | گام | نتیجه‌ی **اثبات‌شده** |
|---|---|---|
| ✅ ۵٫۱ | ★★ گیجِ حافظه‌ی اتاق | `hamboom_rt_adr048_trigger_ratio` — **خودِ تریگرِ ADR-048، از قبل حساب‌شده**. ضریب با [`infra:probe-metrics`](scripts/infra-probe-metrics.ts) مستقلاً اندازه‌گیری شد |
| ✅ ۵٫۲ | `/metrics` روی هر دو سرور (غیرعمومی) | ۱۲ متریکِ realtime · ۱۵ متریکِ api — با درخواستِ **واقعی** اثبات شد. ★ غیرعمومی‌بودنش **شبکه‌ای** است، و گیتِ پروکسی نگهش می‌دارد |
| ✅ ۵٫۳ | تفکیکِ `/healthz` و `/readyz` | ⚠️ **از قبل روی هر دو سرویس بود** — یافته‌ی فاز ۱ غلط بود (پایین). دو نقصِ **واقعی** رفع شد |
| ✅ ۵٫۴ | آستانه‌های هشدار + سند | [`docs/observability.md`](docs/observability.md) — هر آستانه به یک اندازه‌گیری یا ADR وصل است |

### ⚠️ یک یافته‌ی فاز ۱ **غلط** بود

«`apps/realtime` هیچ health endpointی ندارد» — با یک درخواستِ واقعی سنجیده شد:
`/healthz` → **۲۰۰ `ok`**، `/readyz` → **۲۰۰ `ready`**، هر دو از M2. ⇒ healthcheckِ
ضعیفِ TCPیِ کانتینر (که فاز ۲ به‌خاطرِ همان یافته گذاشته بود) به `/healthz` عوض شد.
★ فرقش واقعی است: چکِ TCPی یک فرایندِ **قفل‌شده** را سالم می‌دید.

### ★★ ولی دو نقصِ واقعی در `/readyz`ِ api پیدا شد

**۱. علت را می‌بلعید** — `catch` خطا را کاملاً دور می‌ریخت: ۵۰۳ بدونِ هیچ سرنخی.
**۲. سقفِ زمانی نداشت** — با میزبانِ **غیرقابلِ دسترس** اندازه‌گیری شد: **۲۱ ثانیه**
(نه ۸ ثانیه‌ی ثبت‌شده؛ و با اتصالِ **refused** فقط **۸ میلی‌ثانیه** — دو حالتِ متفاوت).
⇒ `connectionTimeoutMillis` پنج ثانیه، و علت لاگ می‌شود (فقط `message`؛ رشته‌ی اتصال رمز دارد).

### ★ الگویی که دو بار لازم شد: **استثنای صریح**

`GET /metrics` دو گیت را قرمز کرد و **هر دو حق داشتند**: گیتِ پروکسی («مسیری که پروکسی
نمی‌بیندش») و گاردِ دریفتِ OpenAPI («مسیرِ بی‌سند»). ولی هر دو راهِ آسان بد بود — بردنش
به فهرستِ پروکسی یعنی nginx آن را **به دنیا** سرو کند، و مستندکردنش در `openapi.json`ِ
**عمومی** یعنی تبلیغش. ⇒ در هر دو، یک استثنای **نام‌دار با دلیل** اضافه شد
(`NEVER_PROXIED` و `NOT_IN_PUBLIC_SPEC`) — و **هر دو استثنا خودشان آزموده می‌شوند**:
اگر مسیرش حذف شود، «استثنای مرده» قرمز می‌شود.

### ✅ فاز ۶ — انتخابِ رهبر و درستیِ چندنودی — **تمام شد (۱۴۰۵/۰۶/۱۸)**

| # | گام | نتیجه‌ی **اثبات‌شده** |
|---|---|---|
| ✅ ۶٫۱ | advisory lock در پلاگینِ آشتی‌دهی | [`leader-lock.ts`](apps/api/src/plugins/leader-lock.ts) + [`infra:probe-leader`](scripts/infra-probe-leader.ts) — **۳ چک** روی Postgresِ زنده |
| ✅ ۶٫۲ | روشن‌کردنِ `BILLING_RECONCILE_ENABLED` | در [`.env.production.example`](.env.production.example) **`true`** شد؛ ⚠️ پیش‌فرضِ schema و compose همچنان `false` (fail-safe اگر متغیر نباشد) |
| ✅ ۶٫۳ | ★ ممیزیِ بقیه‌ی حالتِ تک‌نودی | جدولِ «پیش از افزودنِ نودِ دوم» در [`infra/README`](infra/README.md) — و **یک نقصِ واقعی که همان‌جا رفع شد** |

### ★★ سنجه‌ای که می‌توانست دروغ بگوید، و نگفت

درسِ M4: **یک چکِ همزمانی هم‌زمان نیست، مگر ثابت شود.** اگر کارِ داخلِ قفل لحظه‌ای بود،
نودِ اول قفل را می‌گرفت و آزاد می‌کرد **پیش از** تلاشِ نودِ دوم، و سنجه بی‌آنکه چیزی
اثبات کند سبز می‌شد. ⇒ کارِ داخلِ ناحیه‌ی بحرانی یک `pg_sleep`ِ **واقعی** است.

★ و **دو** خودآزمون: بدونِ قفل هر دو نود کار می‌کنند (پس سبزِ چکِ ۱ از قفل می‌آید) · و با
پیاده‌سازیِ عمداً شکسته (`held = true`) سنجه **قرمز** شد.

### ★★ ممیزیِ ۶٫۳ یک نقصِ واقعی پیدا کرد — و اندازه‌گیری پیدایش کرد، نه خواندنِ کد

سوال: میان‌افزارِ `idempotency.ts` **حافظه‌ای** است؛ با دو نود چه می‌شود؟

**خواندنِ کد می‌گفت «امن است»** — و درست هم می‌گفت: `createCheckout` اول با
`idempotency_key` در **دیتابیس** جست‌وجو می‌کند و ایندکسِ `payments_idem_uq` یکتاست.
پس retryِ **پشتِ سرِ هم** حتی بینِ دو نود درست replay می‌شود.

⚠️ **ولی با هم‌پوشانیِ اجباری** (`pg_sleep` بینِ SELECT و INSERT، روی دو استخرِ جدا)
معلوم شد: ردیف **یکی** می‌مانَد ✅ ولی بازنده **`23505`** می‌گیرد ⇒ کاربرِ دوم **۵۰۰**
می‌دید. رفع: [`createCheckoutIdempotent`](apps/api/src/services/billing.ts) یک بار
دوباره تلاش می‌کند. ⚠️ **بیرونِ** تراکنش، چون بعد از ۲۳۵۰۵ کلِ تراکنش abort است.

⊕ **حکمِ نهاییِ ممیزی:** دو موردِ باقی‌مانده (`Idempotency-Key`ِ POSTهای غیرمالی، و
سقفِ نرخِ حافظه‌ای) **شرطِ نودِ دوم**اند، نه بلاک‌کننده‌ی امروز — جدولش در `infra/README`.

### ✅ فاز ۷ — دوامِ داده (۱۴۰۵/۰۶/۱۸)

| # | گام | معیار پذیرش | نتیجه |
|---|---|---|---|
| ✅ ۷٫۱ | `pg_dump`ِ روزانه به **باکتِ جدا** (M5-D7) | اسکریپت + کانتینرِ یک‌بارمصرف | [`backup-db.ts`](scripts/backup-db.ts) — dump + **مانیفست** + راستی‌آزماییِ اندازه سمتِ سرور · `--prune=N` (پیش‌فرض خاموش) |
| ✅ ۷٫۲ | ★★ **مشقِ بازیابیِ واقعی** به‌عنوانِ گیت | restore روی دیتابیسِ خالی و `db:fk-test` سبز | [`restore-drill.ts`](scripts/restore-drill.ts) — **۵ چک**، و خودآزمون با **۴ شکستنِ عمدی** که هرکدام باید چکِ **درستش** را قرمز کند |
| ✅ ۷٫۳ | داستانِ migration در استقرار | چه کسی می‌زند و اگر وسطش بیفتد چه؟ | هر migration در تراکنشِ خودش · `api` تا موفق‌شدنش بالا نمی‌آید · ★★ **advisory lockِ تازه** (`811_451_002`) با هم‌پوشانیِ واقعی سنجیده شد |
| ✅ ۷٫۴ | پشتیبانِ Object Storage | snapshot + دارایی | [`backup-storage.ts`](scripts/backup-storage.ts) — آینه‌ی **افزایشی** + مانیفستِ تاریخ‌دار · ⛔ Redis عمداً نه |

★★ **یافته‌ای که شکلِ گیت را عوض کرد** — پیش از نوشتنِ گیت، یک dumpِ واقعی عمداً بریده و
بازیابی شد: `pg_restore` کدِ **۱** داد **ولی هر ۳۱ جدول ساخته شده بودند**. یعنی گیتی که
فقط `db:fk-test` را روی نسخه‌ی بازیابی‌شده بزند، روی پشتیبانِ ناقص **سبز می‌شود** (آن تست
ردیف‌های خودش را می‌سازد و rollback می‌کند). ⇒ پنج چک، نه یکی؛ و **شمارشِ ردیف در بازه**
تنها چکی است که «schemaی سالم، داده‌ی غایب» را می‌گیرد.

⚠️ **و انتظارِ خودم در خودآزمون غلط بود:** فکر می‌کردم با پشتیبانِ `--schema-only`،
`db:fk-test` سبز می‌مانَد. نماند — چون یک چکِ صریح دارد که `free`/`pro`/`team` باید فعال
باشند، یعنی به **seedِ `plans`** تکیه می‌کند. پس اتفاقی چکِ داده هم هست، ولی فقط برای یک
جدول. سناریو عوض شد به **«داده‌ی یک جدولِ برگ جا مانده»** که واقعاً فقط `rows` می‌گیردش.

⏳ **باقی‌مانده (ثبت‌شده، نه پنهان):** پورتِ `ObjectStore` نه stream دارد نه کپیِ سمتِ
سرور ⇒ هر شیء کامل در حافظه بارگذاری می‌شود. امروز بی‌اهمیت (dump ~۳۰۰KB)، بالای ۵۱۲MB
هشدارِ صریح می‌دهد. تغییرِ پورت کارِ **M6** است.

⚠️ **یک مبدأ شبکه‌ایِ تازه در buildِ ایمیج:** کلاینتِ Postgres 16 از `apt.postgresql.org`
می‌آید (bookworm فقط ۱۵ دارد). این هم باید از VMِ آروان probe شود؛ اگر
در دسترس نبود، ایمیج در CI ساخته و به VM برده می‌شود. حجمِ ایمیجِ api: **۴۷۰MB → ۵۶۲MB**.

---

### فاز ۸ — نگهداشت و پاک‌سازی ⏳ (منتظرِ M5-D9)

| # | گام | معیار پذیرش |
|---|---|---|
| ۸٫۱ | purgeی حذفِ نرم بعد از N روز | عددِ N از مالک |
| ۸٫۲ | ★ جاروبِ **بلابِ یتیم** | ⚠️ حذفِ بورد بلابِ S3 را پاک نمی‌کند — نشتیِ ثبت‌شده از M3 |
| ۸٫۳ | migrationِ حذفِ `usage_counters` | جدول نه نوشته می‌شود نه خوانده (B-5) |
| ۸٫۴ | نگهداشتِ لاگ و audit | عددها از مالک |

---

### فاز ۹ — سخت‌سازیِ ایران

| # | گام | معیار پذیرش |
|---|---|---|
| ۹٫۱ | بازبینیِ heartbeat/timeoutِ WS در برابرِ ریسکِ ثبت‌شده‌ی PLAN §۱۰ | با `rt:reconnect` سنجیده شود، نه با حدس |
| ۹٫۲ | TLS/ingress + سقفِ نرخ در لبه | — |
| ۹٫۳ | ★ ممیزیِ نهاییِ P2: صفر فراخوانیِ خارجی در runtime جز زرین‌پال | با grep روی سورس اثبات شود |

---

### فاز ۱۰ — تحویل

| # | گام | معیار پذیرش |
|---|---|---|
| ۱۰٫۱ | `infra/README` + **runbook** (استقرار، rollback، بازیابی، «چه کنم اگر…») | — |
| ۱۰٫۲ | `docs/m6-handoff.md` | همان ساختارِ `m5-handoff.md` |
| ۱۰٫۳ | به‌روزرسانیِ `CLAUDE.md` و PROGRESS | — |
| ۱۰٫۴ | ★ گیتِ نهایی | `pnpm verify` + هر ۷ سنجه‌ی realtime + سنجه‌های billing + **مشقِ بازیابی** |

---

## ⚠️ تله‌های محیطی (میراثِ M1…M4، هنوز زنده)

- **رنجِ excludedِ ویندوز بعد از هر ری‌استارت جابه‌جا می‌شود** — در M4 دو بار خورد
  (DB ۵۴۳۳→۵۵۴۴، api ۳۰۰۲→۳۴۱۰). پورت‌های امروز: **DB ۵۵۴۴ · api ۳۴۱۰ · Redis ۷۶۰۰ ·
  MinIO ۱۵۹۰۰ · web ۱۵۳۸۰**. `netsh interface ipv4 show excludedportrange protocol=tcp`.
- **`VITE_API_TARGET` باید در `apps/web/.env.local` باشد**، نه `.env`ِ ریشه.
- **فریمِ مختصاتِ کلیکِ Browser pane با ویوپورت یکی نیست** — تبدیل: `x * frameW / innerWidth`.
- **`node --env-file-if-exists=.env`** الگوی اجرای اسکریپت‌هاست.
