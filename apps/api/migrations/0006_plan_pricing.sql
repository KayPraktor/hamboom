-- 0006_plan_pricing.sql — قیمتِ واقعیِ `pro` و `team` و فعال‌کردنشان (ماژول M4، فاز ۹).
--
-- ═══ چرا این migration ═══════════════════════════════════════════════════════
--
-- seedِ `0004` هر دو پلن را با قیمتِ **صفر** و `is_active = false` گذاشت، با این جمله:
-- «قیمت جای‌نگهدار است و تصمیمِ قیمت‌گذاری مالِ مالک است، نه این migration». آن تصمیم
-- امروز (۱۴۰۵/۰۶/۱۵) گرفته شد:
--
--   • مدل: **قیمتِ هر صندلی × تعدادِ صندلی** — همان چیزی که `computeCharge` از فاز ۳
--     حساب می‌کند (`subtotal = unit × seats`). هیچ کدی برای این تغییر نکرد.
--   • `pro`  : ۱۹۹٬۰۰۰ تومان ماهانه  (۱٬۹۹۰٬۰۰۰ ریال)  ·  ۱٬۹۹۰٬۰۰۰ تومان سالانه
--   • `team` : ۴۹۹٬۰۰۰ تومان ماهانه  (۴٬۹۹۰٬۰۰۰ ریال)  ·  ۴٬۹۹۰٬۰۰۰ تومان سالانه
--   • سالانه = **ده برابرِ ماهانه** ⇒ دو ماه هدیه. عددِ گِردی است و در صفحه‌ی قیمت
--     قابلِ گفتن.
--
-- ⚠️ **در دیتابیس ریال است، همیشه** (P5/[ADR-015]). تومان فقط در لایه‌ی نمایش زاده
--    می‌شود (`formatToman` ÷۱۰) — تصمیمِ مالک: **تومان در صفحه، ریال در فاکتور**.
--
-- ⚠️ `personal` عمداً `is_active = false` می‌مانَد و این **ربطی به قیمت ندارد**: آن پلن
--    به فضای شخصی **تخصیص** داده می‌شود، خریدنی نیست. گیتِ `pnpm db:fk-test` همین تفکیک
--    را می‌سنجد.
--
-- ★ چرا `UPDATE` و نه `INSERT … ON CONFLICT`: ردیف‌ها از `0004` وجود دارند و ممکن است
--   `features`شان دستی عوض شده باشد؛ این migration فقط قیمت و وضعیت را می‌گذارد.

UPDATE plans
   SET price_monthly_rial = 1990000,      -- ۱۹۹٬۰۰۰ تومان
       price_yearly_rial  = 19900000,     -- ۱٬۹۹۰٬۰۰۰ تومان (دو ماه هدیه)
       is_active          = true,
       description        = 'برای تیم‌هایی که جدی کار می‌کنند — بوردِ نامحدود و فضای بیشتر.'
 WHERE code = 'pro';

UPDATE plans
   SET price_monthly_rial = 4990000,      -- ۴۹۹٬۰۰۰ تومان
       price_yearly_rial  = 49900000,     -- ۴٬۹۹۰٬۰۰۰ تومان (دو ماه هدیه)
       is_active          = true,
       description        = 'برای سازمان‌ها — فضای بزرگ و مدیریتِ دسترسیِ پیشرفته.'
 WHERE code = 'team';

-- ★ نگهبانِ همین تصمیم: پلنِ **فعال** با قیمتِ صفر یعنی «رایگان»، و رایگان فقط یکی است.
--   بدونِ این، یک `UPDATE`ِ اشتباه در آینده می‌تواند `pro` را رایگان کند و هیچ‌چیز نگیردش —
--   `createCheckout` مبلغِ صفر را با ۴۰۹ رد می‌کند، یعنی کاربر فقط یک خطای گنگ می‌بیند.
ALTER TABLE plans
  ADD CONSTRAINT plans_paid_price_ck
    CHECK (
      code = 'free'
      OR is_active = false
      OR (price_monthly_rial > 0 AND price_yearly_rial > 0)
    );
