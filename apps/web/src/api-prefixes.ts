/**
 * ★★ **تنها منبعِ حقیقتِ «کدام مسیرها مالِ apiاند و نه SPA».** — M5 گام ۲٫۳
 *
 * سه مصرف‌کننده دارد و هر سه باید **دقیقاً یکی** باشند:
 *
 * ۱. پروکسیِ devِ Vite ([`vite.config.ts`](../vite.config.ts)) — روی ۱۵۳۸۰.
 * ۲. reverse proxyِ production ([`infra/nginx/api-locations.conf`](../../../infra/nginx/api-locations.conf))
 *    که **از همین فهرست تولید می‌شود**، نه دستی نوشته.
 * ۳. گیتِ [`scripts/infra-check-proxy.ts`](../../../scripts/infra-check-proxy.ts) که
 *    ثابت می‌کند این فهرست با مسیرهای واقعیِ ثبت‌شده‌ی api می‌خوانَد.
 *
 * ── چرا این فایل وجود دارد ──────────────────────────────────────────────
 *
 * ⚠️ در فاز ۹ی M4 **دو** نقصِ مسیر فقط با اجرای واقعی پیدا شدند (پیشوندِ `/api/v1`ی که
 * هیچ‌جا ثبت نمی‌شد). درسش این بود که یک فهرستِ دستیِ مسیرها که هیچ چیزی آن را با
 * واقعیت مقایسه نمی‌کند، **بی‌صدا** کهنه می‌شود؛ و در production نتیجه‌اش این است که
 * کاربر به‌جای JSON، `index.html` می‌گیرد و کلاینت با یک خطای پارسِ گنگ می‌شکند.
 *
 * ★★ و همان لحظه‌ی ساختنِ این فایل، گیت **دو دریفتِ واقعی** پیدا کرد که از M3 مانده
 * بودند: `/public` (مسیرِ `POST /public/boards/resolve` که `sdk.links.resolve`
 * صدایش می‌زند) **اصلاً در فهرست نبود**، و `/links` در فهرست بود ولی **هیچ مسیری
 * ندارد** — نامِ فضای‌نامِ sdk بود، نه پیشوندِ URL.
 *
 * ── قاعده‌ی پوشش ────────────────────────────────────────────────────────
 *
 * مسیرِ `r` را پیشوندِ `p` می‌پوشاند اگر `r === p` یا `r.startsWith(p + "/")`.
 * پس `/api/v1` مسیرِ `/api/v1/docs` را می‌پوشاند بدونِ اینکه کلِ `/api…` را ببلعد.
 *
 * ⚠️ **هر مسیرِ SPA باید بیرونِ این فهرست بماند.** به همین دلیل صفحه‌ی قیمت
 * `/pricing` است و بازگشت از درگاه `/payment/result` — نه `/billing/…`.
 */
export const API_PREFIXES: readonly string[] = [
  "/auth",
  // ⚠️ **`/billing` کاملاً مالِ api است** (plans، callbackِ درگاه، verify، صفحه‌ی mock).
  "/billing",
  "/me",
  "/teams",
  "/folders",
  "/boards",
  "/invites",
  // ★ `POST /public/boards/resolve` — resolveِ مهمانِ لینک (`sdk.links.resolve`).
  //   تا M5 گام ۲٫۳ جا افتاده بود؛ در dev یعنی `index.html` به‌جای JSON.
  "/public",
  "/assets",
  "/healthz",
  "/readyz",
  "/openapi.json",
  // ★ مرورگرِ مستنداتِ خودمیزبان (`GET /api/v1/docs`) — تنها مسیرِ `/api/v1`ی اپ،
  //   و **تحت‌اللفظی** است نه پیشوندِ نسخه‌بندی. بقیه‌ی مسیرها پیشوند ندارند.
  "/api/v1",
];
