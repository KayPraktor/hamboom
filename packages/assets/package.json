{
  "name": "@hamboom/assets",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "لایه‌ی دامنه‌ی دارایی — presign/commit-validate/resolve. مصرف‌کننده‌ی @hamboom/storage، نه بخشی از آن (ADR-029)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke": "node smoke/round-trip.ts"
  },
  "dependencies": {
    "@hamboom/shared-types": "workspace:*",
    "@hamboom/storage": "workspace:*"
  },
  "devDependencies": {
    "@hamboom/eslint-config": "workspace:*",
    "@hamboom/tsconfig": "workspace:*",
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
