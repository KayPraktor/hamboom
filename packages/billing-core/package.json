{
  "name": "@hamboom/billing-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "منطقِ خالصِ پرداخت و اشتراک — پورتِ PaymentGateway، ریاضیِ ریال، دوره و شماره‌ی فاکتور. بدونِ DB و بدونِ UI (M4 فاز ۳)",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@hamboom/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@hamboom/eslint-config": "workspace:*",
    "@hamboom/tsconfig": "workspace:*",
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
