/**
 * `@hamboom/shared-types` — قرارداد مشترک بین همه‌ی ماژول‌ها.
 *
 * ⚠️ **تغییر این پکیج نیاز به تایید مالک دارد** ([ADR-021](../../../ARCHITECTURE_DECISIONS.md#adr-021)).
 * هر تغییری اینجا می‌تواند ماژول‌های دیگر را بشکند. پیشنهاد را در `PROGRESS.md`
 * بنویس و متوقف شو.
 *
 * این پکیج پایین‌ترین لایه است و **به هیچ پکیج دیگری وابسته نمی‌شود** —
 * یک قاعده‌ی ESLint این را اعمال می‌کند.
 */

export * from "./canvas/index";
export * from "./text/index";
