packages:
  - "apps/*"
  - "packages/*"

# پکیج‌هایی که اجازه‌ی اجرای اسکریپت نصب دارند (pnpm 10 به‌صورت پیش‌فرض همه را مسدود می‌کند).
# قبل از افزودن هر مورد به این فهرست، دلیلش را در همین‌جا کامنت کن.
onlyBuiltDependencies:
  - esbuild # وابستگی build خود Vite — باینری پلتفرم را نصب می‌کند
