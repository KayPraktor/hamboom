{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Hamboom Base (root alias)",
  "_comment": "منبع حقیقت در packages/tsconfig/base.json است. این فایل فقط برای مصرف از ریشه نگه داشته می‌شود.",
  "extends": "./packages/tsconfig/base.json"
}
